# 状態検証 hooks

チケットの状態機械を機械的に強制するための Claude Code hook 群。
登録は `.claude/settings.json` の `hooks` セクションで行う。

| ファイル | イベント | 役割 |
|---|---|---|
| `validate_ticket_state.py` | PreToolUse（Write\|Edit） | 書き込み前に不変条件を検証し違反を `permissionDecision: deny` でブロック（スキーマ・状態遷移・retry上限）。遷移元は `.metrics_state.json` の最終観測値を正とする（ドリフト後も「最後に検証された状態」から検証）。人間の判断に委ねる操作は `ask` に回す — **L2: カウンタ減少（=リトライ予算のリセット。人間承認で正規化）** と **in_progress上限（todo→investigation_done 着手時に他チケットが3件進行中なら確認）** |
| `check_loop_integrity.py` | Stop | 応答終了時に tickets/active/*.md を一括検査し `decision: block` で継続強制。schema・blocker・本文同期に加え **ドリフト検知（`.metrics_state.json` の最終観測値と status / retry_counts を突き合わせ、Write/Edit を経ない書き換えを検出。tickets/done/ もドリフト検知のみ実施）** と **L3: 差し戻し履歴（.metrics.jsonl）とカウンタの整合照合（retry_reset イベントをエポックとして「リセット時の値+以降の差し戻し回数」で照合）** と **SPEC.mdドリフト検知（KLK-016。`docs/.spec_state.json` のハッシュ最終観測値と `docs/SPEC.md`〔プロジェクト直下のみ〕の内容を突き合わせ。SPEC.md不在・サイドカー未記録はいずれもfail-open）** |
| `record_metrics.py` | PostToolUse（Write\|Edit） | ステータス遷移を `tickets/.metrics.jsonl` に追記する（観測性）。人間承認済みのカウンタ減少は `retry_reset` イベントとして記録する（L3のエポック）。最終観測値（status・retry_counts）は `tickets/.metrics_state.json` で保持し、validator の prior と Stop のドリフト検知の基準を提供する。docs/SPEC.md（プロジェクト直下のみ）への書き込み後は内容のsha256ハッシュを `docs/.spec_state.json` へ記録し、SPEC.mdドリフト検知（KLK-016）の基準を提供する |
| `guard_spec_writes.py` | PreToolUse（Write\|Edit） | SPEC.md（basename一致・.claude/配下を除く）への書き込みを `permissionDecision: ask` で人間確認に回す。autoモードでも SPEC 改訂の diff 承認を機械的に担保する（`--dangerously-skip-permissions` は貫通） |
| `guard_bash_writes.py` | PreToolUse（Bash） | tickets/active\|done/・SPEC.md に言及するコマンドのうち、読み取り・移動系ホワイトリスト（cat/grep/ls/cd/awk/mv/git add 等）以外を `permissionDecision: deny` でブロック。リダイレクト・sed -i（全表記）・**sed の「プログラム本文のホワイトリスト」（SED-7。`w`/`W`/`r`/`R`/`e`/`a`/`i`/`c`/`b`/`t`/`T`等の書き込み・情報開示・実行・分岐系コマンドを許可せず、安全と確認した読み取り系コマンド〔`p`/`d`/`n`/`N`/`g`/`G`/`h`/`H`/`x`/`l`/`q`/`Q`/`s`/`y`/`=`〕のみを許可する）**・awk のプログラム内リダイレクト・**awk の書き出し／外部コード読み込みオプション（`-i inplace`〔gawk の in-place 拡張〕・`-f`・`-l`・`-E`・`-D`・`-o`・`-p`・`-d`）**・**awk の「プログラム本文のホワイトリスト」（awk が実際に実行するテキスト＝`-e`/`--source` の値と、無ければ位置引数の先頭オペランドだけを取り出し、文字列・正規表現リテラルと **`#` コメント**を1パスで除去したうえで〔許可文字集合〕〔呼び出し名の許可集合〕〔`print`/`printf` 出力構文の不在〕の3条件を満たす形以外をすべて deny する。**除去の健全性は「1パスであること」では保証されない** — 1パス走査が保証するのは除去スパンの**開始位置**の一致だけで、**終端の一致は独立した前提**である〔`#` コメントを扱わず文字列リテラルの終端に改行を含めなかったため、コメント内に `"` を1つ置くだけで `system(…)` を文字列の中身として飲み込ませることができた。実機 gawk で削除を確認〕。したがって除去規則は開始条件と終端条件を対にして規定し〔文字列は未エスケープの `"` で終端・**改行に達したら未終端**〔awk の文字列は行をまたげない〕・`\`＋次の1文字は終端判定より**先に**対として消費する〔gawk の文字列内行継続と一致させるため〕／正規表現は未エスケープの `/` で終端・スパンが `;` か改行を含むなら正規表現と認めない・**直前の非空白トークンが「閉じた正規表現」なら正規表現の開始と認めない**〔awk ではその位置の `/` は必ず除算。文字集合ではなくトークンの出自で見るのは、`prev` に残る `/` が「除算演算子の直後」からも来るためで、集合へ `/` を足すと現に deny している形が allow へ転じる。改行は文の終端なのでこの状態はリセットする。開始条件を誤ると `awk 'BEGIN{/x/ / system("rm SPEC.md") / 1}'` のように**実行コードが丸ごとリテラルとして消えて** allow になり、**この経路は「除算として据え置く」分岐を通らないため下記の曖昧ガードが一度も発火しない**〕／コメントは改行または EOF まで〔改行は文終端として残す〕〕、**どちらの解釈も成り立つ位置〔正規表現か除算か確定できない位置〕では除去せず「判定不能」として deny する**〔除去しすぎ＝allow 方向（危険）／除去しなさすぎ＝deny 方向（安全）という方向の非対称があるため〕。**この主張が破れる形は「除去スパンの終端が awk と食い違う入力」と「除去スパンの開始が awk と食い違う入力」であり、検出器は `tests/test_guard_bash_writes.py` の T-C9a〜h・T-C10a／b・T-AW7h と設計書 §4-9 の V-15〔実機 gawk との二方向突き合わせ〕である。**`system()`／`system (`／`@load`／`@include`／間接関数呼び出し `@f(…)`〔gawk 4.1.2 以降は関数名を文字列変数から供給できるため `system(` のリテラルが現れないまま `system()` を呼べる〕／`print \| "cmd"`／`"cmd" \| getline`／コプロセス `\|&` はいずれもこの3条件に掛かる。**未知の文字・未知の関数名は自動的に deny 側へ落ちる**ため、gawk に新しい実行経路が加わっても収載しない限りすり抜けない。ファイル名オペランド・変数束縛値・リテラルの中身は awk が実行しないため判定対象にしない）**・**sort の `-o` / `--output=`**・**uniq の第2位置引数**・tee・インタプリタワンライナー・find -exec・ヒアドキュメント・コマンド置換/プロセス置換の内側による検証迂回をベストエフォートで遮断する（パス非リテラルの迂回は Stop のドリフト検知が捕捉。SPEC.md 宛て〔プロジェクト直下のみ〕にも KLK-016 でドリフト検知のバックストップが追加された）。判定は3レイヤ: **L1 字句**（クォート状態を追跡してクォート外の演算子だけを切り出し、語は `shlex.split` でクォート解除。正規表現内の `\|` `<<` `>` `#` を特殊構文と誤認しない。**行継続〔`\`＋改行〕は bash と同じ条件でのみ削除して前後の語を連結する** — 削除するのは①バックスラッシュ自身がエスケープされていない〔連が奇数個で終わる〕②`#` コメントの内側でない③`'…'`〔`$'…'` を含む〕の内側でない、をすべて満たすときだけで、`"…"` の内側では bash と同じく連結する。**「安全側の近似」は存在しない**（連結しすぎるとステートメント区切りという証拠を失い〔許可コマンドの後ろに `\\`＋改行 を足すだけで後続コマンドの head が前の語へ吸収されホワイトリスト判定が無効化される〕、連結しなさすぎると語の同一性という証拠を失う〔`rm tickets/acti\`＋改行＋`ve/APP-001.md` のパスが2語に割れる〕）。正規化は `find_violation` の入口で1回だけ行い、正規化後の同じ文字列を字句解析と degraded 判定の双方へ渡す。コマンド置換・プロセス置換の内側は無加工で残し、再帰評価の入口で**内側の文脈として**改めて正規化する〔ダブルクォート内のコマンド置換の中では bash がコマンド文脈へ戻り `#` がコメントになるため〕。`#` はこの正規化走査の中だけでモデル化し〔クォート外で語頭に現れたときにコメントを開始し改行で終わる〕、**用途は行継続の適用可否の判定に限る。コメント本文は除去しない**〔除去は deny → allow 方向の変化を生むため。相違の向きは deny 側＝保守的〕。**クォート外の `$'…'`／`$"…"` は「判定不能」として印を付ける** — bash はこの2形を展開時にデコード／翻訳するため hook が見る字面とコマンドが実際に受け取る文字列が一致せず〔`$'…\x22…'` の `\x22` は実際には `"` になり `\x7c` は `\|` になる〕、ホワイトリストでもブラックリストでも判定できない。印の付いたステートメントは保護対象への言及または保護対象 cwd が成立した時点で無条件に deny する〔証拠が無ければ従来どおり allow〕。判定は head 非依存で degraded 経路にも同じ規則を置く）／**L2 パス**（テキストからパス候補を抽出し `os.path.normpath` で正規化してから `.claude` 成分を除外。トークンに埋め込まれたリテラルパスを検出し、パストラバーサルによる除外の悪用を防ぐ）／**L3 構文**（証拠ベース。出力リダイレクトはステートメント単位で判定し、リダイレクト先は保護対象パスか同一コマンド内で保護対象を代入された変数のときのみ deny）。コマンド置換は内側方向（置換の中身が書き込み）と外側方向（置換の結果が外側の書き込み先・引数になる。`rm $(echo docs/SPEC.md)`・`echo x > $(…)`）の両方を評価する。`cd` は作業ディレクトリを追跡し、保護対象配下へ移動した後の書き込みを捕捉する（`cd` 自体は常に許可。`cd "$DIR"` / `cd $(…)` は追跡不能）。cwd と結合するのは**構文的に書き込み先と確定したオペランド**（リダイレクト先・`sort -o` の値・`uniq` の第2位置引数）のみで（`cd tickets/active && rm APP-001.md`・`… && sort -o APP-001.md in.md`）、オペランドを取り出せない形（awk の `print > "REL"`・sed の `w REL`）は構文の存在で deny する。保護対象 cwd 下で許可する先頭コマンドは `CWD_SAFE_HEADS` の明示列挙で、`ALLOWED_HEADS` へ足すだけでは cwd 下の許可を得られない（棚卸し忘れが deny 側へ落ちる既定）。**捕捉できない形**: cwd と相対パスの両方に接頭辞が分割された形（`cd tickets/active && cd .. && rm active/APP-001.md`）。**意図的な許可**: `mv`（active/ ↔ done/ の移動が Bash `mv` の正規手段であるため cwd 下でも許可）。字句解析できない入力（未閉じクォート・未閉じバッククォート・未閉じ `$(`・末尾の孤立エスケープの5系統。`don't` のような英文コメントでも成立する）は allow せず、旧版と同じ素朴な分割（ステートメント／パイプ／セグメント）による簡易判定へ落とす。**H6修正（KLK-014）**: 閉じているコマンド置換（`$(...)`）／バッククォート／プロセス置換の境界は `_extract_degraded_substs` が機械的に抽出し、内側テキストを既存の再帰評価（`subst_violation`／`find_violation`。正常経路と同じ機構）へ無条件で回すため、置換の**内側**にある書き込み（`rm`／`sed -i`／`tee`等）は degraded でも検出される（閉じていない置換は従来どおり素の文字列のまま簡易判定に残る）（この経路ではクォート内の `\|` を誤ってパイプと解釈する誤denyが旧版と同じく残る。**ステートメント区切りには `&&`・`\|\|`・`;`・改行に加えて単独の `&` も含む** — 正常経路が `&` を区切りとして扱うのに degraded だけが扱わないと、` #'` の2文字を足すだけで `&` の後段が先頭 head のセグメントへ埋もれて全防御が消えるため。その結果「クォート内の `&`」も同じクラスの誤denyになる〔`grep 'R&D' tickets/… #'`〕）。保護対象の判定は `_ticket_lib.is_ticket`（厳密判定）とは目的が異なるため**意図的に別基準**（詳細はモジュール docstring）。例外: 読み取り専用と確認済みのスクリプト（`READONLY_SCRIPTS`。SPEC構造チェッカー）を `python3 <スクリプトパス> <引数>` の形で実行するセグメントは許可。**既知の未対応**（いずれも本 hook の導入当初から遮断できていない・別チケット候補）: `sed -f SCRIPT`・`sort --compress-program=PROG`・`git diff --output=FILE`・パスがリテラルに現れない形（変数・置換の出力・断片の結合）・**シェル展開による保護対象パスの分断**（ブレース展開 `rm tickets/{active,done}/APP-001.md`／パス中に `*` が入る glob `rm tickets/acti*e/APP-001.md`／パラメータ展開 `f=…; rm $f` の引数側。正しく閉じるにはブレース展開の実装が要り、`PATHISH_RE` を単純に拡張すると awk のプログラム語が保護対象と誤判定されうる。`tickets/active/*.md` のように保護対象ディレクトリが素で残る形は従来どおり deny。判定は `INVENTORY_CASES` の INV-SH-07・09・11 に `allow` として登録し**穴をコード上で可視化**してある）。sed/awkのプログラム本文に対する言及ゲートは、バックスラッシュエスケープを畳み込んだ字面でも判定する（KLK-029）。**degraded mode（字句解析できない入力）にはこの緩和が及ばない**——`degraded_violation`は独立した文処理を持ち`statement_violation`を経由しないため、字句解析に失敗する形へ意図的に落とし込んだ場合のエスケープ回避は残存する（本チケットのUnknown・§6 R-U1参照）。**保守側へ倒している判定**（誤deny方向。保護対象に言及する／保護対象を cwd とする場合に限る）: **sed のプログラム本文がホワイトリスト（SED-7）の許可文字集合を満たさない形**（`r`/`R`・`a`/`i`/`c`・`b`/`t`/`T`/`:`・GNU拡張`z`/`F`/`v`・`s`/`y`の`i`/`I`/`m`/`M`フラグ・その他未収載の1文字コマンド。詳細は `docs/designs/KLK-015.md` §6 R1）・mawk/BWK awk 固有のオプション（`-W …`）と gawk の未収載オプション（`-I`・`--trace`・`-O`・`-L`）・awk のプログラム本文がホワイトリストの3条件を満たさない形（`@namespace` 指令／収載していない組み込み・拡張関数の呼び出し／`\|` を伴う形／リテラル**外**のバックスラッシュ・バッククォート・シングルクォート・非ASCII文字／`{print a (b)}` のような `IDENT (` の連結／プログラムが `$(…)` 由来でシェル引用符を含む形／**字句を確定できない形**〔未終端の文字列リテラル〔EOF・生の改行に達する形。後者は awk 自身が fatal error にする〕・**除算と判定した `/` と同じ行に除去対象があり、その位置以降にも `/` が残る形**〔`awk '{print $1/2}  # 割合 a/b'`・`awk '{if ($1/$2 > 0.5 && /foo/) print}'`。awk 側の解釈で実行に到達しうる曖昧位置であるため判定不能として deny する。回避策はコメントから `/` を外す〕〕。**コメント〔`#` 以降〕に `@`・`\|`・`system(` を含むだけの形は deny しない**〔awk はコメントを実行しないため deny する根拠が無い。第7版で誤denyを解消した〕。deny 理由文が原因の文字・関数名を示すので、`cat`／`grep` の単独使用や保護対象パスを引数に取らない形へ回避できる。なお `PROCINFO["sorted_in"]="cmp"` のような文字列の受け渡しやリテラル内の `a@b (…)`・ファイル名 `system(1).md` は判定対象外＝allow）・**クォート外に `$'…'` を含む形**（`awk -F$'\t' …`・`grep $'\t' …`・`sed $'s/\t/ /' …`。**エスケープを使わない等価な書き方が存在する** — `awk -F'\t'`〔awk 自身がエスケープを解釈する〕・`grep -P '\t'`・リテラルのタブを書く。deny 理由文に回避策を載せている。なお `"$(printf '\t')"` は `printf` が意図的にホワイトリスト非収載のため使えない）・同じく `$"…"` を含む形・**degraded mode では引用符の内側に `$'`／`$"` の2文字並びがあるだけで deny**（クォート状態を追えないため部分文字列判定＝保守側）・ANSI-C 復号で保護対象パスが現れる語を含む形・**`"…"` の内側の行継続で連結された結果、保護対象に見える語ができる形**〔bash も同じ語を作るため誤denyではない〕・**保護対象 cwd 下で degraded へ落ちた置換の内側**が `CWD_SAFE_HEADS` 未収載 head を含む形／`$'`・`$"` の2文字並びを含む形／`<<` を含む形〔いずれも正常経路が既に同じ判定を行っており、degraded との非対称を解消する方向〕・**`#` コメントの本文を除去しない**ことによる相違〔コメント内の `> path`・`rm` を証拠として拾う。除去は deny → allow 方向の変化を生む〔`cat a #> docs/SPEC.md` が allow になる〕ため行わない。コメント内の未閉じクォート〔`#'` 等〕が degraded 経路を誘発する性質も残る〕。**degraded mode（字句解析できない入力）にはこのホワイトリストが及ばず**、`system(`／`@load`／`@include`／`@f(` のブラックリスト（語単位＋空白結合テキスト）で見る。ホワイトリスト（`ALLOWED_HEADS`）へコマンドを追加するときは `docs/designs/KLK-010.md` §3-9 の書き込みベクタ棚卸し表へ行を追加し、**4次元**（書き込みオプション／外部コード読み込みオプション／内部構文／位置引数の出力先）で経路を列挙し、`CWD_SAFE_HEADS` の収載可否まで判定してから追加すること（手順の詳細はモジュール docstring「既知の限界」末尾の5手順） |
| `_ticket_lib.py` | -（共有） | frontmatterパーサと検証ルール（status enum・必須キー・遷移表・retry上限）を集約。加えて hook 横断の共有ヘルパを提供する: チケットパス判定（`is_ticket`。パス正規化のうえ絶対形・相対形の双方を受理）・hook 入力の読み取りと型正規化（`read_hook_payload` / `as_dict`）・PreToolUse の decision 出力（`emit_pretooluse_decision`）・サイドカー/イベントログ読み込み（`load_state` / `load_events` / `group_events_by_ticket`） |

メトリクスの集計は `.claude/metrics/aggregate.py`（`/metrics` コマンドが実行）が担う。

## メトリクスの運用

- `tickets/.metrics.jsonl`・`tickets/.metrics_state.json`・`docs/.spec_state.json` は hook が自動生成する。**LLM・エージェントは直接編集しない**（サイドカーの直接編集はドリフト検知の基準を壊す）
- `docs/.spec_state.json` はプロジェクト直下の `docs/SPEC.md` 1件のみを対象とする（ネストした SPEC.md は対象外。`guard_spec_writes.py` の basename一致判定とは意図的に基準を分ける。理由は `docs/designs/KLK-016.md` §3 D3 を参照）
- これらのファイルはデフォルトで `.gitignore` 対象（環境固有のため）。チームで履歴を共有したい場合は `.gitignore` の該当行を解除する
- 記録されるのは「ステータス遷移」のみ。ログ追記や related_files 更新など状態を変えない編集は記録しない

## 設計方針

- **fail-open**: hook内部のエラー（IO・パース不能など）ではループをブロックしない。検知した違反のみ deny する。
- **依存なし**: Python3標準ライブラリのみ。
- **単一の真実**: 検証ルールは `_ticket_lib.py` に集約。CLAUDE.md のステータス定義・orchestrator.md「リトライカウンタ管理」のリトライ上限と定数を同期させること。
- **無限ループ防止**: Stop hook は `stop_hook_active` が真のとき再ブロックしない。

## ローカルでの動作確認

```bash
# PreToolUse: 不正な状態遷移を deny できるか
printf '{"tool_name":"Edit","tool_input":{"file_path":"/abs/path/tickets/active/APP-001.md","old_string":"status: design_done","new_string":"status: done"}}' \
  | python3 .claude/hooks/validate_ticket_state.py

# Stop: active配下の不整合を検出できるか
printf '{"hook_event_name":"Stop","cwd":"'"$PWD"'","stop_hook_active":false}' \
  | python3 .claude/hooks/check_loop_integrity.py
```

exit 0 かつ無出力なら allow、JSON を出力すれば block。

> 注意: 上記のようなペイロードにチケットパスを含むコマンドは、Claude Code の
> Bash ツール経由では `guard_bash_writes.py` に deny される（printf が
> ホワイトリスト外のため）。動作確認は人間のターミナルで直接実行するか、
> `tests/` の自動テストを使うこと。

自動テストは `tests/` にある（`python3 -m unittest discover -s tests -v`）。検証ロジックを変更したら必ず実行すること。

## 注意

- `retry_counts` を持たない旧チケットは検証エラーになる。既存チケットには frontmatter へ `retry_counts` を追加すること
- hook を一時的に無効化したい場合は `.claude/settings.json` の `hooks` セクションを編集する

## ルールを変更するとき

1. `_ticket_lib.py` の定数（`VALID_STATUS` / `REQUIRED_KEYS` / `RETRY_CAPS` / `LEGAL_TRANSITIONS`）を更新する
2. 対応する定義（CLAUDE.md ステータス定義・orchestrator.md リトライカウンタ管理）も合わせて更新する
3. `tests/` のテストを更新し、`python3 -m unittest discover -s tests -v` で全件パスを確認する
