---
ticket: "KLK-010"
title: "guard_bash_writesの誤denyとすり抜け解消（字句解析のshlex化）"
spec_version: "なし（docs/SPEC.md 不在。Kit自体の改善のため KLK-001〜009 の前例に従う）"
created: "2026-08-14"
updated: "2026-08-15"
revision: "第6版（C7／C7-2／C8／H4 の解消＝行構造の近似を bash の字句規則へ一致させる）"
---

# [KLK-010] guard_bash_writesの誤denyとすり抜け解消（字句解析のshlex化） 設計

> このファイルはarchitectが生成・更新するチケット単位の設計書です。
> チケット KLK-010 に対応します。implementer・tester・reviewerが参照します。

**受け入れ条件の番号付け:** チケット本文のチェックボックスを出現順に **AC1〜AC7** と呼ぶ。以降すべてこの番号で記述する。

| 本設計書 | 要旨 |
|---|---|
| AC1 | コマンド解析をシェルの字句解析を尊重する方式へ改める。クォート内の `\|` `<<` `>` `#` で分割・特殊構文判定をしない |
| AC2 | 再現手順 A-1〜A-3（A-1 / A-2 / A-3a / A-3b / A-3c）がすべて allow される |
| AC3 | `ALLOWED_HEADS` に書き込み経路を持たないコマンド（`cd`・`pwd`・`awk`・`cut`・`tr`・`echo` 等）を追加。`cd` は無条件許可 |
| AC4 | 再現手順 B-4〜B-6 が deny される（sed in-place 全表記・`$(...)`/バッククォートの再帰評価・`normpath` 後の除外判定） |
| AC5 | インタプリタワンライナー内のリテラルパス（B-5）が deny される。検出を空白トークン分割に依存させない |
| AC6 | 既存の deny 対象（素の `sed -i`・リダイレクト・`tee`・`find -exec`・heredoc・`READONLY_SCRIPTS` 例外）を回帰させない |
| AC7 | `tests/test_guard_bash_writes.py` に A・B 各ケースの回帰テストを追加し、既存（スイート全体293件）と合わせて全件パス |

---

## 0. 本改訂の要旨（第6版・行継続とコメントを bash の字句規則へ一致させ、degraded の関数レベルゲートを cwd 対応にする）

**改訂理由:** 第5版（Phase 10・11・実装済み）の **QT-1（判定不能→deny）と AW-7 は正面からは突破されなかった**（reviewer 3回目: C1／C2／H1／C3／C4／H2 の閉塞を再確認、既存ドライバの範囲で分類外の WEAKER 行は0件）。しかし**第5版が自ら入れた QT-2（`lex()` 冒頭の `command.replace("\\\n", "")`）が新しい穴を開けた**。reviewer が独自に攻めて Critical 2件を検出し、orchestrator が独立に再実測して7件の回帰を確認した。

| 攻め口（reviewer 3回目） | 結果 |
|---|---|
| 既存ドライバ（`cmp`〜`cmp5`・`rv1`〜`rv7`・`probe`・`v13`）の全再実行 | **分類外の WEAKER 行は0件**（第5版の主張どおり） |
| **行継続の除去（QT-2）× bash のステートメント区切り** | **Critical（C7）— 任意の読み取りコマンドに2文字足すだけで hook 全体が無効化される** |
| **`degraded_violation` の関数レベルゲート × 保護対象 cwd** | **Critical（C8）— 置換の内側を1つ挟むだけで全判定を回避できる** |

**C7（本改訂で塞ぐ対象その1）— 「連結しすぎ」による区切りの喪失:**

bash が `\`+改行を行継続として扱うのは、**バックスラッシュが自身エスケープされていないとき**だけである。第5版の実装は**無条件に削除**するため、bash が「本物のコマンド区切り」として扱う次の2条件で**区切りを飲み込む**。

| 条件 | bash の解釈 | 第5版の hook | 帰結 |
|---|---|---|---|
| バックスラッシュ**奇数**個（`\`+LF） | 行継続＝連結 | 連結 | **一致**（allow が正しい） |
| バックスラッシュ**偶数**個（`\\`+LF） | リテラルの `\` ＋改行＝**区切り** | 連結 | **相違＝後続コマンドの head が前の語へ吸収される** |
| **`#` コメント末尾**のバックスラッシュ | コメント内で `\` は特別扱いされず改行は**区切り** | 連結 | **相違＝同上** |

`ALLOWED_HEADS` 判定に使われる head が先頭の許可コマンドになるため、**head が許可コマンドなら何でも成立する**（`echo`/`ls`/`cat`/`pwd`/`cd`/`grep`/`awk`/`sed`/`cut` で実測。書き込み側も `rm`/`sed -i`/`truncate`/`cp`/リダイレクトいずれも可）。実機で `docs/SPEC.md` の削除と `sed -i` の実行を確認済み。**構造的に初回レビューの C2（` #'` 1つで全判定を回避）と同型の「接尾辞1つで防御が消える」欠陥**である。

| コマンド（`<LF>` は生の改行） | old | new（第5版） | 第6版 | 実機 bash |
|---|---|---|---|---|
| `echo x\\<LF>rm docs/SPEC.md`（2個＝偶数） | deny | **allow** | deny | SPEC.md を削除 |
| `ls \\<LF>rm docs/SPEC.md` | deny | **allow** | deny | 削除 |
| `cat README.md #\<LF>sed -i 's/orig/pwned/' tickets/active/APP-001.md` | deny | **allow** | deny | in-place 書き換え実行 |
| `echo x\\\\<LF>rm docs/SPEC.md`（4個＝偶数） | deny | **allow** | deny | 削除 |
| `echo x\<LF>rm docs/SPEC.md`（1個＝奇数） | deny | allow | **allow のまま維持** | 実行されない（連結される）＝**allow が正しい** |
| `cat "$(ls #\<LF>rm docs/SPEC.md)"`（ダブルクォート内の置換＋コメント） | deny | **allow** | deny | 削除 |

**⚠️ C7 の修正は二方向の要求である。** 最終行（奇数個）は **allow のまま維持しなければならない**（bash も連結するため deny は誤り）。過剰deny側へ倒すと AC2 の趣旨（読み取り操作を誤denyしない）を壊し、V-12 の誤deny予算の列挙外になる。本改訂は §3-2 の **LX-1／LX-2** でこの境界を明文化し、**T-C7c（奇数形の allow 固定）と T-C7a／T-C7b（偶数・コメント形の deny 固定）を対で置く**。

**C8（本改訂で塞ぐ対象その2）— 関数レベルゲートの cwd 欠落:**

`degraded_violation()` の1行目 `if not mentions_guarded(command.split()): return None` に `cwd_is_guarded(cwd)` が入っていない。**同関数のステートメント単位のゲートは正しく cwd を見ているのに、その手前の関数レベルのゲートで早期 return する。** 到達経路は `find_violation` → `subst_violation` → `find_violation(inner, depth+1, cwd)` → `lex(inner)` が `ValueError` → `degraded_violation(inner, cwd)`。**`_take_backtick()` はクォート状態を追わない**（bash も追わない＝仕様どおり）ため、バッククォート内側に未閉じクォートを置くと**外側の `lex()` は成功したまま内側だけが degraded へ落ちる**。

| コマンド | old | new（第5版） | 第6版 |
|---|---|---|---|
| `` cd tickets/active && ls `rm APP-001.md #'` `` | deny | **allow**（実機で削除） | deny |
| `` cd tickets/active && ls `echo x > APP-001.md #'` `` | deny | **allow**（実機で上書き） | deny |
| `` cd tickets/active && ls `sed -i 's/a/b/' APP-001.md #'` `` | deny | **allow** | deny |
| `` cd tickets/active && ls `find . -delete #'` `` | deny | **allow** | deny |
| `cd tickets/active && ls $(rm APP-001.md)`（対照） | deny | deny | deny |

**根因はいずれも設計にある。**

| 指摘 | 設計側の欠落箇所 | 本改訂での対処 |
|---|---|---|
| **C7**（Critical・reviewer 実機実証・orchestrator 独立再現） | §4-2 の疑似コードが `replace("\\\n", "")` を明示し、さらに**「連結は部分文字列としての証拠を失わせないため、証拠は増える方向のみ」という誤った安全性の主張**を添えていた（同じ文言が実装 docstring にも転記された） | **LX-1／LX-2**: 行継続の正規化を**専用の走査 `normalize_line_continuations()` へ置き換える**（バックスラッシュの対消費で偶奇が自動的に決まる／クォート外の語頭 `#` 以降は行継続を適用しない／`'…'` の内側では連結しない）。`lex()` から2つの `replace` を**両方とも撤去**し、改行の解釈を走査の中で行う（§3-2・§4-2） |
| **C8**（Critical・同上） | §4-4(h) の疑似コードが同一の欠落を持つ（**C2・H2 に続き同じ関数で3度目**） | **1行修正**: `if not (cwd_is_guarded(cwd) or mentions_guarded(command.split())): return None`（§3-5・§4-4(h)）。あわせて**同関数の全ゲートを cwd 観点で棚卸しし、cwd を見ないゲートが1つも残っていないことを設計に明記**する |
| **H4**（High・構造的な根） | 正常経路（`lex()` が結合後を見る）と degraded 経路（結合前の生の `command` を受け取る）が**異なる行構造を見ていた**。これが C7 と tester 報告の「偶発的な安全網」（M13）の**両方の根** | **正規化を `find_violation` の入口で1回だけ行い、正規化後の同じ文字列を `lex()` と `degraded_violation()` の双方へ渡す**（§4-4(i)）。M13 が同時に解消され、当該入力は degraded でも `$'` が隣接して `ANSI_QUOTE_RE` に一致し**意図された理由で deny になる** |
| **H3**（High・維持機構の欠陥） | §3-9-2 は「行継続」を**1行**として持ち、V-14(5) も `語内・語間・'…' 内側` の3形しか挙げていなかった。**bash の挙動が真逆になるサブケース（エスケープされた `\`・`#` コメント末尾）が列挙から漏れていた**＝V-14 の合否条件そのものへの違反 | **原則 P7 を新設**（棚卸し表の各行に「**bash の挙動が分岐する条件**」を明示列挙し、分岐ごとに INV ID と期待判定を持たせる）。§3-9-2 の「行継続」行を**6サブケースへ分解**し、**`#` コメントの行を新設**する（§3-1・§3-9-2・§3-11・V-14(5)(7)） |
| **SV-14**（原則 P2 の趣旨違反） | P2「フォールバックは旧版の上位互換」が **degraded にしか適用されておらず**、正常経路では旧版の「**改行では必ず分割する**」性質が申告なく落ちていた | **P2 の適用範囲を正常経路へ拡張**する（§3-1）。正常経路が旧版の判定要素を落とすときは、§9 での申告と V-14 の実測を伴わなければならない |
| **M14**（誤った安全性の主張） | §4-2・docstring の「証拠は増える方向のみ」 | **誤りとして訂正**（R4 の訂正と同じ扱い）。実態は「**行継続の近似は bash の字句規則と一致していなければ両方向へ倒れる**」であり、§6 **R32** として新規リスクに登録する |
| **M15**（`#` の未モデル化） | `#` コメントが字句レイヤで全くモデル化されておらず、**行継続処理と組み合わさった瞬間に allow 方向へ反転する**ことが未記載 | 正規化走査でのみ `#` をモデル化し（**行継続の適用可否の判定に限る**）、`lex()`／degraded がコメントを除去しないこと（＝コメント本文を語として走査する＝deny 方向の保守的な相違）を**§3-9-2 の新しい行と §6 R35 に限界として明記**する |
| **M13**（tester の「偶発的な安全網」） | reviewer は**許容しないと判定**。H4 の解で明示規則へ置き換わる | H4 の正規化により当該入力は `ANSI_QUOTE_RE` で deny になる。**tester の回帰ガードは残す**（理由文字列を assert していない現在の形が適切） |
| **M18**（保守性） | `lex()` 冒頭の2つの `replace` が最も壊れやすい（文字列置換で bash の字句規則を近似する唯一の箇所で、正規化とスキャンが分離しているため文脈を一切考慮できない） | 2つの `replace` を**撤去**し、行構造の近似を `normalize_line_continuations()` **1箇所**へ集約する。あわせて `_take_subst`／`_take_backtick` を**共通の走査ヘルパ `_skip_balanced`／`_skip_backtick` の上に再実装**して重複を減らす（§4-2） |

**本改訂で受け入れ条件（AC1〜AC7）は1文字も変更していない。** 弱体化・再定義もしていない。判定の変化はほぼ allow → deny 方向であり、deny → allow 方向は次の2クラスに限る（いずれも **bash が実際には保護対象を触らない**形＝§4-9 の許容分類8・9）。

1. 第5版から引き継ぐ分類8（`docs/SPEC.md\`＋改行＋`.bak`）
2. **第6版で新設する分類9** — `'…'` の内側の `\`+改行を bash と同じくリテラルとして扱う結果、連結後の語が保護対象でなくなる形（**old=allow のため AC6 の回帰ではない**）

**Phase 1〜11 は実装済みのため再実装せず、Phase 12・13 を既存実装への差分**として定義する（§4-8）。

---

### 0-1. 第5版の要旨（履歴・`$'...'` のデコード相違を「判定不能」として扱う＝L1 字句レイヤの穴を閉じる）

**改訂理由:** 第4版（Phase 8・9・実装済み）の **AW-7（プログラム形のホワイトリスト）は正面からは突破されなかった**が、**その外側にある L1 字句レイヤの近似**によって全面的にバイパスされた（tester が実機実証・orchestrator が独立再現）。

| 攻め口（tester の探索） | 結果 |
|---|---|
| `awk_strip_literals` を騙す（エスケープ `\"` `\\` `\/`・文字クラス `[/]` `["]`・空正規表現 `//`・除算曖昧性 `a/b/c`・後置 `++` 直後の `/`） | **すべて意図どおり deny**（S1 健全性は維持） |
| 許可文字集合の抜け（`ENVIRON`・`getline <`・`PROCINFO`・`gsub`/`sub`/`split`） | 書き込み・実行への到達なし |
| `AWK_SAFE_CALLS` 収載37件の副作用（実機検証） | 副作用なし |
| `awk_program_texts` の抽出漏れ（`-e` 併用・`--source` の2形・`--` 区切り） | 漏れなし |
| degraded での AW-7 回避 | 穴なし（後述のとおり**この結論は `$'...'` を含む入力には当てはまらない**） |
| **`$'...'`（ANSI-C 引用）の hex エスケープ** | **唯一の実行可能なバイパス（Critical）** |

**C6（本改訂で塞ぐ対象）— 2箇所の独立した近似の相互作用:**

1. **`lex()` は `$'...'` を素の `'...'` と同一に扱い、エスケープを解釈しない**（L1 字句レイヤ）。
2. `awk_strip_literals()` は文字列リテラル内の `\X`（バックスラッシュ＋任意の1文字）を「1個のエスケープ済み文字」として読み飛ばす（AW-7 の一部・**awk の字句規則とは一致している**＝§3-6 D10）。

実際の bash は `$'...'` 内の `\x22` を `"` へ、`\x7c` を `|` へ**デコードする**。hook から見ると全体が1つの巨大な文字列リテラル（`""` へ丸ごと除去され安全）に見えるが、**bash がデコードするとその位置で文字列が途中で閉じ、`system(...)` や `"cmd" | getline` が実行コードとして残る**。

| コマンド | old | new（第4版） | 第5版 |
|---|---|---|---|
| `awk $'BEGIN{x="foo\x22system(\x22rm tickets/active/APP-001.md\x22)\x22"}'` | deny | **allow** | deny |
| 同・`docs/SPEC.md` 宛て（**バックストップ無し**） | allow | **allow** | deny |
| `cd tickets/active && awk $'…system(…rm APP-001.md…)…'` | deny | **allow** | deny |
| `awk $'BEGIN{x="foo\x22(\x22rm tickets/…\x22\x7cgetline x)\x22"}'`（**C5 再開通**） | deny | **allow** | deny |
| 同・cwd 版 | deny | **allow** | deny |

**実機実証済み（tester・使い捨てディレクトリ）:** `gawk $'BEGIN{x="foo\x22system(\x22rm F\x22)\x22"}'` が EXIT 0 で F を実際に削除する。**規則を1つも変えずに C5（入力パイプ getline）を再び開通させる。**

**第4版が持っていた誤った前提（本改訂で訂正する・R4）:** 設計書 §6 R4 と docstring は「`$'...'` は shlex が bash と同一には解釈しないが、**語として1トークンに収まるため判定は保守側（deny 方向）へ倒れる**」と明記していた。**本ケースでこの主張は成立しない**（allow 方向へ倒れ、かつ実機で実行される）。既存の唯一の関連テスト `test_ansi_c_quoted_path_with_write_head_deny` はエスケープを含まない `$'tickets/active/APP-001.md'` しか検証しておらず、**hex エスケープによるデコード相違は一度も検証されていなかった**。**「文書化された前提が実測で裏付けられていなかった」ことが本ラウンドの根因であり、原則 P6（§3-1）と検証項目 V-14（§4-9）で構造的に対処する。**

**人間の決定（2026-08-15 11:51・チケットのブロッカーセクションとログに記録済み）:** 修正方針は tester 提示の第3案「**`$'...'` を含むコマンドを degraded 相当（ホワイトリスト非適用のブラックリスト）へフォールバックさせる**」。選定理由は (a) 最小の変更 (b) **deny 方向のみ** (c) **既存の原則 P4（未知は保守側へ倒す）と一致** (d) `$'...'` は日常操作でほぼ使われないため誤denyの実害が小さい、の4点。

**設計側の重要な訂正（透明性のための明記・§3-6 D9）:** 本設計は「degraded 相当へのフォールバック」を**そのままの形では採用しない**。**degraded 経路を語単位でトレースした結果、C5 再開通形（上表4・5行目）が degraded でも allow のままになることが判明した**（`\x7c` はコマンド文字列上に `|` として現れないため `degraded_violation` の `statement.split("|")` がパイプ分割せず、後段 head が `getline` にならない＝第4版が「degraded に C5 の穴は無い」と論じた根拠がこの入力では成立しない。全段のトレースは §3-6 D9）。**符号化された入力をブラックリストへ回すのは、AW-7 をホワイトリストへ反転した理由（ブラックリストは列挙外の形を素通しする）と同じ失敗の繰り返しになる。** したがって本設計は、人間が挙げた選定理由 (a)〜(d) をすべて満たし**かつ穴を閉じる**次の形を採る:

> **QT-1: クォート外の `$'…'`／`$"…"` を含むステートメントは「判定不能」として扱い、保護対象への言及または保護対象 cwd が成立する時点で無条件に deny する**（ホワイトリストにもブラックリストにも掛けない）。

**この変更は人間の決定より deny 方向へ強いだけで、緩くなる箇所は1つも無い。** 判断の差分は orchestrator 経由で人間へ報告する（§10）。

| 指摘 | 設計側の欠落箇所 | 本改訂での対処 |
|---|---|---|
| **C6**（Critical・tester 実機実証・orchestrator 独立再現） | L1 字句レイヤが bash の `$'...'` デコードを再現せず、かつ「1トークンに収まるので保守側」という**未検証の前提**を設計・docstring に記載していた | **QT-1**（判定不能→deny・§3-2・§3-6 D9・§4-2・**§4-4(g)**）＋ **QT-3**（証拠収集に限った ANSI-C 復号・§4-3）。degraded にも同じ規則を置く（§3-5・§4-4(h)） |
| **同種の穴（棚卸し）** | §3-9 の棚卸し表は「head × 書き込みベクタ」の4次元だけを見ており、**シェル展開の次元（bash が解釈するがコマンド文字列上は見えない変換）を1行も持っていなかった** | **§3-9-2「シェル展開の次元」を新設**（`$'…'`／`$"…"`／行継続／ブレース展開／glob／チルダ／パラメータ展開／算術展開／履歴展開）。相違を生むものは規則化（QT-1・QT-2）し、生むが本チケットのスコープ外のものは **`INVENTORY_CASES` へ期待値 `allow` で登録**して可視化する（P5・§3-11 の INV-SH-01〜12） |
| **行継続の畳み込み（QT-2）** | `lex()` は `\`+改行を**空白へ**置換していたが、bash は**削除して語を連結する**。`rm tickets/acti\`＋改行＋`ve/APP-001.md` は bash では保護対象への `rm` になるが、hook では語が割れて言及ゲートが偽になる（old=allow・同じ「証拠の分断」クラス） | **QT-2**: 削除（連結）へ訂正する（§3-2・§4-2）。**⚠️ 第5版がここに書いた「1文字の変更で bash と一致し、証拠は増える方向にしか動かない」は誤りであった（M14・第6版で訂正）** — 無条件の削除は bash が区切りとする位置でも連結してしまい、**ステートメント区切りという証拠を失う**（C7）。正しい対処は §3-2-1 の LX-1〜LX-5 |
| **R4 の誤った限界記述** | 設計 §6 R4・docstring の「`$'...'` は保守側へ倒れる」が誤り | 実態（**allow 方向へ倒れ実機で実行される**）へ訂正し、QT-1 による対処を記載（§6 R4・Phase 11） |
| **設計内部の矛盾（implementer 報告）** | §4-7 のコードブロックが `xyzzy(` の `LEGACY_DENY_COMMANDS` 追記を明示する一方、同節の維持規律4 は「誤deny側は本リストへ入れない」と定めている | **維持規律4 の判定基準を「old=deny か否か」に明確化して解消**する（`awk 'BEGIN{xyzzy("rm docs/SPEC.md")}'` は旧版が `awk` を `ALLOWED_HEADS` に持たないため **old=deny** であり、収載は規律に反しない）。**コード変更は不要**（§4-7） |

**本改訂で受け入れ条件（AC1〜AC7）は1文字も変更していない。** 弱体化・再定義もしていない（判定の変化はすべて allow → deny 方向。唯一の例外は QT-2 に伴う `docs/SPEC.md\`＋改行＋`.bak` 形の allow 化であり、これは**bash が実際には保護対象を触らない**形に対する誤denyの解消である＝§4-9 の許容分類8）。

**Phase 1〜9 は実装済みのため再実装せず、Phase 10・11 を既存実装への差分**として定義する（§4-8）。

---

### 0-2. 第4版の要旨（履歴・awk のプログラム内構文次元をブラックリストからホワイトリストへ反転）

**改訂理由:** 第3版（Phase 6・7・実装済み）に対し、**awk のプログラム内構文の次元で4ラウンド連続して穴が出た**。

| 回 | 検出内容 | 検出者 | 第3版時点の規則 |
|---|---|---|---|
| 1 | H1（awk 変数代入形 `-v f=path '{print > f}'`） | reviewer | AW-2 を新設して閉塞 |
| 2 | C4（`system("rm …")`） | reviewer | AW-5 を新設して閉塞 |
| 3 | `@f()` 間接関数呼び出し（`f="system"` を代入して間接呼び出し） | tester | AW-6 を追加して閉塞（設計書未反映） |
| 4 | **`"cmd" \| getline` / `"cmd" \|& getline`（コプロセス）** | tester | **未閉塞**（列挙されていたのに規則化されていなかった） |

他の次元は止まっている。**オプション次元**は第3版の AW-4（`AWK_SAFE_FLAGS` に無い `-` 始まりの語は未知＝deny）で漏れがゼロになり、**cwd 次元**は `CWD_SAFE_HEADS` でフォールセーフ化された。**この次元だけがブラックリスト（AW-1／AW-3／AW-5／AW-6）のまま**であり、第3版が自ら **R23「プログラム内構文の次元には機械的な既定が無い」**として申告していた弱点がそのまま4回目の穴になった。

さらに、第3版 §3-9 の棚卸し表は `cmd | getline` と `|&` を**列挙していた**が、対応する規則が実装されていなかった。**表に書いても実装とテストへ写像が無ければ防御にならない**ことが実証された（§3-11 で構造的に対処する）。

**人間の決定（2026-08-14 18:42・チケットのブロッカーセクションとログに記録済み）:** プログラム内構文の次元を**ブラックリストからホワイトリストへ反転する**。保護対象に言及する／保護対象を cwd とする awk は原則 deny とし、**安全と確認できる狭いプログラム形のみ allow** する。オプション次元で漏れを止めたのと同じ手を、この次元へ適用する。**この反転が誤denyを増やすことは人間が認識のうえで選択している**（許容範囲の定義は architect へ委ねられた＝§3-6 D8・§4-9 V-12）。

| 指摘 | 設計側の欠落箇所 | 本改訂での対処 |
|---|---|---|
| **C5**（Critical・tester 実機実証） | §3-6 D1 の AW-3（`AWK_OUTPUT_RE`）は `print`/`printf` の**直後**の `>`／`\|` しか見ておらず、`print` を伴わない**入力方向**のパイプ（`"cmd" \| getline`・`\|& getline`）に一致しない。AW-1 は「保護対象に言及する語自身が `>`／`\|` を含む」形にしか発火せず、**cwd 保護シナリオ（リテラルパスが現れない）では証拠が立たない** | **AW-7 を新設（プログラム形のホワイトリスト）。** プログラム本文からリテラル（文字列・正規表現）を1パスで除去し、(1) 許可文字集合（`@` と `\|` を**含めない**） (2) 呼び出し名のホワイトリスト（`system` を含めない・未知名は deny） (3) `print`/`printf` の出力構文の不在 の3条件を満たす形だけを allow する（§3-6 D1・D8・§4-4(d)） |
| **M11**（Medium・誤deny・tester 実測） | AW-6（`@\s*IDENT\s*\(`）を**文字列リテラル除去前**の生テキストへ当てるため、`awk 'BEGIN{print "contact: a@b (see docs)"}' T` の**出力メッセージ中の `a@b (`** が一致する | AW-7 は**リテラル除去後**に判定するため解消する。AW-5／AW-6 は degraded mode 専用へ縮小する（§3-6 D1・§4-5） |
| **M12**（Medium・誤deny・tester 実測・Phase 6 から存在） | AW-5（`\bsystem\s*\(`）を awk の**全引数**へ当てるため、プログラムと無関係な**ファイル名オペランド** `'system(1).md'` が一致する | AW-7 は**プログラム本文の語だけ**を検査する（`awk_program_texts`）。ファイル名・変数束縛値は検査対象外になり構造的に解消する（§4-4(d)） |
| **SV-12**（棚卸し表の実効性） | §3-9 の表は `cmd \| getline`・`\|&` を列挙していたが、規則もテストも存在しなかった | **§3-11 を新設**し、棚卸し表の各構文に「規則ID・`INVENTORY_CASES` の ID・期待判定」の写像を必須にする（原則 **P5**）。写像が無いセルは欠陥とする |
| **M9-2**（設計書と実装の乖離） | AW-6（`AWK_INDIRECT_CALL_RE`・結合テキスト評価）・`head_index` 一元化以降の実装が設計書に未反映（implementer 報告の10項目） | §3-6 D1・§3-9・§4-1・§4-4(d)・§4-5・§4-6・§4-7・§4-8・§4-9・§6 へ全件反映（本改訂）|

**本改訂で受け入れ条件（AC1〜AC7）は変更していない。** チケット本文のチェックボックスは1文字も書き換えていない。ただし**設計側の言い換え（§9）は deny 方向へ強める**（透明性のための明記）:

- 第3版 §9 の AC3 elaboration は「`awk`・`sed`・`sort`・`uniq` も `ALLOWED_HEADS` に残る（**書き込み形のみを deny する**）」と書いていた。第4版では awk について「**安全と確認できるプログラム形以外を deny する**（保護対象に言及する／保護対象を cwd とする場合に限る）」へ改める。`awk` が `ALLOWED_HEADS` に残るという AC3 の要求自体は満たしている。
- チケット「期待する動作」が名指しする読み取り形（`grep`/`awk`/`cut`/`tr`/`echo`、正規表現に `|`・`<<`・`>` を含むもの）は**すべて allow のまま**であることを §4-5 のトレースと V-12 で固定する。
- **新たに deny になる読み取り形は §6 R26 に漏れなく列挙する。**列挙外の新規 deny が1件でも出た場合はブロッカーとして報告する（V-12(d)）。

**Phase 1〜7 は実装済みのため再実装せず、Phase 8・9 を既存実装への差分**として定義する（§4-8）。

---

### 0-3. 第3版の要旨（履歴・reviewer 2回目の rejected を受けた設計改訂）

**改訂理由:** 第2版（Phase 4・5・実装済み・テスト358件全pass）に対し、reviewer が **AC6 に抵触する新規 false negative をさらに3系統（C3／C4／H2＝実ペイロード14形）** 検出して rejected とした。第2版で対処した C1／C2／H1／M1／M4-2／M4-5 は reviewer 自身の再実測で**閉塞が確認済み**である。今回の3件も第2版と同じく、実装ミスではなく**本設計書の規則集合の不足**に起因する（implementer は設計に忠実に実装している）。

| 指摘 | 設計側の欠落箇所 | 本改訂での対処 |
|---|---|---|
| **C3** (Critical) | §3-6 D1 の AW-1〜AW-3 が awk の**オプション次元**を一切見ていない。`awk -i inplace`（gawk の in-place 拡張）は `>` も `\|` も変数束縛も使わずに in-place 書き込みする＝**AC6 が名指しする `sed -i` の awk 版** | **AW-4 を新設**。awk のオプションを**ホワイトリスト化**し、未知・危険なオプションを deny（§3-6 D1・§4-4(d)） |
| **C4** (Critical) | 同 AW-1〜AW-3 が awk の**外部コマンド実行次元**を見ていない。`system("rm …")` は `>`／`\|` を含まないため AW-1 に掛からない | **AW-5 を新設**（`system(` / `@load` / `@include`）（§3-6 D1・§4-4(d)） |
| **H2** (High) | §3-8 が「言及ゲートを真にする」だけで、**書き込み先オペランドを cwd で解決する規定**を持たなかった。加えて §4-4(i) の置換再帰へ `cwd` を渡していない | (1) AW-3・sed `w`・sort `-o`・uniq 出力引数の判定を **cwd 対応**にする (2) **置換再帰へ cwd を伝播**する (3) 書き込み先オペランドの解決を単一ヘルパ `guarded_write_target` へ集約し `cwd_redirect_violation` を吸収（§3-8・§4-4(b)(c)(d)(e)(i)） |
| **SV-7**（最も重い指摘） | §3-9 の棚卸し表（原則 P3・H1 の再発防止のために新設した機構）**自体が同じ抜け方をした**（`awk` 行に `-i inplace`・`system()` が無く、`cd` 行に「相対パス書き込み全般」が無い） | §3-9 を**4次元 × head の走査結果**として全面改訂し、列挙の根拠と未対応を明記。**列挙の完全性に依存しない第2の防御を2つ導入**（awk オプションのホワイトリスト＝未知は deny／`CWD_SAFE_HEADS` 未収載 head は保護対象 cwd 下で deny＝**フォールセーフ既定**）。原則 **P4** として §3-1 に規定（§3-10） |

**あわせて対処する reviewer 指摘:** M5（`cd ..` 部分接頭辞＝文書化のみ）・M6（`mv` の意図的許可の未文書）・M7（CHANGELOG のテスト件数のずれ）・M8（README/docstring の過大主張）・**M9（設計書と実装の乖離＝逸脱7点の反映）**・M10（degraded の二重実装面）。

**本改訂で受け入れ条件（AC1〜AC7）は一切変更していない。** 弱体化・再定義もしていない（判定の変化はすべて allow → deny 方向）。**Phase 1〜5 は実装済みのため再実装せず、Phase 6・7 を既存実装への差分**として定義する（§4-8）。

**§9 AC6 の言い換えの精度について（透明性のための明記）:** 第2版 §9 は AC6 の elaboration として「`cd` + 相対パス」と書いていた。第3版ではこれを「**cwd が保護対象配下と解決できる形**」へ限定する。M5（`cd tickets/active && cd .. && rm active/APP-001.md`）は reviewer 自身が「一般解は誤deny爆発を招くため実装ではなく限界として明記が妥当」と判定した形であり、§3-8・§3-9・docstring に**未閉塞の限界**として記載する。これは**チケット本文の AC6（対象を素の `sed -i`・リダイレクト・`tee`・`find -exec`・heredoc・`READONLY_SCRIPTS` 例外と名指しする）を変更するものではなく**、設計側の言い換えの精度を上げるものである。

---

## 1. 目的

対応するSPEC ID: **なし**（本プロジェクトは kenesis-loop-kit 自体の改善であり `docs/SPEC.md` は存在しない。SPEC不在のまま進行することは KLK-001〜009 と同じ前例に従い、人間承認済みの前提として扱う）。

`.claude/hooks/guard_bash_writes.py` の字句解析を「素朴な文字列分割」から「クォート境界を尊重する走査 + `shlex.split` による語のクォート解除」の2層へ置き換え、**同一根因の表裏である誤deny（A）とすり抜け（B）を一体で解消する**。

**第3版の追加目的:** 「旧版（develop）が deny していたコマンドは新版でも deny される」という不変条件を、**個別ベクタの列挙に依存しない形で**担保する。すなわち、
1. 新設された false negative（C3／C4／H2）をゼロにする、
2. かつ**同じ抜け方が3度起きない構造**（未知の入力が deny 側へ落ちる既定＝原則 P4）を導入する。

**第4版の追加目的:** awk の**プログラム内構文の次元**を、規則を積み増すブラックリストから**ホワイトリスト（未知の構文は deny）へ反転**し、この次元にも機械的な既定を持たせる。すなわち、
1. 未閉塞の C5（`"cmd" | getline`・`|& getline`）をゼロにする、
2. かつ**5回目の穴が「未知の構文」として自動的に deny 側へ落ちる構造**にする（原則 P4 の3つ目の既定）、
3. かつ**棚卸し表の各セルが実装とテストへ写像を持つこと**を機械的に担保する（原則 P5）、
4. その代償として増える誤denyの許容範囲を設計側で明示し、検証項目（V-12）として固定する。

**第5版の追加目的:** L3（構文）レイヤの防御が **L1（字句）レイヤの近似によって迂回されない**ようにする。すなわち、
1. C6（`$'...'` の hex エスケープによる AW-7 全面バイパス）をゼロにする、
2. かつ**「bash が解釈するがコマンド文字列上は見えない変換」を1つの次元として棚卸しし**、相違を生むものは規則化するか `INVENTORY_CASES` へ `allow` として登録して可視化する（§3-9・§3-11）、
3. かつ**設計・docstring に書いた前提を実測で裏付ける手順を持つ**（原則 P6・V-14）。**本ラウンドの根因は「未検証の前提を『保守側へ倒れる』と書いていたこと」であり、これを構造的に断つ。**

**第6版の追加目的:** L1（字句）レイヤが**行の構造**（どこがステートメント区切りで、どこが継続か）について bash と一致することを保証する。すなわち、
1. C7（行継続の除去がステートメント区切りを飲み込む）・C8（degraded の関数レベルゲートが cwd を見ない）をゼロにする、
2. かつ**正常経路と degraded 経路が同一の行構造を見る**ようにする（H4。第5版は両者が別の文字列を見ており、それが C7 と M13 の共通の根だった）、
3. かつ**棚卸し表の各行を「bash の挙動が分岐する条件」まで分解する規律**を持つ（原則 P7・H3／M17。**第5版は「行継続」という行を持っていたのにサブケースが分解されておらず、V-14 が合格してしまった**）、
4. かつ**原則 P2（旧版の判定要素を落とさない）を正常経路にも適用**する（SV-14。第5版は degraded にしか適用しておらず、正常経路で旧版の「改行では必ず分割する」性質が申告なく落ちていた）。

## 2. 現状

### 2-1. 旧版（develop・merge-base `72ae730`）の性質 — 「old」の定義

```
main()
 └ mentions_guarded(command)            ← command.split() の各トークンに is_guarded_token
      ├ False → allow
      └ True  → find_violation(command)
           ① REDIRECT_RE.finditer(command)   ② "<<" in command
           ③ STATEMENT_RE.split → statement.split("|") → segment_violation
                head_of / ALLOWED_HEADS（cd・awk 不在）/ sed -i 3表記 / find / git
```

**第3版で重要な旧版の性質（H2 の「old=deny」の理由）:** 旧版は `cd` も `awk` も `ALLOWED_HEADS` に持たない。したがって

- `cd tickets/active` を含むコマンド文字列は、**後続に何が来ても** `cd` セグメントの時点で deny された。
- `awk` を含むコマンドは、保護対象に言及すれば**すべて** deny された。

つまり H2 の11形・C3 の5形・C4 の4形が「old=deny」なのは、旧版がそれらを**検出していたからではなく、コマンドごと拒否していたから**である。この事実は §3-8 の設計判断（保護対象 cwd 下では保守側に倒してよい）と、§4-9 V-9 の WEAKER 行分類の根拠になる。

### 2-2. 現ブランチ（`fix/KLK-010-guard-bash-writes-shlex`・**Phase 1〜9 実装済み**）のフロー

```
main()
 └ find_violation(command, depth=0, cwd="")
      ├ lex(command) → (tokens, substs)          ← L1。ValueError なら degraded_violation
      │   ↑ ここが C6 の根因。`$'...'` を素の `'...'` と同一に扱い、
      │     bash の ANSI-C デコード（\x22→" / \x7c→|）を再現しない。
      │     行継続 `\`+改行を「空白へ置換」しており bash（削除して連結）と異なる
      └ split_statements(tokens) → 各ステートメント
           statement_subst_indexes → subst_violation（その文の cwd で再帰）
           record_assignments(statement, guarded_vars, substs)
           statement_violation(statement, guarded_vars, substs, cwd)
             ① redirect_violation()（guarded_write_target で cwd 相対も解決）
             ② 言及ゲート（cwd_is_guarded で無条件真）
             ③ heredoc
             ④ pipe_segments() → segment_violation(words, substs, cwd)
                  head_index/head_of → ALLOWED_HEADS → CWD_SAFE_HEADS（フォールセーフ）
                    → sed（-i 全表記 / w の strict）/ awk / sort -o / uniq / find / git
                       awk_violation(words, cwd):
                         AW-1（語が保護対象＋`>`/`|`）→ AW-2（束縛）→ ゲート
                         → 語ごとに AW-4（オプションのホワイトリスト）
                                    AW-5（system( / @load / @include）
                                    AW-6（@ident( 間接呼び出し）
                                    AW-3（print/printf → > >> |）
                         → 結合テキストへ AW-5／AW-6（degraded の語跨ぎ対策）
                                                     ↑ ここまでが**ブラックリスト**。
                                                       `"cmd" | getline` / `|& getline`
                                                       がどの規則にも掛からない（C5 根因）
           cwd = next_cwd(segments[0], cwd)
      degraded_violation(command, cwd)  ← 旧版同等（P2）＋ cwd 追跡
```

**C5 の根因（関数レベル）:** `AWK_OUTPUT_RE = \b(?:print|printf)\b[^;}\n]*(?:>>?|\|)` は `print`/`printf` を起点にしか一致せず、`"cmd" | getline`（入力方向のパイプ＝gawk は子プロセスを起動する）には構造的に一致しない。AW-1 は「保護対象に**言及する語**が `>`／`|` を含む」形にしか発火しないため、**リテラルパスを書いた形（`awk 'BEGIN{"rm tickets/active/APP-001.md" | getline}'`）は偶然 deny になるが、cwd 保護シナリオ（`cd tickets/active && awk 'BEGIN{"rm APP-001.md" | getline x}'`）は証拠が立たず allow になる**。この非対称性が cwd 追跡機構全体の弱点である（tester 実測）。

### 2-3. 未解決の欠陥

**第6版で塞ぐ対象（reviewer 3回目・実機実証／orchestrator が独立に7件を再実測）:**

| ID | 症状（実測） | 根因（関数レベル） | old / new |
|---|---|---|---|
| **C7** | `echo x\\`＋改行＋`rm docs/SPEC.md`（バックスラッシュ**偶数**個）／`cat README.md #\`＋改行＋`sed -i 's/orig/pwned/' tickets/active/APP-001.md`（**`#` コメント末尾**）。head は `echo`/`ls`/`cat`/`pwd`/`cd`/`grep`/`awk`/`sed`/`cut` のいずれでも成立し、書き込み側も `rm`/`sed -i`/`truncate`/`cp`/リダイレクトいずれでも成立する | **`lex()`** 冒頭の `command.replace("\\\n", "")`（第5版の QT-2）が**無条件に**削除するため、bash が区切りとして扱う2条件（バックスラッシュが自身エスケープされている／コメント内）でも連結し、**後続コマンドの head が前の語へ吸収されて `ALLOWED_HEADS` 判定に使われる head が先頭の許可コマンドになる** | deny → **allow**（`docs/SPEC.md` 宛ても含む） |
| **C7-2**（同型・同時に閉じる） | `cat "$(ls #\`＋改行＋`rm docs/SPEC.md)"`（ダブルクォート内のコマンド置換にコメント） | 同上。加えて**正規化が置換の内側にも一律に適用されていた**ため、内側を再帰評価する前に区切りが失われる | deny → **allow** |
| **C8** | `` cd tickets/active && ls `rm APP-001.md #'` `` ほか（`echo x > REL`／`sed -i`／`find . -delete`／`cp`／`truncate`／`python3 -c`） | **`degraded_violation()`** の**関数レベルゲート** `if not mentions_guarded(command.split())` に `cwd_is_guarded(cwd)` が無い。同関数のステートメント単位のゲートは cwd を見ているのに手前で早期 return する。到達経路は `find_violation` → `subst_violation` → `find_violation(inner, depth+1, cwd)` → `lex(inner)` の `ValueError` → `degraded_violation(inner, cwd)`。**`_take_backtick()` はクォート状態を追わない**（bash と同じ仕様）ため、バッククォート内側に未閉じクォートを置くと外側の `lex()` は成功したまま内側だけが degraded へ落ちる | deny → **allow** |
| **H4**（構造的な根） | 正常経路は「結合後」、degraded は「結合前の生の `command`」を見ており、**同じ入力に対して異なる行構造**を評価していた | `lex()` が前処理として結合し、`degraded_violation()` は `find_violation` から**未加工の `command`** を受け取る | C7 と M13（tester の「偶発的な安全網」）の共通の根 |
| **H3**（維持機構の欠陥） | §3-9-2 の「行継続」行がサブケース分解されておらず、**bash の挙動が真逆になる2条件が列挙から漏れたまま V-14 が合格した** | 棚卸し表の行の粒度を人間（architect）が決めており、**「表の行が現実の構文空間を尽くしているか」を保証する規律が無かった**（M17） | － |
| **M13** | `awk $\`＋改行＋`'BEGIN{…}' #'` が「**偶発的な安全網**」（head_of が引用符付きの語を未知の head として扱う）でのみ deny になっていた | 同じく H4（degraded が結合前の文字列を見て `$` と `'` が別ピースへ分かれ、`ANSI_QUOTE_RE` に一致しない） | deny（ただし意図しない理由） |

**第5版で塞いだ対象（tester 実測・実機実証／orchestrator が独立再現）:**

| ID | 症状（実測） | 根因（関数レベル） | old / new |
|---|---|---|---|
| **C6** | `awk $'BEGIN{x="foo\x22system(\x22rm tickets/active/APP-001.md\x22)\x22"}'` と `\x7cgetline` 版（**C5 の再開通**）。`tickets/` 宛て・`docs/SPEC.md` 宛て・cwd 版の**5形**。実機 gawk が EXIT 0 でファイルを削除することを tester が実証 | **`lex()`** が `$'...'` のエスケープを解釈しないため、bash がデコードした後のプログラム（文字列が途中で閉じ `system(` や `\|` が実行コードになる）を hook が見られない。`awk_strip_literals()` は awk の字句規則には**一致している**が、bash デコード前のテキストを受け取るため巨大な1文字列リテラルとして丸ごと除去する | deny → **allow**（`docs/SPEC.md` 宛ての1形のみ old=allow） |
| **C6-2**（同型・本改訂で同時に閉じる） | `sed $'s/a/b/\x77 tickets/active/APP-001.md' in.md`（`\x77` は `w`＝sed のファイル書き出しコマンド） | 同上。`SED_WRITE_RE` はデコード前のテキストに `w` が現れないため不一致 | allow → **deny**（old=allow・D6-2(a) の同一機構による同時閉塞） |
| **SH-1**（同型・棚卸しで発見） | `rm tickets/acti\`＋改行＋`ve/APP-001.md`（行継続で語を分断） | `lex()` が `\`+改行を**空白へ**置換するため語が割れ、言及ゲートが偽になる。bash は**削除して連結**する | allow → **deny**（old=allow・QT-2 で閉じる） |
| **SH-2**（同型・棚卸しで発見・**本チケットではスコープ外**） | `rm tickets/{active,done}/APP-001.md`（ブレース展開）／`rm tickets/acti*e/APP-001.md`（glob） | `PATHISH_RE` が `{` `}` `,` `*` を含まないため保護対象パスが再構成されない | allow → allow（**未対応**。`INVENTORY_CASES` へ `allow` で登録し可視化＝§3-11 INV-SH-07・09） |

**第4版で塞いだ対象（tester 実測・orchestrator が独立再現）:**

| ID | 症状（実測） | 根因（関数レベル） | old / new |
|---|---|---|---|
| **C5** | `cd tickets/active && awk 'BEGIN{"rm APP-001.md" \| getline x}'` と `\|& getline`（gawk コプロセス）。tester が実機 gawk で**実際に相対パスのファイルを削除できる**ことを実証。orchestrator も独立に2形を再実測 | `AWK_OUTPUT_RE`（AW-3）が `print`/`printf` 起点にしか一致しない。AW-1 は語単位の証拠に依存し cwd シナリオでは発火しない（§2-2 末尾） | deny → **allow** |
| **M11** | `awk 'BEGIN{print "contact: a@b (see docs)"}' tickets/active/APP-001.md` が誤deny | `AWK_INDIRECT_CALL_RE`（AW-6）を**文字列リテラル除去前**の語へ当てるため、出力文字列中の `a@b (` が一致する（意図的な保守側判断だったが過剰） | deny → deny（**AC2 が解消を求める側**） |
| **M12** | `awk '{print}' 'system(1).md' tickets/active/APP-001.md` が誤deny | `AWK_EXEC_RE`（AW-5）を awk の**全引数**へ当てるため、プログラムと無関係なファイル名オペランドが一致する（結合テキスト評価とは無関係な単一語レベルの過剰一致。**Phase 6 から存在**し AW-6 固有ではない） | deny → deny（**AC2 が解消を求める側**） |
| **SV-12** | §3-9 の棚卸し表が `cmd \| getline`・`\|&` を列挙していたのに規則・テストが存在しなかった | 表と実装・テストの間に写像が無い（P3 の限界。P5 で対処） | － |
| **M9-2** | AW-6（`AWK_INDIRECT_CALL_RE`・結合テキスト評価）と `head_index` 一元化以降の実装が設計書に未反映（implementer 報告の10項目） | 設計書のみ | － |

**第3版で塞いだ対象（reviewer 実測・orchestrator が14形を独立再現・tester／implementer が閉塞を再実測済み）:**

| ID | 症状（実測） | 根因（関数レベル） | old / new |
|---|---|---|---|
| **C3** | `awk -i inplace '{gsub(/a/,"b")}1' tickets/active/APP-001.md` ほか4表記（`-i inplace` / `-iinplace` / `--include inplace` / `--include=inplace`）＋degraded 版。`tickets/` と `docs/SPEC.md` の双方で成立。reviewer が GNU Awk 5.2.1 で実ファイルの in-place 書き換えを実証 | `awk_violation` の AW-1〜AW-3 が**オプション語を一切見ない**。gawk の `-i`／`--include` は awk ソースライブラリの読み込みであり、`inplace` ライブラリは in-place 書き込みを行う | deny → **allow** |
| **C4** | `awk 'BEGIN{system("rm …")}'` / `'{system(…)}'` / `'END{system(…)}'`。`rm`/`cp`/`truncate`/`mv` を使えば `>`／`\|` を含まないため AW-1 に掛からない | 同じく AW-1〜AW-3 が**外部コマンド実行構文を見ない** | deny → **allow** |
| **H2** | 保護対象を cwd とした相対パス書き込み11形 — awk 4形（`print > "REL"`・`printf >> "REL"`・`-v f=REL`・位置引数 `f=REL`）／sed `w REL` 2形（正常経路・degraded）／`sort -o REL`／`uniq in REL`／置換内3形（`ls $(rm REL)`・バッククォート・`<(rm REL)`） | (1) `awk_violation`・sed `w` 判定・sort/uniq が `cwd` を受け取らず、保護対象の**認識がリテラルパス依存**のまま (2) `sort -o`／`uniq` 第2位置引数は判定規則が存在しない (3) `find_violation` の置換再帰が `cwd` を伝播しない | deny → **allow** |
| **M5** | `cd tickets/active && cd .. && rm active/APP-001.md`（cwd 接頭辞＋相対パスの分割） | cwd が `tickets` へ戻るため `cwd_is_guarded` が偽になり、`active/APP-001.md` 単体は保護対象パターンに一致しない | deny → **allow**（reviewer 判定: **文書化のみが妥当**。一般解は `cd . && rm foo` を deny する誤deny爆発を招く） |
| **M6** | `cd tickets/active && mv /tmp/e APP-001.md` | `mv` を §3-9 が意図的に許可している（active/↔done/ 移動が Bash `mv` の正規手段）。**意図どおりだが未文書** | deny → allow（意図的） |
| M7 | CHANGELOG のテスト件数が実測とずれ（記載「62件追加・91件・355件」→ 実測 **65件追加・94件・358件**） | 文書のみ | － |
| M8 | README/docstring が実装より強い保護を主張（「awk のプログラム内リダイレクトを遮断」「cd 後の相対パス書き込みを捕捉する」が成立していない）。`inplace`・`gawk` が設計書・docstring・README・テストのいずれにも1度も出現しない | 文書のみ | － |
| **M9** | 設計書と実装の乖離（implementer の逸脱7点のうち**5点は設計書への反映が必須**と reviewer が判定）。特に §4-1 の「重要（最長一致の順序）」注記**自体が誤り** | 設計書のみ | － |
| M10 | degraded の二重実装面が Phase 4 で拡大（cwd 追跡と `cwd_redirect_violation` も複製） | `degraded_violation` が独自に cwd を追跡する | － |
| **旧版から存在（本チケットが開けた穴ではない）** | `sed -f SCRIPT`／GNU sed の `e` フラグ（**任意コマンド実行**）／`sort --compress-program=PROG`（外部プログラム実行）／`git diff --output=FILE`／`is_readonly_script_call` の許可経路偽装（`python3 x=y {READONLY_SCRIPT} docs/SPEC.md`＝**任意コード実行**） | いずれも `ALLOWED_HEADS`／`ALLOWED_GIT_SUBCOMMANDS`／`READONLY_SCRIPTS` 収載の引数だけで書き込み／実行できる経路 | allow → allow |

### 2-4. 関連する既存ファイル

- `.claude/hooks/guard_bash_writes.py`（**本チケットで変更する唯一の実装ファイル**）
- `tests/test_guard_bash_writes.py`（**Phase 12 着手時点でスイート全体 405件・全パス**＝reviewer 3回目の実測。全ケースがブラックボックス＝サブプロセス起動 + stdin ペイロード投入。**件数は implementer が着手時に再実測して実装メモへ記録する**＝D3）
- `tests/_util.py`（`GUARD_BASH` パスと `run_script` / `hook_output`。**変更不要**）
- `.claude/hooks/_ticket_lib.py`（`emit_pretooluse_decision` のみ import。**変更しない** — §3-6 D2）
- `.claude/hooks/README.md`／`CHANGELOG.md`（Phase 11 まで整合済み。**Phase 13 で LX-1〜LX-5・C8・M14 の訂正・M15 の限界を反映**）
- `.claude/settings.json`（`permissions.allow` と `ALLOWED_HEADS` は別レイヤ。**変更しない**）

**前提（README.md「設計方針」より）:** 本hookは **fail-open**・**依存なし（標準ライブラリのみ）**・**ベストエフォート**。パスをリテラルに含まない迂回は `check_loop_integrity.py`（Stop）のドリフト検知がバックストップ。**ただし `docs/SPEC.md` にはバックストップが無い**（ドリフト検知は `tickets/active|done/*.md` のみ走査）。

## 3. 設計方針

### 3-1. 採用する方針: 責務を3レイヤに分離する（初版から不変）+ 原則 P1〜P6

| レイヤ | 責務 | 実現手段 |
|---|---|---|
| **L1 字句** | クォート境界を尊重して演算子と語を分ける。コマンド置換の抽出と帰属付け | 自前の走査（クォート状態のみ追跡）＋ 語チャンクは `shlex.split` |
| **L2 パス** | 任意のテキストから保護対象パスを検出する | 候補抽出 → `os.path.normpath` → `.claude` 除外 → 分類 |
| **L3 構文** | ステートメント／パイプ／リダイレクト／**書き込み先オペランド**／cwd の単位で書き込みベクタを判定する | L1のトークン列に対する規則適用 |

**不変条件（原則）— 同じ抜け方を繰り返さないための規約:**

- **P1「証拠は失わない」** — L1 がテキストを `substs` へ退避するときは、退避先を元の位置から一意に引ける識別子を残す。L3 の判定は「語トークンそのもの」ではなく「語トークンを解決したテキスト」に対して行う。（C1 の再発防止・第2版で導入・維持）
- **P2「旧版の判定要素を落とすときは1点ずつ申告する」（第2版で導入・第6版で適用範囲を正常経路へ拡張）** — degraded mode は旧版が持っていた判定要素をすべて持つ。1つでも落としてはならない。（C2 の再発防止）
  - **第6版の拡張（SV-14 への対処）:** **この原則は degraded だけのものではない。** 正常経路（L1〜L3）が旧版の判定要素を落とすときも、**落とす要素を §9 で1点ずつ申告し、落とした結果が bash の挙動と一致することを V-14 の実測で裏付けなければならない**。
  - **破れた実例:** 旧版は `LEGACY_STATEMENT_RE` により「**改行では必ず分割する**」性質を持っていた。第5版の正常経路は QT-2（行継続の無条件削除）でこの性質を**申告なく**落とし、それが C7 になった（reviewer SV-14）。第6版はこの性質を「bash が区切りとして扱う条件では必ず分割する」形で回復する（§3-2 LX-1／LX-2）。
  - **旧版の判定要素の一覧（正常経路がどう扱っているかを明記する）:** ① `LEGACY_STATEMENT_RE` の無条件の改行分割 → **第6版で bash 一致の形へ回復**（LX-2） ② `statement.split("\|")` の素朴なパイプ分割 → 意図的に落とした（AC1 の要求そのもの・§9 で申告済み） ③ `"<<" in command` の部分文字列判定 → 意図的に落とした（AC2 A-3b の要求・§9 で申告済み） ④ `REDIRECT_RE` のコマンド全体走査 → 意図的に落とした（AC2 A-3c の要求・§9 で申告済み）。**この4点以外に落としている要素は無い**（第6版で棚卸し）。
- **P3「ホワイトリスト追加には書き込みベクタ棚卸しを伴う」** — `ALLOWED_HEADS` にコマンドを足すときは §3-9 の棚卸し表へ行を追加し、**4次元**（§3-10）で経路を列挙する。（H1 の再発防止・第2版で導入）
- **P4「列挙が漏れても deny 側へ落ちる既定を持つ」（第3版で新設・第4版で3つ目を追加）** — **P3 は人間が「気づく」ことに依存しており、C3／C4 で実際に破れた。** したがって、列挙の完全性に依存しない機械的な既定を、**危険が入りうる次元ごとに1つ**持たなければならない。本設計は次の3つを持つ。
  1. **awk のオプションはホワイトリスト**（`is_safe_awk_flag`）。既知の読み取り専用オプションに一致しない語は deny。新しい gawk オプションが増えても、収載しない限り自動的に deny 側へ落ちる。
  2. **`CWD_SAFE_HEADS`（明示列挙）**。保護対象ディレクトリを cwd とする状況では、この集合に**明示的に収載されていない** head を deny する。`ALLOWED_HEADS` へ head を足しただけでは cwd 下の許可は得られない（＝忘れると deny になる）。
  3. **awk のプログラム本文はホワイトリスト**（`awk_program_violation`＝**AW-7**・第4版で新設）。リテラル除去後のプログラムが「許可文字集合」「呼び出し名の許可集合」「`print`/`printf` 出力構文の不在」の3条件を満たさなければ deny。**未知の構文文字・未知の関数名は自動的に deny 側へ落ちる**ため、gawk に新しい実行経路が加わっても、設計者が経路を1つ見落としても、収載しない限りすり抜けない。
  - **第3版が「プログラムテキストの次元はホワイトリスト化できない」と書いたのは誤りであった**（§3-7 で訂正）。ホワイトリスト化できないのは「awk プログラム言語**全体**を安全構文の集合として列挙すること」であり、**「読み取り用途で実際に必要な狭い形」を列挙することは可能**である（根拠の列挙は §3-6 D8・§4-5）。
  - なお「保護対象に言及するセグメントでホワイトリスト済みコマンドが『疑わしい構文』を含めば deny」という**文字レベルの包括規則**は引き続き**採用しない**（§3-7 の却下理由。`NR>1` や `-F'\|'` を誤denyし、AC2 に抵触することが H1 で実測済み）。AW-7 が包括規則と異なるのは (a) 適用対象を**プログラム本文の語だけ**に限る (b) 判定を**リテラル除去後**に行う (c) `>` は「`print`/`printf` より後に現れる」順序条件でのみ deny する、の3点である。
- **P6「前提は実測で裏付ける」（第5版で新設）** — **設計書・docstring に「〜だから判定は保守側（deny 方向）へ倒れる」と書くときは、その主張を実測で裏付け、対応する固定テストを置く。** 裏付けが取れていない前提は「**未検証**」と明記し、`INVENTORY_CASES` へ期待判定つきで登録する。
  - **R4（`$'...'` は1トークンに収まるので保守側へ倒れる）は、裏付けの無い前提がそのまま6ラウンド目の穴（C6）になった実例である。** 既存テストは `$'tickets/active/APP-001.md'`（エスケープ無し）しか置いておらず、主張の核心（**エスケープを含む場合の挙動**）を1度も検証していなかった。
  - 実行手順は次元ごとに固定する: オプション次元＝**V-10**（`awk --help` との突き合わせ）／プログラム内構文次元＝**V-11**（実機実行と hook 判定の突き合わせ）／**字句レイヤ＝V-14（第5版で新設。bash が実際にコマンドへ渡す argv と `lex()` の語トークンを突き合わせる）**。
  - 「保守側へ倒れる」と書けない（＝実測で裏付けられない）ときは、**その入力クラスを「判定不能」として扱い証拠が立つ時点で deny する**（QT-1 がこの形）。
- **P5「棚卸し表の各セルは実装とテストへの写像を持つ」（第4版で新設）** — **P3 の表は「書いてあるのに実装されていない」状態を許し、C5 で実際にそれが起きた**（`cmd | getline`・`|&` は §3-9 に列挙されていたが規則もテストも無かった）。したがって §3-9 に列挙した各構文は、**規則ID**（AW-x 等）と **`INVENTORY_CASES` の ID**（判定を固定するテストケース）を必ず持つ。写像を持たないセルは**欠陥**として扱う（§3-11）。「未対応（別チケット）」の構文も `INVENTORY_CASES` へ `allow` として登録し、**穴がコード上で可視になる**ようにする。
- **P7「棚卸し表の行は『bash の挙動が分岐する条件』まで分解する」（第6版で新設・H3／M17 への対処）** — **P5 は「表の行 → 実装」の写像は保証するが、「表の行が現実の構文空間を尽くしているか」は保証しない**（reviewer M17）。C7 は §3-9-2 に「行継続」という行が**存在していたのに**サブケースが分解されていなかったために出た。したがって §3-9／§3-9-2 の各行は次を必ず持つ。
  1. **分岐条件の明示列挙** — その構文について **bash の挙動が変わる条件**を列挙する（行継続なら「バックスラッシュ連の偶奇」「コメント内かどうか」「`'…'`／`"…"` の内側かどうか」）。**「条件を1つも書けない」行は『未検証』として扱い、V-14 の対象へ入れる。**
  2. **分岐ごとの INV ID と期待判定** — 分岐が n 通りあれば `INVENTORY_CASES` に n 件以上を置く。**分岐のうち1つでも INV ID を持たない行を残してはならない。**
  3. **実測での裏付け（P6 との接続）** — 分岐条件は「こう解釈されるはず」ではなく、V-14 の実機観測（bash の argv・実行効果）で確定させる。
  - **この規律の適用対象は「hook が bash の字句規則を近似している箇所」すべて**であり、第6版時点では §3-9-2（シェル展開の次元）が該当する。**新しい近似を入れるときは、その近似の分岐条件を先に列挙してから実装する。**

### 3-2. L1: 字句レイヤ（第5版で2点追加＝C6 の修正本体／**第6版で行構造の近似を全面的に置き換え＝C7・H4 の修正本体**）

0. **正規化（第6版で新設・`find_violation` の入口で1回だけ実行する）**: **`normalize_line_continuations()`** が bash と同じ条件でのみ `\`+改行を削除する。**正規化後の同じ文字列を `lex()` と `degraded_violation()` の双方へ渡す**（H4）。
1. **走査**: 通常／`'` 内／`"` 内の状態を追跡し、**クォート外の演算子だけ**を最長一致で切り出す。**残った改行は走査の中でステートメント区切り（`("op", ";")`）として扱う**（第6版。第5版までの `command.replace("\n", ";")` を撤去）。`$(` / バッククォート / `<(` / `>(` は内側テキストを `substs[i]` へ退避し、**インデックス付きプレースホルダ `__KLK_SUBST_{i}__`** を置く。
2. **クォート外の `$'` / `$"` を検出して「判定不能の印」を残す（QT-1・第5版で新設）。** 語の内容の取り込み方は従来どおり変更しない（証拠は保つ）。
3. **失敗時**: 未閉じクォート・未閉じ括弧・末尾の孤立エスケープで `ValueError` → degraded mode（§3-5）。

#### 3-2-1. 行継続の近似（第6版・LX-1／LX-2／LX-3）

**M14 の訂正（第5版の記述は誤りであった）:** 第5版は「連結は部分文字列としての証拠を失わせないため、**証拠は増える方向にしか動かない**」と主張したが、これは**誤りである**。連結は「ステートメント区切り」という**別種の証拠を失わせる方向にも作用する**（C7）。**正しい主張は次のとおり:**

> **行継続の近似は bash の字句規則と一致していなければ両方向へ倒れる。**
> - **連結しすぎる**（bash が区切りとする位置で連結する）→ **ステートメント区切りという証拠を失う**（C7・head が前の語へ吸収され `ALLOWED_HEADS` 判定が無効化される）
> - **連結しなさすぎる**（bash が連結する位置で区切る）→ **語の同一性という証拠を失う**（SH-1・保護対象パスが2語に割れて言及ゲートが偽になる）
>
> **したがって「安全側の近似」は存在しない。bash と一致させることだけが正解であり、一致していることは実測（V-14）で裏付けなければならない**（原則 P6）。

**LX-1（正規化の位置と唯一性）:** 行継続の処理は **`find_violation` の入口の `normalize_line_continuations()` ただ1箇所**で行う。`lex()` は行継続を一切処理しない。**この関数が L1 で bash の「行構造」を近似する唯一の場所である**（M18 への対処。第5版は `lex()` 冒頭の2つの `replace` が正規化を担い、走査と分離していたため文脈を一切考慮できなかった）。

**LX-2（bash と一致させる分岐条件・P7 の適用）:** `\`+改行を**削除する（連結する）のは次の条件をすべて満たすときだけ**である。

| # | 条件 | bash の根拠 | 一致しないときの hook の挙動 |
|---|---|---|---|
| 1 | バックスラッシュ自身がエスケープされていない（**連の長さが奇数個目の `\`**） | `\\` はリテラルのバックスラッシュであり、続く改行は**制御演算子**（区切り） | 連結せず、改行を区切りとして残す |
| 2 | `#` コメントの内側でない | コメント内で `\` は特別な意味を持たず、改行がコメントを終わらせる（区切り） | 同上 |
| 3 | `'…'`（および `$'…'`）の内側でない | シングルクォート内は `\` も改行もリテラル。**改行は区切りにならない**（語の一部になる） | 連結せず、両方の文字を語の内容として残す |
| 4 | （`"…"` の内側では**連結する**） | ダブルクォート内でも `\`+改行は行継続として削除される | — |

**条件1の実現方法（設計の要点）:** 偶奇を数える専用のカウンタは**置かない**。走査が**バックスラッシュを左から順に「`\` ＋次の1文字」の対として消費する**ため、`\\`+改行では最後の `\` が直前の `\` と対になり、改行は「対の外」に残る。**偶奇は対消費の自然な帰結として決まる**（数え間違いという故障モードが構造的に存在しない）。

**LX-3（`#` コメントのモデル化範囲・M15 への対処）:** `#` は「**クォート外で語頭に現れたとき**（直前が入力先頭・空白・`;` `&` `|` `(` `)` `<` `>` のいずれか）にコメントを開始し、改行で終わる」という bash の規則で**正規化走査の中だけ**モデル化する。**目的は行継続の適用可否（LX-2 の条件2）を決めることに限られる。**

- **コメント本文は除去しない。** `lex()`・`degraded_violation()` はコメント本文を語として走査し続ける。この相違の向きは **deny 側（保守的）**である（コメント本文中の `> path` や `rm` を証拠として見るため）。**除去は deny → allow 方向の変化を生む**（`cat a #> docs/SPEC.md` が allow になる）ため本改訂では行わない（§6 **R35**・別チケット候補）。
- **AC1 との関係:** AC1 は「クォート内の `|` `<<` `>` **`#`** でステートメント分割・特殊構文判定を行わない」ことを要求している。LX-3 は**クォート外の語頭 `#` だけ**をコメントとして扱うため、AC1 の要求（クォート内 `#` を特別扱いしない）と整合する。`grep -c '#' T` のような形は影響を受けない（**T-C7f で固定**）。
- **失敗方向の非対称性が無いことの明記:** コメントの**過剰検出**（bash はコメントでないのにコメントと見る）は「連結しない」＝SH-1 方向の穴を開け、**過小検出**は C7 方向の穴を開ける。**どちらも危険であるため、判定は bash の規則をそのまま実装し、V-14(7) で両方向を実測する**（「安全側に倒す」という逃げは取れない＝M14 の訂正の帰結）。

**LX-4（コマンド置換の内側は正規化しない）:** `$(…)`／`` `…` ``／`<(…)`／`>(…)` の内側は**読み飛ばして原文のまま残す**。内側は `lex()` が `substs` へ退避し、`find_violation` が再帰評価する際に**改めてコマンド文脈として `normalize_line_continuations()` を通る**。

- **理由:** ダブルクォートの内側にあるコマンド置換の中では、bash は**コマンド文脈へ戻る**ため `#` がコメントになる。外側の文脈（`"` の内側）のまま内側を正規化すると `cat "$(ls #\`＋改行＋`rm docs/SPEC.md)"` の区切りを失う（**C7-2**）。**「内側は内側の文脈で正規化する」ことを再帰に委ねるのが最も単純で、文脈スタックを持たずに正しくなる。**
- **`lex()` が失敗して degraded へ落ちる場合**、内側は正規化されないまま `degraded_violation` の `LEGACY_STATEMENT_RE`（改行で無条件分割）に掛かる。**これは旧版と同じ扱いであり保守側**（P2）。

**LX-5（正規化後の不変条件）:** 正規化後の文字列において、**`lex()` の通常状態に残る `\`+改行は「コメント末尾のバックスラッシュ」しかありえない**（LX-2 の条件1・3は対消費とクォート状態で処理済み、条件4は削除済み、置換内側は `_take_subst`／`_take_backtick` が消費するため通常状態に現れない）。したがって `lex()` はこの形を「リテラルの `\` ＋区切り」として扱ってよい。**実装ではバックスラッシュ1文字を落として改行を区切りにする**（`\` は `PATHISH_RE` の候補文字ではないため、**パスの証拠は1文字も減らない**。落とさないと `shlex.split` が末尾の孤立エスケープで `ValueError` を出し、不要に degraded へ落ちる）。

**QT-1（ANSI-C 引用／ロケール翻訳）の検出位置と粒度:**

| 論点 | 決定 | 根拠 |
|---|---|---|
| どこで検出するか | **`lex()` の走査中（通常状態のみ）** | `$'...'` が ANSI-C 展開されるのは**クォート外**に現れたときだけである。`"$'...'"` のようにダブルクォート内に現れる `$'` は bash では展開されない（`$` は素の文字）。`'...$'...'` のようにシングルクォート内も同様。`lex()` は既にクォート状態を追跡しているため、**この区別は走査中にしか正しくできない** |
| 何を印にするか | **トークン列へ「語でも演算子でもない印トークン」を1つ挿入する**（`("ansiq", "$'")`） | 語の値を書き換えると L2 のパス検出（証拠）に影響する。別トークンにすれば**語の内容は1文字も変わらない**（P1） |
| 印を置く位置 | **チャンクの `flush()` 完了後**（＝そのチャンクの語トークンをすべて出した直後） | 演算子と「その次の語」の隣接関係を壊さないため。`redirect_violation` はリダイレクト演算子の**直後のトークン**を書き込み先として見るので、印が演算子と語の間に入ると `echo x > $'tickets/active/APP-001.md'` の**リダイレクト判定が外れる**（deny→allow の新規回帰になる）。`flush()` は演算子トークンを積む直前と入力末尾でしか呼ばれないため、**印が演算子と語の間に入ることは構造的に起こらない** |
| 判定の粒度 | **ステートメント単位**（語単位でもコマンド全体でもない） | 語単位では「その語がどのコマンドへ渡るか」を追う必要があり、コマンド全体では `printf $'a\tb'; cat tickets/active/APP-001.md` のような無関係な文まで巻き添えにする。ステートメント単位なら `cd tickets/active && awk $'…'` の cwd 追跡（第2ステートメントで cwd が保護対象）とも自然に噛み合う |
| head 別にするか | **しない（head 非依存）** | デコード相違は awk だけの問題ではない。`sed $'s/a/b/\x77 tickets/active/APP-001.md' in.md`（`\x77`＝`w`）が実際に sed のファイル書き出しへ到達する（§2-3 C6-2）。**awk 限定の規則にすると同型の穴が sed に残る** |

**プレースホルダ契約（第2版で明文化・第3版で1行追加）:**

| 用途 | 解決するか | 理由 |
|---|---|---|
| 言及ゲート（証拠の収集） | **する** | 置換結果が外側コマンドの引数になる（C1-A） |
| リダイレクト先の判定 | **する** | 置換結果がリダイレクト先になる（C1-B） |
| 変数代入値の判定（`record_assignments`） | **する** | `f=$(echo docs/SPEC.md); cat x > "$f"` |
| `sed` / `awk` の追加規則 | **する** | フラグ・プログラム断片が置換から供給されうる |
| **書き込み先オペランド（`sort -o` / `uniq` 出力引数）（第3版）** | **する** | `sort -o $(echo docs/SPEC.md)` を取りこぼさない |
| 先頭コマンド名（`head_of`） | **しない** | 置換をコマンド名の位置に使う形は解決せず不許可（保守側） |
| `find` / `git` の完全一致規則 | **しない** | 完全一致の意味論を保つ（解決は前後に空白を付けるため一致が壊れる） |
| **`cd` のオペランド（`next_cwd`）** | **しない** | 置換は「コマンドの出力」であって移動先パスそのものではない。解決しても意味のあるパスにならないため追跡打ち切り（allow 方向・§3-8 の限界） |

**解決は「証拠テキストの拡張」であり、判定を緩める方向には決して働かない。**

### 3-3. L2: パスレイヤ（第5版で証拠の復号を1点追加）

```
① 候補抽出   PATHISH_RE.findall(text)   ② 正規化 os.path.normpath
③ 除外       正規化後のパス成分に ".claude" があれば対象外
④ 分類       tickets/active|done（Templates・_index.md を除く） / basename == "SPEC.md"
```

**QT-3（証拠収集に限った ANSI-C 復号・第5版で新設）:** `is_guarded_token(text)` は、素のテキストで保護対象が見つからない場合に限り、**ANSI-C エスケープを復号したテキストでもう一度**①〜④を行う（`guarded_paths(text) or guarded_paths(ansi_c_decode(text))`）。

- **目的:** QT-1 は「印 ＋ 証拠」で deny する規則であるため、**証拠そのものを符号化して隠す形**（`rm $'\x74ickets/active/APP-001.md'`）には発火しない。復号を証拠側へ入れることで、この最後の抜けを閉じる。
- **適用範囲:** `is_guarded_token` の**追加判定のみ**。復号結果を `awk_program_texts`／`awk_program_violation`／`head_of`／`next_cwd` など**判定用のテキストとして使ってはならない**（復号結果を「安全な形」と誤認する余地を作らないため。QT-1 が先に deny するので必要も無い）。
- **安全性:** 判定に使えるテキストが**増える**方向にのみ作用するため、allow → deny 方向にしか働かない（P1 の「解決は証拠の拡張」と同じ性質）。復号器が誤っても最悪「証拠が増えない＝現状維持」になる。
- **失敗方向:** 復号が過剰に働くと誤deny（可視）になる（R29 ④）。`\xHH`／`\nnn`（8進）／`\uHHHH`／`\UHHHHHHHH`／`\cX`／`\a\b\e\E\f\n\r\t\v\\\'\"\?` のみを対象とし、**bash が認識しないエスケープ（`\|` `\d` 等）は変換しない**（bash と同じくバックスラッシュを残す）。

**第3版で明示する帰結（H2 の設計に直結）:** `_is_guarded_path` は tickets を**部分文字列**（`"tickets/active" in path`）で判定する。したがって `os.path.join(cwd, 任意の語)` を無条件に適用すると、**cwd が `tickets/active` のとき任意の語が保護対象と判定される**。実測: `_is_guarded_path("tickets/active/NR>1 {print}")` は **True**。この性質が「cwd を全語に join する素朴な方式」を不可能にしている（§3-7・§3-8）。

### 3-4. L3: 構文レイヤ（第3版で「書き込み先オペランド」・第4版で「プログラム本文」を概念として追加）

**「証拠があるときだけ deny する」を原則とする。** 判定順序（ステートメント単位）は第2版から不変:

① 出力リダイレクト（証拠ベース・ゲートなし） → ② heredoc（言及ゲートあり） → ③ 言及ゲート → ④ パイプ区間ごとのセグメント検査

①を先に置くことで `check_spec_structure.py … > docs/SPEC.md` が引き続き deny される（AC6）。

**第3版で追加する概念「書き込み先オペランド」:** 語のうち、**構文的に書き込み先と確定している**もの（出力リダイレクトの直後の語／`sort -o` の値／`uniq` の第2位置引数）だけを「書き込み先オペランド」と呼ぶ。cwd との結合（相対パスの解決）は**このオペランドに限って**行い、判定は単一のヘルパ `guarded_write_target(target, cwd, substs)` に集約する（§4-4(b)）。任意の語へ cwd を結合してはならない（§3-3 の帰結）。

**「書き込み先オペランドを取り出せない」書き込み形（awk のプログラム内リダイレクト・sed の `w`）は、オペランドを解決せず「構文の存在」で判定する**（AW-3・sed `w` 規則）。cwd が保護対象配下のときは、この構文が現れた時点で deny する（保護対象 cwd 下で書き込む awk/sed は、書き込み先が cwd 外であっても保守側に倒す。old=deny のため回帰にならない＝§2-1）。

**第4版で追加する概念「プログラム本文」:** awk へ渡る語のうち、**awk が実際にプログラムとして解釈・実行するテキスト**だけを「プログラム本文」と呼ぶ。すなわち (1) `-e TEXT`／`-eTEXT`／`--source TEXT`／`--source=TEXT` の値 (2) 位置引数の**先頭のオペランド**（`-e`／`--source` が無い場合のみ。awk は最初の非オプション引数をプログラムとして扱い、2番目以降は `NAME=VALUE` 代入と入力ファイル名として扱う）。`-f FILE`／`--file=FILE`（プログラムが外部ファイル＝不可視）は AW-4 が既に deny する。

プログラム内構文の判定（AW-7）は**このプログラム本文に限って**行う。ファイル名オペランドや変数束縛値は awk が実行しないため判定対象にしない。**この区別が M12（ファイル名 `system(1).md` の誤deny）を構造的に解消する。** 任意の語へプログラム判定を当ててはならない（第3版の AW-5／AW-6 がそれを行って M11・M12 を生んだ）。

**第5版で追加する概念「判定不能なステートメント」:** ステートメントに**クォート外の `$'…'`／`$"…"` が含まれる**とき、そのステートメントの語は「hook が見ている字面と、bash が実際にコマンドへ渡す文字列が一致しない」ことが確定している。この状態を「判定不能」と呼び、**ホワイトリスト（AW-7）にもブラックリスト（AW-5／AW-6・`SED_WRITE_RE` 等）にも掛けず**、証拠（言及ゲート／保護対象 cwd）が立つ時点で deny する（QT-1）。

- **なぜ「ホワイトリストで見る」ではないのか:** ホワイトリストは「見えているテキストが安全である」ことを確認する仕組みであり、**見えているテキストが実行されるテキストと異なる**入力に対しては意味を持たない（C6 の本体がまさにこれ）。
- **なぜ「ブラックリストで見る」ではないのか:** 同じ理由に加え、**符号化は照合対象そのものを隠す**ため（`\x7c` は `|` の照合に掛からない）。§3-6 D9 のトレースで degraded 経路が実際に allow になることを確認済み。
- **判定順序:** 言及ゲート（③）の**直後・heredoc 判定（②）の前**に置く。ゲートより前に置くと `printf $'a\tb'` 単独のような無関係なコマンドまで deny になる。

### 3-5. degraded mode（第2版で確立・第3版で2点追加・第4版で AW-7 の適用範囲を規定）

発火条件5系統（未閉じ `'`／`"`／バッククォート／`$(`・`<(`・`>(`／**末尾の孤立エスケープ**）と、旧版の判定要素をすべて持つこと（P2）は第2版から不変。`don't` / `it's` 等の日常的な英文コメントで成立するため「稀な異常入力の受け皿」ではない。

**第3版の追加（2点）:**

1. **`degraded_violation(command, cwd="")`** — 呼び出し元 `find_violation` が持つ cwd を受け取り、ローカルの cwd 追跡の初期値にする（置換の内側が degraded になった場合にも cwd が効く）。
2. **`segment_violation(words, substs=(), cwd=cwd)`** — degraded から渡す。**H2 の新規判定はすべて `segment_violation` / `awk_violation` の内側に置く**ため、degraded は引数を1つ渡すだけで同じ保護を得る（**M10 への対処**: 新しい判定ロジックを degraded 側へ複製しない）。
   - degraded は空白分割のため `w FILE` が2語に割れる。この1形だけは segment 内で**空白結合した текст**も併せて評価する（§4-4(c) の `strict` 分岐・保護対象 cwd 下限定）。

**第4版の追加（1点）— degraded mode は AW-7 の対象外とし、AW-5／AW-6 は degraded 専用として維持する:**

3. degraded mode は空白分割のため**プログラム本文の同一性が失われる**（`'BEGIN{@ f("rm x")}'` が `BEGIN{@`・`f("rm`・`x")}` の3語に割れ、`system (` も2語に割れる）。したがって degraded では
   - **AW-7 を適用しない。** 語に割れたシェル由来の断片（`#'` 等）が許可文字集合に一致せず、旧版が allow していた読み取り形（既存テスト `awk '{print $1}' tickets/active/APP-001.md #'`）を誤denyするため。
   - **AW-5／AW-6（生テキストのブラックリスト＋空白結合テキスト評価）を従来どおり維持する。** これが degraded における `system (`・`@ f(` の唯一の防御である。
   - `"cmd" | getline`／`|& getline` の degraded 版は、**`degraded_violation` の `statement.split("|")` によってパイプ区間に割れ、後段区間の head が `getline`／`&` になって `ALLOWED_HEADS` 非収載で deny される**（設計時に語単位でトレース済み。§4-5 に記載）。したがって degraded に C5 の穴は無い。
   - degraded の awk はいずれも旧版（develop）では常に deny だったため、この非対称は**保護の後退ではない**（P2 の要求は「旧版の判定要素をすべて持つこと」であり、旧版に AW-7 相当は存在しない）。

**第5版の追加（1点）— degraded mode にも QT-1 を置く:**

4. degraded は字句解析に失敗した経路であり、**クォート状態を追えない**。したがって `$'`／`$"` の検出は**コマンド文字列の部分文字列判定**（`ANSI_QUOTE_RE = \$['"]`）で行い、ステートメントの言及ゲート（または保護対象 cwd）が真のときに deny する。
   - **必須である理由:** `awk $'…\x7cgetline…' #'` のように**未閉じクォートを1つ足すだけで degraded へ落ちる**ため、QT-1 を正常経路にしか置かないと C6 が degraded 経由でそのまま通る（`$'` を含む入力は、そもそも `\'` を含む形で `lex()` が `ValueError` を出しやすい）。**degraded を「フォールバック先」にするのではなく、degraded 自身にも同じ規則を置く**のが本設計の要点である。
   - **失敗方向:** クォートの内側に `$'`／`$"` の2文字並びがあるだけで deny になる（`awk '{print "cost: $"}' T #'`）。degraded は元から保守側の経路であり、旧版は保護対象に言及する awk を常に deny していたため保護の後退にはならない。R29 ③ に列挙する。

**第6版の追加（2点）— 関数レベルゲートの cwd 対応（C8）と、正規化済み文字列の受け渡し（H4）:**

5. **`degraded_violation` の関数レベルゲートに `cwd_is_guarded(cwd)` を加える（C8 の修正本体・1行）。**
   ```
   if not (cwd_is_guarded(cwd) or mentions_guarded(command.split())):
       return None
   ```
   - **欠落していた理由の分析:** 第3版で「degraded は cwd を渡すだけで同じ保護を得る（判定を複製しない＝M10）」という方針を採ったとき、**cwd を渡す先はステートメント単位のゲートだけ**で、関数の入口にある**もう1つのゲート**が見落とされた。§4-4(h) の疑似コードが同じ欠落を持っていたため実装は設計に忠実であった（**C2・H2 に続き同じ関数で3度目**）。
   - **`degraded_violation` の全ゲートの棚卸し（第6版で明記・同じ見落としを4度目にしないため）:** ① 関数レベルの言及ゲート → **第6版で cwd 対応**（C8） ② `LEGACY_REDIRECT_RE` の全体走査（`is_guarded_token` のみ・cwd 非対応）→ **ステートメント単位のループで `guarded_write_target(target, cwd)` が cwd 対応済み**のため相補的に閉じている ③ `"<<" in command` の heredoc 判定 → ①のゲートの後段にあるため①の修正で cwd 対応になる ④ ステートメント単位のゲート `cwd_is_guarded(cwd) or any(mentions_guarded(w) …)` → 第3版から cwd 対応済み ⑤ `segment_violation`／`awk_violation` の内側 → 第3版から cwd 対応済み。**cwd を見ないゲートは①以外に存在しない**（第6版で全件確認）。
   - **到達経路の性質:** この経路は「外側の `lex()` は成功し、置換の内側だけが degraded へ落ちる」形である。`_take_backtick()` がクォート状態を追わないのは **bash と同じ仕様**（バッククォートは最初のエスケープされていない `` ` `` で閉じる）であり、**修正すべきは `_take_backtick` ではなくゲートの側**である。
6. **`degraded_violation` は `find_violation` が正規化した文字列を受け取る（H4）。** 第5版までは `lex()` だけが結合後を見て degraded は生の `command` を見ていたため、**同じ入力に対して2つの経路が異なる行構造を評価していた**。これが C7（正常経路が区切りを失う）と M13（degraded が余分に分割して「偶発的な安全網」になる）の**両方の根**である。正規化後の文字列を渡すことで、M13 の入力（`awk $\`＋改行＋`'BEGIN{…}' #'`）は degraded でも `$'` が隣接して `ANSI_QUOTE_RE` に一致し、**意図された理由（QT-1）で deny になる**。
   - **P2 との関係:** `LEGACY_STATEMENT_RE` は正規化後も改行で無条件に分割するため、旧版の判定要素は失われない。**正規化が削除するのは「bash が連結する `\`+改行」だけ**であり、これは旧版が持っていなかった（旧版は連結せず語を割っていた＝SH-1 の穴）**保護の追加**である。

**degraded mode の既知の誤deny（旧版と同一・許容）:** 「クォート内 `|`」と「未閉じクォート」が同時に成立する入力は誤denyになる。**旧版と完全に同じ挙動**であり回帰ではない。

### 3-6. 設計判断（D1〜D10）

#### D1: awk の書き込み・実行経路は **本チケットのスコープに含める**（第3版で AW-4／AW-5 を追加し、規則の構造を「次元」で整理）

**理由（第2版から不変）:** (1) 旧版は `awk` が `ALLOWED_HEADS` に無く全 awk が deny だった。AC3 に従って追加した結果、awk の書き込み経路は**本チケットの変更が原因で新設された** false negative である。(2) `docs/SPEC.md` にはバックストップが無い。(3) AC を満たしたまま採れる代替が存在しない（awk 除外＝AC3 抵触／共起案＝AC2 抵触／先送り＝AC6 抵触）。

**規則を「次元」で整理する（第3版で導入・第4版で③をホワイトリスト化）。** awk が引数だけで書き込み／実行に至る経路は次の3次元に分かれる。第2版は③のみを、第3版は③をブラックリストで見ていた。

| 次元 | 経路 | 規則 | 既定（P4） |
|---|---|---|---|
| ① オプション（書き出し） | `-o[FILE]`／`--pretty-print[=FILE]`、`-p[FILE]`／`--profile[=FILE]`、`-d[FILE]`／`--dump-variables[=FILE]` | **AW-4** | **ホワイトリスト**（未知は deny） |
| ② オプション（外部コード読み込み） | `-f FILE`／`--file=`（プログラム本体が不可視）、`-i FILE`／`--include=`（awk ソースライブラリ。**`inplace` が in-place 書き込み**＝C3）、`-l LIB`／`--load=`（動的拡張）、`-E FILE`／`--exec=`、`-D[FILE]`／`--debug[=FILE]` | **AW-4** | 同上 |
| ③ プログラム内構文 | `print`／`printf` の `>` `>>` `\|` `\|&`、**`"cmd" \| getline`／`"cmd" \|& getline`（＝C5）**、`system(…)`／`system (…)`（＝C4）、**間接関数呼び出し `@f(…)`／`@ f(…)`**、`@load`／`@include`／`@namespace` 指令、`ENVIRON`／`PROCINFO` 経由の間接参照、**未知の組み込み関数・拡張関数** | AW-1／AW-2／AW-3／**AW-7（第4版・本体）**／AW-5・AW-6（degraded 専用） | **ホワイトリスト**（未知の文字・未知の呼び出し名は deny）＝**第4版で新設** |
| （参考）変数束縛 | `-v NAME=VALUE`／`-vNAME=VALUE`／`--assign[=]`／位置引数 `NAME=VALUE` | AW-2 | 証拠ベース（束縛値が保護対象なら deny） |

**規則一覧（AW-1〜AW-7）。** 先頭コマンドが `awk` のセグメントで、次のいずれかが成立すれば deny する。**評価順は AW-1 → AW-2 → ゲート → 語ごとに「AW-4 →（degraded なら AW-5・AW-6）→ AW-3」→ 最後に「AW-7（正常経路）／結合テキストへの AW-5・AW-6（degraded）」**（§4-4(d)。現行実装の語ごとの評価順をそのまま保つため deny 理由文字列も現行と同一になる）。

| ID | 状態 | 適用対象 | ゲート | 条件 |
|---|---|---|---|---|
| **AW-1** | 継続（不変） | 全語 | なし（証拠ベース） | 保護対象に言及する語が `>` または `\|` を含む |
| **AW-2** | 継続（不変） | 変数束縛の VALUE | なし（証拠ベース） | awk 変数束縛の VALUE が保護対象に言及する |
| **AW-3** | 継続（不変） | 全語（文字列リテラル除去後） | **言及 or cwd 保護対象** | 語が `AWK_OUTPUT_RE`（`print`/`printf` の後、`;` `}` 改行に達する前に `>` `>>` `\|` が現れる）に一致 |
| **AW-4** | 継続（不変） | `-` で始まる語 | **言及 or cwd 保護対象** | `is_safe_awk_flag()`（既知の読み取り専用オプションのホワイトリスト）に一致**しない** |
| **AW-5** | **degraded 専用へ縮小（第4版）** | 全語（リテラル除去**前**） | **言及 or cwd 保護対象** | `AWK_EXEC_RE`（`\bsystem\s*\(`／`@load`／`@include`）に一致。語をまたぐ形のため**空白結合テキスト**へも当てる |
| **AW-6** | **degraded 専用へ縮小（第4版・第3版では未記載のまま実装）** | 全語（リテラル除去**前**） | **言及 or cwd 保護対象** | `AWK_INDIRECT_CALL_RE = @\s*[A-Za-z_][A-Za-z_0-9]*\s*\(` に一致。空白結合テキストへも当てる |
| **AW-7** | **新設（C5・M11・M12）** | **プログラム本文のみ**（§3-4・リテラル除去**後**） | **言及 or cwd 保護対象**／degraded では適用しない | `awk_program_violation()` が「許可文字集合」「呼び出し名の許可集合」「`print`/`printf` 出力構文の不在」の**3条件すべて**を満たさないと判定した |

**AW-5／AW-6 を degraded 専用へ縮小する理由（M11・M12 の解消本体）:** 両規則は「リテラル除去前の全語」に当てるため、(a) プログラム内の**文字列／正規表現リテラルの中身**（`print "contact: a@b (see docs)"`）と (b) **プログラム以外のオペランド**（ファイル名 `'system(1).md'`・変数束縛値）に対しても発火する。awk には eval が無いため、**リテラルの中身とファイル名オペランドは実行され得ない**。したがって両者を deny する根拠は存在せず、AC2 が解消を求める誤denyである。正常経路では AW-7 が同じ構文を**プログラム本文に限って・リテラル除去後に**判定するため、保護は失われない（§4-5 に閉塞トレース）。degraded では語の同一性が失われて AW-7 を適用できないため、従来のブラックリストを維持する（§3-5）。

**AW-6 の妥当性（第3版で実装済み・本改訂で設計へ反映）:** gawk 4.1.2 以降の間接関数呼び出し `@varname(...)` は関数名を文字列変数から供給できるため、`system(` というリテラルがコマンド文字列に一切現れないまま `system()` を呼べる。**値の中身は追跡せず「構文の存在」を証拠とする**（AW-2 の束縛・AW-3 の出力構文と同じ設計思想。`f="sys" g="tem"` のような結合で値追跡は容易に迂回されるため防御にならない）。呼び先が組み込み `system()` かユーザー定義関数かは**静的に判別不能**であり、判別を試みることが穴になる。第4版では AW-7 の許可文字集合が `@` を含まないことでこの構文全体が deny になる（規則の数を増やさずに同じ保護を得る）。

**AW-4 をホワイトリスト（未知は deny）にする理由 — reviewer 提案の評価と採否:**

- reviewer 提案（AW-4 原案）は「語が `-i` / `-iVALUE` / `--include` / `--include=VALUE` に一致 → deny」というブラックリストだった。**方向は妥当だが不十分**である。同じ次元に `-f`／`-l`／`-E`／`-o`／`-p`／`-d`／`-D` が並んでおり、C3 を1つ塞いでも同型の穴が残る。実測でも `awk -f prog.awk tickets/active/APP-001.md`（プログラム不可視・束縛なし）は **old=deny → new=allow の未報告の同型回帰**である（AW-2 は束縛が無いと発火しない）。
- **オプションは man/`--help` で閉じた集合として列挙できる**唯一の次元であり、ホワイトリスト化の失敗方向は「安全なオプションの収載漏れ＝誤deny（可視・修正容易）」であって「危険なオプションの列挙漏れ＝黙ってすり抜け」ではない。**P4 の要求を満たす唯一の実装可能な形**である。
- `-i` を含む gawk のオプション群は読み取り用途でほぼ使われないため、`mentions_guarded or cwd_is_guarded` のゲートを前置すれば A-3a 系（`-F'\|'`）を壊さない（reviewer の評価と一致）。実測トレース: §4-5。
- 副作用として mawk/BWK awk 固有のオプション（`-W …` 等）は未収載のため保護対象に言及する場合 deny になる。**安全側であり、必要になれば安全性を確認してから収載する**（docstring に明記）。

**第3版の誤った主張の訂正（C5 の直接原因）:** 第3版は「`cmd | getline` と `print |& "sh"` は **AW-1／AW-3 が既に捕捉済み**であり追加規則を要しない」と書いた。**これは誤りである。** AW-1 が捕捉するのは「保護対象パスが**リテラルで同じ語に現れる**形」だけで、`cd tickets/active && awk 'BEGIN{"rm APP-001.md" | getline x}'` のように**保護対象を cwd で解決する形では証拠が立たない**。AW-3 は `print`/`printf` 起点にしか一致しないため入力方向のパイプに構造的に無関係である。この「リテラル形は偶然 deny・cwd 形は allow」という非対称性を「捕捉済み」と読み違えたことが4回目の穴の直接原因であり、**個別規則の追加ではなく次元の反転が必要**であることの根拠になった。

#### D1 の中核: AW-7 — プログラム形のホワイトリスト（第4版の本体）

**判定対象:** §3-4 の「プログラム本文」（`-e`／`--source` の値と、位置引数の先頭オペランド）。ファイル名オペランド・変数束縛値・オプション語は対象外。

**判定手順（3条件をすべて満たすときだけ allow）:**

1. **リテラル除去（`awk_strip_literals`）** — 文字列リテラル `"…"` を `""` へ、正規表現リテラル `/…/` を `//` へ、**左→右の1パス走査**で置換する。未閉じ文字列リテラルは「判定不能」＝deny。
2. **許可文字集合（`AWK_SAFE_PROGRAM_CHARS`）** — 除去後のテキストに、許可集合外の文字が1つでもあれば deny。許可するのは英数字・`_`・空白・タブと `$ ( ) { } [ ] , ; : ? ! ~ = < > + - * / % ^ & . " #` のみ。**意図的に含めない文字とその根拠:**
   - **`@`** — 間接関数呼び出し `@f(`・`@load`・`@include`・`@namespace` の入口。gawk の拡張機構と間接呼び出しはすべて `@` を必要とする。
   - **`\|`** — 出力パイプ `print \| "cmd"`・入力パイプ `"cmd" \| getline`・コプロセス `\|&` のすべてが `\|` を必要とする（**C5 はこの1文字で閉じる**）。
   - **`\`・バッククォート・`'`** — awk のプログラム本文（リテラル外）に現れる正当な用途が無い。行継続の `\`+改行は L1 が空白へ正規化するため到達しない。
   - **非ASCII文字・制御文字** — gawk はリテラル外の非ASCII識別子を受け付けない（実測）。文字列・正規表現の**中身**は除去済みなので、日本語メッセージを含む読み取り awk は影響を受けない。
3. **呼び出し名の許可集合（`AWK_SAFE_CALLS` ∪ 同一プログラム内で宣言された関数名）** — 除去後のテキストから `IDENT\s*\(` を全件抽出し、いずれかが許可集合外なら deny。**`system` は意図的に非収載**（`system (` の空白形も `\s*` で捕捉する）。`function NAME(` / `func NAME(` で宣言された名前は許可集合へ加える（ユーザー定義関数の呼び出しは読み取り用途で普通に使われる。gawk は組み込み関数名の再定義を fatal error にするため、宣言名の収集が抜け道にならない）。
4. **出力構文の不在** — 除去後のテキストが `AWK_OUTPUT_RE` に一致すれば deny（AW-3 と同一の正規表現。`print`/`printf` の**後に** `>` `>>` `\|` が同一ステートメント内で現れる形）。`>` を許可文字に含めたままにできるのはこの順序条件があるためで、`NR>1`・`{if ($1 > 2) print $1}` のような**比較の `>`**（`print` より前に現れる `>`）は allow のまま維持される。

**健全性の前提（4つ）— V-11 で実機検証する:**

| # | 前提 | 破れたときの向き | 検証 |
|---|---|---|---|
| S1 | リテラル除去の1パス走査が awk の字句規則と一致し、**awk が実行するコードをリテラルとして飲み込まない** | 飲み込むと allow（**危険**） | V-11(3) の6形 |
| S2 | 許可文字集合に、書き込み・実行の入口となる文字が残っていない | 残ると allow（危険） | V-11(2)・§3-11 の写像 |
| S3 | `AWK_SAFE_CALLS` に副作用（ファイル書き込み・プロセス起動）を持つ関数が無い | 混入すると allow（危険） | V-11(4) の突き合わせ |
| S4 | 出力リダイレクトは `print`/`printf` の後にしか現れない（awk の文法上） | 別構文があれば allow（危険） | V-11(2) |

**なぜ1パス走査でなければならないか（設計の要点。実装は必ずこの形にすること）:** 「文字列を全部除去してから正規表現を除去する」2パス方式には**両方向に穴がある**。
- 文字列先: `/"/ {system("rm F")}` — 正規表現内の `"` を文字列開始と誤認し、`/ {system(` を文字列の中身として飲み込む → `system(` が消えて allow。
- 正規表現先: `BEGIN{x="(/a"; system("rm /F")}` — 文字列内の `(/` を正規表現開始と誤認し、`/a"; system("rm /` を飲み込む → allow。

1パス走査なら「その位置で何のリテラルが始まるか」を awk と同じ順序で決めるため、**awk が実行するコードがリテラルとして消えることは起こらない**。正規表現と除算の区別は「直前の非空白文字がオペランドの終端になりうるか」（`AWK_OPERAND_END_CHARS`）で行い、加えて**候補スパンが `;` や改行を含む場合は正規表現と認めない**（二重の安全網）。誤ると「除算と判定してリテラルを除去しない」＝内容が判定対象に残る＝**deny 方向**へ倒れる。

**一般化（sed）:** `sed` の `w`／`W` コマンドと `-i` 全表記は第2版どおり維持し、第3版で **cwd 対応**にした（§3-8）。**sed のプログラム内構文次元は第4版でもブラックリストのままである**（`w`／`W` のみ・`-f SCRIPT`／GNU sed の `e` フラグ・`s///e` は D4 のとおり**スコープ外**＝old=allow）。同じホワイトリスト化を sed へ適用することは技術的に可能だが、(a) old=allow の穴の閉塞は D6-2／D6-3 でスコープ外 (b) `sed` を `ALLOWED_HEADS` に残すかという人間承認を要する上位判断を含む (c) リトライ枠がゼロでレビュー面を広げられない、の3点により**本チケットでは行わない**。§6 R25・§10-1 申し送り2に明記する。

#### D2: `_ticket_lib.is_ticket` との判定基準は **統一しない**（初版から不変）

目的（厳密判定 vs 再現率優先）・スコープ（related_files 外・KLK-012 と衝突）・影響半径（3hookへ波及）の3点が根拠。意図的な不統一である旨は docstring に記載済み。

#### D3: 回帰確認スコープは **スイート全体 + 追加分**（第3版で基準線を更新）

- 基準線: `python3 -m unittest discover -s tests` の全件。**Phase 8 着手時点は384件・うち4件 FAIL**（`test_guard_bash_writes.py` は **120件**。FAIL の4件は tester が `e7b2dc0` でコミットした失敗実証テスト）。
- pytest は使わない。**既存の120件は1行も変更しない**（追加のみ）。**とくに tester の4件は期待値を1文字も変えない。** 変更が必要になった時点で設計の誤りなので、implementer は変更せず orchestrator へ報告する。
- **件数は設計書に固定しない。** 文書へ書く件数は必ず実測値とする（M7 の再発防止）。

#### D4: スコープ外の穴（第3版で `sort -o` / `uniq` 第2位置引数のみ**スコープ内へ変更**）

| 経路 | 第2版 | 第3版 | 根拠 |
|---|---|---|---|
| `sort -o FILE` / `--output=FILE` | スコープ外 | **スコープ内** | cwd 相対形（`cd tickets/active && sort -o APP-001.md in.md`）が **old=deny → new=allow の回帰**（H2）。閉じるための書き込み先オペランド抽出（§4-4(e)・約8行）は絶対パス形も同時に閉じる。**cwd 相対だけ deny・絶対パスは allow という非対称は攻撃者が即座に迂回でき、説明もできない**ため、絶対パス形も同時に閉じる。誤denyリスクは実質ゼロ（`sort -o` の値が保護対象を指すなら書き込みで確定） |
| `uniq INPUT OUTPUT` の第2位置引数 | スコープ外 | **スコープ内** | 同上。フラグの引数消費（`-f`/`-s`/`-w` 等）を考慮した位置引数の数え方が必要（§4-4(e)）。`uniq -f 2 FILE` を誤denyしないことを T-H2f で固定 |
| `sed -f SCRIPT`（プログラム不可視） | スコープ外 | **スコープ外（据え置き）** | old=allow。閉じるには「外部スクリプトを使う sed を保護対象に言及する場合に deny」という新規判断が必要で、正当な読み取り用途（整形スクリプト）を誤denyしうる |
| **GNU sed の `e` フラグ**（任意コマンド実行） | スコープ外 | **スコープ外（据え置き）** | old=allow。書き込みではなく**任意コマンド実行**で脅威クラスが異なり、`sed` を `ALLOWED_HEADS` に残すか否かという人間承認を要する上位判断を含む。§3-9・§6 R12・docstring・§10 に明記 |
| `sort --compress-program=PROG`（外部プログラム実行） | 未列挙 | **スコープ外（新規列挙）** | old=allow。§3-9 の4次元走査で新規に発見。一時ファイルが作られる規模の入力が必要で実効性は低いが、経路として存在するため表と §10 に記載 |
| `git diff --output=FILE`（diff 系オプション） | 未列挙 | **スコープ外（新規列挙）** | old=allow。`ALLOWED_GIT_SUBCOMMANDS` 収載サブコマンドの引数だけで書ける。`git commit -o` は `--only` の意味であり短縮形の一律 deny は誤denyになるため、規則化には判断を要する。§3-9・§10 に記載 |
| **`is_readonly_script_call` の許可経路偽装** | 未列挙 | **スコープ外（D7 で判断）** | D7 を参照 |
| `find -fprint0`（**自前の列挙の漏れ**） | 未列挙 | **スコープ内（1語追加）** | 上記と異質。`FIND_WRITE_FLAGS` は「本 hook 自身が持つ閉じた列挙」であり、そこに `-fprint0` が欠けているのは**設計の主張（investigator が網羅性を確認）の誤り**である。1語の追加で閉じ、誤denyリスクはゼロ（`-fprint0 FILE` は書き込みで確定）。old=allow だが「表を正すこと」自体が SV-7 への対処であるため含める |

#### D5: 再発防止テストは **固定リスト方式を正とし、old/new 差分は reviewer の手順とする**（第2版から不変）

| 方式 | 採否 | 理由 |
|---|---|---|
| **固定リスト**（`LEGACY_DENY_COMMANDS` + T-LEGACY） | **採用（正）** | hermetic・git 依存ゼロ・**old=allow の潜在穴も同じリストへ書ける**・差分の理由がコード上に残る |
| `git show develop:…` によるテスト内 old/new 差分 | 不採用（テストには入れない） | 非hermetic（ブランチ名参照）・現行アーキテクチャからの逸脱・「両方 allow」の穴は原理的に検出不能 |
| 同上を reviewer の検証手順として残す | **採用（補助）** | 未知の新規回帰を自動で拾える唯一の手段。§4-9 **V-9**（`scratchpad/klk010/` の `rv1`〜`rv5` と `cmp`〜`cmp5` を `git merge-base develop HEAD` 固定で再実行） |
| **書き込みベクタ棚卸し（§3-9）+ フォールセーフ既定（P4）** | **採用（プロセス統制 + 機械的既定）** | 上記2方式のどちらも拾えない「old=allow の穴」への対策。**第3版では棚卸しだけに依存させない**（P4） |

#### D6: スコープ判断の基準（第3版で新設・以後の判断をこの基準で行う）

C3／C4／H2 のような追加が繰り返されたため、「何を本チケットで閉じるか」の基準を明文化する。

1. **`old=deny → new=allow`（本チケットが開けた穴）は必ず閉じる。** 例外なし（AC6・SV-8 の「自分で穴を開けることは許容できない」原則）。
2. **`old=allow`（旧版から存在する穴）は原則スコープ外**（別チケット）。ただし次の2つは例外として含める。
   - (a) **1で作る機構が同時に閉じるもの**で、追加コストが数行かつ誤denyリスクが無いもの（例: `sort -o` の絶対パス形。cwd 相対形を閉じる機構が同じオペランド抽出であるため）。**「cwd 相対だけ deny・絶対パスは allow」のような説明不能な非対称を残さない**ことを優先する。
   - (b) **本 hook 自身が持つ閉じた列挙の誤り**（例: `FIND_WRITE_FLAGS` の `-fprint0` 欠落）。設計書が「網羅した」と主張している箇所の誤りは、主張ごと訂正する。
3. **脅威クラスの拡大（任意コマンド実行）や、`ALLOWED_HEADS`／`READONLY_SCRIPTS` の収載可否そのものを問う変更は、old=allow なら人間の判断を要する上位の設計変更**として扱い、本チケットに黙って混ぜない（GNU sed `e`・D7）。
4. 上記に該当しないものは §3-9 の棚卸し表・§6 のリスク・§10 の申し送りへ**必ず記載する**（黙って落とさない）。

#### D7: `is_readonly_script_call()` の許可経路偽装は **本チケットのスコープ外**（別チケットで即時起票を推奨）

**内容:** `python3 x=y .claude/skills/spec-interview/scripts/check_spec_structure.py docs/SPEC.md` が allow になるが、python3 が実際に実行するのは `x=y` という名前のファイルである。`IDENTIFIER=任意文字列` 形のファイルを用意できれば **READONLY_SCRIPTS の許可経路を偽装して任意コード実行に到達**でき、バックストップの無い `docs/SPEC.md` を含め本 hook を完全に迂回できる。

**スコープ外とする根拠（D6 の基準を適用）:**
1. **old=new=allow** — 旧版も同じ `re.fullmatch` で代入語をスキップしており、本チケットが開けた穴ではない（orchestrator・reviewer が独立検証済み）。
2. どの AC も要求していない。
3. **`READONLY_SCRIPTS` 許可経路の意味論を変える変更**であり、D6-3（脅威クラス・収載可否を問う変更）に該当する。人間は「設計デルタ → implementer」の運用を承認したが、**この脆弱性のスコープ拡張は明示的に承認していない**。
4. リトライ枠が残1のため、AC 外の変更でレビュー面を広げない。

**ただし implementer の逸脱4の理由付けは誤りであるため設計として訂正する（reviewer の指摘を反映）。** 実装メモは「`python3 FOO=bar script.py` のような形での挙動差を避けるための意図的な非統一」としているが、**その挙動差こそが脆弱性である**。したがって逸脱4は「安全・中立な判断」ではなく「**無償の修正を見送った判断**」として記録する。reviewer は `head_args()` へ寄せた版（`patched_guard.py`）を実測し、**5行の統一で偽装が deny になり、正規経路（`python3 {SCRIPT} docs/SPEC.md`・`FOO=bar python3 {SCRIPT} docs/SPEC.md`）は一切壊れない**ことを確認している。**別チケットでは、この5行の統一が第一候補の解**である。

**本チケットで行うこと:** §3-9 の棚卸し表に**未対応（深刻度 high・任意コード実行）**として明記／§6 R21 として記録／§10-1 の申し送り筆頭として起票を推奨／docstring の「既知の限界」に記載（Phase 7）。

#### D8: awk のプログラム内構文次元を**ホワイトリストへ反転する**（第4版・人間の決定に基づく）

**決定の出所:** 2026-08-14 18:42 の人間の判断（チケットのブロッカーセクション・ログに記録）。「保護対象に言及する awk は原則 deny とし、安全と確認できる狭いプログラム形のみ allow する。オプション次元で漏れを止めたのと同じ手」。**誤denyが増えることを人間が認識のうえで選択しており、許容範囲の定義は architect へ委ねられた。**

**allow する形の定義 — 実際に必要な読み取り用途の洗い出し（この列挙が AW-7 の設計根拠）。** 出所は reviewer の `rv3.py`（日常読み取り70件）と既存テスト（**T-H1f** 5形・**T-AWKOK** 5形・**T-AW6C** 8形・**T-H2f** 2形・A-3a）、および tester が AC2 側として報告した2形である。

| # | プログラム本文 | 出所 | AW-7 が allow と判定する理由（リテラル除去後） |
|---|---|---|---|
| 1 | `{print $1}` / `{print}` / `{print $1,$2}` / `{print $n}` / `{print -$2}` | rv3・T-H1f・T-AWKOK・implementer メモ | 許可文字のみ。呼び出しなし。`print` の後に `>`／`\|` なし |
| 2 | `NR>1 {print}` | rv3・T-H1f | `>` は `print` **より前**（`AWK_OUTPUT_RE` は順序依存） |
| 3 | `{if ($1 > 2) print $1}` | rv3・T-H1f | 同上。`if (` は許可呼び出し名 |
| 4 | `{printf "%s\|%s\n", $1, $2}` | rv3・T-H1f | `\|` は**文字列リテラル内**→除去される |
| 5 | `BEGIN{OFS="\|"} {print $1}` | rv3・T-AWKOK | 同上 |
| 6 | `$1==m {print}` / `$1==s {print}` | T-AW6C | 許可文字のみ |
| 7 | `/user@example.com/ {print}` / `/@(id\|title)/ {print}` / `/@/ {print}` | T-AW6C | `@`・`\|` は**正規表現リテラル内**→除去される（1パス走査が必須の理由） |
| 8 | `function p(x){print x} {p($1)}` | T-AW6C | `p` は同一プログラム内の宣言名→許可集合へ加わる |
| 9 | `BEGIN{PROCINFO["sorted_in"]="cmp"} {print}` | T-AW6C | 文字列除去後 `PROCINFO[""]=""`。`[` は呼び出しではない |
| 10 | `BEGIN{print "contact: a@b (see docs)"}` | **tester（M11・AC2 側）** | `@b (` は**文字列リテラル内**→除去される |
| 11 | `{print}`（第2オペランドが `'system(1).md'`） | **tester（M12・AC2 側）** | ファイル名オペランドは**プログラム本文ではない**→判定対象外 |
| 12 | `{gsub(/a/,"b")}1` / `{sub(/^ +/, ""); print}` | rv2・日常整形 | `gsub`／`sub` は許可呼び出し名。正規表現・文字列は除去 |
| 13 | `{print toupper($1)}` / `{print length($0)}` / `{print substr($0,1,5)}` | 日常読み取り | いずれも許可呼び出し名（副作用なし） |
| 14 | `{a[$1]++} END{for (k in a) print k, a[k]}` | 日常集計 | `for (` は許可呼び出し名 |
| 15 | `BEGIN{getline l < "tickets/active/APP-001.md"; print l}` | 読み取り（`\|` を使わない getline） | `getline` は呼び出し構文を持たない。`<` は許可文字（読み取り方向） |
| 16 | `cd tickets/active && awk '{print $1}' APP-001.md`（cwd 保護下） | rv3・T-H2f | ゲートは真になるがプログラム本文が安全形 |

**この表に無い形は deny になる。** 新規に deny となる読み取り形は §6 **R26** に漏れなく列挙し、V-12(d) で「列挙外の新規 deny が1件でも出たらブロッカー」とする。

**誤denyの許容範囲（設計としての線引き。詳細な合否条件は §4-9 V-12）:**
1. **既存の allow 固定テスト（上表の出所テスト全件）が1件も deny に転じないこと** — 転じた場合は設計の欠陥であり、implementer はテストを緩めず orchestrator へ報告する。
2. **`rv3.py`（日常読み取り70件）の新版 deny が 1/70 のまま**であること（`cat T | tee /tmp/x` の1件のみ。2件以上になったらブロッカー）。
3. 新規 deny は **R26 の列挙内**に収まること。
4. 上記を満たす限り、**「awk の珍しい読み取り形が deny される」ことは許容する**（deny 理由文と docstring で回避方法＝`cat`／`grep` の単独使用・保護対象パスを引数に取らない形を案内する）。

**「`awk` を `ALLOWED_HEADS` から外す」案を採らない理由:** AC3 が `awk` の収載を明示的に要求しており、外すと A-3a（AC2）も再び誤denyになる。AW-7 は「`awk` は収載したまま、プログラム本文が安全形でない場合に限り deny」であり、**AC2・AC3 の両方を満たす唯一の形**である。なお `ALLOWED_HEADS` 自体の縮小（読み取り専用ラッパーへの移行）は AC3 の改訂を伴う上位判断であり §10-1 申し送り8 に据え置く。

#### D9: `$'…'`／`$"…"` を含むステートメントは **「判定不能」として無条件 deny する**（第5版・QT-1）

**人間の決定（2026-08-15 11:51）:** 「`$'...'` を含むコマンドを degraded 相当（ホワイトリスト非適用のブラックリスト）へフォールバックさせる」（tester の第3案）。選定理由は (a) 最小の変更 (b) deny 方向のみ (c) 原則 P4 と一致 (d) `$'...'` は日常操作でほぼ使われず誤denyの実害が小さい。

**設計側の検証結果 — 「degraded 相当へのフォールバック」はこの穴を閉じない。** C5 再開通形を `degraded_violation(command, cwd="")` へ通した**語単位のトレース**（設計時に実施）:

入力: `awk $'BEGIN{x="foo\x22(\x22rm tickets/active/APP-001.md\x22\x7cgetline x)\x22"}'`

| 段 | 判定 | 結果 |
|---|---|---|
| 1. 全体の言及ゲート `mentions_guarded(command.split())` | 3語目に `tickets/active/APP-001.md` が**素で見えている** | **通過**（deny 候補として先へ進む） |
| 2. `LEGACY_REDIRECT_RE.finditer` | `>` がコマンド文字列上に存在しない | 不発火 |
| 3. `"<<" in command` | 無い | 偽 |
| 4. `LEGACY_STATEMENT_RE.split`（`&&` `\|\|` `;` 改行） | いずれも存在しない | 1ステートメント |
| 5. `statement.split("\|")` | **`\|` はコマンド文字列上に存在しない（`\x7c` のまま）** | **パイプ分割されない**＝第4版が「degraded に C5 の穴は無い（後段 head が `getline`／`&` になって非ホワイトリストで deny）」と論じた根拠が**この入力では成立しない** |
| 6. `segment_violation(words, cwd="", degraded=True)` | head は `awk`＝`ALLOWED_HEADS` 収載／`CWD_SAFE_HEADS` 収載 | 先へ |
| 7. `awk_violation(words, "", degraded=True)` の AW-1 | 保護対象に言及する語に `>`・`\|` は**無い**（`\x7c`） | 不発火 |
| 8. 同 AW-2（束縛） | 束縛なし | 不発火 |
| 9. ゲート（言及 or cwd） | 真 | 先へ |
| 10. 語ごとの AW-4／AW-5／AW-6／AW-3 | `-` 始まりの語なし／`system(`・`@load`・`@include` なし／`@` なし／`print` なし | **全て不発火** |
| 11. 結合テキストへの AW-5／AW-6 | 同上 | 不発火 |
| **結果** | | **None ＝ allow（穴は閉じない）** |

**一般化:** **C6 の本質は「字面を符号化して照合対象から隠す」ことである。** 符号化された入力をブラックリスト（degraded）へ回すのは、AW-7 をホワイトリストへ反転した理由（**ブラックリストは列挙外の形を素通しする**）と同じ失敗を繰り返すことになる。ホワイトリスト（AW-7）も同様に無力である — ホワイトリストは「**見えているテキストが安全である**」ことしか確認できず、見えているテキストと実行されるテキストが異なる入力に対しては意味を持たない。

**したがって採用する形（QT-1）:**

> ステートメントに**クォート外の `$'…'`／`$"…"`** が含まれるとき、そのステートメントを「判定不能」とし、**`cwd_is_guarded(cwd)` または `mentions_guarded(語)` が真になった時点で無条件に deny する**（ホワイトリストにもブラックリストにも掛けない）。degraded 経路にも同じ規則を置く（§3-5・部分文字列判定）。

**人間の選定理由 (a)〜(d) との整合（この形が決定の趣旨を満たすことの確認）:**

| 理由 | QT-1 での成立 |
|---|---|
| (a) 最小の変更 | `lex()` に検出2行＋`flush()` に印の出力／`pipe_segments` に1行／`statement_violation` に3行／`degraded_violation` に2行。**degraded へのフォールバック実装（分岐と経路の切り替え）より小さい** |
| (b) deny 方向のみ | 新規の判定は deny だけを生む。**allow に転じる形は QT-2 の1クラスのみ**（bash が実際には保護対象を触らない形＝許容分類8） |
| (c) 原則 P4（未知は保守側）と一致 | 「判定不能＝deny」は P4 の最も直接的な適用形。ホワイトリスト／ブラックリストのどちらでもない**第4のフォールセーフ既定**として §3-10 に登録する |
| (d) 誤denyの実害が小さい | `$'...'` は Kit のコマンド・スキル・スクリプトに**1件も存在しない**（リポジトリ全体の grep 実測）。実用形（`awk -F$'\t'`・`grep $'\t'`）には**エスケープを使わない等価な書き方が存在する**（`awk -F'\t'`＝awk 自身がエスケープを解釈する／`grep -P '\t'`）。回避策は deny 理由文と docstring に載せる（R29） |

**この設計判断は人間の決定より deny 方向へ強いだけで、緩くなる箇所は1つも無い。** 判断の差分（機構の置き換え）は Open Question ではなく**報告事項**として orchestrator 経由で人間へ伝える（§10）。理由は、(1) 決定の目的（穴を閉じる）は変えていない (2) 決定の選定理由 (a)〜(d) をすべて満たす (3) 決定どおりの機構では**目的を達成できないことがトレースで確定している**、の3点である。

#### D10: `awk_strip_literals` のエスケープ処理は **変更しない**（tester の第2案は採らない・第5版）

**tester の第2案:** 「`awk_strip_literals` のエスケープスキップを『bash が `$'...'` 内で認識するエスケープ表現かどうか』を考慮せず**一律に安全側（deny）へ倒す**」。

**採らない理由（3点）:**

1. **`awk_strip_literals` は awk の字句規則と一致しており、誤っていない。** awk（gawk）の字句解析は文字列リテラルを「最初の**エスケープされていない** `"`」で終端する。`\x22` は gawk の拡張エスケープであり、**文字列の中身として `"` という文字**になるだけで**終端にはならない**。したがって「`\`＋次の1文字を読み飛ばす」近似は**リテラルのスパン（範囲）について awk と一致する**。tester の全数探索でも「`awk_strip_literals` を騙す形」は**1件も成立しなかった**（エスケープ・文字クラス・空正規表現・除算曖昧性のすべてが意図どおり deny）。**相違は bash 側のデコードにのみ存在する。**
2. **QT-1 が入口を閉じるため、保護上の利得がゼロである。** `$'...'` を含むステートメントは AW-7 へ到達する前に deny される。`$'...'` を伴わない入力では相違が存在しない（1 のとおり）。
3. **誤denyの代償が大きい。** 一律に安全側へ倒すと、リテラル内にバックスラッシュを持つ**日常的な読み取りプログラム**（`{printf "%s\t%s\n", $1, $2}`／`{gsub(/\|/,"-"); print}`／`split($0, a, "\\.")`）がすべて deny になる。これは §4-9 V-12 の誤deny予算（既存 allow 固定テスト全件 allow・`rv3.py` の新版 deny を 1/70 に保つ）を確実に破る。

**代わりに行うこと:** 1 の主張（awk の字句規則との一致）を**未検証の前提のまま放置しない**（P6）。V-11(3) の S1 健全性検証に加え、**V-14 で「bash が awk へ渡す文字列」と「hook が見る文字列」を突き合わせる**手順を新設し、相違が存在する入力クラスを網羅的に洗い出す。

### 3-7. 代替案と却下理由

| 案 | 却下理由 |
|---|---|
| `shlex.shlex(punctuation_chars=True)`（posix=True／False） | posix=True はクォート済み `>` と演算子 `>` を区別できず AC1 を満たせない。posix=False は `-F'\|'` が3分割され A-3a を満たせない |
| 外部パーサ（`bashlex` 等） | README「依存なし（Python3標準ライブラリのみ）」に反する |
| **H2: cwd を全語に join して `_is_guarded_path` する素朴な方式** | **採用不可（reviewer 実測）。** `_is_guarded_path("tickets/active/NR>1 {print}")` が True になり、AW-1（`">" in word`）が `cd tickets/active && awk 'NR>1 {print}' in.txt` を誤denyする。H1 で共起案を却下したのと同じ罠。cwd と結合するのは**構文的に書き込み先と確定したオペランドのみ**（§3-4） |
| **H2: 保護対象 cwd 下では `sed`/`awk`/`sort`/`uniq` を head レベルで一律 deny する** | **却下（可用性）。** 構造的に単純で old=deny のため回帰も起きないが、reviewer の日常読み取り検査（`rv3.py` 70ケース）に含まれる `cd tickets/active && awk '{print $1}' APP-001.md`／`cd tickets/active && awk -F'\|' '{print $1}' APP-001.md`／`cd tickets/active && sed -n '1,5p' APP-001.md` の**3件が新規に誤denyになる**。本チケットは誤deny（可用性）解消が半分の目的であり、reviewer が「新規誤denyゼロ」を AC2 の達成根拠としているため採れない。代わりに、規則ごとに cwd 対応させる（AW-3 のゲート拡張・sed `w` の `strict` 分岐・sort/uniq のオペランド抽出）方式を採る。**フォールセーフ既定としての head レベル判定は `CWD_SAFE_HEADS` 未収載 head に限って残す**（現行の全 head は収載済みのため誤deny増はゼロ） |
| H2: `sort`/`uniq` を `ALLOWED_HEADS` から外す | AC3 の趣旨（読み取りコマンドは保護対象を引数に取っても allow）に反し、`cat T \| sort \| uniq -c` のような日常読み取りを壊す |
| **AW-4 をブラックリスト（`-i`/`--include` のみ）にする**（reviewer 原案） | **不十分。** 同次元の `-f`／`-l`／`-E`／`-o`／`-p`／`-d`／`-D` が残り、`awk -f prog.awk T` は実際に old=deny → new=allow の未報告の同型回帰である。P4 を満たさない |
| **「保護対象に言及するセグメントでホワイトリスト済みコマンドが疑わしい構文を含めば deny」という包括的な第2の防御（文字レベル・コマンド全体に適用）** | **却下（維持）。** 「疑わしい構文」を `>`／`\|`／`(` 等の文字レベルで**コマンド全体**に対して定義すると `awk 'NR>1 {print}'`・`awk -F'\|'`・`sed 's/a\|b/c/'`・`grep -E '^(id\|title):'` が誤denyになり **AC1・AC2 に直接抵触する**（H1 の共起案却下と同じ実測根拠）。**ただし第3版がここに書いた「プログラムテキストの次元はホワイトリスト化できない」という結論は誤りであった**（第4版で訂正）。適用対象を**awk のプログラム本文だけ**に限り、判定を**リテラル除去後**に行い、`>` を**順序条件**で扱えば、上記4形はいずれも allow のまま `\|`・`@`・未知呼び出しを deny できる（§3-6 D8 の16形の根拠列挙・AW-7） |
| **C5 を個別規則（`getline` を伴うパイプの検出）で閉じる** | **却下（第4版）。** 「`\|` の直後または直前に `getline` があれば deny」という規則は5回目の穴を防げない。**同一次元で4ラウンド連続して穴が出ており、規則追加方式の失敗が実証されている**（H1 → C4 → `@f()` → C5）。人間が方針として反転を選択した（D8） |
| **間接呼び出し・パイプの「値」を追跡して危険な呼び先だけ deny する** | **却下。** `f="sys" g="tem"` の結合や `g=f` のエイリアスで容易に迂回でき防御にならない（AW-2・AW-6 で同じ判断を確立済み）。**構文の存在**を証拠にする方式を維持する |
| **awk のプログラムを構文解析（パーサ実装）して判定する** | **却下。** 依存追加なしで awk の完全な文法を実装するコストが本チケットの規模に見合わず、パーサ自身のバグが穴になる。AW-7 は「字句レベルの3条件」に限定し、**誤りが deny 方向へ倒れる**構造（未知の文字・未知の名前は deny）で健全性を担保する |
| **AW-5／AW-6 を（degraded 専用へ縮小せず）正常経路にも残したまま AW-7 を追加する** | **却下。** M11・M12（AC2 が解消を求める誤deny2件）が残る。AW-7 は同じ構文を「プログラム本文のみ・リテラル除去後」で判定するため、正常経路で AW-5／AW-6 を残す保護上の利得はゼロである（§4-5 の閉塞トレースで全形を確認） |
| M5（`cd .. && rm active/X.md`）を実装で閉じる | cwd 接頭辞と相対パスの分割は一般解が `cd . && rm foo` を deny する誤deny爆発を招く（reviewer 判定）。docstring・README・§3-9 の限界として明記する |
| `is_guarded_token` を「SPEC.md の部分文字列包含」へ単純変更 | `MYSPEC.md` / `SPEC.md.bak` で誤denyが増える（AC5） |
| **C6: `$'...'` を含むコマンドを degraded 相当（ブラックリスト）へフォールバックさせる**（tester 第3案・人間が選択した機構） | **却下（穴が閉じない・§3-6 D9 に11段の語単位トレース）。** `\x7c` はコマンド文字列上に `\|` として現れないため `degraded_violation` の `statement.split("\|")` がパイプ分割せず、AW-1／AW-5／AW-6／AW-3 のいずれにも一致しない＝**C5 再開通形が degraded でも allow のまま**。**符号化された入力をブラックリストへ回すのは、AW-7 をホワイトリストへ反転した理由と同じ失敗の繰り返しである。** 決定の目的と選定理由を満たすより強い形（QT-1＝判定不能→無条件 deny）へ置き換える |
| **C6: `lex()` で `$'...'` を ANSI-C デコードしてから解析する**（tester 第1案） | **却下（部分採用）。** デコード結果を**判定テキストとして**使うと、(1) デコード器の実装差がそのまま allow 方向の穴になる（bash のエスケープ仕様は `\cX`・`\U` を含み網羅が難しい） (2) デコード後の文字列を再度シェル字句解析すべきか（bash は**再解析しない**）という新しい設計問題を生む (3) `$"..."`（ロケール翻訳）は**そもそもデコードできない**（カタログ依存）。**証拠収集に限った復号（QT-3）だけを採用**し、判定は QT-1 の「判定不能→deny」で行う |
| **C6: `awk_strip_literals` のエスケープスキップを一律に安全側へ倒す**（tester 第2案） | **却下（§3-6 D10）。** `awk_strip_literals` は awk の字句規則と一致しており誤っていない（相違は bash 側のデコードにのみ存在する）。保護上の利得はゼロで、`{printf "%s\t%s\n", …}` のような日常読み取りが deny になり V-12 の誤deny予算を破る |
| **C6: `$'...'` を含むコマンドを（証拠の有無に関わらず）一律 deny する** | **却下（過剰）。** 本 hook の判定は「保護対象に触れる証拠があるときだけ deny する」という設計原則（§3-4）に立っており、`printf $'a\tb'` 単独のような無関係なコマンドまで deny すると原則が崩れる。**証拠（言及ゲート／保護対象 cwd）を条件に残す**ことで、誤denyを「保護対象に触れるコマンドで `$'...'` を使った場合」だけに限定できる |
| **C6: `$'...'` の検出を awk セグメントに限る** | **却下。** デコード相違は awk 固有ではない。`sed $'s/a/b/\x77 tickets/active/APP-001.md' in.md`（`\x77`＝`w`）が実際に sed のファイル書き出しへ到達する（§2-3 C6-2）。head 非依存のステートメント単位規則にすることで同型の穴を同時に閉じる（D6-2(a)） |
| **行継続の畳み込みを「空白へ置換」のまま据え置く** | **却下。** bash は（条件付きで）削除して語を連結する。据え置くと `rm tickets/acti\`＋改行＋`ve/APP-001.md` が言及ゲートを偽にして allow のまま残る（§2-3 SH-1） |
| **行継続を「無条件に削除」する（第5版の QT-2）** | **却下（第6版で訂正）。** bash が区切りとする位置（バックスラッシュ偶数個・`#` コメント末尾）でも連結してしまい、**ステートメント区切りという証拠を失う**（C7）。「1文字の置換で安全側」という第5版の判断は**誤りであった**（M14・§6 R32）。正しい対処は条件付きの走査（§3-2-1 LX-1〜LX-5） |
| **行継続の解釈が曖昧なときに「連結した形」と「連結しない形」の両方を評価し、いずれかが deny なら deny にする（和集合方式）** | **却下。** 曖昧さの無い分岐（バックスラッシュの偶奇）にまで適用すると、**bash が連結する奇数形（`echo x\`＋改行＋`rm docs/SPEC.md`）まで deny になり、reviewer が明示した二方向要求（奇数形は allow のまま維持する）に反する**。曖昧さを残さず bash の規則をそのまま実装する方が、誤denyも穴も少ない |
| **`#` コメントを `lex()` の段階で本文ごと除去する** | **却下（本改訂では）。** 除去すると `cat a #> docs/SPEC.md`（old=deny）が allow に転じるなど **deny → allow 方向の変化**を生む。行継続の適用可否に必要なのは「コメントの範囲を知ること」だけであり、除去は不要（§6 R35・別チケット候補） |
| **ブレース展開・glob をパス候補抽出（`PATHISH_RE`）へ取り込む** | **本チケットではスコープ外（D6-2）。** old=allow の既存穴であり、正しく閉じるにはブレース展開の実装（入れ子・直積）が要る。`PATHISH_RE` へ `{},*` を単純に加えると `awk '{print}'` のようなプログラム語が「保護対象パス」と判定されうる（`_is_guarded_path("tickets/active/…")` の部分文字列判定と組み合わさると誤deny）。**`INVENTORY_CASES` へ `allow` として登録し（INV-SH-07・09）、§10-1 申し送り9 で別チケットを推奨する** |

### 3-8. cwd 追跡（第2版で新設・第3版で書き込み先オペランドの解決と伝播を追加）

**追跡（第2版から不変）:** `find_violation` はステートメントを左から右へ処理しながら `cwd`（追跡不能なら `None`）を持ち回る。`cd` セグメント自体は**常に allow**（AC3）で、更新後の cwd が保護対象なら以降のステートメントの**言及ゲートを無条件に真**とする。`cd` のオペランドが `$`・プレースホルダ・`~`・不在（`cd` / `cd -`）なら追跡打ち切り。

**第3版で追加する3点（H2 の修正本体）:**

1. **書き込み先オペランドの解決を単一ヘルパへ集約する。** `guarded_write_target(target, cwd, substs)` が「プレースホルダ解決 → `$`・未解決プレースホルダなら偽 → リテラルで保護対象なら真 → cwd が保護対象なら `normpath(join(cwd, target))` で判定」を担う。第2版で implementer が追加した `cwd_redirect_violation`（逸脱5・設計未記載の場当たり対応）を**この一般規則へ吸収して削除する**。呼び出し箇所は3つ（出力リダイレクト・`sort -o`・`uniq` 出力引数）＋degraded のリダイレクト走査。
2. **オペランドを取り出せない書き込み構文は「構文の存在」で判定する。** AW-3（awk の `print`/`printf` 出力リダイレクト）と sed の `w`／`W` は、cwd が保護対象なら**言及ゲート無しで**発火させる。これにより H2 の awk 4形・sed 2形が閉じる。**読み取り形（`{print $1}`・`NR>1 {print}`・`-F'\|'`・`sed -n '1,5p'`）は構文が一致しないため allow のまま**（§4-5 のトレースで確認）。
3. **置換再帰へ cwd を伝播する。** `find_violation` の置換再帰は現状ステートメント走査の**前**に一括で行われるため、`cd` 実行後の cwd を知らない。第3版では**置換をそれが出現したステートメントの評価時に、そのステートメントの cwd で再帰**させる（インデックス付きプレースホルダにより帰属が取れる＝P1 の副産物）。これで H2 の置換内3形が閉じる。

**フォールセーフ既定（P4-2）:** `CWD_SAFE_HEADS` に**明示収載されていない** head は、`cwd_is_guarded(cwd)` が真のとき deny する。現行の `ALLOWED_HEADS` 全 head は収載済み（根拠は §3-9 の「cwd 安全性」列）であり、**この規則は現時点では到達しない**。目的は「将来 `ALLOWED_HEADS` へ head を追加したとき、cwd 次元の棚卸しを忘れたら deny 側へ落ちる」という既定を持つことである（`op_kind` の到達不能な既定 `"sep"` と同じ性質の防御的既定であり、reviewer が第2版で「既定 `sep` は到達不能」を改善と評価した先例に倣う）。

**安全性:** 本機構は言及ゲートを真にする／書き込み先オペランドの解決範囲を広げる／未収載 head を deny する、のいずれも **allow → deny 方向にしか作用しない**。

**限界（docstring・README へ明記）:**
- `cd "$DIR"` / `cd $(…)` / `cd` / `cd -` は追跡できない（allow 方向）。
- **M5: `cd tickets/active && cd .. && rm active/APP-001.md`** のように、cwd と相対パスの**両方に接頭辞が分割**された形は閉じない（old=deny → new=allow の**未閉塞の限界**）。一般解は誤deny爆発を招くため実装せず、限界として明記する（reviewer 判定）。
- **M6: `cd tickets/active && mv /tmp/e APP-001.md` は allow（意図どおり）。** `mv` は CLAUDE.md が active/↔done/ の移動の正規手段と定めているため §3-9 で意図的に許可している。cwd 下でも同じ扱いとする。

### 3-9. 書き込みベクタ棚卸し表（P3・第3版で4次元 × head へ全面改訂・第4版で awk 行をホワイトリスト化し §3-11 へ写像）

**`ALLOWED_HEADS` にコマンドを追加するときは、必ず本表へ行を追加し、下記4次元すべてを走査すること。** 表に行が無いコマンドを `ALLOWED_HEADS` へ入れてはならない。**本表の主張は「危険な経路を漏れなく列挙した」ではなく「各 head について4次元を走査し、その結果と未対応を明記した」である**（§3-10）。

**4次元:** ① 書き込みを行うオプション ② 外部コード（プログラム・ライブラリ・拡張）を読み込むオプション ③ プログラム／スクリプト内の書き込み・実行構文 ④ 位置引数そのものが出力先

| head | ① 書き出しオプション | ② 外部コード読み込み | ③ 内部構文 | ④ 位置引数の出力先 | 本設計の対応 | cwd 安全性（`CWD_SAFE_HEADS`） |
|---|---|---|---|---|---|---|
| `ls` `cat` `head` `tail` `wc` `diff` `stat` `file` `basename` `dirname` `test` `[` `pwd` `echo` `nl` `rev` `realpath` `readlink` `cut` `tr` | なし | なし（`grep -f`・`file -f` は入力のみ） | なし | なし | 追加規則なし | **収載**（書き込み経路が無いため cwd に依らず安全） |
| `grep` | なし | `-f FILE` は**パターン入力**のみ | なし | なし | 追加規則なし | **収載** |
| `cd` | — | — | — | — | §3-8 cwd 追跡（セグメント自体は無条件許可＝AC3） | **収載** |
| `mv` | — | — | — | **引数そのものが書き込み** | **意図的に許可**（CLAUDE.md が active/↔done/ 移動の正規手段と定める）。cwd 下でも同じ（**M6**） | **収載**（意図的許可） |
| `sed` | `-i`（全表記） | **`-f SCRIPT`（不可視）→ 未対応** | `w FILE` / `W FILE`／**GNU `e` フラグ・`e` コマンド（任意コマンド実行）→ 未対応** | なし | `-i`＝`SED_INPLACE_SHORT_RE`＋`--in-place[=]`／`w`＝`SED_WRITE_RE`（**cwd 対応**＝§3-8）。`-f`・`e` は**未対応**（§3-6 D4・§6 R12）。**この次元は第4版でもブラックリストのまま**（awk と異なりホワイトリスト化していない＝§6 R25）。写像は **§3-11 の INV-SED-01〜08** | **収載**（`w`／`-i` が cwd 対応。`-f`・`e` の残存は cwd に依存しない既存穴） |
| `awk` | **`-o[F]` `-p[F]` `-d[F]` → AW-4** | **`-f` `-i`(=C3) `-l` `-E` `-D` → AW-4** | **プログラム本文は AW-7 のホワイトリスト**（許可文字集合・呼び出し名・出力構文の3条件）。個別構文と写像は **§3-11 の INV-AWK-01〜20 を正とする**（`print`→`>` `>>` `\|` `\|&`／**`"cmd" \| getline`・`\|&`(=C5)**／`system(`・`system (`(=C4)／**`@f(`・`@ f(`**／`@load`・`@include`・`@namespace`／`ENVIRON`・`PROCINFO` 経由／**未知の組み込み関数**／**許可外の文字**） | なし | AW-1／AW-2／AW-3（**cwd 対応**）／**AW-4（オプションのホワイトリスト）**／**AW-7（プログラム本文のホワイトリスト＝この次元の機械的既定）**。AW-5／AW-6 は degraded 専用 | **収載**（AW-3 が cwd 対応・AW-4/7 が cwd ゲートを持つ） |
| `sort` | **`-o FILE` / `--output=FILE` → 本改訂で対応** | **`--compress-program=PROG`（外部プログラム実行）→ 未対応** | なし | なし | `sort_output_operands` +  `guarded_write_target`（**絶対パス形・cwd 相対形の両方**＝§3-6 D4） | **収載** |
| `uniq` | なし | なし | なし | **第2位置引数がそのまま出力先 → 本改訂で対応** | `uniq_output_operand` + `guarded_write_target`（フラグの引数消費を考慮） | **収載** |
| `find` | `-fprint` `-fprintf` `-fls` **`-fprint0`（列挙漏れ→本改訂で追加）** | なし | `-exec` `-execdir` `-ok` `-okdir` `-delete` | なし | `FIND_WRITE_FLAGS`（**`-fprint0` を追加**＝§3-6 D4） | **収載**（フラグ名で判定＝パス非依存） |
| `git` | **`--output=FILE`（diff 系オプション）→ 未対応** | なし（`GIT_EXTERNAL_DIFF` は環境変数） | 書き換え系サブコマンド | なし | `ALLOWED_GIT_SUBCOMMANDS` のホワイトリスト。`--output` は**未対応**（§3-6 D4・§6 R12） | **収載**（サブコマンド名で判定＝パス非依存） |
| `python3` / `python` | 任意実行 | 任意実行 | 任意実行 | 任意実行 | `READONLY_SCRIPTS` の例外経路のみ（`is_readonly_script_call`）。**許可経路の偽装は未対応（深刻度 high・任意コード実行）**＝§3-6 D7・§6 R21 | **`ALLOWED_HEADS` 非収載＝既定で deny のため `CWD_SAFE_HEADS` にも入れない。** `READONLY_SCRIPTS` 例外は CW-1 判定より**前**に return するため cwd に依らず許可される（AC6 の維持に必要） |
| `printf` | — | — | — | — | **意図的に非ホワイトリスト**（README のローカル動作確認の注記と対。tester テストで固定） | — |

#### 3-9-2. シェル展開の次元（第5版で新設 — 「bash が解釈するがコマンド文字列上は見えない変換」）

**この次元は第4版まで棚卸し表に1行も無かった。** C6 はこの空白から出た。**hook はコマンド**文字列**を見るが、コマンドが実際に受け取るのは bash が展開した後の argv である。** 両者が食い違う変換をすべて列挙し、相違の**向き**（allow 方向＝危険／deny 方向＝保守的）と対処を明記する。写像は §3-11 の INV-SH-01〜12。

| 変換 | bash の挙動 | hook の見え方 | 相違の向き | 本設計の対応 | INV ID |
|---|---|---|---|---|---|
| **`$'…'`（ANSI-C 引用）** | `\xHH`・`\nnn`・`\uHHHH`・`\cX`・`\t` 等を**デコードする**。展開結果は**再解析されない**（クォート・演算子として作用しない） | 素の `'…'` と同一視。エスケープは字面のまま語へ入る | **allow 方向（＝C6・実行される）** | **QT-1**（判定不能→deny）。証拠が符号化される形は **QT-3**（復号して証拠に加える） | INV-SH-01〜04 |
| **`$"…"`（ロケール翻訳）** | メッセージカタログがあれば**任意の文字列へ置換**（無ければ字面。`TEXTDOMAIN`/`TEXTDOMAINDIR` 依存） | `"…"` と同一視 | **allow 方向**（カタログを置ける攻撃者に限る・理論上） | **QT-1**（同じ規則で deny）。**デコードは原理的に不可能**なので証拠復号（QT-3）は効かない＝判定不能として扱うことが唯一の正解 | INV-SH-05 |
| **行継続（`\`＋改行）** | **条件によって「連結」と「区切り」に分岐する（第6版でサブケースへ分解）** | 第5版は**無条件に連結**していた | **両方向**（連結しすぎ＝区切りの喪失／連結しなさすぎ＝語の分断） | **LX-1〜LX-5**（§3-2-1）。**分岐条件と期待判定は下の 3-9-2-1 の表を正とする** | INV-SH-06・13〜17 |
| **`#` コメント（第6版で新設）** | クォート外で**語頭**に現れた `#` から**改行まで**を捨てる。コメント内で `\` は特別な意味を持たない | **正規化走査でのみモデル化**（行継続の適用可否の判定に限る）。`lex()`／degraded は**コメント本文を語として走査する** | **行継続との相互作用では allow 方向（＝C7）／それ以外は deny 方向（保守側）** | **LX-3**（コメント末尾のバックスラッシュに行継続を適用しない）。**本文の除去は行わない**（除去は deny→allow 方向・§6 R35） | INV-SH-14・18〜20 |
| **ブレース展開 `{a,b}`** | `tickets/{active,done}/X.md` → 2語へ展開 | 1語のまま。`PATHISH_RE` が `{},` を含まないため保護対象パスが**再構成されない** | **allow 方向（old=allow・未対応）** | **未対応（D6-2・別チケット）。`INVENTORY_CASES` へ `allow` で登録**し docstring の限界・§10-1 申し送り9 に明記 | INV-SH-07 |
| **パス名展開（glob `*` `?` `[…]`）** | パターンに一致するパスへ展開 | 断片は見える。`tickets/active/*.md` は `tickets/active` が残るため **deny になる**が、`tickets/acti*e/X.md` は分断されて allow | 部分的に **allow 方向**（old=allow・未対応） | 同上（`{}` と同じ行で扱う） | INV-SH-08（deny 側）／INV-SH-09（限界） |
| **チルダ展開 `~`** | `$HOME` 等へ | `~` は `PATHISH_RE` 外だが**残りのパスは見える**。`cd ~` は `next_cwd` が追跡を打ち切る | 相違なし（証拠は残る／`cd` は保守側で打ち切り） | **対応済み**（既存動作。テストで固定する） | INV-SH-10 |
| **パラメータ展開 `$VAR` `${v:-x}` `${v/a/b}`** | 変数の値へ | **値は不可視**。同一コマンド内の代入だけ `guarded_vars` が追跡し、**リダイレクト先**については deny する | **allow 方向（構造的限界・old=allow・R6 として既知）** | **閉じられない**（別コマンドで export された値は hook から原理的に見えない）。`guarded_write_target` は `$` を含む先を偽として扱う。`INVENTORY_CASES` へ `allow` で登録し可視化 | INV-SH-11 |
| **算術展開 `$((…))`** | 数値へ | `$(` として**コマンド置換扱い**になり内側を再帰評価する | **deny 方向（保守側）** | 対応不要（保守側の副作用）。テストで固定する | INV-SH-12 |
| **履歴展開 `!!` `!$`** | **非対話シェルでは無効**（`histexpand` は既定オフ。Bash ツールは `bash -c` 相当の非対話実行） | そのまま | 相違なし | 対応不要。**根拠を明記**（P6: 前提を書くなら根拠を書く） | — |
| **コマンド置換 `$(…)`・`` `…` ``・プロセス置換 `<( )` `>( )`** | 子プロセスの出力／FIFO へ | 内側テキストを `substs` へ退避し**再帰評価**する | 対応済み（deny 方向） | 既存（C1・H2 で対処済み） | 既存 T-NEST／T-PROC |

##### 3-9-2-1. 行継続と `#` コメントの分岐条件（第6版で新設・原則 P7）

**第5版はこの分解を持たず「行継続」を1行として扱ったため、bash の挙動が真逆になる2条件（バックスラッシュの偶奇・コメント内）が列挙から漏れ、V-14 が合格したまま C7 になった（H3・SV-15）。** 以下が本改訂の正である。

| INV ID | サブケース（`<LF>` は生の改行） | bash の挙動 | hook の期待判定 | 代表ケース |
|---|---|---|---|---|
| INV-SH-06 | **語内・語間**の行継続（クォート外・バックスラッシュ**1個**） | **連結**（語が繋がる） | **deny**（連結した語が保護対象になる） | `rm tickets/acti\<LF>ve/APP-001.md` |
| **INV-SH-13** | バックスラッシュが**偶数個**（`\\`+LF・`\\\\`+LF） | **リテラルの `\` ＋区切り**（改行は制御演算子） | **deny**（後続文の head を独立に判定する＝**C7**） | `echo x\\<LF>rm docs/SPEC.md` |
| **INV-SH-14** | **`#` コメント末尾**のバックスラッシュ | **区切り**（コメント内で `\` は特別扱いされない） | **deny**（同上＝**C7**） | `cat README.md #\<LF>sed -i 's/a/b/' tickets/active/APP-001.md` |
| **INV-SH-15** | バックスラッシュが**奇数個**で後続コマンドの head が前の語へ吸収される形 | **連結**（後続コマンドは実行されない） | **allow**（deny は誤り＝**二方向要求の固定点**） | `echo x\<LF>rm docs/SPEC.md` |
| **INV-SH-16** | **`'…'` の内側**の `\`+改行 | **連結しない**（両方リテラル。改行は区切りにもならない） | **allow**（bash が触るのは保護対象ではない別名。old=allow・§4-9 の**許容分類9**） | `rm 'tickets/acti\<LF>ve/APP-001.md'` |
| **INV-SH-17** | **`"…"` の内側**の `\`+改行 | **連結する**（ダブルクォート内でも行継続） | **deny**（連結した語が保護対象になる） | `rm "tickets/acti\<LF>ve/APP-001.md"` |
| **INV-SH-18** | **ダブルクォート内のコマンド置換**の内側にコメント（＝置換の中はコマンド文脈へ戻る） | 内側の `#` は**コメント**＝改行は区切り | **deny**（**C7-2**。LX-4 が内側の正規化を再帰へ委ねることで閉じる） | `cat "$(ls #\<LF>rm docs/SPEC.md)"` |
| **INV-SH-19** | 行継続で連結した結果、**basename が保護対象でなくなる**形 | 連結（触るのは `SPEC.md.bak`） | **allow**（§4-9 の許容分類8・第5版から維持） | `echo x > docs/SPEC.md\<LF>.bak` |
| **INV-SH-20** | **語中**の `#`（語頭でない）／**クォート内**の `#` | コメントではない＝行継続はそのまま適用される | **deny**（`#` を理由に連結を止めてはならない。**AC1 の「クォート内 `#` で特殊構文判定をしない」の固定点**） | `rm 'x#y' tickets/acti\<LF>ve/APP-001.md` |

**この表の維持規律（第6版で P7 を追記）:** `lex()` の走査・`normalize_line_continuations()` を変更するときは、**必ず本表の行と 3-9-2-1 の分岐を見直し、V-14（字句レイヤの実機検証）を再実行する**。新しい変換を見つけたら、まず**その変換について bash の挙動が分岐する条件を列挙し**（P7）、分岐ごとに規則化するか、スコープ外なら `INVENTORY_CASES` へ `allow` で登録する（P5）。**「相違を生まない」と書くときは V-14 の実測を根拠として添える（P6）。「安全側の近似だから分岐は不要」と書いてはならない（M14 の訂正）。**

**未対応（別チケット）の一覧 — 本改訂後に残る穴:** `sed -f SCRIPT`／GNU sed の `e`（任意コマンド実行）／`sort --compress-program`（外部プログラム実行）／`git diff --output=FILE`／**`is_readonly_script_call` の許可経路偽装（任意コード実行・最優先）**／**M5（cwd 接頭辞 + 相対パスの分割）**／パスがリテラルに現れない形（変数・置換の出力・`awk -v d=tickets -v f=active/X.md '{print > (d"/"f)}'` のような断片結合。old=allow）／**sed のプログラム内構文次元がブラックリストのまま**（§6 R25）／**ブレース展開・glob による保護対象パスの分断**（第5版で新規に列挙＝§3-9-2・§6 R31）。

**本表の維持規律（第4版・P5）:** 本表に構文を1つ書き加えたら、**必ず §3-11 の写像表と `INVENTORY_CASES` へ対応する行を追加する。** 表にだけ書いて実装・テストへ写像しない状態は欠陥である（C5 がその実例）。

### 3-10. 棚卸しの網羅根拠と維持手順（SV-7 への回答）

**SV-7 の指摘:** §3-9 は H1 の再発防止のために新設した機構（P3）だったが、その機構自体が同じ抜け方をした（`awk` 行に `-i inplace`・`system()` が無く、`cd` 行に相対パス書き込み全般が無い）。**したがって「表を書いた」ことは網羅の根拠にならない。**

**第3版が「網羅した」と主張できる根拠（3層）:**

1. **走査の方法を固定した。** 各 head について**4次元**（§3-9）を走査する。第2版の表は「引数だけで書ける／任意実行できる経路」という単一列で、次元が混在していたため「オプション次元」が丸ごと落ちた（C3・C4 はどちらもオプション次元／内部構文次元の別セル）。次元を列にしたことで**空白セルが可視になる**。
2. **根拠の出所を記録し、実機と突き合わせる。** 表の各セルは GNU gawk・GNU sed・GNU coreutils（`sort`・`uniq`・その他読み取り系）・GNU findutils・git のオプション仕様に基づく。
   - **オプション次元:** `awk` の安全オプション集合（`AWK_SAFE_FLAGS` / `AWK_SAFE_VALUE_FLAGS`）は実機の `awk --help` / `man` と突き合わせて検証する（§4-9 **V-10**・Phase 6 で実施済み）。
   - **プログラム内構文次元（第4版で新設）:** AW-7 の健全性の前提 S1〜S4（§3-6 D1）と、`AWK_SAFE_CALLS` の組み込み関数の副作用の無さを実機で検証する（§4-9 **V-11**）。第3版までこの次元には**実機検証手順が無く、「非成立形を実測した」という一回性の確認がブラックリストの妥当性の唯一の根拠だった**（implementer の提案9）。V-11 はそれを手順として固定するものである。
   - **字句レイヤ（第5版で新設）:** `lex()` が語へ分解した結果と、**bash が実際にコマンドへ渡す argv** を突き合わせる（§4-9 **V-14**）。第4版までこの次元には検証手順が無く、**「`$'...'` は1トークンに収まるので保守側へ倒れる」という未検証の前提が設計書と docstring に書かれたまま6ラウンド目の穴（C6）になった**。V-14 は「相違があるのに規則も限界記載も無い形」をゼロにすることを合否条件とする（P6）。
   - いずれも突き合わせの結果は「収載漏れ＝誤deny（可視・修正容易）」の方向にしか外れない（P4）。
3. **列挙が漏れても deny 側へ落ちる既定を持つ（P4）。** これが SV-7 への本質的な回答である。**第4版で3次元すべてが既定を持つようになった。**
   - **オプション次元**（C3 が属する次元）: `awk` はホワイトリスト。**未知のオプションは deny。** 新しい gawk オプションが追加されても、収載しない限りすり抜けない。
   - **cwd 次元**（H2 が属する次元）: `CWD_SAFE_HEADS` は明示列挙。**未収載 head は保護対象 cwd 下で deny。** `ALLOWED_HEADS` へ足すだけでは cwd 下の許可を得られない。
   - **プログラム内構文次元**（C4・`@f()`・C5 が属する次元）: **AW-7 のプログラム形ホワイトリスト。未知の文字・未知の呼び出し名は deny。** gawk に新しい実行構文が追加されても、その構文が `@`・`\|` を使うか新しい関数名を要するなら自動的に deny 側へ落ちる。**第3版の「この次元には機械的な既定が無い」（R23）は第4版で解消した**（§6 R23 の行を参照）。
   - **字句レイヤ（C6 が属する次元・第5版で新設）: QT-1 の「判定不能→deny」。** hook が見る字面と bash が実行する文字列が一致しないことが確定した入力クラス（`$'…'`／`$"…"`）は、ホワイトリストにもブラックリストにも掛けず、証拠が立つ時点で deny する。**この既定は degraded 経路にも置く**（§3-5・部分文字列判定）ため、字句解析の成否に関わらず有効である。
   - **なお degraded mode（字句解析不能な入力）には AW-7 の既定が及ばない**（§3-5 の第4版追加3点）。degraded は P2 により旧版の上位互換であることのみを保証する（§6 R28）。**ただし QT-1 は degraded にも置く**（第5版）。

**`ALLOWED_HEADS` に新コマンドを追加するときの手順（docstring と本節に同内容を置く）:**

1. `man <cmd>` / `<cmd> --help` を**4次元で走査**し、該当する経路をすべて洗い出す。判断に迷う経路は「危険」側に分類する。
2. §3-9 の表へ行を追加する（4列すべてを埋める。「なし」も明記する。空白セルを残さない）。
3. **cwd 安全性を判定する。** 「書き込み経路が無い」「書き込み判定がパスに依存しない（フラグ名・サブコマンド名で決まる）」「書き込み判定が cwd 対応済み」のいずれかを満たすときだけ `CWD_SAFE_HEADS` へ収載する。**満たさない／判定できない場合は収載しない**（保護対象 cwd 下で deny になる＝安全側）。
4. 新たに deny できるようになった形と、意図的に allow のままにする形の**両方**を `LEGACY_DENY_COMMANDS`（§4-7）と allow 固定テストへ追加する。
5. **§3-11 の写像表と `INVENTORY_CASES` へ、表に書いた各構文の行を追加する（P5・第4版で追加）。** 規則ID・INV ID・期待判定の3つが揃わない構文を表に残してはならない。
6. docstring の「既知の限界」を更新する。

### 3-11. 棚卸し表と実装・テストの機械的対応（P5・第4版で新設）

**解決する問題:** 第3版 §3-9 の `awk` 行は `cmd | getline` と `|&` を**列挙していた**が、対応する規則もテストも存在せず、4回目の穴（C5）になった。**表は「気づき」の道具でしかなく、実装・テストへの写像が無ければ防御にならない。**

**規約（P5）:** §3-9 に列挙した各構文は、次の3つを必ず持つ。
1. **規則ID**（AW-x／`SED_*`／`FIND_WRITE_FLAGS` 等。未対応なら「未対応（別チケット）」と明記）
2. **INV ID**（`tests/test_guard_bash_writes.py` の `INVENTORY_CASES` に置く判定固定ケースの識別子）
3. **期待判定**（`deny` / `allow`）。「未対応の穴」は `allow` として登録し、**穴がコード上で可視になる**ようにする

`INVENTORY_CASES` は `(INV ID, 構文の説明, コマンド, 期待判定)` のリストとし、1つのテストが全件を `subTest` で検証する（`LEGACY_DENY_COMMANDS`／T-LEGACY と同じ hermetic なブラックボックス方式）。**`LEGACY_DENY_COMMANDS` が「旧版が deny していた集合を守る」ためのものであるのに対し、`INVENTORY_CASES` は「棚卸し表の各セルが実装されていることを守る」ためのものである**（目的が異なるため統合しない）。

**awk のプログラム内構文（§3-9 ③列）の写像 — 第4版で確定する正:**

| INV ID | 構文 | 規則 | 期待 |
|---|---|---|---|
| INV-AWK-01 | `print`/`printf` … `>` `>>`（出力リダイレクト） | AW-1／AW-3／AW-7(4) | deny |
| INV-AWK-02 | `print` … `\| "cmd"`（出力パイプ） | AW-1／AW-3／**AW-7(2) 許可文字 `\|`** | deny |
| INV-AWK-03 | `print` … `\|& "cmd"`（コプロセス出力） | 同上 | deny |
| INV-AWK-04 | **`"cmd" \| getline`（入力パイプ＝C5）** | **AW-7(2) 許可文字 `\|`** | deny |
| INV-AWK-05 | **`"cmd" \|& getline`（コプロセス入力＝C5）** | **AW-7(2) 許可文字 `\|`** | deny |
| INV-AWK-06 | `system("…")` | **AW-7(3) 呼び出し名**／AW-5(degraded) | deny |
| INV-AWK-07 | `system ("…")`（組み込み名と `(` の間の空白。gawk は受け付ける＝実測） | 同上（`IDENT\s*\(`） | deny |
| INV-AWK-08 | `@f("…")` 間接関数呼び出し | **AW-7(2) 許可文字 `@`**／AW-6(degraded) | deny |
| INV-AWK-09 | `@ f("…")`／`@<TAB>f("…")`（`@` 直後の空白。gawk は受け付ける＝実測） | 同上 | deny |
| INV-AWK-10 | `@load` / `@include` | 同上 | deny |
| INV-AWK-11 | `@namespace`（gawk 5 の名前空間指令） | 同上（**第4版で新規に deny＝誤deny側・R26 に列挙**） | deny |
| INV-AWK-12 | 未収載の関数呼び出し（未知の組み込み・拡張関数） | **AW-7(3) 未知名は deny＝この次元の機械的既定** | deny |
| INV-AWK-13 | 許可文字集合外の文字（`\`・バッククォート・`'`・非ASCII） | **AW-7(2)** | deny |
| INV-AWK-14 | `ENVIRON`／`PROCINFO` 経由の出力先間接参照（`print > ENVIRON["O"]`） | AW-1／AW-3／AW-7(4) | deny |
| INV-AWK-15 | 変数束縛経由の出力先（`-v f=…` / 位置引数 `f=…`） | AW-2 | deny |
| INV-AWK-16 | `getline < "FILE"`（`\|` を伴わない読み取り） | 規則なし（**読み取りのため意図的に allow**） | allow |
| INV-AWK-17 | ユーザー定義関数の宣言と呼び出し（`function p(x){…} {p($1)}`） | AW-7(3) の宣言名収集 | allow |
| INV-AWK-18 | 比較演算子の `>`（`NR>1`・`{if ($1 > 2) print $1}`） | AW-7(4) の順序条件 | allow |
| INV-AWK-19 | 文字列／正規表現リテラル内の `@`・`\|`・`system(` | AW-7(1) の1パス除去（**M11 の解消**） | allow |
| INV-AWK-20 | プログラム以外のオペランド（ファイル名・束縛値）中の `system(`・`@f(` | AW-7 の適用対象がプログラム本文のみ（**M12 の解消**） | allow |

**INV-AWK-16 のケース選定に関する注意（設計時に語単位でトレース済み）:** `getline` を使う読み取りでも、**プログラム語に保護対象パスがリテラルで現れ、かつ同じ語に `>` が含まれる形**（`awk 'BEGIN{while((getline l < "tickets/active/APP-001.md")>0) print l}'`）は **AW-1 が deny する**（第2版から続く既存の保守的判定。§6 R11）。したがって INV-AWK-16 のケースには `>` を含まない形（`awk 'BEGIN{getline l < "tickets/active/APP-001.md"; print l}'`）を用いる。**この非対称は第4版で変更しない**（AW-1 を触ると既存 deny が動くため）。

**sed のプログラム内構文（§3-9 ③列）の写像:**

| INV ID | 構文 | 規則 | 期待 |
|---|---|---|---|
| INV-SED-01 | `s/a/b/w FILE` | `SED_WRITE_RE` | deny |
| INV-SED-02 | `/re/W FILE` | `SED_WRITE_RE` | deny |
| INV-SED-03 | `-i` 全表記（`-i` / `-i.bak` / `-ni` / `--in-place[=SUFFIX]`） | `SED_INPLACE_SHORT_RE` ほか | deny |
| INV-SED-04 | 保護対象 cwd 下の `w REL`（正常経路・degraded） | `strict` 分岐＋空白結合 | deny |
| INV-SED-05 | **`-f SCRIPT`（プログラム不可視）** | **未対応（D6-3・別チケット）** | allow |
| INV-SED-06 | **GNU sed の `s///e`（置換結果をコマンド実行）** | **未対応（D6-3・別チケット）** | allow |
| INV-SED-07 | **GNU sed の `e` コマンド** | **未対応（D6-3・別チケット）** | allow |
| INV-SED-08 | `-n '1,5p'`（読み取り） | 規則なし（意図的に allow） | allow |

**シェル展開の次元（§3-9-2）の写像 — 第5版で新設:**

| INV ID | 変換 | 規則 | 期待 | 代表ケース（`INVENTORY_CASES` へ置くコマンド） |
|---|---|---|---|---|
| INV-SH-01 | `$'…'` の hex エスケープで `system(` を隠す（C6） | **QT-1** | deny | `awk $'BEGIN{x="foo\x22system(\x22rm tickets/active/APP-001.md\x22)\x22"}'` |
| INV-SH-02 | `$'…'` の hex エスケープで `\|`（入力パイプ）を隠す（C6・C5 再開通） | **QT-1** | deny | `awk $'BEGIN{x="foo\x22(\x22rm tickets/active/APP-001.md\x22\x7cgetline x)\x22"}'` |
| INV-SH-03 | `$'…'` × 保護対象 cwd（リテラルパスが現れない形） | **QT-1**（cwd ゲート） | deny | `cd tickets/active && awk $'BEGIN{x="foo\x22system(\x22rm APP-001.md\x22)\x22"}'` |
| INV-SH-04 | `$'…'` で**保護対象パス自体**を符号化して証拠を隠す | **QT-3**（証拠復号）＋QT-1 | deny | `rm $'\x74ickets/active/APP-001.md'` |
| INV-SH-05 | `$"…"`（ロケール翻訳） | **QT-1** | deny | `awk $"BEGIN{print}" tickets/active/APP-001.md` |
| INV-SH-06 | 行継続による語の分断（**クォート外・奇数1個＝連結**） | **LX-2 条件1** | deny | `rm tickets/acti\`＋改行＋`ve/APP-001.md` |
| INV-SH-07 | **ブレース展開**による分断（**未対応・別チケット**） | **未対応（D6-2）** | **allow** | `rm tickets/{active,done}/APP-001.md` |
| INV-SH-08 | glob（証拠が残る形） | 既存の候補抽出 | deny | `rm tickets/active/*.md` |
| INV-SH-09 | glob（パス中に `*` が入り分断される形。**未対応・別チケット**） | **未対応（D6-2）** | **allow** | `rm tickets/acti*e/APP-001.md` |
| INV-SH-10 | チルダ展開（残りのパスが証拠として残る） | 既存の候補抽出 | deny | `rm ~/kit/tickets/active/APP-001.md` |
| INV-SH-11 | パラメータ展開（**値は不可視＝構造的限界**） | **未対応（R6・原理的に閉じられない）** | **allow** | `f=tickets/active/APP-001.md; rm $f`（**リダイレクト先**は `guarded_vars` で deny される＝`test_redirect_to_variable_deny`） |
| INV-SH-12 | 算術展開（`$(` としてコマンド置換扱いになり内側を再帰評価する＝保守側） | 既存の置換再帰 | allow | `echo $((1+2)) && cat tickets/active/APP-001.md`（**読み取り＝allow が正**。置換扱いが誤denyを生んでいないことの固定。内側が保護対象に言及すれば deny になる＝保守側であることは §3-9-2 に記載） |
| **INV-SH-13** | 行継続・バックスラッシュ**偶数**個（＝**区切り**・C7） | **LX-2 条件1** | **deny** | `echo x\\`＋改行＋`rm docs/SPEC.md` |
| **INV-SH-14** | 行継続・**`#` コメント末尾**（＝**区切り**・C7） | **LX-2 条件2／LX-3** | **deny** | `cat README.md #\`＋改行＋`sed -i 's/a/b/' tickets/active/APP-001.md` |
| **INV-SH-15** | 行継続・**奇数**個で後続コマンドが語へ吸収される形（＝**連結**） | **LX-2 条件1** | **allow** | `echo x\`＋改行＋`rm docs/SPEC.md`（**bash も連結するため deny は誤り＝二方向要求の固定点**） |
| **INV-SH-16** | 行継続・**`'…'` の内側**（bash は連結しない） | **LX-2 条件3** | **allow** | `rm 'tickets/acti\`＋改行＋`ve/APP-001.md'`（触るのは保護対象ではない別名。old=allow） |
| **INV-SH-17** | 行継続・**`"…"` の内側**（bash は連結する） | **LX-2 条件4** | **deny** | `rm "tickets/acti\`＋改行＋`ve/APP-001.md"` |
| **INV-SH-18** | **ダブルクォート内のコマンド置換**の中のコメント（C7-2） | **LX-4** | **deny** | `cat "$(ls #\`＋改行＋`rm docs/SPEC.md)"` |
| **INV-SH-19** | 行継続で連結した結果 basename が保護対象でなくなる形 | **LX-2 条件1**（許容分類8） | **allow** | `echo x > docs/SPEC.md\`＋改行＋`.bak` |
| **INV-SH-20** | **クォート内・語中**の `#` はコメントではない（行継続は適用される） | **LX-3**（AC1 との整合） | **deny** | `rm 'x#y' tickets/acti\`＋改行＋`ve/APP-001.md` |

**INV-SH-11・INV-SH-07・INV-SH-09 は「意図的に未対応の穴」である。** 期待値 `allow` で登録することにより、**塞いだ時点で `deny` へ変える意識的な行為**が必要になる（P5）。**INV-SH-15・16・19 は「未対応の穴」ではなく「bash と一致した結果としての allow」であり、`deny` へ変えたら誤りである**（両者を混同しないよう `INVENTORY_CASES` のコメントで区別する）。

**運用:** reviewer／tester は「§3-9／§3-9-2／§3-9-2-1 の各構文が §3-11 の写像表に現れ、その全件が `INVENTORY_CASES` に存在すること」を件数で突き合わせる（§4-9 **V-13**）。第5版で **28件 → 40件**、**第6版で 40件 → 48件**（INV-AWK 20＋INV-SED 8＋**INV-SH 20**）になる。

## 4. 実装詳細

**ファイル構成（変更・追加）:**
```
.claude/hooks/guard_bash_writes.py   ← 変更（Phase 10。docstring も同一コミットで更新）
tests/test_guard_bash_writes.py      ← 変更（追加のみ。既存129件は無変更。Phase 10・11）
.claude/hooks/README.md              ← 変更（Phase 11）
CHANGELOG.md                         ← 変更（Phase 11）

tests/_util.py                       ← 変更なし
.claude/hooks/_ticket_lib.py         ← 変更なし（§3-6 D2）
.claude/settings.json                ← 変更なし
```

### 4-1. 定数（第3版の追加・訂正＋第4版の追加）

**訂正（M9・逸脱1）— 第2版 §4-1 の「重要（最長一致の順序）」注記は誤りであった。** 第2版は「`OPERATOR_KINDS` の並びを『3文字 → 2文字 → 1文字』に保つこと」を要求したが、**表の literal な並びに依存する設計そのものが脆い**（実際に第2版の疑似コードは `"|"` が `"||"` より前にあり、`next(o for o in OPERATORS if command.startswith(o, i))` が `||` を `|` と誤検出するバグを持っていた）。**現行実装が採った「長さの降順へ明示的に整列して導出する」方式を設計の正とする。** 表の並び順は最長一致の正しさに影響しない。

```python
OPERATOR_KINDS = (
    ("<<<", "heredoc"),
    ("<<", "heredoc"), (">>", "redirect_out"), ("&>", "redirect_out"),
    (">&", "redirect_out"), (">|", "redirect_out"), ("<>", "redirect_out"),
    ("<&", "redirect_in"), ("|&", "pipe"),
    ("&&", "sep"), ("||", "sep"), (";;", "sep"),
    (">", "redirect_out"), ("<", "redirect_in"), ("|", "pipe"),
    (";", "sep"), ("&", "sep"),
)
# 走査は先頭一致の最初の候補を採るため、長さの降順へ明示的に整列して導出する
# （安定ソートなので同じ長さの相対順序は表のまま）。表の並びには依存しない。
OPERATORS = tuple(op for op, _ in
                  sorted(OPERATOR_KINDS, key=lambda kv: -len(kv[0])))
OPERATOR_KIND_MAP = dict(OPERATOR_KINDS)
```

**第3版で追加する定数:**

```python
# 保護対象ディレクトリを作業ディレクトリとする状況でも許可できる先頭コマンド。
# 収載の条件は §3-9「cwd 安全性」列の3条件のいずれかを満たすこと。
# ALLOWED_HEADS へ head を追加しても、ここへ収載しない限り保護対象 cwd 下では
# deny になる（棚卸しの忘れが deny 側へ落ちるフォールセーフ既定＝原則 P4）。
CWD_SAFE_HEADS = {
    "ls", "cat", "head", "tail", "wc", "grep", "diff", "sort", "uniq",
    "stat", "file", "basename", "dirname", "test", "[", "cd", "pwd", "echo",
    "cut", "tr", "nl", "rev", "realpath", "readlink", "mv", "sed", "awk",
    "find", "git",
}

# awk の「読み取りだけで完結する」オプション（値を取らない形）。
AWK_SAFE_FLAGS = {
    "-", "--",
    "-b", "-c", "-C", "-g", "-h", "-n", "-M", "-N", "-P", "-r", "-s", "-S",
    "-t", "-V",
    "--characters-as-bytes", "--traditional", "--compat", "--copyright",
    "--gen-pot", "--help", "--non-decimal-data", "--bignum",
    "--use-lc-numeric", "--posix", "--re-interval", "--no-optimize",
    "--sandbox", "--lint-old", "--version", "--lint",
}
# 値を取る安全なオプション（-FVALUE / -F VALUE / --long=VALUE / --long VALUE）。
AWK_SAFE_VALUE_FLAGS = ("-F", "-v", "-e",
                        "--field-separator", "--assign", "--source", "--lint")

# awk の外部コマンド実行・拡張読み込み（AW-5）。文字列リテラル除去**前**に当てる。
AWK_EXEC_RE = re.compile(r"\bsystem\s*\(|@(?:load|include)\b")

# sort / uniq の出力先オペランド（§3-6 D4）
SORT_OUTPUT_FLAGS = ("-o", "--output")
UNIQ_VALUE_FLAGS = ("-f", "-s", "-w",
                    "--skip-fields", "--skip-chars", "--check-chars")

FIND_WRITE_FLAGS = {"-exec", "-execdir", "-ok", "-okdir", "-delete",
                    "-fprint", "-fprint0", "-fprintf", "-fls"}   # -fprint0 追加
```

**AW-6 の定数（第3版で実装済み・本改訂で設計へ反映＝M9-2 の①④）:**

```python
# gawk（4.1.2 以降）の間接関数呼び出し `@varname(...)`。関数名を文字列変数から
# 供給できるため、`system(` というリテラルが現れないまま system() を呼べる。
# 値は追跡せず**構文の存在**を証拠とする。正規表現は成立形より広く取る（保守側）。
# 第4版では degraded mode 専用（正常経路は AW-7 が担う）。
AWK_INDIRECT_CALL_RE = re.compile(r"@\s*[A-Za-z_][A-Za-z_0-9]*\s*\(")
```

**第4版で追加する定数（AW-7）:**

```python
# --- AW-7: awk プログラム本文のホワイトリスト -------------------------------

# リテラル（文字列・正規表現）除去後のプログラム本文に現れてよい文字。
# **ここに無い文字が1つでもあれば deny**（未知は deny＝原則 P4-3）。
# 意図的に含めない文字と根拠:
#   @  … 間接呼び出し @f( ・@load ・@include ・@namespace（gawk 拡張の入口）
#   |  … 出力パイプ print | "cmd" ・入力パイプ "cmd" | getline ・コプロセス |&
#   \ ` ' … リテラル外に現れる正当な用途が無い（行継続の \+改行は L1 が正規化）
#   非ASCII・制御文字 … gawk はリテラル外の非ASCII識別子を受け付けない（実測）
AWK_SAFE_PROGRAM_CHARS = frozenset(
    "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789_"
    " \t"
    '$(){}[],;:?!~=<>+-*/%^&."#'
)

# 正規表現リテラルの開始か除算かを分けるための「直前の非空白文字がオペランドの
# 終端になりうるか」の集合（awk の字句規則の近似）。判定を誤っても「除算＝
# リテラルを除去しない＝内容が判定対象に残る」＝deny 方向へ倒れる。
AWK_OPERAND_END_CHARS = frozenset(
    "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789_"
    '$)]".+-'
)

# 呼び出しを許可する名前。**ここに無い名前の `IDENT(` は deny。**
# system は意図的に非収載（AW-7 の中核）。getline は関数呼び出し構文を持たない
# ため収載不要（`getline(` は awk の構文ではない）。収載を増やすときは
# man/info の説明にファイル書き込み・プロセス起動が無いことを根拠として記録する。
AWK_SAFE_CALLS = frozenset({
    # `(` を伴いうる構文キーワード
    "if", "else", "while", "for", "do", "switch", "case", "default",
    "return", "exit", "print", "printf", "delete", "break", "continue",
    "next", "nextfile", "function", "func",
    # 副作用の無い組み込み関数（GNU Awk のマニュアル Built-in Functions）
    "length", "substr", "index", "split", "sub", "gsub", "gensub", "match",
    "sprintf", "tolower", "toupper", "sin", "cos", "atan2", "exp", "log",
    "sqrt", "int", "rand", "srand", "patsplit", "asort", "asorti",
    "strtonum", "mktime", "strftime", "systime", "and", "or", "xor",
    "compl", "lshift", "rshift", "typeof", "isarray",
    # ストリームの解放のみで、プロセス起動・書き込みを行わない
    "close", "fflush",
})

AWK_CALL_RE = re.compile(r"([A-Za-z_][A-Za-z_0-9]*)\s*\(")
AWK_FUNC_DECL_RE = re.compile(
    r"\bfunc(?:tion)?\s+([A-Za-z_][A-Za-z_0-9]*)\s*\(")

# プログラム本文を供給するオプション（値は必ずプログラムとして検査する）。
AWK_PROGRAM_TEXT_FLAGS = ("-e", "--source")
# 値を「次の語」から取る安全オプション。**--lint は含めない**
# （gawk の --lint は省略可能引数のため `--lint=fatal` 形でしか値を取らない）。
# 載せ忘れると「その値をプログラム候補として検査する」＝deny 方向へ倒れる。
AWK_VALUE_CONSUMING_FLAGS = ("-F", "-v", "-e",
                             "--field-separator", "--assign", "--source")
```

`ALLOWED_HEADS` / `ALLOWED_GIT_SUBCOMMANDS` / `READONLY_SCRIPTS` / `PATHISH_RE` / `SED_*` / `AWK_ASSIGN_RE` / `AWK_STRING_RE` / `AWK_OUTPUT_RE` / `AWK_EXEC_RE` / `AWK_INDIRECT_CALL_RE` / `AWK_SAFE_FLAGS` / `AWK_SAFE_VALUE_FLAGS` / `CWD_SAFE_HEADS` / `SORT_OUTPUT_FLAGS` / `UNIQ_VALUE_FLAGS` / `LEGACY_*` / `SUBST_PLACEHOLDER*` は**第4版・第5版のいずれでも変更しない**。

**第5版で追加する定数（QT-1・QT-3）:**

```python
# --- L1: 行継続とコメントの正規化（第6版・LX-2／LX-3） -----------------------
# bash が `#` をコメントの開始として扱うのは「語頭に現れたとき」だけである。
# 直前の文字がこの集合に含まれる（または入力の先頭である）ときに語頭とみなす。
# 用途は**行継続の適用可否の判定だけ**であり、コメント本文は除去しない
# （除去は deny → allow 方向の変化を生む。設計書 §3-2-1 LX-3・§6 R35）。
COMMENT_START_PREV = frozenset(" \t\n;&|()<>")
# コマンド置換・バッククォートの直後は「語の途中」であり `#` は語頭にならない
# （`echo $(ls)#x` の `#` はコメントではない）。読み飛ばした領域の後に置く
# 「直前の文字」の代用値。COMMENT_START_PREV に含まれない任意の文字でよい。
MIDWORD_SENTINEL = "\x00"

# --- L1: ANSI-C 引用 $'...' / ロケール翻訳 $"..." の扱い（第5版・QT-1） ---
# bash はこの2形を**展開時にデコード／翻訳する**ため、hook が見る字面と
# コマンドが実際に受け取る文字列が一致しない（`$'…\x22…'` は `"` になる）。
# 一致しないことが確定している以上、ホワイトリスト（AW-7）でもブラックリスト
# （AW-5/AW-6・SED_WRITE_RE）でも判定できない。「判定不能」の印を字句レイヤで
# 残し、保護対象への言及・保護対象 cwd が成立した時点で deny する。
ANSI_QUOTE_KIND = "ansiq"          # 語でも演算子でもない印トークンの種別
ANSI_QUOTE_RE = re.compile(r"\$['\"]")   # degraded（クォート状態を追えない）用
ANSI_QUOTE_REASON = (
    "ANSI-C 引用（$'...'）／ロケール翻訳（$\"...\"）を含むコマンドは判定でき"
    "ません（bash が展開時にエスケープを復号・翻訳するため、hook が見る文字列と"
    "実際にコマンドが受け取る文字列が一致しません）。保護対象に触れるコマンドで"
    "は使わず、エスケープを使わない書き方（awk -F'\\t' / grep -P '\\t' 等）を"
    "使ってください")

# 証拠収集専用の ANSI-C 復号（QT-3）。**判定用テキストには決して使わない。**
# bash が $'...' 内で認識するエスケープのみを対象とし、認識しない形
# （`\|`・`\d` 等）はバックスラッシュを残す（bash と同じ）。
ANSI_C_SIMPLE_ESCAPES = {"a": "\a", "b": "\b", "e": "\x1b", "E": "\x1b",
                         "f": "\f", "n": "\n", "r": "\r", "t": "\t",
                         "v": "\v", "\\": "\\", "'": "'", '"': '"', "?": "?"}
ANSI_C_ESCAPE_RE = re.compile(
    r"\\(x[0-9A-Fa-f]{1,2}|u[0-9A-Fa-f]{1,4}|U[0-9A-Fa-f]{1,8}"
    r"|[0-7]{1,3}|c.|[abeEfnrtv\\'\"?])")
```

### 4-2. L1（第5版で `lex` を2点変更・`ansi_c_decode` を新設／**第6版で正規化を分離し `lex` の2つの `replace` を撤去**）

`expand_substs` / `expand_all` / `display_text` は現行のまま。

**設計への反映（M9・逸脱3）:** `display_text()`（deny 理由から内部プレースホルダを隠す・表示のみ・判定不変）は実装の正であり、本設計でも新規の deny 理由に用いる。

#### 4-2-1. 走査ヘルパの共通化（第6版・M18 への対処）

`_take_subst` / `_take_backtick` の「終端を探す走査」を**位置だけを返すヘルパへ切り出し**、正規化走査（`normalize_line_continuations`）と `lex` の双方が**同一の規則**で置換領域の範囲を決めるようにする。**2箇所が別々に置換の終端を判断すると、正規化とスキャンで文脈がずれる**（C7-2 と同種の故障モード）。

```python
def _skip_balanced(command, start):
    """`$(` / `<(` / `>(` の内側を読み飛ばし、対応する `)` の**次**の位置を返す。

    start は開き括弧の次の位置（深さ1から開始）。クォートとエスケープを尊重する。
    対応する `)` が無ければ **-1** を返す（呼び出し側が ValueError／打ち切りを選ぶ）。
    """
    depth, i, n, quote = 1, start, len(command), ""
    while i < n:
        c = command[i]
        if quote:
            if c == "\\" and quote == '"' and i + 1 < n:
                i += 2
                continue
            if c == quote:
                quote = ""
            i += 1
            continue
        if c == "\\" and i + 1 < n:
            i += 2
            continue
        if c in "'\"":
            quote = c
            i += 1
            continue
        if c == "(":
            depth += 1
        elif c == ")":
            depth -= 1
            if depth == 0:
                return i + 1
        i += 1
    return -1


def _skip_backtick(command, start):
    """バッククォート置換の終端の**次**の位置を返す。無ければ -1。

    bash と同じく、**クォート状態は追わない**（バッククォートは最初の
    エスケープされていない ` で閉じる）。内側に未閉じクォートが残ることが
    あるのは仕様どおりであり、その場合は内側の再帰評価が degraded へ落ちる
    （その経路のゲートは §4-4(h) の C8 修正で cwd 対応になっている）。
    """
    i, n = start, len(command)
    while i < n:
        if command[i] == "\\" and i + 1 < n:
            i += 2
            continue
        if command[i] == "`":
            return i + 1
        i += 1
    return -1


def _take_subst(command, start, buf, substs):
    end = _skip_balanced(command, start)
    if end < 0:
        raise ValueError("no closing parenthesis")
    substs.append(command[start:end - 1])
    buf.append(SUBST_PLACEHOLDER % (len(substs) - 1))
    return end


def _take_backtick(command, start, buf, substs):
    end = _skip_backtick(command, start)
    if end < 0:
        raise ValueError("no closing backquote")
    substs.append(command[start:end - 1])
    buf.append(SUBST_PLACEHOLDER % (len(substs) - 1))
    return end
```

**挙動は現行と同一である**（現行の `_take_subst` は `)` の位置 `i` で `command[start:i]` を退避し `i + 1` を返す。`end = i + 1` なので `command[start:end - 1]` は同じ文字列であり、返り値も同じ）。**変わるのは「終端探索が1箇所に集約されること」だけである。**

#### 4-2-2. 行継続の正規化（第6版で新設・**C7／C7-2／H4 の修正本体**）

```python
def normalize_line_continuations(command):
    """bash と同じ条件でのみ行継続（`\\`+改行）を削除する（設計書 §3-2-1 LX-1〜LX-5）。

    **削除するのは次をすべて満たすときだけ**である。
      1. バックスラッシュ自身がエスケープされていない（走査が `\\`+次の1文字を
         対として消費するため、連の偶奇は自動的に決まる。カウンタは持たない）
      2. `#` コメントの内側でない（コメント内で `\\` は特別扱いされず、改行が
         コメントを終わらせる＝**区切り**）
      3. `'…'`（`$'…'` を含む）の内側でない（内側は `\\` も改行もリテラル）
      4. `"…"` の内側では bash も連結するため削除する

    **「安全側の近似」は存在しない。** 連結しすぎればステートメント区切りという
    証拠を失い（C7）、連結しなさすぎれば語の同一性という証拠を失う（SH-1）。
    bash と一致させることだけが正解であり、一致は V-14 の実測で裏付ける。

    `$(…)` / `` `…` `` / `<(…)` / `>(…)` の内側は**読み飛ばして原文のまま残す**。
    内側は lex() が substs へ退避し find_violation が再帰評価する際に、改めて
    **コマンド文脈として**本関数を通る（ダブルクォート内のコマンド置換の中では
    bash がコマンド文脈へ戻り `#` がコメントになる＝C7-2。文脈スタックを持たず
    正しくなる＝LX-4）。
    """
    out, state, prev, i, n = [], "cmd", "", 0, len(command)

    def emit(text):
        out.append(text)

    while i < n:
        c = command[i]

        if state == "sq":                 # '…' の内側は全てリテラル（条件3）
            emit(c)
            prev = c
            if c == "'":
                state = "cmd"
            i += 1
            continue

        if state == "cmt":                # コメント内で `\` は特別扱いされない
            emit(c)
            prev = c
            if c == "\n":
                state = "cmd"             # 改行はコメントを終わらせ、区切りとして残る
            i += 1
            continue

        # ---- ここから "cmd" と "dq" の共通処理 ----
        # バックスラッシュは左から順に「`\` ＋次の1文字」の対として消費する。
        # これだけで条件1（連の偶奇）が決まる（`\\`+改行では最後の `\` が直前の
        # `\` と対になり、改行は対の外に残る＝区切りになる）
        if c == "\\" and i + 1 < n:
            if command[i + 1] == "\n":
                i += 2                    # 行継続＝削除して連結（条件1・4）
                # **prev は更新しない。** bash は `\`+改行を取り除いて前後を
                # 直接隣接させるため、語頭判定に効くのは「削除前の直前の文字」
                # である（`echo a \<改行>#b` は連結後 `echo a #b` となり `#` は
                # **語頭＝コメント**。ここで prev を書き換えると、コメントを
                # 見落として次の `\`+改行を連結し C7 が再発する）
                continue
            emit(command[i:i + 2])
            prev = command[i + 1]
            i += 2
            continue

        # コマンド置換・プロセス置換・バッククォートは**読み飛ばす**（LX-4）
        if c == "$" and i + 1 < n and command[i + 1] == "(":
            end = _skip_balanced(command, i + 2)
            if end < 0:
                emit(command[i:])         # 未閉じ＝lex が ValueError → degraded
                break
            emit(command[i:end])
            prev = MIDWORD_SENTINEL
            i = end
            continue
        if c == "`":
            end = _skip_backtick(command, i + 1)
            if end < 0:
                emit(command[i:])
                break
            emit(command[i:end])
            prev = MIDWORD_SENTINEL
            i = end
            continue

        if state == "dq":
            emit(c)
            prev = c
            if c == '"':
                state = "cmd"
            i += 1
            continue

        # ---- ここから "cmd" のみ ----
        if c in "<>" and i + 1 < n and command[i + 1] == "(":
            end = _skip_balanced(command, i + 2)
            if end < 0:
                emit(command[i:])
                break
            emit(command[i:end])
            prev = MIDWORD_SENTINEL
            i = end
            continue
        if c == "'":
            emit(c); prev = c; state = "sq"; i += 1
            continue
        if c == '"':
            emit(c); prev = c; state = "dq"; i += 1
            continue
        if c == "#" and (prev == "" or prev in COMMENT_START_PREV):
            emit(c); prev = c; state = "cmt"; i += 1   # 語頭の `#`＝コメント開始
            continue
        emit(c)
        prev = c
        i += 1

    return "".join(out)
```

**設計の自己検証（疑似コードを1文字ずつ追った結果）:**

| 入力（`<LF>` は生の改行） | 走査の要点 | 出力 | 最終判定 |
|---|---|---|---|
| `echo x\<LF>rm docs/SPEC.md`（**奇数1個**） | `\` の次が `\n` → 2文字削除 | `echo xrm docs/SPEC.md` | head `echo` → **allow**（bash も同じ＝INV-SH-15） |
| `echo x\\<LF>rm docs/SPEC.md`（**偶数2個**） | 1個目の `\` が2個目と対になって消費 → 改行は対の外 | 変化なし | 改行が区切り → 第2文の head `rm` → **deny**（INV-SH-13） |
| `echo x\\\<LF>rm …`（**奇数3個**） | 対を1つ消費 → 残る `\`+改行を削除 | `echo x\\rm docs/SPEC.md` | head `echo` → **allow** |
| `echo x\\\\<LF>rm …`（**偶数4個**） | 対を2つ消費 → 改行が残る | 変化なし | **deny**（INV-SH-13） |
| `cat README.md #\<LF>sed -i … T` | `#` の直前が空白 → `cmt` へ。`\` は本文として emit、`\n` で `cmd` へ | 変化なし | 改行が区切り → 第2文 `sed -i` → **deny**（INV-SH-14） |
| `rm 'tickets/acti\<LF>ve/…'` | `'` で `sq` へ。`\` も改行もそのまま emit | 変化なし | 語が割れる → **allow**（bash も別名を触る＝INV-SH-16） |
| `rm "tickets/acti\<LF>ve/…"` | `dq` でも共通処理の対消費に入り、`\`+改行を削除 | `rm "tickets/active/…"` | **deny**（INV-SH-17） |
| `cat "$(ls #\<LF>rm docs/SPEC.md)"` | `dq` の中で `$(` を検出 → `_skip_balanced` で丸ごと emit（内側は無加工） | 変化なし | `lex` が内側を `substs` へ退避 → 再帰で内側だけを `cmd` 文脈として正規化 → 内側で `#` がコメント → 区切り → `rm` → **deny**（INV-SH-18） |
| `rm 'x#y' tickets/acti\<LF>ve/…` | `#` は `sq` の内側なので `cmt` に入らない | `rm 'x#y' tickets/active/…` | **deny**（INV-SH-20・AC1 と整合） |
| `echo $(ls)#x tickets/acti\<LF>ve/…` | 置換の直後は `prev = MIDWORD_SENTINEL` → `#` は語頭でない | 連結される | **deny**（コメント誤検出で連結が止まらないこと） |
| `echo a \<LF>#c\<LF>rm docs/SPEC.md`（**`prev` の扱いの検出器**） | 第1の `\`+改行を削除するが **`prev` は空白のまま** → 続く `#` は語頭＝コメント → 第2の `\`+改行は連結しない | `echo a #c\<LF>rm docs/SPEC.md` | 改行が区切り → 第2文 head `rm` → **deny**（`prev` を書き換える実装だと **allow** に転じる＝C7 の再発） |
| `echo a\<LF>#b tickets/active/APP-001.md` | 連結するが `prev` は `a` のまま → `#` は語中 | `echo a#b tickets/active/APP-001.md` | head `echo` → **allow**（bash も `echo` に2引数を渡すだけ） |
| A-1／A-2／A-3a／A-3b／A-3c | `\`+改行も語頭 `#` も含まない | **入力と完全に同一** | **判定は完全に不変**（AC2） |

**冪等性:** 正規化後の文字列を再度通しても出力は変わらない（削除済みの `\`+改行は存在せず、残った `\\`+改行は再び対消費で保存され、コメント内の `\`+改行も再びコメント内に入る）。**置換の内側が再帰で2回正規化されることはない**（外側は読み飛ばすため無加工のまま渡る）。

#### 4-2-3. `lex()`（第6版で2つの `replace` を撤去し、改行を走査の中で扱う）

```python
def lex(command):
    """コマンドを (tokens, substitutions) へ分解する。

    tokens: [("w", クォート解除済みの語), ("op", 演算子文字列),
             ("ansiq", "$'" | '$"')]  ← 第5版で追加した「判定不能」の印

    **前提（LX-5・第6版）:** command は normalize_line_continuations() を通した
    文字列であること（呼び出しは find_violation の入口の1箇所のみ）。行継続は
    既に削除されているため、**通常状態に残る `\\`+改行は「コメント末尾の
    バックスラッシュ」しかありえない**。
    """
    tokens, substs, buf, pending = [], [], [], []

    def flush():
        chunk = "".join(buf)
        del buf[:]
        if chunk.strip():
            for word in shlex.split(chunk, comments=False, posix=True):
                tokens.append(("w", word))
        # QT-1: 印は**チャンクの語をすべて出した後**に置く。演算子と「その次の
        # 語」の間に割り込ませると redirect_violation がリダイレクト先を
        # 見失う（`echo x > $'tickets/active/APP-001.md'` が allow になる）。
        # flush() は演算子トークンを積む直前と入力末尾でしか呼ばれないため、
        # この位置なら印が演算子と語の間へ入ることは構造的に起こらない。
        for mark in pending:
            tokens.append((ANSI_QUOTE_KIND, mark))
        del pending[:]

    i, n, quote = 0, len(command), ""
    while i < n:
        c = command[i]
        if quote == "'":
            ...                      # 現行のまま（変更なし。改行は語の内容として buf へ）
        if quote == '"':
            ...                      # 現行のまま（変更なし。同上）
        # ---- 通常状態（クォート外）----
        # LX-5（第6版）: 正規化済みのため、ここに来る `\`+改行は「コメント末尾の
        # バックスラッシュ」だけである。バックスラッシュ1文字を落として、次の
        # ループで改行を区切りとして扱う。`\` は PATHISH_RE の候補文字ではない
        # ため**パスの証拠は1文字も減らない**。落とさないと shlex.split が
        # 「末尾の孤立エスケープ」で ValueError を出し不要に degraded へ落ちる。
        # **この分岐は下の汎用エスケープ分岐より前に置かなければならない**
        # （後ろに置くと `\`+改行が対として消費され、区切りが消える＝C7 の再発）
        if c == "\\" and i + 1 < n and command[i + 1] == "\n":
            i += 1
            continue
        # 第6版: 改行をステートメント区切りとして扱う（第5版までの
        # `command.replace("\n", ";")` を撤去した代替。前処理での置換だと直前の
        # バックスラッシュに `;` がエスケープされて区切りが消えるため、
        # **走査の中で扱う必要がある**）
        if c == "\n":
            flush()
            tokens.append(("op", ";"))
            i += 1
            continue
        if c == "\\" and i + 1 < n:
            ...                      # 現行のまま（汎用のエスケープ対消費）
        # QT-1（第5版で追加）: クォート**外**の `$'` / `$"` だけが ANSI-C 引用・
        # ロケール翻訳になる。`"$'...'"` のようにダブルクォート内に現れる `$'`
        # は bash では展開されない（素の `$` + `'`）ため、この判定は
        # 「通常状態」の分岐にしか置いてはならない。
        # **語の取り込み方は一切変えない**（`$` はこの後の共通処理で buf へ
        # 積まれ、続く `'` が従来どおりクォート状態を開始する）＝証拠は不変。
        if c == "$" and i + 1 < n and command[i + 1] in "'\"":
            pending.append(command[i:i + 2])     # "$'" または '$"'
        if c == "$" and i + 1 < n and command[i + 1] == "(":
            ...                      # 以降すべて現行のまま
```

**第6版の変更が既存判定を動かさないことの確認（設計時のトレース）:**

| 論点 | 確認内容 |
|---|---|
| 改行の扱い | 第5版は前処理で `\n` → `;` とし、走査が `OPERATOR_CHARS` の `;` として拾っていた。第6版は走査が直接 `("op", ";")` を積む。**`op_kind(";") == "sep"` は同じ**であり `split_statements` の挙動は不変。`flush()` を先に呼ぶ順序も `OPERATOR_CHARS` 分岐と同一（印トークンの位置も不変＝T-QT1e） |
| クォート内の改行 | 第5版は `;` へ置換されて語の内容に入っていた。第6版は改行のまま語の内容に入る。**`PATHISH_RE` は `;` も改行も候補文字に含まない**ため、パス候補の切れ方は同一＝**証拠は不変** |
| heredoc（`cat > T <<EOF\nx\nEOF`） | 第1文の `redirect_violation` が従来どおり deny。区切りの作られ方が同じため判定不変 |
| パイプの後の改行（`cat a \|<改行>grep b`） | 第5版と同じく `;` 相当の区切りになる（bash の「パイプ継続」とは異なるが、**分割は判定を弱めない**。`cat x \|<改行>tee docs/SPEC.md` は第2文の head `tee` で deny） |
| `\`+改行 を含まない全入力 | `normalize_line_continuations` は**入力と同一の文字列**を返し、`lex` の新分岐にも到達しない＝**判定は完全に不変** |

```python
def ansi_c_decode(text):
    """$'...' の ANSI-C エスケープを復号する（QT-3）。

    **証拠収集専用。** 復号結果を is_guarded_token の追加判定以外で使っては
    ならない（プログラム本文の判定に渡すと「復号後は安全な形」と誤認する
    余地が生まれる。QT-1 が先に deny するため必要も無い）。
    復号できない・未知のエスケープは元の字面を返す（allow 方向へ倒れるが、
    その場合も QT-1 が印による deny を行うため防御は残る）。
    """
    if "\\" not in text:
        return text

    def replace(match):
        body = match.group(1)
        try:
            if body[0] in "xuU":
                return chr(int(body[1:], 16))
            if body[0] == "c":
                return chr(ord(body[1].upper()) ^ 0x40)
            if body[0] in "01234567":
                return chr(int(body, 8) & 0xFF)
            return ANSI_C_SIMPLE_ESCAPES[body]
        except (ValueError, KeyError, IndexError):
            return match.group(0)   # 例外は出さない（hook 全体は fail-open）

    return ANSI_C_ESCAPE_RE.sub(replace, text)
```

### 4-3. L2（第5版で `is_guarded_token` に証拠の復号を1点追加）

`_is_guarded_path` / `guarded_paths` / `mentions_guarded` は現行のまま。

```python
def is_guarded_token(text):
    """テキストが保護対象に言及するか。

    QT-3（第5版）: 素の字面で見つからないときに限り、ANSI-C エスケープを
    復号した字面でももう一度判定する（`rm $'\\x74ickets/active/APP-001.md'`
    のように**証拠そのものを符号化して隠す**形を捕捉するため）。
    判定に使えるテキストが増える方向にのみ作用し、allow へ転じることはない。
    """
    if guarded_paths(text):
        return True
    decoded = ansi_c_decode(text)
    return decoded != text and bool(guarded_paths(decoded))
```

**呼び出し側への影響（設計時に全箇所を確認済み）:** `is_guarded_token` は `mentions_guarded`（言及ゲート）・`redirect_violation`・`record_assignments`・`guarded_write_target`・sed の `w` 判定・AW-1・`subst_violation` から呼ばれる。**いずれも「真になると検査が増える／deny になる」方向にしか使われておらず、真になることで検査が省略される箇所は無い。** したがって QT-3 は既存の allow を deny へ変えることはあっても、deny を allow へ変えることはない。

### 4-4. L3 の変更

#### (a) 先頭コマンド位置の一元化（M9・逸脱7 の設計反映。第2版 §4-4(b)(c)(g) の疑似コードを訂正）

第2版の疑似コードは `head_of` が代入前置き・`(` を読み飛ばす一方で、`next_cwd` のオペランド抽出と git のサブコマンド抽出が `words[1:]`（先頭語の次から）を前提にしていた。**この前提の食い違い自体が設計のバグ**であり（`X=1 cd tickets/active && rm APP-001.md` が old=deny → new=allow）、implementer が導入した位置一元化を**設計の正とする**。

```python
def is_assignment_word(word):
    """`FOO=bar` 形式の環境変数代入前置きか。"""
    return bool(re.fullmatch(r"[A-Za-z_][A-Za-z_0-9]*=.*", word))


def head_index(words):
    """先頭コマンド名に当たる語の位置を返す（無ければ -1）。
    代入前置きと `(` 単独語を読み飛ばす。**先頭コマンドの位置を必要とする
    すべての箇所（head_of / next_cwd / git サブコマンド / awk 引数走査）は
    この1関数を共有しなければならない。**"""


def head_of(words):    """head_index の結果の basename を返す。"""
def head_args(words):  """words[head_index + 1:]。先頭コマンドが無ければ空。"""
```

`head_of` は `(` を剥がすと空になる語を読み飛ばす（M4-5）。副次的に `( cd tickets/active && rm APP-001.md )` の cwd 追跡も成立する。

#### (b) `guarded_write_target`（**H2 の修正本体その1**・`cwd_redirect_violation` を吸収して削除）

```python
def guarded_write_target(target, cwd, substs=()):
    """書き込み先オペランドが保護対象を指すか（cwd 相対も解決する）。

    **構文的に書き込み先と確定しているオペランド**（出力リダイレクトの直後の語・
    sort の -o の値・uniq の第2位置引数）にだけ適用する。任意の語へ適用すると
    cwd と結合した結果が保護対象パターンに一致して誤denyになる
    （実測: _is_guarded_path("tickets/active/NR>1 {print}") は True）。
    解決できない先（シェル変数・未解決プレースホルダ）は偽＝allow 方向。
    """
    if not target:
        return False
    target = expand_substs(target, substs).strip()
    if not target or "$" in target or SUBST_PLACEHOLDER_RE.search(target):
        return False
    if is_guarded_token(target):
        return True
    if not cwd_is_guarded(cwd):
        return False
    return _is_guarded_path(os.path.normpath(os.path.join(cwd, target)))
```

- `cwd_is_guarded(cwd)` は第2版のまま（`bool(cwd) and _is_guarded_path(cwd)`。`None` でも安全に偽）。
- **第2版の `cwd_redirect_violation` は削除する**（逸脱5の場当たり対応をこの一般規則へ吸収＝reviewer 提案3・**M10 の縮小**）。呼び出していた2箇所（`redirect_violation`・`degraded_violation`）は `guarded_write_target` を使う。
- 絶対パスの `target` は `os.path.join` の性質で cwd を無視するため、cwd 外への書き込みは allow のまま（既存テスト `cd tickets/active && cat APP-001.md > /tmp/x.md` を維持）。

#### (c) `segment_violation`（cwd を受け取り、sed を cwd 対応にし、sort / uniq 規則を追加）

```python
def segment_violation(words, substs=(), cwd="", degraded=False):
    # degraded は「字句解析に失敗して空白分割へ落ちた入力か」。awk の
    # プログラム本文の同一性が失われるため AW-7 を適用せず、AW-5／AW-6 の
    # ブラックリストを使う（§3-5）。渡すのは degraded_violation だけ。
    head = head_of(words)
    if not head:
        return None
    if SUBST_PLACEHOLDER_RE.search(head):
        return "コマンド名の位置にコマンド置換が使われています"
    if head in ("python3", "python") and is_readonly_script_call(words):
        return None
    if head not in ALLOWED_HEADS:
        return "'%s' は許可されていません" % head
    # フォールセーフ既定（P4-2・§3-8）。現行の ALLOWED_HEADS は全て収載済みの
    # ため到達しない。ALLOWED_HEADS へ追加したときに棚卸し忘れを deny 側へ倒す。
    if cwd_is_guarded(cwd) and head not in CWD_SAFE_HEADS:
        return ("保護対象ディレクトリを作業ディレクトリとする '%s' は"
                "許可されていません" % head)
    words = list(words)
    expanded = expand_all(words, substs)
    # 解決語では完全一致（--in-place 等）が壊れるため、raw と expanded の
    # 両方を評価する（M9・逸脱2 の設計反映。置換が無ければ従来と同一の1回評価）
    variants = [words] if expanded == words else [words, expanded]
    if head == "sed":
        flat = [t for variant in variants for t in variant]
        if any(SED_INPLACE_SHORT_RE.match(t) or t == "--in-place" or
               t.startswith("--in-place=") for t in flat):
            return "sed -i（in-place編集）は許可されていません"
        strict = cwd_is_guarded(cwd)
        if strict:
            # degraded は空白分割のため `w FILE` が2語に割れる。保護対象 cwd 下
            # に限り空白結合したテキストも評価する（§3-5）
            flat = flat + [" ".join(words)]
        if any(SED_WRITE_RE.search(t) for t in flat
               if strict or is_guarded_token(t)):
            return "sed の w コマンド（ファイル書き出し）は許可されていません"
    if head == "awk":
        for variant in variants:
            reason = awk_violation(variant, cwd, degraded)
            if reason:
                return reason
    if head == "sort":
        for operand in sort_output_operands(words):
            if guarded_write_target(operand, cwd, substs):
                return "sort の -o / --output（出力先指定）は許可されていません"
    if head == "uniq":
        if guarded_write_target(uniq_output_operand(words), cwd, substs):
            return "uniq の第2引数（出力ファイル）への書き込みは許可されていません"
    if head == "find" and any(t in FIND_WRITE_FLAGS for t in words):
        return "find の書き込み系フラグ（-exec/-delete等）は許可されていません"
    if head == "git":
        sub = next((t for t in head_args(words) if not t.startswith("-")), "")
        if sub not in ALLOWED_GIT_SUBCOMMANDS:
            return "git %s は許可されていません" % sub
    return None
```

**`strict` 分岐の誤deny評価:** 保護対象 cwd 下で sed スクリプトに ` w <非空白> ` が現れる形（`sed 's/a w b/c/'` 等）は保守的に deny になる。cwd が `tickets/active|done` に限られ、かつ old=deny のため回帰にはならない。docstring に限界として記載する。

#### (d) `awk_violation` / `is_safe_awk_flag` / **AW-7 のヘルパ3種**（**C3・C4・C5・M11・M12 の修正本体**）

```python
def is_safe_awk_flag(word):
    """awk のオプション語が「読み取りだけで完結する」既知の形か。

    **未知のオプションは False（deny 方向）へ倒す。** gawk の
    -f/-i/-l/-E/-D（外部プログラム・ライブラリ・拡張の読み込み）と
    -o/-p/-d（ファイルへの書き出し）はここへ載せないことで自動的に deny になる。
    収載を増やすときは man の説明にファイル書き込み・コード読み込みが無いことを
    根拠として記録すること。迷うオプションは収載しない。
    """
    if word in AWK_SAFE_FLAGS:
        return True
    for flag in AWK_SAFE_VALUE_FLAGS:
        if word == flag or word.startswith(flag + "="):
            return True
        if len(flag) == 2 and len(word) > 2 and word.startswith(flag):
            return True    # -FVALUE / -vNAME=VALUE / -eTEXT の結合形
    return False


def awk_strip_literals(text):
    """awk プログラムの文字列リテラルと正規表現リテラルを**1パス**で除去する。

    左→右の1パスで「その位置に何のリテラルが始まるか」を awk の字句規則に
    倣って決める。2パス（文字列を全部消してから正規表現を消す等）にすると、
    片方のリテラルの内側に見える引用符／スラッシュが、実際には外側にある
    コードを飲み込む（`/"/ {system("rm F")}` は文字列先だと system( が消え、
    `BEGIN{x="(/a"; system("rm /F")}` は正規表現先だと消える）。1パスなら
    リテラルの開始位置が awk と一致するため、awk が実行するコードが
    リテラルとして消えることは起こらない。

    戻り値: 除去後テキスト（文字列は `""`・正規表現は `//` へ置換）。
            未閉じ文字列リテラルは None（判定不能＝呼び出し側で deny）。
    """
    out, i, n, prev = [], 0, len(text), ""
    while i < n:
        char = text[i]
        if char == '"':
            j, closed = i + 1, False
            while j < n:
                if text[j] == "\\":
                    j += 2
                    continue
                if text[j] == '"':
                    closed = True
                    break
                j += 1
            if not closed:
                return None
            out.append('""')
            prev, i = '"', j + 1
            continue
        if char == "/" and prev not in AWK_OPERAND_END_CHARS:
            j, closed = i + 1, False
            while j < n:
                if text[j] == "\\":
                    j += 2
                    continue
                if text[j] == "/":
                    closed = True
                    break
                if text[j] in ";\n":
                    break        # 正規表現リテラルは文・行をまたがない
                j += 1
            if closed:
                out.append("//")
                prev, i = "/", j + 1
                continue
        out.append(char)          # 除算・その他の文字はそのまま
        if not char.isspace():
            prev = char
        i += 1
    return "".join(out)


def awk_program_texts(words):
    """awk が**プログラムとして実行する**テキストを列挙する（§3-4）。

    - `-e TEXT` / `-eTEXT` / `--source TEXT` / `--source=TEXT` の値（複数可）
    - `-e` / `--source` が無い場合に限り、位置引数の**先頭のオペランド**
      （awk は最初の非オプション引数をプログラムとして扱い、2番目以降は
      NAME=VALUE 代入と入力ファイル名として扱う＝実行しない）
    - `-f FILE` / `--file=FILE` は AW-4 が deny するためここへ到達しない

    値を「次の語」から取るオプションは AWK_VALUE_CONSUMING_FLAGS に限る。
    載せ忘れた場合はその値をプログラム候補として検査する＝deny 方向。
    """
    texts, positionals, expect = [], [], ""
    for word in head_args(words):
        if expect:
            if expect in AWK_PROGRAM_TEXT_FLAGS:
                texts.append(word)
            expect = ""
            continue
        if word in AWK_VALUE_CONSUMING_FLAGS:
            expect = word
            continue
        if word.startswith("-e") and len(word) > 2:
            texts.append(word[2:])
            continue
        if word.startswith("--source="):
            texts.append(word[len("--source="):])
            continue
        if word.startswith("-"):
            continue                      # 値を取らないオプション・`-`・`--`
        positionals.append(word)
    if not texts and positionals:
        texts.append(positionals[0])
    return texts


def awk_program_violation(text):
    """プログラム本文が「安全と確認できる形」でなければ理由を返す（AW-7）。

    **ホワイトリスト。** 3条件をすべて満たす形だけを許可し、未知の文字・
    未知の関数名はすべて deny 側へ落ちる（原則 P4-3）。判定はリテラル除去後に
    行うため、文字列・正規表現の中身に `@` や `system(` が現れるだけの
    読み取りは許可される（awk に eval が無いため実行され得ない）。
    """
    stripped = awk_strip_literals(text)
    if stripped is None:
        return ("awk プログラムの文字列リテラルが閉じていません"
                "（安全と確認できるプログラム形に限り許可します）")
    unsafe = [c for c in stripped if c not in AWK_SAFE_PROGRAM_CHARS]
    if unsafe:
        return ("awk プログラムに許可されていない文字 %r があります"
                "（@ 間接呼び出し・@load・| パイプ／コプロセス等の"
                "外部コマンド実行経路になりうるため）" % unsafe[0])
    declared = set(AWK_FUNC_DECL_RE.findall(stripped))
    for name in AWK_CALL_RE.findall(stripped):
        if name not in AWK_SAFE_CALLS and name not in declared:
            return ("awk プログラムの関数呼び出し %s() は許可されていません"
                    "（副作用の無いことを確認した組み込み関数と、同じ"
                    "プログラム内で定義された関数のみ許可します）" % name)
    if AWK_OUTPUT_RE.search(stripped):
        return ("awk のプログラム内リダイレクト（print > file 等）は"
                "許可されていません")
    return None


def awk_violation(words, cwd="", degraded=False):
    """awk セグメントの違反理由を返す（§3-6 D1 の AW-1〜AW-7）。"""
    # AW-1: 保護対象に言及する語そのものが > / | を含む（証拠ベース・ゲートなし）
    if any(">" in t or "|" in t for t in words if is_guarded_token(t)):
        return "awk のプログラム内リダイレクト（print > file 等）は許可されていません"
    # AW-2: 保護対象パスを awk 変数へ束縛している（証拠ベース・ゲートなし）
    if any(is_guarded_token(v) for v in awk_bindings(words)):
        return "awk の変数へ保護対象パスを束縛しています（-v / NAME=VALUE）"
    # 以降は「保護対象に言及する／保護対象を cwd とする awk」に限って適用する
    if not (mentions_guarded(words) or cwd_is_guarded(cwd)):
        return None
    for word in head_args(words):
        # AW-4: 未知・危険なオプション（外部コード読み込み／ファイル書き出し）
        if word.startswith("-") and not is_safe_awk_flag(word):
            return ("awk のオプション %s は許可されていません（外部プログラム・"
                    "拡張の読み込みとファイル書き出しの経路になりうるため）"
                    % display_text(word))
        if degraded:
            # AW-5 / AW-6（degraded 専用）: 空白分割でプログラム本文の同一性が
            # 失われるため、生テキストのブラックリストで見る。**現行実装の
            # 評価順（AW-4 → AW-5 → AW-6 → AW-3）をそのまま保つ**ことで、
            # degraded 経路の deny 理由文字列も現行と同一になる
            if AWK_EXEC_RE.search(word):
                return ("awk の system() / @load / @include（外部コマンド実行・"
                        "拡張読み込み）は許可されていません")
            if AWK_INDIRECT_CALL_RE.search(word):
                return ("awk の間接関数呼び出し（@f(...) 形式）は許可されて"
                        "いません（関数名を変数経由で供給して system() を"
                        "呼び出せるため）")
        # AW-3: print / printf の出力リダイレクト（文字列リテラル除去後で判定）
        if AWK_OUTPUT_RE.search(AWK_STRING_RE.sub('""', word)):
            return ("awk のプログラム内リダイレクト（print > file 等）は"
                    "許可されていません")
    if degraded:
        # 語をまたぐ形（`@ f(` が `@` と `f(` に、`system (` が `system` と
        # `(` に割れる。gawk は `@` 直後と関数名直後の空白をどちらも受け付ける
        # ＝実測）。結合は証拠テキストを増やす操作で判定を緩めない。オプション
        # 判定（AW-4）と出力構文（AW-3）は結合テキストへ当てない（語の意味が
        # 変わり `awk --posix ...` 等が誤denyになるため）
        args = head_args(words)
        if len(args) > 1:
            joined = " ".join(args)
            if AWK_EXEC_RE.search(joined):
                return ("awk の system() / @load / @include（外部コマンド実行・"
                        "拡張読み込み）は許可されていません")
            if AWK_INDIRECT_CALL_RE.search(joined):
                return ("awk の間接関数呼び出し（@f(...) 形式）は許可されて"
                        "いません（関数名を変数経由で供給して system() を"
                        "呼び出せるため）")
        return None
    # AW-7（正常経路の本体）: プログラム本文がホワイトリストの安全形か
    texts = awk_program_texts(words)
    for text in texts:
        reason = awk_program_violation(text)
        if reason:
            return reason
    if len(texts) > 1:
        # gawk は複数の -e / --source を連結して1つのプログラムにする。
        # 語をまたいで `sys` + `tem(` のように割った形を取りこぼさない
        reason = awk_program_violation(" ".join(texts))
        if reason:
            return reason
    return None
```

`awk_bindings` は現行のまま（`head_args` を使用。`--assign=NAME=VALUE` 対応済み＝M9・逸脱6）。`is_safe_awk_flag`・`AWK_OUTPUT_RE`・`AWK_STRING_RE` も**変更しない**。

**設計上の注意（implementer へ）:**
- **AW-1／AW-2／AW-3／AW-4 は1行も変えない。** 第4版の変更は「AW-5／AW-6 を `degraded` 分岐の内側へ移す」ことと「AW-7 を足す」ことだけである。これにより**既存の deny がすべて保存される**ことを構造的に保証する（§4-5 の閉塞トレース参照）。
- AW-7 は `segment_violation` の `variants`（raw ∪ expanded）の**両方**に対して評価される。プログラムが `$(...)` 由来の場合は expanded 側で検査され、置換の内側テキストがシェル引用符を含むと deny になる（§6 R26 に記載する誤deny）。
- `awk_program_texts` は**先頭オペランドしか**プログラム候補にしない。2番目以降のオペランド（ファイル名・`NAME=VALUE`）を候補に含めてはならない（M12 が再発する）。

#### (e) `sort_output_operands` / `uniq_output_operand`（新規・§3-6 D4）

```python
def sort_output_operands(words):
    """sort の出力先を列挙する（-o FILE / -oFILE / --output FILE / --output=FILE）。"""
    operands, expect = [], False
    for word in head_args(words):
        if expect:
            expect = False
            operands.append(word)
            continue
        if word in SORT_OUTPUT_FLAGS:
            expect = True
        elif word.startswith("-o") and len(word) > 2:
            operands.append(word[2:])
        elif word.startswith("--output="):
            operands.append(word[len("--output="):])
    return operands


def uniq_output_operand(words):
    """uniq の第2位置引数（出力ファイル）を返す。無ければ空文字列。

    -f/-s/-w とその長形式は値を取るため、位置引数の数え方を誤ると
    `uniq -f 2 FILE` の `2` を第1位置引数と数えて FILE を出力先と誤認する。
    """
    positionals, expect = [], False
    for word in head_args(words):
        if expect:
            expect = False
        elif word in UNIQ_VALUE_FLAGS:
            expect = True
        elif word.startswith("-") and word != "-":
            continue                       # 結合形・その他のフラグ
        else:
            positionals.append(word)
    return positionals[1] if len(positionals) > 1 else ""
```

#### (f) `redirect_violation`（`cwd_redirect_violation` を `guarded_write_target` へ置換）

```python
def redirect_violation(statement, guarded_vars, substs=(), cwd=""):
    for index, (kind, value) in enumerate(statement):
        if kind != "op" or op_kind(value) != "redirect_out":
            continue
        if index + 1 >= len(statement):
            continue
        next_kind, target = statement[index + 1]
        if next_kind != "w":
            continue
        if "&" in value and (target.isdigit() or target == "-"):
            continue                                   # 2>&1 等の fd 複製
        resolved = expand_substs(target, substs)
        if (is_guarded_token(resolved) or
                any(n in guarded_vars for n in VAR_REF_RE.findall(resolved)) or
                guarded_write_target(target, cwd, substs)):
            return "リダイレクト（> %s）による書き込み" % display_text(target)
    return None
```

deny 理由には**解決前の `target`**（`display_text` 済み）を出す。

#### (g) `statement_violation`（第5版で QT-1 を追加）と `pipe_segments`（印トークンの読み飛ばし）

```python
def pipe_segments(statement):
    """ステートメントをパイプ区間の語リストへ分割する（リダイレクト先の語は除く）。"""
    segments, current, skip_next = [], [], False
    for kind, value in statement:
        if kind == "op":
            ...                       # 現行のまま
            continue
        if kind != "w":
            continue   # QT-1（第5版）: 印トークンは語ではない。**skip_next を
                       # 消費させない**ため、この判定は skip_next の処理より
                       # 前に置く（印は演算子と語の間に入らない設計だが、
                       # 防御的にこの順序にする）
        if skip_next:
            ...                       # 現行のまま
        current.append(value)
    segments.append(current)
    return segments


def statement_violation(statement, guarded_vars, substs=(), cwd=""):
    """cwd は「このステートメントを実行する時点の作業ディレクトリ」（追跡不能なら
    None）。**第2版の疑似コードは cwd_guarded(bool) を渡していたが、実装は
    cwd 文字列を渡す。書き込み先オペランドの解決にパスが必要なため文字列が正。**"""
    reason = redirect_violation(statement, guarded_vars, substs, cwd)      # ①
    if reason:
        return reason
    words = [v for k, v in statement if k == "w"]   # 印トークンは語に含めない
    if not (cwd_is_guarded(cwd) or mentions_guarded(expand_all(words, substs))):
        return None                                                       # ③
    # QT-1（第5版）: この文には bash が展開時にデコード／翻訳する語がある。
    # hook が見ている字面と実行される文字列が一致しないことが確定しているため、
    # ホワイトリスト（AW-7）でもブラックリスト（AW-5/AW-6・SED_WRITE_RE）でも
    # 判定できない。**証拠（③）が立った時点で無条件に deny する。**
    # ゲート（③）の後に置くのは、`printf $'a\tb'` 単独のような保護対象と
    # 無関係なコマンドを巻き添えにしないため。
    if any(k == ANSI_QUOTE_KIND for k, _ in statement):                   # ③'
        return ANSI_QUOTE_REASON
    if any(k == "op" and op_kind(v) == "heredoc" for k, v in statement):  # ②
        return "ヒアドキュメントによる書き込みの可能性"
    for segment_words in pipe_segments(statement):                        # ④
        reason = segment_violation(segment_words, substs, cwd)
        if reason:
            return reason
    return None
```

**この配置で既存判定が動かないことの確認（設計時のトレース）:**

| 既存の形 | 影響 |
|---|---|
| `echo x > $'tickets/active/APP-001.md'` | 印は語の**後**に出るため `redirect_violation` の「演算子の次のトークン」は語のまま。①で従来どおり deny（理由文字列も従来どおり） |
| `printf $'a\tb'`（保護対象と無関係） | ③のゲートが偽 → **allow のまま** |
| `cat tickets/active/APP-001.md && printf $'a\tb'` | 第1文は印なし＝従来どおり allow／第2文はゲートが偽＝allow。**ステートメント単位の粒度により巻き添えが起きない** |
| `$'…'` を含まない全入力 | トークン列に印が1つも入らないため**判定は完全に不変** |

#### (h) `degraded_violation`（cwd を受け取り `segment_violation` へ渡す。**第5版で QT-1 を追加・第6版で関数レベルゲートを cwd 対応＝C8 の修正本体**）

```python
def degraded_violation(command, cwd=""):
    """字句解析できない入力向けのフォールバック（P2: 旧版の判定要素をすべて持つ）。
    新しい判定は segment_violation / awk_violation の内側にあるため、degraded は
    cwd を渡すだけで同じ保護を得る（判定ロジックを複製しない＝M10）。

    **command は find_violation が正規化した文字列**（第6版・H4）。正常経路と
    degraded 経路が同じ行構造を見ることで、M13 の「偶発的な安全網」は
    ANSI_QUOTE_RE による**意図された deny** に置き換わる。
    """
    # C8（第6版）: **関数レベルのゲートにも cwd を入れる。** 入れないと
    # `cd tickets/active && ls `rm APP-001.md #'`` のように、置換の内側だけが
    # degraded へ落ちる経路（find_violation → subst_violation →
    # find_violation(inner, depth+1, cwd) → lex の ValueError）で、
    # ステートメント単位のゲートに到達する前に early return してしまう。
    # 本関数のゲートは①（ここ）②LEGACY_REDIRECT_RE ③heredoc ④文単位ゲート
    # ⑤segment/awk の5つで、cwd を見ないのは①だけであった（第6版で全件確認）
    if not (cwd_is_guarded(cwd) or mentions_guarded(command.split())):
        return None
    for match in LEGACY_REDIRECT_RE.finditer(command):
        target = match.group(1)
        if target.startswith("&"):
            continue
        if is_guarded_token(target):
            return "リダイレクト（> %s）による書き込み" % target
    if "<<" in command:
        return "ヒアドキュメントによる書き込みの可能性"
    for statement in LEGACY_STATEMENT_RE.split(command):
        parts = [p for p in statement.split("|") if p.strip()]
        if not parts:
            continue
        words_list = [p.split() for p in parts]
        if cwd_is_guarded(cwd) or any(mentions_guarded(w) for w in words_list):
            # QT-1（第5版）: degraded はクォート状態を追えないため、`$'` / `$"`
            # の検出は**部分文字列判定**で行う（クォート内側の `$'` にも発火する
            # ＝保守側）。**この規則を degraded にも置くことが必須である** —
            # 置かないと `awk $'…\x7cgetline…' #'` のように未閉じクォートを
            # 1つ足すだけで C6 がそのまま通る（degraded のブラックリストは
            # 符号化された字面に一致しない。§3-6 D9 のトレース）
            if ANSI_QUOTE_RE.search(statement):
                return ANSI_QUOTE_REASON
            for words in words_list:
                # degraded=True: awk は AW-7 を使わず AW-5／AW-6 で見る（§3-5）
                reason = segment_violation(words, cwd=cwd, degraded=True)
                if reason:
                    return reason
        for match in LEGACY_REDIRECT_RE.finditer(statement):
            if guarded_write_target(match.group(1), cwd):
                return ("リダイレクト（> %s）による書き込み" % match.group(1))
        cwd = next_cwd(words_list[0], cwd)
    return None
```

#### (i) `find_violation`（**H2 の修正本体その3**・置換をステートメントの cwd で再帰する。**第6版で正規化を入口へ集約＝H4 の修正本体**）

```python
def find_violation(command, depth=0, cwd=""):
    # H4（第6版）: 行継続の正規化は**ここで1回だけ**行い、正規化後の**同じ
    # 文字列**を lex() と degraded_violation() の双方へ渡す。第5版は lex() が
    # 前処理で結合し degraded は生の command を受け取っていたため、両経路が
    # 異なる行構造を評価していた（C7 と M13 の共通の根）。
    # 置換の内側は正規化されずに substs へ入り、この関数の再帰呼び出しで
    # **内側の文脈として**改めて正規化される（LX-4・C7-2 の閉塞）
    command = normalize_line_continuations(command)
    try:
        tokens, substs = lex(command)
    except ValueError:
        return degraded_violation(command, cwd)
    guarded_vars, seen = set(), set()
    for statement in split_statements(tokens):
        # 内側方向（置換の中身が書き込み）。**この文の cwd で評価する**ことで
        # `cd tickets/active && ls $(rm APP-001.md)` が閉じる。プレースホルダに
        # インデックスがあるため、どの置換がどの文に属するかが判る（P1 の副産物）
        for index in statement_subst_indexes(statement):
            seen.add(index)
            reason = subst_violation(substs, index, depth, cwd)
            if reason:
                return reason
        record_assignments(statement, guarded_vars, substs)
        reason = statement_violation(statement, guarded_vars, substs, cwd)
        if reason:
            return reason
        segments = pipe_segments(statement)
        cwd = next_cwd(segments[0] if segments else [], cwd)
    # どの文の語にも現れなかった置換（構造上起きないが、取りこぼしを作らない）。
    # cwd を与えず従来どおり評価する
    for index in sorted(set(range(len(substs))) - seen):
        reason = subst_violation(substs, index, depth, "")
        if reason:
            return reason
    return None


def statement_subst_indexes(statement):
    """ステートメントの語トークンに現れるプレースホルダのインデックスを返す。"""
    return [int(m.group(1)) for kind, value in statement if kind == "w"
            for m in SUBST_PLACEHOLDER_RE.finditer(value)]


def subst_violation(substs, index, depth, cwd):
    if index >= len(substs):
        return None                    # 利用者が書いた同名リテラル（R15）
    inner = substs[index]
    if depth >= MAX_SUBST_DEPTH:
        # 深追いはせず保守側で打ち切る（$($($(rm ...))) を抜け道にしないため）
        if is_guarded_token(inner) or cwd_is_guarded(cwd):
            return "深いコマンド置換の内側で保護対象パスに言及しています"
        return None
    return find_violation(inner, depth + 1, cwd)
```

**評価順序の変化（設計上の帰結）:** 第2版は全置換を先に再帰していたが、第3版はステートメント順に評価する。**allow/deny の結果は変わらない**（どちらも両方を評価する）。変わるのは deny 理由の**選ばれ方**のみで、既存テストは理由文字列を assert していない（設計時に確認済み）。深さ上限の判定は「親の depth」で行うため境界も不変（`ls $(echo $(echo $(cat T)))` は allow、4段は deny）。

`main()` は変更しない（`try/except Exception → allow()` の fail-open を維持）。

### 4-5. 閉塞トレース（設計の自己検証）

#### 4-5-1. 第4版（C5／AW-6 の各形／M11・M12）のトレース

**閉塞側 — 「どの関数のどの規則が deny を返すか」を語単位で追う。**

| # | コマンド | 閉塞点 |
|---|---|---|
| C5-1 | `cd tickets/active && awk 'BEGIN{"rm APP-001.md" \| getline x}'` | 第2文の cwd = `tickets/active` → `cwd_is_guarded` 真 → `awk_violation` のゲート通過 → **AW-7**: プログラム本文 `BEGIN{"rm APP-001.md" \| getline x}` → リテラル除去 `BEGIN{"" \| getline x}` → **許可文字集合に `\|` が無い** |
| C5-2 | `cd tickets/done && awk 'BEGIN{"truncate -s 0 APP-001.md" \| getline x}'` | 同上 |
| C5-3 | `cd tickets/active && awk 'BEGIN{"rm APP-001.md" \|& getline x}'`（コプロセス） | 同上（`\|` は `\|&` の一部として現れる） |
| C5-4 | `awk 'BEGIN{"rm tickets/active/APP-001.md" \| getline}'`（リテラル形） | AW-1（語が保護対象＋`\|`）で従来どおり deny。**AW-7 でも二重に deny** |
| C5-5 | `awk 'BEGIN{while(("ls" \| getline l)>0) print l}' tickets/active/APP-001.md` | ゲートは `mentions_guarded` 真 → **AW-7**（`\|`） |
| C5-6 | degraded 版（`cd tickets/active; awk 'BEGIN{"rm APP-001.md" \| getline x}' #'`） | `degraded_violation` の `statement.split("\|")` でパイプ区間に割れ、後段区間の `head_of` が **`getline`**（`\|&` 版は **`&`**）＝`ALLOWED_HEADS` 非収載 → deny。ゲートは `cwd_is_guarded` 真（あるいは前段区間の言及）で真 |
| AW-6-1〜9 | `@f(`／`@ f(`／`@<TAB>f(`／`@_x9(`／`-e'…@f(…'`／`--source='…@f(…'`／`-e '…@f(…'`／cwd 版／degraded 版 | 正常経路は **AW-7**（許可文字 `@`。`-e`／`--source` の値は `awk_program_texts` が取り出す）。degraded 版は **AW-6**（生テキスト＋空白結合） |
| AW-5-1 | `awk 'BEGIN{system ("rm docs/SPEC.md")}'`（組み込み名と `(` の間の空白・正常経路） | **AW-7**（`AWK_CALL_RE` の `IDENT\s*\(` が `system (` に一致し、`system` は `AWK_SAFE_CALLS` 非収載） |
| AW-5-2 | 同 degraded 版（`… #'`） | **AW-5**（空白結合テキストに `system (` が現れる） |
| C4 再確認 | `awk 'BEGIN{system("rm T")}'`／`'{system(…)}'`／`'END{system(…)}'`／`system("rm " "T")`／`@load "inplace"` | **AW-7**（前4形は呼び出し名 `system`、`@load` は許可文字 `@`）。degraded 版は AW-5 |
| S1-1 | `awk '/"/ {system("rm docs/SPEC.md")}' in.txt`（正規表現内の `"`） | **AW-7**: 1パス走査が先頭 `/` を正規表現と判定し `/"/`だけを除去 → `// {system("")}` → 呼び出し名 `system` |
| S1-2 | `awk 'BEGIN{x="(/a"; system("rm /docs/SPEC.md")}'`（文字列内の `(/`） | **AW-7**: 1パス走査が `"(/a"` を文字列として除去 → `BEGIN{x=""; system("")}` → 呼び出し名 `system` |
| S1-3 | `awk 'BEGIN{x=1/2; system("rm docs/SPEC.md")/3}'`（除算） | **AW-7**: `/` の直前が `1`（オペランド終端）＝除算と判定しリテラル除去しない → `system(` が残る |
| S1-4 | `awk 'BEGIN{i=0; i++ /2; system("rm docs/SPEC.md")/3}'`（後置 `++` の直後の `/`） | **AW-7**: `+` を `AWK_OPERAND_END_CHARS` に含めるため除算と判定 → `system(` が残る。加えて候補スパンに `;` があるため二重に正規表現と認めない |
| S3-1 | `awk 'BEGIN{xyzzy("rm docs/SPEC.md")}'`（未知の関数名） | **AW-7**（呼び出し名の未知＝deny。**この次元の機械的既定が働くことの固定**） |
| S2-1 | `awk '@namespace "util"; {print $1}' tickets/active/APP-001.md` | **AW-7**（許可文字 `@`）。**第4版で新規に deny＝誤deny側だが安全側で受容**（§6 R26） |

**M11・M12（誤deny）の解消トレース:**

| # | コマンド | 第3版 | 第4版 | 解消の機序 |
|---|---|---|---|---|
| M11 | `awk 'BEGIN{print "contact: a@b (see docs)"}' tickets/active/APP-001.md` | deny（AW-6 が生テキストの `a@b (` に一致） | **allow** | AW-6 は degraded 専用へ。AW-7 は**リテラル除去後**の `BEGIN{print ""}` を見る（許可文字のみ・呼び出しなし・`print` の後に `>` なし） |
| M12 | `awk '{print}' 'system(1).md' tickets/active/APP-001.md` | deny（AW-5 がファイル名オペランドの `system(` に一致） | **allow** | AW-5 は degraded 専用へ。AW-7 は `awk_program_texts` が返す**先頭オペランド `{print}` のみ**を見る。ファイル名は判定対象外 |

**この2件は develop 版でも deny だったため V-8／V-9 では WEAKER 行として現れる。§4-9 の許容分類7として明示的に登録する**（AC2 が解消を求めた誤denyであり、tester がその旨を報告している）。

#### 4-5-2. 第3版（C3／C4／H2）のトレース

**閉塞側（14形＋同型の未報告分）。** 「どの関数のどの規則が deny を返すか」を示す。

| # | コマンド | 閉塞点 |
|---|---|---|
| C3-1〜4 | `awk -i inplace '{gsub(/a/,"b")}1' tickets/active/APP-001.md`／`-iinplace`／`--include inplace`／`--include=inplace`（`docs/SPEC.md` 版も同じ） | `awk_violation` の **AW-4**（`is_safe_awk_flag("-i")` 等が偽）。ゲートは `mentions_guarded` が真 |
| C3-5 | 上記の degraded 版（`… #'`） | degraded → `segment_violation` → `awk_violation` **AW-4**（空白分割でも `-i` は独立語） |
| C3-6（未報告の同型） | `awk -f prog.awk tickets/active/APP-001.md`（束縛なし・プログラム不可視） | **AW-4**（`-f` 非収載）。第2版は AW-2 が束縛依存のため allow だった |
| C3-7（未報告の同型） | `awk -o prof.out '{print}' T`／`awk -d '{print}' T`／`awk -l lib '{print}' T`／`awk -E p.awk T`／`awk -D T` | **AW-4** |
| C4-1〜4 | `awk 'BEGIN{system("rm T")}'`／`'{system(…)}' /dev/null`／`'END{system(…)}' /dev/null`／SPEC 版 | **AW-5**（`\bsystem\s*\(` が raw 語に一致） |
| C4-5 | `awk 'BEGIN{system("rm " "T")}'`（文字列連結）／degraded 版 | **AW-5**（連結しても `system(` は残る。degraded の空白分割でも先頭語に `system(` が残る） |
| C4-6 | `awk '@load "inplace"; {print}' T` | **AW-5**（`@load`） |
| （既存で捕捉） | `awk 'BEGIN{"rm S" \| getline}'`／`awk 'BEGIN{print "x" \| "cat > S"}'`／`awk 'BEGIN{print "x" > ("docs/" "SPEC.md")}'`／`awk 'BEGIN{f="docs/"; f=f "SPEC.md"; print "x" > f}'` | **AW-1**（保護対象に言及する語が `>`／`\|` を含む。reviewer 実測と一致） |
| H2-1 | `cd TD && awk '{print > "APP-001.md"}' in.txt` | **AW-3（cwd ゲート）**: 文字列除去後 `{print > ""}` が `AWK_OUTPUT_RE` に一致 |
| H2-2 | `cd TD && awk '{printf "x" >> "APP-001.md"}' in.txt` | **AW-3（cwd ゲート）**: `{printf "" >> ""}` が一致 |
| H2-3 | `cd TD && awk -v f=APP-001.md '{print > f}' in.txt` | **AW-3（cwd ゲート）**: `{print > f}` が一致（`-v` は AW-4 で安全） |
| H2-4 | `cd TD && awk '{print > f}' f=APP-001.md in.txt` | **AW-3（cwd ゲート）** |
| H2-5 | `cd TD && sed 's/a/b/w APP-001.md' in.md` | `segment_violation` sed 規則の `strict`（`SED_WRITE_RE` を全語へ適用） |
| H2-6 | 同上の degraded 版（`cd TD; sed … #'`） | 同 sed 規則の `strict` + **空白結合テキスト**（`w` と `APP-001.md` が別語に割れるため） |
| H2-7 | `cd TD && sort -o APP-001.md in.md` | `sort_output_operands` → `guarded_write_target` が cwd と結合して真 |
| H2-8 | `cd TD && uniq in.md APP-001.md` | `uniq_output_operand`（第2位置引数）→ `guarded_write_target` |
| H2-9〜11 | `cd TD && ls $(rm APP-001.md)`／`` ls `rm APP-001.md` ``／`cat <(rm APP-001.md)` | `find_violation` が置換を**その文の cwd** で再帰 → 内側の `statement_violation` が `cwd_is_guarded` でゲート真 → head `rm` が非ホワイトリスト |
| D4 分（絶対パス形） | `sort -o tickets/active/APP-001.md in.md`／`sort --output=docs/SPEC.md x`／`uniq in.md tickets/active/APP-001.md` | `guarded_write_target` の `is_guarded_token` 分岐 |
| D4 分 | `find tickets/active -name '*.md' -fprint0 /tmp/x` | `FIND_WRITE_FLAGS`（`-fprint0` 追加） |

#### 4-5-3. allow を壊さないことの確認（最重要。reviewer の `rv3.py` 70ケースの新規 deny を1件に保つ）

**AW-7 が §3-6 D8 の16形をすべて allow のまま通すことのトレース**（リテラル除去後のテキストと3条件の判定）。**この表が V-12(a) の根拠である。**

| プログラム本文 | リテラル除去後 | 許可文字 | 呼び出し名 | 出力構文 |
|---|---|---|---|---|
| `{print $1}` / `{print}` / `{print $1,$2}` / `{print $n}` / `{print -$2}` | 同一 | ○ | なし | `print` の後は `}` で打ち切り＝不一致 |
| `NR>1 {print}` | 同一 | ○（`>` は許可文字） | なし | **`>` は `print` より前**＝`AWK_OUTPUT_RE` は不一致 |
| `{if ($1 > 2) print $1}` | 同一 | ○ | `if` ○ | 同上（`print` の後は ` $1` → `}`） |
| `{printf "%s\|%s\n", $1, $2}` | `{printf "", $1, $2}` | ○ | `printf` は `(` を伴わない＝呼び出し検出なし | `printf` の後は `""`,`$1`,`$2` → `}`。**`\|` は除去済み** |
| `BEGIN{OFS="\|"} {print $1}` | `BEGIN{OFS=""} {print $1}` | ○ | なし | 不一致 |
| `$1==m {print}` / `$1==s {print}` | 同一 | ○ | なし | 不一致 |
| `/user@example.com/ {print}` | `// {print}` | ○（**`@` は除去済み**） | なし | 不一致 |
| `/@(id\|title)/ {print}` | `// {print}` | ○（**`@`・`\|` とも除去済み**） | なし | 不一致 |
| `/@/ {print}` | `// {print}` | ○ | なし | 不一致 |
| `function p(x){print x} {p($1)}` | 同一 | ○ | `p` は宣言名として許可集合へ | 不一致 |
| `BEGIN{PROCINFO["sorted_in"]="cmp"} {print}` | `BEGIN{PROCINFO[""]=""} {print}` | ○ | `PROCINFO[` は `(` ではない＝検出なし | 不一致 |
| `BEGIN{print "contact: a@b (see docs)"}`（**M11**） | `BEGIN{print ""}` | ○ | なし | 不一致 |
| `{print}` ＋ ファイル名 `'system(1).md'`（**M12**） | `{print}`（ファイル名は**判定対象外**） | ○ | なし | 不一致 |
| `{gsub(/a/,"b")}1` / `{sub(/^ +/, ""); print}` | `{gsub(//,"")}1` / `{sub(//, ""); print}` | ○ | `gsub`・`sub` ○ | 不一致 |
| `{print toupper($1)}` / `{print length($0)}` / `{print substr($0,1,5)}` | 同一 | ○ | `toupper`・`length`・`substr` ○ | 不一致 |
| `{a[$1]++} END{for (k in a) print k, a[k]}` | 同一 | ○ | `for` ○ | 不一致 |
| `BEGIN{getline l < "tickets/active/APP-001.md"; print l}` | `BEGIN{getline l < ""; print l}` | ○（`<` は許可文字） | `getline` は `(` を伴わない＝検出なし | 不一致 |
| `--posix` / `--` / `-v n=3` / `--field-separator='\|'` 付きの各形（T-AWKOK） | オプション語は AW-4 が安全と判定し、`awk_program_texts` は**プログラム本文だけ**を取り出す | ○ | ○ | 不一致 |
| `cd tickets/active && awk '{print $1}' APP-001.md`（cwd 保護下・T-H2f） | `{print $1}`（ファイル名 `APP-001.md` は判定対象外） | ○ | なし | 不一致 |
| `awk '{print $1}' tickets/active/APP-001.md #'`（degraded） | **AW-7 を適用しない**（§3-5）。AW-4／AW-5／AW-6 も不一致 | － | － | － |

**第3版までの allow 維持トレース（変更なし）:**

| コマンド | allow のまま維持される理由 |
|---|---|
| `awk -F'\|' '{print $1}' tickets/active/APP-001.md`（A-3a） | AW-1: 保護対象語に `>`／`\|` なし／AW-2: 束縛なし（`-F\|` は `-` 始まりで束縛走査からスキップ）／AW-4: `-F\|` は `-F` の結合形で**安全**／AW-5: `system(` なし／AW-3: `-F\|` に `print` なし、`{print $1}` は `print` の後 `}` に達し `>`・`\|` へ到達しない |
| `awk 'NR>1 {print}' T` | AW-3: `>` は `print` の**前**。`print` の後は `}` で打ち切り |
| `awk '{if ($1 > 2) print $1}' T` | AW-3: `print` の後は ` $1` → `}` で打ち切り |
| `awk '{printf "%s\|%s\n", $1, $2}' T` | AW-3: **文字列リテラル除去**により `\|` が消える |
| `awk -v OFS=, '{print $1,$2}' T` | AW-2: 束縛値 `,` は保護対象でない／AW-4: `-v` は安全／AW-3: 不一致 |
| `awk 'BEGIN{OFS="\|"} {print $1}' T` | AW-3: 文字列除去後 `BEGIN{OFS=""} {print $1}` は不一致 |
| `awk --posix '{print $1}' T`／`awk -- '{print}' T`／`awk -v n=3 '{print $n}' T` | AW-4: いずれも収載済み（T-AWKOK で固定） |
| **`cd tickets/active && awk '{print $1}' APP-001.md`** | CW フォールセーフ: `awk` は `CWD_SAFE_HEADS` 収載／AW-3（cwd ゲート真）でも `{print $1}` は不一致／AW-4: フラグなし／AW-1・AW-2: 証拠なし |
| **`cd tickets/active && awk -F'\|' '{print $1}' APP-001.md`** | 同上＋`-F\|` は AW-4 で安全 |
| **`cd tickets/active && sed -n '1,5p' APP-001.md`** | `-i` 判定は `-[A-Za-z]*i` に不一致（`-n`）／`strict` の `SED_WRITE_RE` は `1,5p` にも空白結合テキスト `sed -n 1,5p APP-001.md` にも `w\s+\S` が無く不一致 |
| **`cd tickets/active && sort APP-001.md`／`uniq APP-001.md`／`uniq -f 2 APP-001.md`** | `sort_output_operands` は空／`uniq_output_operand` は位置引数1個（`-f` の値 `2` を消費）で空文字列 |
| `cat T \| sort \| uniq -c` | 各区間で出力先オペランドが無い |
| `sort -o /tmp/out tickets/active/APP-001.md` | `guarded_write_target("/tmp/out", …)` は偽（絶対パスは cwd を無視） |
| `cd tickets/active && ls`（A-2）／`pwd`／`cat`／`grep`／`wc -l *.md`／`git status .` | `CWD_SAFE_HEADS` 収載。cwd ゲートで言及ゲートは真になるが head 判定は従来どおり |
| `cd tickets/active && cat APP-001.md > /tmp/x.md`／`> ../../out.txt`／`> "$TMP"` | `guarded_write_target`: 絶対パス／cwd 外へ抜ける相対パス／`$` を含む先はいずれも偽 |
| `cd tickets/active && mv /tmp/e APP-001.md` | `mv` は意図的に許可（M6） |
| `cd tickets/active && cd ../.. && rm foo.md` | 2つ目の `cd` で cwd が `.` になり `cwd_is_guarded` が偽 |
| `ls $(cat T); rm /tmp/junk`（T-C1g） | 置換の帰属が厳密なため第2ステートメントのゲートは偽 |
| `cat T && echo x > "$TMP"`（A-3c）／`grep -E '^(id\|title):' T`（A-1）／`sed -n '1,10p' T` | 第2版から変更なし |

#### 4-5-4. 第5版（C6 の5形・C6-2・SH-1）の閉塞トレース（設計の自己検証）

**すべて語・トークン単位で追った結果である。疑似コードが実際に動くことを、この表で確認してから設計を確定している。**

| # | 入力 | `lex()` の出力（要点） | 判定経路 | 結果 |
|---|---|---|---|---|
| 1 | `awk $'BEGIN{x="foo\x22system(\x22rm tickets/active/APP-001.md\x22)\x22"}'` | 前処理: 行継続なし。走査: `$` の次が `'` → **印を pending へ**。以降は従来どおり `'` クォート状態。末尾 `flush()` で `shlex.split` が **2語**（`awk` ／ `$BEGIN{x="foo\x22system(…)…"}`）を出し、**その後に印トークン**を積む | ①リダイレクトなし → ③ゲート: 2語目に `tickets/active/APP-001.md` が**素で見える**→真 → **③' 印あり → deny** | **deny** |
| 2 | 同・`docs/SPEC.md` 宛て | 同上 | ③ゲート: `basename == "SPEC.md"` で真 → ③' | **deny**（old=allow・**バックストップの無い経路を新たに塞ぐ**） |
| 3 | `cd tickets/active && awk $'…rm APP-001.md…'` | 第1文 `cd tickets/active`（印なし）／第2文 `awk` + 語 + **印** | 第1文: 従来どおり allow → `next_cwd` で cwd=`tickets/active` → 第2文: ③ゲートは `cwd_is_guarded` で真 → ③' | **deny** |
| 4 | `awk $'BEGIN{x="foo\x22(\x22rm tickets/…\x22\x7cgetline x)\x22"}'`（**C5 再開通**） | 同 1 | ③ゲート真（リテラルパスが見える）→ ③' | **deny**（**AW-7 の許可文字集合に頼らずに閉じる**＝符号化された `\|` を照合する必要がない） |
| 5 | 同・cwd 版 | 同 3 | `cwd_is_guarded` → ③' | **deny** |
| 6 | 上記5形の degraded 版（末尾に ` #'` を付けて未閉じクォートにする） | `lex()` が `ValueError` → `degraded_violation` | 全体ゲート真（パスが素で見える／cwd）→ ステートメント走査で **`ANSI_QUOTE_RE.search(statement)` が `$'` に一致** → deny | **deny**（§3-6 D9 が示した「degraded では閉じない」問題を、degraded 自身に QT-1 を置くことで解消） |
| 7 | **C6-2** `sed $'s/a/b/\x77 tickets/active/APP-001.md' in.md` | 1語（`$s/a/b/\x77 tickets/…`）＋印 | ③ゲート真 → ③'（**sed の `SED_WRITE_RE` は `\x77` に一致しないが、QT-1 は head 非依存**） | **deny**（old=allow・同一機構で同時閉塞＝D6-2(a)） |
| 8 | **SH-1** `rm tickets/acti\`＋改行＋`ve/APP-001.md` | 前処理 QT-2 で `\`+改行を**削除** → 1語 `tickets/active/APP-001.md` | ③ゲート真 → ③'（印なし）→ ④ `segment_violation`: head `rm` は `ALLOWED_HEADS` 非収載 | **deny**（old=allow） |
| 9 | **INV-SH-04** `rm $'\x74ickets/active/APP-001.md'` | 1語 `$\x74ickets/active/APP-001.md` ＋印。**素の字面には `tickets/active` が無い**（`\x74ickets`） | ③ゲート: `is_guarded_token` が素で偽 → **QT-3 が復号** → `$tickets/active/APP-001.md` → 真 → ③' | **deny**（QT-3 が無ければ allow のまま＝証拠の符号化） |

**allow を壊さないことの確認（第5版で新たに検査した形）:**

| 入力 | 経路 | 結果 |
|---|---|---|
| `printf $'a\tb'`（保護対象と無関係） | ③ゲート偽（`a\tb` に保護対象なし・復号しても `a<TAB>b`） | **allow** |
| `cat tickets/active/APP-001.md && printf $'a\tb'` | 第1文は印なしで従来どおり allow／第2文はゲート偽 | **allow**（ステートメント粒度の効果） |
| `echo "$'not ansi'" tickets/active/APP-001.md` | `$'` が**ダブルクォート内**に現れるため印を置かない（bash も展開しない）→ head `echo` は許可 | **allow**（クォート状態を追う位置に検出を置いた効果） |
| `awk -F'\t' '{print $1}' tickets/active/APP-001.md` | `$'` を含まない。`-F\t` は AW-4 の安全形。QT-3 の復号は `-F<TAB>` でパスにならない | **allow**（`$'...'` の**代替となる推奨形**であり、R29 の回避策の実在を担保する） |
| `awk '{printf "%s\t%s\n", $1, $2}' tickets/active/APP-001.md` | 印なし。QT-3 は `%s<TAB>%s<LF>` でパスにならない。AW-7 は従来どおり | **allow**（D10 で tester 第2案を却下した理由の実例） |
| `grep -E '^(id\|title):' tickets/active/APP-001.md`（A-1）ほか既存 allow 全形 | **トークン列に印が入らないため判定は完全に不変** | **allow**（AC2 維持） |
| `docs/SPEC.md\`＋改行＋`.bak` 宛ての書き込み（`echo x > docs/SPEC.md\<改行>.bak`） | QT-2 で連結され `docs/SPEC.md.bak` になる。bash も同じ語を作るため**実際に保護対象は触られない** | **allow**（第4版までは誤deny。§4-9 の**許容分類8**として登録する唯一の allow 方向の変化） |

**（第6版の注記）** 上表の 8 行目・最終行の「前処理 QT-2 で削除」は、第6版では **`normalize_line_continuations()` による条件付きの削除**（LX-2）に置き換わる。**いずれの形もバックスラッシュ1個・クォート外であるため、第6版でも同じく連結され判定は変わらない。**

#### 4-5-5. 第6版（C7／C7-2／C8／H4）の閉塞トレース（設計の自己検証）

**すべて文字・語・トークン単位で追った結果である。「疑似コードが実際に動くか」を確認してから設計を確定している（第2版で2件のバグ、第3版で規則化漏れ、第5版で C7／C8 と、3版連続で疑似コードが穴を持ったことへの対処）。**

**(A) C7（区切りの喪失）— reviewer 実測の全形＋広さ検証（`p2.py`／`p3.py`）**

| # | 入力（`<LF>` は生の改行） | 正規化の結果 | `lex()` の出力（要点） | 判定経路 | 結果 |
|---|---|---|---|---|---|
| 1 | `echo x\\<LF>rm docs/SPEC.md` | 変化なし（対消費で改行が対の外に残る） | `echo` / `x\` / **op `;`** / `rm` / `docs/SPEC.md` | 第2文: ③ゲート真（`docs/SPEC.md`）→ ④ head `rm` は `ALLOWED_HEADS` 非収載 | **deny** |
| 2 | `ls \\<LF>rm docs/SPEC.md` | 同上 | `ls` / `\` / op `;` / `rm` / … | 同上 | **deny** |
| 3 | `echo x\\\\<LF>rm docs/SPEC.md`（4個） | 同上（対を2つ消費） | `echo` / `x\\` / op `;` / `rm` / … | 同上 | **deny** |
| 4 | `pwd\\<LF>truncate -s 0 docs/SPEC.md` | 同上 | `pwd\` / op `;` / `truncate` / … | 第2文 head `truncate` 非収載 | **deny** |
| 5 | `cat README.md #\<LF>sed -i 's/orig/pwned/' tickets/active/APP-001.md` | 変化なし（`#` は語頭＝コメント → 条件2で不連結） | `cat` / `README.md` / `#` / **op `;`** / `sed` / `-i` / … （**LX-5 により `\` を落として改行を区切りにする**） | 第2文: ③ゲート真 → ④ `SED_INPLACE_SHORT_RE` が `-i` に一致 | **deny** |
| 6 | `grep x README.md #\<LF>cat y \| tee docs/SPEC.md` | 変化なし | 第2文が `cat y` ｜ `tee docs/SPEC.md` | 第2区間の head `tee` 非収載 | **deny** |
| 7 | `ls #x\<LF>rm tickets/active/APP-001.md` | 変化なし | `ls` / `#x` / op `;` / `rm` / … | 第2文 head `rm` | **deny** |
| 8 | `p3.py` の head 掃引（`echo`／`ls`／`cat README.md`／`pwd`／`cd .`／`grep x README.md`／`awk '{print}' README.md`／`sed -n 1p README.md`／`cut -f1 README.md`）× `\\`+改行 | いずれも変化なし | 先頭文の内容に関わらず**第2文が独立する** | 第2文 head `rm` | **全形 deny** |
| 9 | `echo x\\<LF>rm docs/SPEC.md #'`（**degraded 併用**） | 変化なし | `lex` が `'` 未閉じで `ValueError` → `degraded_violation(正規化後, cwd)` | 関数ゲート真（`docs/SPEC.md`）→ `LEGACY_STATEMENT_RE` が改行で分割 → 第2文 head `rm` | **deny** |
| 10 | `ls #c\<LF>rm tickets/active/APP-001.md #'`（同上） | 変化なし | 同上 | 同上 | **deny** |

**(B) C7-2（ダブルクォート内のコマンド置換＋コメント）**

| 入力 | 正規化 | 経路 | 結果 |
|---|---|---|---|
| `cat "$(ls #\<LF>rm docs/SPEC.md)"` | `dq` 状態で `$(` を検出 → `_skip_balanced` が対応する `)` を見つけ、**領域を丸ごと無加工で emit**（LX-4） | `lex` が `_take_subst` で内側を `substs[0]` へ退避 → `find_violation` が `subst_violation` → **`find_violation(inner, 1, cwd)` の入口で内側を `cmd` 文脈として正規化** → 内側の `#` は語頭＝コメント → 改行が区切り → 第2文 head `rm` | **deny** |
| `cat "$(ls \<LF>rm docs/SPEC.md)"`（**奇数1個＝真の行継続**） | 同上（内側は無加工） | 再帰の入口で正規化 → `ls rm docs/SPEC.md` の1文 → head `ls` は収載・書き込みベクタなし | **allow**（bash も `ls` に引数を渡すだけ＝正しい） |

**(C) C8（degraded の関数レベルゲート）— reviewer 実測の全形（`p1.py`／`p3.py` の Family 1・D 掃引）**

外側の `lex()` は成功し、`` ` `` の内側だけが degraded へ落ちる。**cwd は第1文 `cd tickets/active` の評価後に `next_cwd` が設定し、`subst_violation` → `find_violation(inner, depth+1, cwd)` → `degraded_violation(inner, cwd)` へ伝播する。**

| 入力（`TD = tickets/active`） | 内側 | degraded の判定経路 | 結果 |
|---|---|---|---|
| `` cd TD && ls `rm APP-001.md #'` `` | `rm APP-001.md #'` | **関数ゲート: `cwd_is_guarded("tickets/active")` が真**（第6版の修正）→ 文単位ゲートも真 → `segment_violation` の head `rm` が非収載 | **deny** |
| `` cd TD && ls `echo x > APP-001.md #'` `` | `echo x > APP-001.md #'` | 関数ゲート真 → 文単位のリダイレクトループ `guarded_write_target("APP-001.md", cwd)` が真 | **deny** |
| `` cd TD && ls `sed -i 's/a/b/' APP-001.md #'` `` | 同形 | head `sed` は収載だが `SED_INPLACE_SHORT_RE` が `-i` に一致 | **deny** |
| `` cd TD && ls `find . -delete #'` `` | 同形 | `FIND_WRITE_FLAGS` に `-delete` | **deny** |
| `` cd TD && ls `cp /tmp/e APP-001.md #'` `` ／ `truncate` ／ `python3 -c x` | 同形 | head が `ALLOWED_HEADS` 非収載 | **deny** |
| `` cd TD && echo `tee APP-001.md #'` `` | 同形 | head `tee` 非収載 | **deny** |
| `` cd TD && ls `cat /tmp/e \| tee APP-001.md #'` `` | 同形 | `statement.split("\|")` の第2区間 head `tee` | **deny** |
| `cd TD && cat <(rm APP-001.md #')` | **外側の `lex` が失敗**（`_take_subst` はクォートを追うため `)` が見つからず `ValueError`）→ 外側全体が degraded | 関数ゲートは `tickets/active` の言及で真（cwd 修正がなくても真）→ 文分割 → 第2文 head `cat`／内側の `rm` は空白分割で同一文に入り head 判定に掛からないが、**`LEGACY_STATEMENT_RE` の第1文で `cd` の cwd 追跡 → 第2文で `cwd_is_guarded` が真 → `segment_violation`** | **deny**（第5版でも deny・回帰なし） |
| `cd /tmp && ls \`rm x.md #'\``（**対照・保護対象外の cwd**） | `rm x.md #'` | 関数ゲート: `cwd_is_guarded("/tmp")` 偽 かつ 言及なし → **early return** | **allow**（**C8 の修正が無関係な入力へ波及しないことの固定＝T-C8b**） |
| `cd TD && ls $(rm APP-001.md)`（**対照・degraded に落ちない形**） | — | 正常経路の `subst_violation` → `find_violation(inner,1,cwd)` → 文単位ゲート `cwd_is_guarded` 真 → head `rm` | **deny**（第3版から不変） |

**(D) H4／M13（正常経路と degraded が同じ行構造を見る）**

| 入力 | 第5版の挙動 | 第6版の挙動 |
|---|---|---|
| `awk $\<LF>'BEGIN{x="foo\x22system(\x22rm tickets/active/APP-001.md\x22)\x22"}' #'` | `lex` は結合して `$'` を認識するが `ValueError` で degraded へ落ち、degraded は**生の command** を見て `\n` で分割 → `$` と `'` が別ピースになり `ANSI_QUOTE_RE` に**不一致** → **`head_of` が引用符付きの語を未知の head として扱う「偶発的な安全網」で deny**（tester 報告・reviewer が許容しないと判定） | **入口で正規化されて `awk $'…' #'` になる** → `lex` は `'` 未閉じで `ValueError` → degraded が**正規化後の文字列**を受け取る → `ANSI_QUOTE_RE` が `$'` に一致 → **QT-1 の理由で deny**（意図された規則） |
| 同・cwd 版（`cd tickets/active && awk $\<LF>'…' #'`） | 同上 | 第1文で cwd を追跡 → 第2文の文単位ゲートが `cwd_is_guarded` で真 → `ANSI_QUOTE_RE` 一致 → **deny** |

**tester がコミット済みの回帰ガード（`test_degraded_line_split_between_dollar_and_quote_still_deny`）は deny のみを assert しており理由文字列を assert していないため、1行も変更せずに pass する**（reviewer M13 の判断どおり残す）。

**(E) 二方向要求の確認（奇数形が allow のまま維持されること）**

| 入力 | 正規化 | 判定 | bash の実挙動 |
|---|---|---|---|
| `echo x\<LF>rm docs/SPEC.md` | `echo xrm docs/SPEC.md` | head `echo` は収載・書き込みベクタなし → ③ゲートは真だが ④で違反なし → **allow** | `echo` が文字列を表示するだけ。**`rm` は実行されない**＝allow が正しい |
| `echo x\\\<LF>rm docs/SPEC.md`（3個） | `echo x\\rm docs/SPEC.md` | 同上 → **allow** | 同上 |
| `ls \<LF>rm tickets/active/APP-001.md` | `ls rm tickets/active/APP-001.md` | head `ls` → **allow** | `ls` が2つの引数を受けるだけ |
| `rm tickets/acti\<LF>ve/APP-001.md`（**INV-SH-06**） | `rm tickets/active/APP-001.md` | head `rm` 非収載 → **deny** | 保護対象を削除する＝deny が正しい |
| `echo x > docs/SPEC.md\<LF>.bak`（**INV-SH-19**） | `echo x > docs/SPEC.md.bak` | リダイレクト先が保護対象でない → **allow** | `SPEC.md.bak` を作る＝allow が正しい |
| `rm 'tickets/acti\<LF>ve/APP-001.md'`（**INV-SH-16**） | 変化なし（`sq` 内は不連結） | 語が `tickets/acti\`＋改行＋`ve/APP-001.md` → `PATHISH_RE` が分断 → ③ゲート偽 → **allow** | **改行を含む別名のファイル**を消そうとする（保護対象ではない）＝allow が正しい。**old=allow のため AC6 の回帰ではない**（§4-9 許容分類9） |
| `rm "tickets/acti\<LF>ve/APP-001.md"`（**INV-SH-17**） | `rm "tickets/active/APP-001.md"` | 語が保護対象 → **deny** | 保護対象を削除する＝deny が正しい |
| `rm 'x#y' tickets/acti\<LF>ve/APP-001.md`（**INV-SH-20**） | `rm 'x#y' tickets/active/APP-001.md` | `#` は `sq` 内でコメントにならない → 連結される → **deny** | 同上。**AC1「クォート内の `#` で特殊構文判定をしない」との整合の固定点** |

### 4-6. 追加テスト（Phase 6〜13）

すべて `tests/_util.py` の既存ブラックボックス方式（サブプロセス起動 + stdin ペイロード）で書く。**既存94件は1行も変更しない。** `TD = tickets/active`、`T = tickets/active/APP-001.md`、`S = docs/SPEC.md`。

| ID | コマンド | 期待 | 目的 |
|---|---|---|---|
| T-C3a | `awk -i inplace '{gsub(/a/,"b")}1' T` ／ 同 `S` | deny | C3（AW-4） |
| T-C3b | `awk -iinplace '{print}' T` ／ `awk --include inplace '{print}' T` ／ `awk --include=inplace '{print}' T` | deny | C3 の全表記 |
| T-C3c | `awk -i inplace '{print}' T #'` | deny | C3 × degraded |
| T-C3d | `awk -f prog.awk T` ／ `awk -E prog.awk T` ／ `awk -l lib '{print}' T` | deny | 外部コード読み込み次元（未報告の同型） |
| T-C3e | `awk -o prof.out '{print}' T` ／ `awk -d '{print}' T` ／ `awk -p '{print}' T` | deny | 書き出しオプション次元 |
| T-C4a | `awk 'BEGIN{system("rm T")}'` ／ `awk '{system("rm T")}' /dev/null` ／ `awk 'END{system("rm T")}' /dev/null` | deny | C4（AW-5） |
| T-C4b | `awk 'BEGIN{system("cp /tmp/e S")}'` ／ `awk 'BEGIN{system("truncate -s 0 S")}'` ／ `awk 'BEGIN{system("rm " "S")}'` | deny | C4（外部コマンド・文字列連結） |
| T-C4c | `awk 'BEGIN{system("rm S")}' #'` | deny | C4 × degraded |
| T-C4d | `awk '@load "inplace"; {print}' T` | deny | AW-5（`@load`） |
| **T-AWKOK** | `awk --posix '{print $1}' T` ／ `awk -- '{print}' T` ／ `awk -v n=3 '{print $n}' T` ／ `awk --field-separator='\|' '{print $1}' T` ／ `awk 'BEGIN{OFS="\|"} {print $1}' T` | **allow** | **AW-4／AW-5 が読み取り awk を誤denyしないことを固定** |
| T-H2a | `cd TD && awk '{print > "APP-001.md"}' in.txt` ／ `printf "x" >> "APP-001.md"` 版 ／ `-v f=APP-001.md` 版 ／ 位置引数 `f=APP-001.md` 版 | deny | H2 awk 4形（AW-3 の cwd ゲート） |
| T-H2b | `cd TD && sed 's/a/b/w APP-001.md' in.md` ／ `cd TD; sed 's/a/b/w APP-001.md' in.md #'` | deny | H2 sed 2形（`strict` と空白結合） |
| T-H2c | `cd TD && sort -o APP-001.md in.md` ／ `cd TD && uniq in.md APP-001.md` | deny | H2 sort/uniq |
| T-H2d | `cd TD && ls $(rm APP-001.md)` ／ `` cd TD && ls `rm APP-001.md` `` ／ `cd TD && cat <(rm APP-001.md)` | deny | H2 置換内3形（cwd 伝播） |
| T-H2e | `sort -o T in.md` ／ `sort --output=S x` ／ `sort -oT in.md` ／ `uniq in.md T` | deny | D4 改訂分（絶対パス形） |
| **T-H2f** | `cd TD && awk '{print $1}' APP-001.md` ／ `cd TD && awk -F'\|' '{print $1}' APP-001.md` ／ `cd TD && sed -n '1,5p' APP-001.md` ／ `cd TD && sort APP-001.md` ／ `cd TD && uniq APP-001.md` ／ `cd TD && uniq -f 2 APP-001.md` ／ `sort -o /tmp/out T` | **allow** | **保護対象 cwd 下の読み取りを誤denyしないことを固定（reviewer `rv3.py` の3件を含む・本改訂で最も重要な allow 固定）** |
| T-FIND0 | `find TD -name '*.md' -fprint0 /tmp/x` | deny | `FIND_WRITE_FLAGS` の列挙漏れ |
| T-LEGACY | `LEGACY_DENY_COMMANDS` の全件 | deny | 固定リスト回帰ガード（§4-7） |

**第4版で追加するテスト（Phase 8・9）。** **tester がコミット済みの4件（`e7b2dc0`）は1行も変更せずに pass させる**こと（2件は deny を、2件は allow を要求する）。テストを緩める修正は禁止（緩めたくなった時点で設計の欠陥＝orchestrator へ報告）。

| ID | コマンド | 期待 | 目的 |
|---|---|---|---|
| **T-AW7a**（tester 既存・無変更） | `cd TD && awk 'BEGIN{"rm APP-001.md" \| getline x}'` ／ `cd tickets/done && awk 'BEGIN{"truncate -s 0 APP-001.md" \| getline x}'` | deny | **C5（入力パイプ）** |
| **T-AW7b**（tester 既存・無変更） | `cd TD && awk 'BEGIN{"rm APP-001.md" \|& getline x}'` | deny | **C5（コプロセス）** |
| **T-AW7c**（tester 既存・無変更） | `awk 'BEGIN{print "contact: a@b (see docs)"}' T` | **allow** | **M11 の解消**（リテラル内の `@ident (`） |
| **T-AW7d**（tester 既存・無変更） | `awk '{print}' 'system(1).md' T` | **allow** | **M12 の解消**（ファイル名オペランド） |
| T-AW7e | `awk 'BEGIN{"rm T" \| getline}'`（リテラル形）／`awk 'BEGIN{while(("ls" \| getline l)>0) print l}' T`／`awk 'BEGIN{print "x" \|& "cat"}' T` | deny | 入力／出力パイプ・コプロセスの他形 |
| T-AW7f | `awk 'BEGIN{system ("rm S")}'`（空白形・**正常経路**）／`awk 'BEGIN{xyzzy("rm S")}'`（**未知の関数名**） | deny | 呼び出し名ホワイトリストと**未知＝deny の機械的既定**の固定 |
| T-AW7g | `awk '@namespace "util"; {print $1}' T` | deny | 許可文字集合（`@`）の固定。**第4版で新規に deny になる形を意図的に固定する**（R26） |
| T-AW7h | `awk '/"/ {system("rm S")}' in.txt`／`awk 'BEGIN{x="(/a"; system("rm /S")}'`／`awk 'BEGIN{x=1/2; system("rm S")/3}'`／`awk 'BEGIN{i=0; i++ /2; system("rm S")/3}'` | deny | **S1（1パスのリテラル除去）の健全性**。2パス実装ならいずれかが allow に転じる＝実装が設計どおりであることの検出器 |
| T-AW7i | プログラム本文にバックスラッシュ／バッククォートを含む形（`awk 'BEGIN{t=1 \ 2}' T` 等） | deny | 許可文字集合の固定 |
| **T-AW7OK** | `awk 'NR>1 && $1!="" {print $2}' T`／`awk -v n=3 '{if (NF > n) print $1}' T`／`awk '{print toupper($1)}' T`／`awk '{print length($0)}' T`／`awk '{gsub(/a/,"b"); print}' T`／`awk '{sub(/^ +/, ""); print}' T`／`awk '{n=split($0,a,"\|"); print n}' T`／`awk '{printf("%s\n", substr($0,1,5))}' T`／`awk '{a[$1]++} END{for (k in a) print k, a[k]}' T`／`awk 'BEGIN{FS=":"} {print $2}' T`／`awk 'BEGIN{getline l < "T"; print l}'`／`awk 'BEGIN{print "日本語のメッセージ"}' T` | **allow** | **AW-7 が日常の読み取り awk を誤denyしないことの固定（第4版で最重要の allow 固定）。§3-6 D8 の16形と §4-5-3 のトレースに対応** |
| **T-INVENTORY** | `INVENTORY_CASES` の全件（INV-AWK-01〜20・INV-SED-01〜08） | 各行の期待値 | **§3-9／§3-11 の棚卸し表と実装の機械的対応（P5）。C5 のような「表にあるが実装が無い」状態を検出する** |

**第5版で追加するテスト（Phase 10・11）。** **tester がコミット済みの2件（`9c9a5ab`・`$'...'` バイパスの実証）は1行も変更せずに pass させる**こと。

| ID | コマンド | 期待 | 目的 |
|---|---|---|---|
| **T-QT1a**（tester 既存・無変更） | `awk $'BEGIN{x="foo\x22system(\x22rm T\x22)\x22"}'` ／ cwd 版 ／ `S` 版 | deny | **C6（`system(` 隠蔽）** |
| **T-QT1b**（tester 既存・無変更） | `awk $'BEGIN{x="foo\x22(\x22rm T\x22\x7cgetline x)\x22"}'` ／ cwd 版 | deny | **C6（C5 再開通形）** |
| T-QT1c | 上記5形の末尾に ` #'` を付けた degraded 版 | deny | **degraded にも QT-1 があること**（§3-6 D9 の穴を塞いだことの検出器。**この規則を落とすと必ず失敗する**） |
| T-QT1d | `sed $'s/a/b/\x77 T' in.md`（**C6-2**）／`sed $'s/a/b/w T' in.md`（エスケープ無し） | deny | **QT-1 が head 非依存であること**（awk 限定にすると1形目が通る） |
| T-QT1e | `echo x > $'tickets/active/APP-001.md'` ／ `rm $'tickets/active/APP-001.md'`（**既存テストと同形**） | deny | **印トークンの挿入位置**（`flush()` 後）が `redirect_violation` を壊していないことの検出器。印を演算子と語の間に置く実装なら1形目が allow に転じる |
| T-QT1f | `awk $"BEGIN{print}" T` | deny | ロケール翻訳 `$"…"`（INV-SH-05） |
| **T-QTOK** | `printf $'a\tb'` ／ `cat T && printf $'a\tb'` ／ `echo "$'not ansi'" T` ／ `awk -F'\t' '{print $1}' T` ／ `awk '{printf "%s\t%s\n", $1, $2}' T` | **allow** | **QT-1／QT-3 の誤deny上限の固定**（ゲート前置き・クォート内 `$'` の非検出・`$'...'` の代替形が使えること） |
| T-QT2 | `rm tickets/acti\`＋改行＋`ve/APP-001.md`（**SH-1**） | deny | **QT-2（行継続を bash と同じく削除）** |
| T-QT2b | `echo x > docs/SPEC.md\`＋改行＋`.bak` | **allow** | QT-2 の帰結（bash が触るのは `SPEC.md.bak`）＝**許容分類8 の固定** |
| T-QT3 | `rm $'\x74ickets/active/APP-001.md'`（**INV-SH-04**）／`rm $'\x64ocs/SPEC.md'` | deny | **QT-3（証拠の復号）** |
| **T-INVENTORY**（拡張） | `INVENTORY_CASES` の全件（INV-AWK-01〜20・INV-SED-01〜08・**INV-SH-01〜12**） | 各行の期待値 | 棚卸し表と実装の機械的対応（P5）。**第5版でシェル展開の次元が加わる** |

**第6版で追加するテスト（Phase 12・13）。** **既存テスト（tester がコミット済みの分を含む）は1行も変更せずに pass させる**こと。**T-C7c・T-C7d(b)・T-C7g は「allow を要求する」テストであり、これらを deny にする実装は過剰denyであって設計違反である**（reviewer の二方向要求）。

| ID | コマンド（`<LF>` は生の改行・`T = tickets/active/APP-001.md`・`S = docs/SPEC.md`） | 期待 | 目的 |
|---|---|---|---|
| **T-C7a** | `echo x\\<LF>rm S` ／ `ls \\<LF>rm S` ／ `echo x\\\\<LF>rm S`（4個） ／ `pwd\\<LF>truncate -s 0 S` ／ `echo x\\<LF>sed -i 's/a/b/' T` ／ `ls a\\<LF>cat b \| tee S` | deny | **C7（偶数個＝区切り）。INV-SH-13。head 側・書き込み側の代表を対で固定する** |
| **T-C7b** | `cat README.md #\<LF>sed -i 's/orig/pwned/' T` ／ `ls #x\<LF>rm S` ／ `grep x README.md #\<LF>cat y \| tee S` ／ `echo x #\<LF>truncate -s 0 T` | deny | **C7（`#` コメント末尾＝区切り）。INV-SH-14** |
| **T-C7c**（**二方向要求の固定点**） | `echo x\<LF>rm S`（1個） ／ `echo x\\\<LF>rm S`（3個） ／ `ls \<LF>rm T` | **allow** | **奇数個は bash も連結するため deny は誤り。INV-SH-15。過剰deny側へ倒した実装をここで機械的に検出する** |
| **T-C7d** | (a) `rm "tickets/acti\<LF>ve/APP-001.md"` → deny ／ (b) `rm 'tickets/acti\<LF>ve/APP-001.md'` → **allow** | 各行のとおり | **クォート種別による分岐（INV-SH-17／INV-SH-16）。`"…"` は連結し `'…'` は連結しない** |
| **T-C7e** | `cat "$(ls #\<LF>rm S)"` ／ `` ls `ls #\<LF>rm S` `` | deny | **C7-2（置換の内側はコマンド文脈へ戻る）。INV-SH-18。LX-4（内側を無加工で残し再帰で正規化する）の検出器** |
| **T-C7f** | `rm 'x#y' tickets/acti\<LF>ve/APP-001.md` ／ `rm a#b tickets/acti\<LF>ve/APP-001.md` | deny | **クォート内・語中の `#` をコメントと誤検出して連結を止めていないこと（INV-SH-20・AC1 との整合）** |
| **T-C7g** | `grep -c '#' T` ／ `grep -n '#\|x' T` ／ `awk '{print}' T # メモ` | **allow** | **`#` のモデル化が日常の読み取りを誤denyしないこと**（LX-3 の誤deny上限） |
| **T-C7h** | `echo a \<LF>#c\<LF>rm S`（**行継続の直後にコメントが始まる形**） | deny | **正規化走査が「連結時に直前の文字（`prev`）を書き換えない」ことの検出器。** 書き換える実装だとコメントを見落として2つ目の行継続まで連結し **allow に転じる**（C7 の再発） |
| **T-C8a** | `` cd TD && ls `rm APP-001.md #'` `` ／ `` cd TD && ls `echo x > APP-001.md #'` `` ／ `` cd TD && ls `sed -i 's/a/b/' APP-001.md #'` `` ／ `` cd TD && ls `find . -delete #'` `` ／ `` cd TD && echo `tee APP-001.md #'` `` ／ `` cd tickets/done && ls `rm APP-001.md #'` `` | deny | **C8（degraded の関数レベルゲートの cwd 対応）。この1行の修正を戻すと全件が失敗する** |
| **T-C8b**（対照） | `` cd /tmp && ls `rm x.md #'` `` ／ `` ls `rm x.md #'` `` | **allow** | **C8 の修正が保護対象外へ波及しないことの固定**（関数レベルゲートが依然として early return すること） |
| **T-H4a** | `awk $\<LF>'BEGIN{x="foo\x22system(\x22rm T\x22)\x22"}' #'` ／ cwd 版 | deny | **H4（正規化後の文字列を degraded へ渡す）。tester がコミット済みの回帰ガードと同形であり、こちらは QT-1 の理由で deny になることを設計として固定する**（テストは理由を assert しない） |
| **T-INVENTORY**（拡張） | `INVENTORY_CASES` の全件（INV-AWK-01〜20・INV-SED-01〜08・**INV-SH-01〜20**） | 各行の期待値 | 棚卸し表と実装の機械的対応（P5・**P7**）。**第6版で行継続と `#` の分岐が加わる** |

**第2版・第3版から引き継ぐテスト（変更しない）:** T-A1〜T-A6・T-B4a〜T-B6b・T-NEST・T-PROC・T-AWK・T-SEDW・T-FP・T-D1／D2・T-SED-N・T-C1a〜T-C1h・T-C2a〜T-C2h・T-H1a〜T-H1f・T-M1a〜T-M1d・T-M45／T-M45b・T-C3a〜T-C3e・T-C4a〜T-C4d・T-AWKOK・T-H2a〜T-H2f・T-FIND0・T-LEGACY・**AW-6 の3件（間接呼び出し9形の deny・degraded の語跨ぎ4形の deny・`@` を含む読み取り8形の allow）**・tester 追加分。**このうち AW-6 の「`@` を含む読み取り8形の allow」と T-AWKOK・T-H2f が、第4版の誤deny予算の主要な固定点である**（V-12(a)）。

### 4-7. 固定リスト回帰ガード（D5・第3版／第4版で追加する項目）と `INVENTORY_CASES`（P5）

`tests/test_guard_bash_writes.py` の `LEGACY_DENY_COMMANDS` へ次を**追記する**（既存項目は削除しない）。

```python
    # --- C3: gawk の in-place 拡張（KLK-010 第3版） ---
    "awk -i inplace '{gsub(/a/,\"b\")}1' tickets/active/APP-001.md",
    "awk -i inplace '{gsub(/a/,\"b\")}1' docs/SPEC.md",
    "awk -iinplace '{print}' tickets/active/APP-001.md",
    "awk --include inplace '{print}' tickets/active/APP-001.md",
    "awk --include=inplace '{print}' tickets/active/APP-001.md",
    "awk -i inplace '{print}' tickets/active/APP-001.md #'",
    "awk -f prog.awk tickets/active/APP-001.md",
    # --- C4: awk の外部コマンド実行 ---
    "awk 'BEGIN{system(\"rm tickets/active/APP-001.md\")}'",
    "awk '{system(\"rm tickets/active/APP-001.md\")}' /dev/null",
    "awk 'END{system(\"rm tickets/active/APP-001.md\")}' /dev/null",
    "awk 'BEGIN{system(\"rm docs/SPEC.md\")}'",
    "awk 'BEGIN{system(\"rm docs/SPEC.md\")}' #'",
    # --- H2: 保護対象 cwd の相対パス書き込み ---
    "cd tickets/active && awk '{print > \"APP-001.md\"}' in.txt",
    "cd tickets/active && awk '{printf \"x\" >> \"APP-001.md\"}' in.txt",
    "cd tickets/active && awk -v f=APP-001.md '{print > f}' in.txt",
    "cd tickets/active && awk '{print > f}' f=APP-001.md in.txt",
    "cd tickets/active && sed 's/a/b/w APP-001.md' in.md",
    "cd tickets/active; sed 's/a/b/w APP-001.md' in.md #'",
    "cd tickets/active && sort -o APP-001.md in.md",
    "cd tickets/active && uniq in.md APP-001.md",
    "cd tickets/active && ls $(rm APP-001.md)",
    "cd tickets/active && ls `rm APP-001.md`",
    "cd tickets/active && cat <(rm APP-001.md)",
    # --- D4 改訂分（old=allow だが本改訂で閉じた。維持規律3が許容する追加） ---
    "sort -o tickets/active/APP-001.md in.md",
    "sort --output=docs/SPEC.md x.md",
    "uniq in.md tickets/active/APP-001.md",
    "find tickets/active -name '*.md' -fprint0 /tmp/x",
```

**AW-6 の追記（第3版の実装で 58 → 71 件へ拡充済み。本改訂で設計へ反映＝M9-2 の⑥）:** 間接呼び出しの9形（`@f(`／変数名違い／SPEC 版／`-v f=system` 併用／`@ f(`／`-e'…'`／`--source='…'`／degraded／cwd 版）と、degraded の語跨ぎ4形（`@ f(` の2形／`system (`／cwd 版）。

**第4版で追記する（`LEGACY_DENY_COMMANDS`）:**

```python
    # --- C5: system() 以外の外部コマンド実行経路（KLK-010 第4版・AW-7） ---
    "cd tickets/active && awk 'BEGIN{\"rm APP-001.md\" | getline x}'",
    "cd tickets/done && awk 'BEGIN{\"truncate -s 0 APP-001.md\" | getline x}'",
    "cd tickets/active && awk 'BEGIN{\"rm APP-001.md\" |& getline x}'",
    "awk 'BEGIN{\"rm tickets/active/APP-001.md\" | getline}'",
    "awk 'BEGIN{while((\"ls\" | getline l)>0) print l}' tickets/active/APP-001.md",
    "awk 'BEGIN{print \"x\" |& \"cat\"}' tickets/active/APP-001.md",
    # --- AW-7: 呼び出し名ホワイトリスト（未知名・空白形） ---
    "awk 'BEGIN{system (\"rm docs/SPEC.md\")}'",
    "awk 'BEGIN{xyzzy(\"rm docs/SPEC.md\")}'",
    # --- AW-7: リテラル除去の健全性（S1。2パス実装だと allow に転じる形） ---
    "awk '/\"/ {system(\"rm docs/SPEC.md\")}' in.txt",
    "awk 'BEGIN{x=\"(/a\"; system(\"rm /docs/SPEC.md\")}'",
    "awk 'BEGIN{x=1/2; system(\"rm docs/SPEC.md\")/3}'",
```

**第5版で追記する（`LEGACY_DENY_COMMANDS`）:**

```python
    # --- C6: $'...'（ANSI-C 引用）の hex エスケープによる AW-7 バイパス（第5版・QT-1） ---
    "awk $'BEGIN{x=\"foo\\x22system(\\x22rm tickets/active/APP-001.md\\x22)\\x22\"}'",
    "cd tickets/active && awk $'BEGIN{x=\"foo\\x22system(\\x22rm APP-001.md\\x22)\\x22\"}'",
    "awk $'BEGIN{x=\"foo\\x22(\\x22rm tickets/active/APP-001.md\\x22\\x7cgetline x)\\x22\"}'",
    "cd tickets/active && awk $'BEGIN{x=\"foo\\x22(\\x22rm APP-001.md\\x22\\x7cgetline x)\\x22\"}'",
    "awk $'BEGIN{x=\"foo\\x22system(\\x22rm tickets/active/APP-001.md\\x22)\\x22\"}' #'",  # degraded
    "rm $'tickets/active/APP-001.md'",   # 既存テストと同形（印の位置の回帰ガード）
    # --- 維持規律3 が許容する追加（old=allow だが本改訂で閉じた） ---
    "awk $'BEGIN{x=\"foo\\x22system(\\x22rm docs/SPEC.md\\x22)\\x22\"}'",   # C6・SPEC.md 宛て
    "sed $'s/a/b/\\x77 tickets/active/APP-001.md' in.md",                   # C6-2（\\x77 = w）
    "rm tickets/acti\\\nve/APP-001.md",                                     # SH-1（QT-2）
    "rm $'\\x74ickets/active/APP-001.md'",                                  # INV-SH-04（QT-3）
```

**第6版で追記する（`LEGACY_DENY_COMMANDS`）— すべて old=deny（維持規律4 に適合）:**

```python
    # --- C7: 行継続の除去がステートメント区切りを飲み込む（第6版・LX-2） ---
    "echo x\\\\\nrm docs/SPEC.md",                       # バックスラッシュ2個（偶数）
    "ls \\\\\nrm docs/SPEC.md",
    "echo x\\\\\\\\\nrm docs/SPEC.md",                   # 4個（偶数）
    "pwd\\\\\ntruncate -s 0 docs/SPEC.md",
    "cat README.md #\\\nsed -i 's/orig/pwned/' tickets/active/APP-001.md",  # コメント末尾
    "ls #x\\\nrm docs/SPEC.md",
    "grep x README.md #\\\ncat y | tee docs/SPEC.md",
    "cat \"$(ls #\\\nrm docs/SPEC.md)\"",                # C7-2（dq 内の置換＋コメント）
    "echo a \\\n#c\\\nrm docs/SPEC.md",                  # 連結の直後にコメントが始まる形
    # --- C8: degraded の関数レベルゲートが cwd を見ない（第6版・1行修正） ---
    "cd tickets/active && ls `rm APP-001.md #'`",
    "cd tickets/active && ls `echo x > APP-001.md #'`",
    "cd tickets/active && ls `sed -i 's/a/b/' APP-001.md #'`",
    "cd tickets/active && ls `find . -delete #'`",
    "cd tickets/done && ls `rm APP-001.md #'`",
```

**収載判断の根拠（維持規律4 の適用）:** C7 の各形は**旧版が `LEGACY_STATEMENT_RE` で改行を無条件に分割していたため old=deny**。C8 の各形は**旧版が `cd` を `ALLOWED_HEADS` に持たず、`cd tickets/active` を含む文字列をコマンドごと deny していたため old=deny**（§2-1）。**いずれも「old=allow なのに新版の保守的既定で deny になった形」ではない。**

**収載しない形（意図的・第6版）:** `echo x\`＋改行＋`rm docs/SPEC.md`（奇数個）は **old=deny だが新版では allow が正しい**（bash も連結する）。**`LEGACY_DENY_COMMANDS` へ入れてはならない**。この形は **T-C7c と `INVENTORY_CASES` の INV-SH-15 で `allow` として固定する**（維持規律4 の「将来 allow へ緩める余地を残したい形は収載せず個別テストと `INVENTORY_CASES` で固定する」に該当。ただし本件は"将来"ではなく**設計として allow が正である**ため、その旨をコメントに明記する）。同様に `rm 'tickets/acti\`＋改行＋`ve/…'`（INV-SH-16・old=allow）も収載しない。

**`@namespace` と「未知の組み込み関数」の扱い（implementer が報告した設計内部の矛盾の解消・第5版）:**

第4版は「`@namespace` と未知の組み込み関数は誤deny側なので `LEGACY_DENY_COMMANDS` へ入れない」と書きながら、同節のコードブロックで `awk 'BEGIN{xyzzy("rm docs/SPEC.md")}'`（未知の関数名）の追記を明示していた。**この矛盾を、リストの収載基準を明確化することで解消する（コード変更は不要）。**

- **収載基準は「old（develop・merge-base `72ae730`）が deny していたか」である。** `awk 'BEGIN{xyzzy("rm docs/SPEC.md")}'` は、旧版が `awk` を `ALLOWED_HEADS` に持たず**保護対象に言及する awk をコマンドごと deny していた**ため（§2-1）、**old=deny であり収載は規律4 に反しない**。実装（`4dc47c3`）の判断は正しく、**この1行を削除してはならない**（削除すると「未知の呼び出し名は deny」という P4 の機械的既定の回帰ガードが失われる）。
- **規律4 が禁じているのは「old=allow なのに新版の保守的既定によって deny になった形」を入れること**である（例: `@namespace` は old=deny だが**判定の固定を T-AW7g と INV-AWK-11 で行い、将来 allow へ緩める余地を残す**ため意図的に収載しない）。**「old=deny だが将来 allow へ緩める余地を残したい形」は収載しない**という運用上の例外も明記する。
- 第5版で追記する C6 の各形のうち、`docs/SPEC.md` 宛て・C6-2・SH-1・INV-SH-04 の4件は **old=allow** であるが、**規律3（将来見つかった穴を塞いだ時点で追加してよい）**に基づき収載する。

**維持規律（テストファイルのコメントとして明記する。第5版で 4 を明確化）:**
1. 本リストは **削除禁止**。項目を消す＝保護を弱めることであり、AC6 の再定義に当たる。
2. `ALLOWED_HEADS` を増やす／判定を緩める変更を行うときは、設計書 §3-9 の棚卸し表へ行を追加し、§3-10 の5手順を踏み、対応する回帰ケースを本リストへ追加する。
3. 本リストは「旧版が deny していた集合」に限らず、**将来見つかった old=allow の穴も塞いだ時点で追加してよい**。
4. **収載の基準は「old が deny していたか」または「本改訂で塞いだ穴か」である。** `old=allow` の形を「新版の保守的既定（未知は deny）によって deny になった」という理由だけで入れてはならない（将来安全性を確認して緩める余地を残すため）。**old=deny の形は、それが現在は誤deny寄りに見えても収載してよい**（例: `xyzzy(` は実機 gawk が拒否するが old=deny であり、収載は「未知名は deny」という既定の回帰ガードとして機能する）。**将来 allow へ緩める余地を残したい形は、収載せず個別テストと `INVENTORY_CASES` で固定する**（例: `@namespace`）。

**`INVENTORY_CASES`（第4版で新設・P5・§3-11）:**

```python
# 設計書 docs/designs/KLK-010.md §3-9 の書き込みベクタ棚卸し表と、実装の
# 対応を機械的に固定する。**表に構文を1行足したら、ここへ1件以上足す。**
# LEGACY_DENY_COMMANDS が「旧版が deny していた集合を守る」ものであるのに対し、
# 本リストは「棚卸し表の各セルが実装されていることを守る」もの（目的が異なる
# ため統合しない）。期待値 "allow" の行は**意図的に未対応の穴**であり、
# 塞いだ時点で "deny" へ変える（変更は意識的な行為になる）。
INVENTORY_CASES = (
    ("INV-AWK-01", "print > FILE",  "awk '{print > \"docs/SPEC.md\"}' x.md", "deny"),
    ...
    ("INV-AWK-04", '"cmd" | getline',
     "cd tickets/active && awk 'BEGIN{\"rm APP-001.md\" | getline x}'", "deny"),
    ...
    ("INV-SED-05", "sed -f SCRIPT（未対応・D6-3・別チケット）",
     "sed -f script.sed tickets/active/APP-001.md", "allow"),
    ...
)
```

全件は §3-11 の**3つ**の写像表（INV-AWK-01〜20・INV-SED-01〜08・**INV-SH-01〜20**）を正とする。1つのテスト（**T-INVENTORY**）が `subTest` で全件を検証する。**第5版で 28件 → 40件、第6版で 40件 → 48件。**

**第6版で `INVENTORY_CASES` のコメントへ追記する区別（P7）:** 期待値 `allow` の行には**2種類**ある。混同すると「穴を塞いだつもりで正しい allow を deny へ変える」事故が起きるため、コード上で区別する。

- **(i) 意図的に未対応の穴**（INV-SH-07・09・11・INV-SED-05〜07 ほか）— 塞いだ時点で `deny` へ変える。
- **(ii) bash と一致した結果としての allow**（**INV-SH-15・16・19**・INV-AWK-16〜20 ほか）— **`deny` へ変えたら誤りである。** bash が実際には保護対象を触らないことが根拠であり、変更するには §3-9-2-1 の分岐条件そのものを覆す実測が要る。

### 4-8. 実装Phase

<!-- Phaseの完了条件は作業チェックポイントであり、チケットの受け入れ条件を再定義・緩和しない -->

**Phase 1〜11 は実装・コミット済み（再実装しない）。** **Phase 12・13 が第6版で新たに定義する未実施の作業である。**

| Phase | 状態 | 内容 | 完了条件 | 対応する受け入れ条件 |
|---|---|---|---|---|
| 1 | **完了**（`4ae9f61`） | L1字句・L2パスの導入と L3 の再構成 | 達成済み | AC1・AC2(A-1/A-3b/A-3c)・AC4(B-4c/B-6b)・AC5・AC6・AC7 |
| 2 | **完了**（`cc3fdcc`） | `ALLOWED_HEADS` 拡張＋awk/sed 規則・置換の再帰 | 達成済み | AC2(A-2/A-3a)・AC3・AC4(B-4a/B-4b/B-6a)・AC6・AC7 |
| 3 | **完了**（`921debf` + tester `8edff18`） | V-1〜V-6 実施、README・CHANGELOG 更新 | 達成済み | AC6・AC7 |
| 4 | **完了**（`91afa8b` + 修正 `778fa62` + tester `2d77030`・`93988a8`） | C1／C2／H1／M1／M4-2／M4-5 の解消 | 達成済み（reviewer が閉塞を再実測） | AC6（中核）＋ AC1〜AC5・AC7 の維持 |
| 5 | **完了**（`30064fd`） | `LEGACY_DENY_COMMANDS`・T-LEGACY・文書整合 | 達成済み（ただし文書に乖離あり＝M7・M8 で是正） | AC6・AC7 |
| 6 | **完了**（`2d4538b`＋tester `9b63b59`） | **C3／C4／H2 の解消（コード＋テスト）**。§4-1（`CWD_SAFE_HEADS`・awk 安全オプション集合・`AWK_EXEC_RE`・`SORT_OUTPUT_FLAGS`・`UNIQ_VALUE_FLAGS`・`FIND_WRITE_FLAGS` へ `-fprint0`）／§4-4(b)（`guarded_write_target` 新設・`cwd_redirect_violation` 削除）／§4-4(c)（`segment_violation` の cwd 受け取り・sed の `strict`・sort/uniq 規則・フォールセーフ既定）／§4-4(d)（`is_safe_awk_flag`・AW-4・AW-5・AW-3 の cwd ゲート）／§4-4(e)（`sort_output_operands`・`uniq_output_operand`）／§4-4(f)(h)（`guarded_write_target` への置換・degraded の cwd 受け渡し）／§4-4(i)（置換再帰への cwd 伝播）。§4-6 の T-C3a〜T-FIND0 を追加 | (1) 既存94件が**無変更のまま**全パス。(2) 追加テストが全パス。(3) スイート全体が failures 0・errors 0。(4) **C3・C4・H2 の各修正が同一コミットに含まれる**（部分適用の中間コミットは穴が開いた状態を作るため作らない）。(5) **V-10（awk 安全オプション集合と実機 `awk --help` の突き合わせ）を実施し結果を報告** | **AC6（中核）**・AC1〜AC5・AC7 の維持 |
| 7 | **完了**（`098ceb9`） | **回帰ガードと文書整合**。§4-7 の `LEGACY_DENY_COMMANDS` 追記と T-LEGACY の再実行。docstring・README・CHANGELOG を実装へ整合（**M7**: 件数は実測値のみ／**M8**: 過大主張の訂正・`awk -i inplace`・`system()` の対応済み化・cwd の実際の保護範囲）。**新たに文書化する限界**: M5（cwd 接頭辞＋相対パスの分割）・M6（`mv` の意図的許可）・sed の `strict` による保守的 deny・mawk 固有オプションの deny・§3-9 の未対応一覧（`sed -f`／sed `e`／`sort --compress-program`／`git diff --output`／**`is_readonly_script_call` の偽装**）・§3-10 の追加手順。§4-9 の V-1〜V-10 を実施 | V-1〜V-10 が全項目 pass。README・CHANGELOG・docstring と実装に齟齬がない（**実装より強い保護を主張する記述が1つも無い**）。T-LEGACY が全件 deny | AC6・AC7 |
| 7 追補 | **完了**（`6d8cfad`＋tester `e7b2dc0`） | **AW-6（`AWK_INDIRECT_CALL_RE`＝間接関数呼び出しの検出）と AW-5／AW-6 の空白結合テキスト評価。** 設計書に Phase 8 が無かったため implementer は「Phase 6 の C4（外部コマンド実行次元）の同一次元の欠落」として Phase 6・7 の追補として1コミットで実施した。**本改訂でこの位置づけを設計として承認する**（M9-2 の⑧） | 達成済み（tester が回避形9形の非成立を実機確認・old/new 80コマンドで WEAKER 0） | AC6・AC7 |
| **8** | **完了**（`4dc47c3`＋tester `9c9a5ab`） | **C5／M11／M12 の解消（コード＋テスト）＝プログラム内構文次元のホワイトリスト化**。§4-1 の定数追加（`AWK_SAFE_PROGRAM_CHARS`・`AWK_OPERAND_END_CHARS`・`AWK_SAFE_CALLS`・`AWK_CALL_RE`・`AWK_FUNC_DECL_RE`・`AWK_PROGRAM_TEXT_FLAGS`・`AWK_VALUE_CONSUMING_FLAGS`）／§4-4(d) の `awk_strip_literals`・`awk_program_texts`・`awk_program_violation` の新設と `awk_violation` の再構成（**AW-1〜AW-4 は1行も変えず、AW-5／AW-6 を `degraded` 分岐へ移し、AW-7 を追加**）／§4-4(c)(h) の `degraded` 引数の受け渡し／モジュール docstring の該当箇所（L3 の説明・既知の限界）を**同一コミットで**更新。§4-6 の T-AW7e〜T-AW7i・T-AW7OK を追加 | (1) **tester がコミット済みの4件（`e7b2dc0`）が無変更のまま pass**（テストを緩めない）。(2) 既存テストの削除行が **0**。(3) スイート全体が failures 0・errors 0。(4) **AW-7 の追加と AW-5／AW-6 の degraded 化が同一コミット**（分けると M11／M12 が残る中間状態か、C5 が開いた中間状態になる）。(5) **V-11（構文次元の実機検証）を実施し報告**。(6) **V-12（誤deny予算）を満たす** | **AC6（中核）**・AC1〜AC5・AC7 の維持 |
| **9** | **完了**（`534de1a`） | **回帰ガード・棚卸しの機械的対応・文書整合**。§4-7 の `LEGACY_DENY_COMMANDS` 追記（C5 の6形＋AW-7 の呼び出し名2形＋S1 の3形。**誤deny側は入れない**）と T-LEGACY 再実行／**`INVENTORY_CASES` と T-INVENTORY の新設**（§3-11 の写像表と1:1）／docstring・README・CHANGELOG を実装へ整合（**プログラム内構文次元がホワイトリストになったことの明記**・R26 の誤deny一覧・**AW-5／AW-6 が degraded 専用であることの明記**・件数は実測値のみ）／V-1〜V-13 の実施 | V-1〜V-13 が全項目 pass。**実装より強い保護を主張する記述が1つも無い**こと。T-LEGACY・T-INVENTORY が全件期待どおり。§3-9／§3-11 の構文数と `INVENTORY_CASES` の件数が一致（V-13） | AC6・AC7 |

| **10** | **完了**（reviewer 3回目が C6／C6-2／SH-1 の閉塞を確認） | **C6／C6-2／SH-1 の解消（コード＋テスト）＝字句レイヤの穴を閉じる**。§4-1 の定数追加（`ANSI_QUOTE_KIND`・`ANSI_QUOTE_RE`・`ANSI_QUOTE_REASON`・`ANSI_C_SIMPLE_ESCAPES`・`ANSI_C_ESCAPE_RE`）／§4-2 の `lex()` 変更（**QT-2** の行継続削除・**QT-1** の `$'`／`$"` 検出と `flush()` での印トークン出力）と `ansi_c_decode` 新設／§4-3 の `is_guarded_token`（**QT-3**）／§4-4(g) の `pipe_segments`（印トークンの読み飛ばし1行）と `statement_violation`（**QT-1** の判定③'）／§4-4(h) の `degraded_violation`（**QT-1** の部分文字列版）／モジュール docstring の該当箇所（**R4 の誤った限界記述の訂正**・新しい保守側判定・既知の限界のシェル展開の次元）を**同一コミットで**更新。§4-6 の T-QT1a〜f・T-QTOK・T-QT2／T-QT2b・T-QT3 を追加 | (1) **tester がコミット済みの2件（`9c9a5ab`）が無変更のまま pass**（テストを緩めない）。(2) 既存テストの削除行が **0**。(3) スイート全体が failures 0・errors 0。(4) **QT-1（正常経路・degraded の両方）・QT-2・QT-3 が同一コミット**（分けると中間状態で穴が残る。特に **degraded の QT-1 を落とすと ` #'` 1つで全形が通る**）。(5) **V-14（字句レイヤの実機検証）を実施し報告**。(6) **V-12（誤deny予算）を満たす**（新規 deny が §6 R29 の列挙内） | **AC6（中核）**・AC1〜AC5・AC7 の維持 |
| **11** | **完了**（`INVENTORY_CASES` 40件・T-LEGACY 拡充・R4 の訂正まで反映済み） | **回帰ガード・棚卸しの機械的対応・文書整合**。§4-7 の `LEGACY_DENY_COMMANDS` 追記（C6 の6形＋old=allow の4形）と T-LEGACY 再実行／**`INVENTORY_CASES` へ INV-SH-01〜12 を追加**（28 → 40件）し T-INVENTORY を拡張／§4-7 の**維持規律4 の明確化をテストファイルのコメントへ反映**（`xyzzy(` を巡る設計内部の矛盾の解消。**コードの変更は無い**）／docstring・README・CHANGELOG を実装へ整合（**R4 の訂正**・R29 の誤deny一覧・**シェル展開の次元の未対応（ブレース展開・glob・パラメータ展開）**・件数は実測値のみ）／V-1〜V-14 の実施 | V-1〜V-14 が全項目 pass。**実装より強い保護を主張する記述が1つも無い**こと（特に「`$'...'` は保守側へ倒れる」という記述が残っていないこと）。T-LEGACY・T-INVENTORY が全件期待どおり。§3-9／§3-9-2／§3-11 の構文数と `INVENTORY_CASES` の件数が一致（V-13） | AC6・AC7 |

| **12** | **未実施** | **C7／C7-2／C8／H4 の解消（コード＋テスト）＝行構造の近似を bash と一致させる**。§4-1 の定数追加（`COMMENT_START_PREV`・`MIDWORD_SENTINEL`）／§4-2-1 の走査ヘルパ共通化（`_skip_balanced`・`_skip_backtick` を新設し `_take_subst`／`_take_backtick` をその上に再実装＝**M18**）／§4-2-2 の **`normalize_line_continuations` 新設（LX-1〜LX-5）**／§4-2-3 の `lex()` から**2つの `replace` を撤去**し `\`+改行と改行を走査の中で扱う／§4-4(h) の `degraded_violation` の**関数レベルゲートに `cwd_is_guarded` を追加（C8・1行）**／§4-4(i) の `find_violation` の入口で**正規化を1回だけ実行**（H4）／モジュール docstring の該当箇所（**M14 の「証拠は増える方向のみ」の訂正**・`#` コメントのモデル化範囲＝**M15**・LX-5 の不変条件）を**同一コミットで**更新。§4-6 の T-C7a〜g・T-C8a／b・T-H4a を追加 | (1) **既存テストが1行も変更されずに全パス**（削除0行。**tester がコミット済みの分と `test_degraded_line_split_between_dollar_and_quote_still_deny` を含む**）。(2) スイート全体が failures 0・errors 0。(3) **C7 の修正・C8 の修正・H4 の正規化集約が同一コミット**（分けると中間状態で穴が残る。特に **H4 を入れずに C7 だけ直すと degraded が別の行構造を見続ける**）。(4) **T-C7c／T-C7d(b)／T-C7g が allow のまま pass すること**（**過剰deny側へ倒した実装を機械的に検出する二方向要求の合否条件**）。(5) **V-14 を新しいサブケース（§3-9-2-1）で再実施し報告**。(6) **V-12（誤deny予算）を満たす**（新規 deny が §6 **R33** の列挙内） | **AC6（中核）**・AC1〜AC5・AC7 の維持 |
| **13** | **未実施** | **回帰ガード・棚卸しの機械的対応・文書整合**。§4-7 の `LEGACY_DENY_COMMANDS` 追記（C7 の9形＋C8 の5形＝**計14件・すべて old=deny**）と T-LEGACY 再実行／**`INVENTORY_CASES` へ INV-SH-13〜20 を追加**（40 → 48件）し T-INVENTORY を拡張／**`INVENTORY_CASES` のコメントへ「未対応の穴」と「bash と一致した結果の allow」の区別を追記**（§4-7）／docstring・README・CHANGELOG を実装へ整合（**M14 の訂正**＝「証拠は増える方向のみ」という記述が1つも残っていないこと・**M15**（`#` の未除去とその向き）・R32〜R35・件数は実測値のみ）／V-1〜V-14 の実施 | V-1〜V-14 が全項目 pass。**実装より強い保護を主張する記述が1つも無い**こと（特に「連結は証拠を増やす方向にしか動かない」という記述が残っていないこと）。T-LEGACY・T-INVENTORY が全件期待どおり。§3-9／§3-9-2／§3-9-2-1／§3-11 の構文数と `INVENTORY_CASES` の件数が一致（V-13） | AC6・AC7 |

**Phase数の自己点検（第6版）:** 第6版で**新たに実施する Phase は 2 つ（12・13）**。表が13行あるのは Phase 1〜11 の実施済み記録を残しているためであり、**未実施スコープは2Phaseである**。Phase 12 単独ではリリース単位にならず（`INVENTORY_CASES` に INV-SH-13〜20 が無く P5／P7 の担保が無い状態で、docstring が **M14 の誤った主張を維持したまま**になる）、Phase 13 はテストと文書のみでコード変更を伴わない。したがって**各Phaseは独立してレビュー・リリースできる粒度ではなく、チケット粒度の見直しは不要**と判断した。

**全受け入れ条件のカバレッジ自己点検（第6版）:**

| AC | カバーする Phase | 第6版での確認事項 |
|---|---|---|
| AC1 | P1（達成済み）＋ **P12** | **本改訂は L1 の「行構造」の近似を bash と一致させる。** AC1 が名指しする4文字のうち **`#`** について、**クォート内の `#` は特別扱いしない**（LX-3・T-C7f／T-C7g／INV-SH-20 で固定）。`\|` `<<` `>` の扱いは第1版から不変。**V-14 が「hook の語トークン」と「bash の argv」を突き合わせ、行継続・コメントの全分岐について相違が無いことを確認する** |
| AC2 | P1・P2・P4・P6・P8・P10（達成済み）＋ **P12** | **A-1〜A-3c は `\`+改行も語頭 `#` も含まないため、`normalize_line_continuations` は入力と同一の文字列を返し判定は完全に不変**（§4-2-2 のトレース表・最終行）。新規誤denyは **§6 R33 の4種類**に限定し V-12 で「列挙外があればブロッカー」とする。**加えて T-C7c／T-C7d(b)／T-C7g が「過剰deny側へ倒れていないこと」を機械的に固定する**（reviewer の二方向要求） |
| AC3 | P2・P4・P6・P8（達成済み） | **第6版で `ALLOWED_HEADS`・`CWD_SAFE_HEADS` を変更しない。** `cd` は引き続き無条件許可。C8 の修正は degraded 経路の**ゲート**であり head の許可集合には触れない |
| AC4 | P1・P2・P4・P6（達成済み）＋ **P12** | 置換・cwd の判定基準は不変。**加えて B-6a（`ls $(rm …)`／バッククォート版）の degraded 変形（内側に未閉じクォートを置く形）が C8 の修正で deny 側へ入る**＝AC4 の履行が強まる方向 |
| AC5 | P1（達成済み） | パス検出は変更なし（`PATHISH_RE`・basename 完全一致・`normpath` は不変）。正規化は文字の削除のみで、**削除するのは `\` と改行だけ＝`PATHISH_RE` の候補文字を1文字も落とさない** |
| AC6 | **P12（中核）**＋ P13（T-LEGACY／T-INVENTORY） | **C7 の8形・C7-2・C8 の5形がすべて deny**（§4-5-5）。**`\`+改行も語頭 `#` も含まない入力では正規化が恒等写像になり判定が完全に不変**であるため、既存 deny が構造的に保存される。V-8／V-9 の WEAKER 行がすべて §4-9 の分類（**第6版で追加する分類9** を含む）に収まること |
| AC7 | P12・P13 | 追加テストとスイート全件パス（件数は実測値。**既存テストの削除0行**） |

**未カバーのACは無い。**

**（履歴）第5版のカバレッジ自己点検:**

| AC | カバーする Phase | 第5版での確認事項 |
|---|---|---|
| AC1 | P1（達成済み）＋ **P10** | **本改訂は L1（字句レイヤ）の変更を含む唯一の改訂である。** QT-2 は bash の挙動（行継続の削除）へ**近づける**変更であり、AC1「シェルの字句解析を尊重する」の趣旨に沿う。QT-1 は「シェルの字句解析を**尊重できないことが確定した入力**を判定不能として扱う」規則であり、クォート内の `\|` `<<` `>` `#` を分割・特殊構文と誤認しない性質は不変（印は語の内容を1文字も変えない） |
| AC2 | P1・P2・P4・P6・P8（達成済み）＋ **P10** | **A-1／A-2／A-3a／A-3b／A-3c はいずれも `$'…'`・`$"…"`・行継続を含まないため判定は完全に不変**（§4-5-4 の allow トレースで確認）。新規誤denyは **§6 R29 の4種類**に限定し、V-12 で「列挙外が1件でもあればブロッカー」とする。`rv3.py` の新版 deny は **1/70 のまま**であること |
| AC3 | P2・P4・P6・P8（達成済み） | **第5版で `ALLOWED_HEADS`・`CWD_SAFE_HEADS` を変更しない。** `cd` は引き続き無条件許可（`cd` セグメント自体は QT-1 の対象外＝`cd` の語に `$'` を書かない限り印は付かない） |
| AC4 | P1・P2・P4・P6（達成済み）＋ P10 | 第5版で置換・cwd の判定は変更なし。**加えて `$'…'` を用いた B-4／B-6 の変形（`sed $'…\x69…'` 等）も QT-1 で deny 側へ入る** |
| AC5 | P1（達成済み）＋ P10 | パス検出は変更なし（`PATHISH_RE`・basename 完全一致は不変）。**QT-3 は「復号後の字面でもう一度同じ判定を行う」だけで判定基準を変えない**ため、`MYSPEC.md`／`docs/SPEC.md.bak` の allow は維持される |
| AC6 | **P10（中核）**＋ P11（T-LEGACY／T-INVENTORY） | **C6 の5形＋C6-2＋SH-1 がすべて deny**（§4-5-4）。**`$'…'`・`$"…"`・行継続を含まない入力ではトークン列に印が入らず判定が完全に不変**であるため、既存 deny が構造的に保存される。V-8／V-9 の WEAKER 行がすべて §4-9 の分類（**第5版で追加する分類8** を含む）に収まること |
| AC7 | P10・P11 | 追加テストとスイート全件パス（件数は実測値。**tester の2件は無変更で pass**） |

**未カバーのACは無い。**

**（履歴）第4版のカバレッジ自己点検:**

| AC | カバーする Phase | 第4版での確認事項 |
|---|---|---|
| AC1 | P1（達成済み）＋ P8 | 第4版の追加規則はすべて L3（構文レイヤ）内であり、字句解析の方式（L1）を変更しない。degraded の分割も不変（AW-7 は degraded に適用しない） |
| AC2 | P1・P2・P4・P6（達成済み）＋ **P8** | **M11・M12（tester が AC2 側として報告した誤deny2件）を解消する**。**T-AW7OK・T-AWKOK・T-H2f・AW-6 の allow 8形が新規誤denyの上限を固定**し、`rv3.py` 70ケースの新版 deny を1件に保つ（**V-12**）。新規 deny は R26 の列挙内に限る |
| AC3 | P2・P4・P6（達成済み）＋ P8 | `cd` は引き続き無条件許可。`awk`・`sed`・`sort`・`uniq` も `ALLOWED_HEADS` に残る。**awk は「安全と確認できるプログラム形以外を deny」へ強まる**（§9 の elaboration を deny 方向へ改訂・§0 に明記） |
| AC4 | P1・P2・P4・P6（達成済み） | 第4版で変更なし（置換の外側／内側方向・cwd 伝播は不変。AW-7 は expanded 変体にも適用される） |
| AC5 | P1（達成済み） | 変更なし |
| AC6 | **P8（中核）**＋ P9（T-LEGACY／T-INVENTORY） | **C5 の6形すべてが deny**（§4-5-1）。**AW-1〜AW-4 を1行も変えないため既存 deny が構造的に保存される**。V-8／V-9 の WEAKER 行がすべて §4-9 の分類（M11・M12 を含む**分類7**）に収まること |
| AC7 | P8・P9 | 追加テストとスイート全件パス（件数は実測値。**tester の4件は無変更で pass**） |

**未カバーのACは無い。** テンプレート §4 の例外（reviewer の確認行為のみで実装を伴わない AC）に該当するものも無い。

### 4-9. 検証チェックリスト（Phase 12・13 で implementer が実施し、tester と reviewer が再実行する）

すべてチケット専用worktree内で実行する。

| # | 検証内容 | コマンド／方法 | 期待結果 | AC |
|---|---|---|---|---|
| V-1 | スイート全体の回帰 | `python3 -m unittest discover -s tests` | **Phase 12 着手時点の実測件数（reviewer 3回目時点で 405件・全パス）＋追加分が全パス**（failures 0・errors 0）。**件数は実測値で報告し設計書に固定しない**（D3） | AC7 |
| V-2 | 対象ファイル単体 | `python3 -m unittest tests.test_guard_bash_writes -v` | **Phase 12 着手時点の実測件数＋追加分が全パス** | AC7 |
| V-3 | 変更ファイルの限定 | `git diff develop...HEAD --stat` | `guard_bash_writes.py` / `test_guard_bash_writes.py` / `README.md` / `CHANGELOG.md` / `docs/designs/KLK-010.md` のみ | AC6・D2 |
| V-4 | 既存テストの非改変 | `git diff {Phase 12 着手直前の HEAD} --numstat -- tests/test_guard_bash_writes.py`（**基準線のコミットハッシュは implementer が実測して実装メモへ記録する**） | **削除 0 行**（追加のみ）。`git diff {基準線} -- tests/ \| grep -E "^-[^-]"` が空。**tester がコミット済みの回帰ガード（`test_degraded_line_split_between_dollar_and_quote_still_deny` を含む）を1文字も変更していない**（reviewer M13: 理由文字列を assert していない現在の形が適切） | AC6・D3 |
| V-5 | 依存の非追加 | import が `json` / `os` / `re` / `shlex` / `sys` / `_ticket_lib` のみ | 標準ライブラリのみ | README 設計方針 |
| V-6 | 文書整合（M7・M8） | README・CHANGELOG・docstring の記述が §4 の実装と一致。**実装より強い保護を主張する記述が無いこと**。テスト件数が実測値であること | 齟齬なし | AC6 |
| V-7 | 固定リスト回帰ガード | T-LEGACY（`LEGACY_DENY_COMMANDS` 全件・§4-7 の追記後） | 全件 deny | AC6 |
| **V-8** | old/new 差分（基本セット） | `scratchpad/klk010/` の `cmp.py`〜`cmp5.py` を、`new_guard.py` を現行実装へ再同期し `old_guard.py` が `git merge-base develop HEAD` と一致することを `diff -q` で確認してから再実行 | WEAKER 行が下記の**分類可能な集合のみ** | AC6 |
| **V-9** | **old/new 差分（第3版・第4版の対象セット）** | 同ディレクトリの **`rv1.py`〜`rv5.py`** を再実行 | **`rv5.py` の24形のうち21形（R1〜R6）が old=deny → new=deny**。残る3行（R7=M5・N1=`mv`・N2=`X=1 git add`）は **`rv5.py` が機械的に REGRESSION と印字するが、設計自身が「意図的 allow／未閉塞の限界」と定めた行**であり、下記の許容分類4・1・2 に対応する（**第3版 V-9 の期待値「REGRESSION 行が無い」は rv5 のケース表と両立せず誤りだったため、第4版で実測に合わせて訂正した**＝M9-2 の⑩）。`rv1`／`rv2`／`rv4` の WEAKER 行が下記の分類に収まる。**`rv3.py`（日常読み取り70件）の新版 deny が1件（`cat T \| tee /tmp/x`）のまま増えていない** | **AC2・AC6** |
| **V-10** | **awk 安全オプション集合の検証（§3-10 の根拠2）** | `awk --version` と `awk --help`（本ホストは GNU Awk 5.2.1）の出力を `AWK_SAFE_FLAGS` / `AWK_SAFE_VALUE_FLAGS` と突き合わせる | (a) 収載した各オプションが man/help の説明上ファイル書き込み・コード読み込みを行わない (b) `-f` `-i` `-l` `-E` `-D` `-o` `-p` `-d` が**いずれも非収載**＝deny 側に落ちる (c) 収載を追加した場合は根拠を報告する（迷うオプションは収載しない） | AC6 |
| **V-11** | **プログラム内構文次元の実機検証（§3-10 の根拠2の構文次元版・第4版で新設）** | 使い捨ての一時ディレクトリで実機 `awk`（本ホストは GNU Awk 5.2.1）を実行し、hook の判定と突き合わせる。**リポジトリ内のファイルを対象にしてはならない** | (1) **安全形の無害性**: §4-6 T-AW7OK の各プログラムを空の一時ディレクトリで実行し、実行前後で `ls -a` が不変（ファイルの生成・改変が起きない）ことを確認する。(2) **危険形の実在性**: `"cmd" \| getline`／`\|& getline`／`system (`／`@f(`／`@ f(`／`@load`／`print > "REL"`／`-i inplace` が**実際にファイルを作成・削除する／プロセスを起動する**ことを確認する（既測分は再確認でよい）。(3) **S1（リテラル除去）の健全性**: §4-6 T-AW7h の4形＋`BEGIN{print "system(" ; system("rm F")}`＋`system` 改行 `(` について「実機が実行するか」と「hook が deny するか」を突き合わせ、**実行するのに allow になる形が1つでもあればブロッカー**。(4) **`AWK_SAFE_CALLS` の突き合わせ（S3）**: 実機 gawk のマニュアル（`man gawk` / `info gawk` の Built-in Functions）の組み込み関数を列挙し、(a) `system` が非収載であること (b) 非収載の関数が deny 側＝可視な誤denyになること (c) 収載した各関数がファイル書き込み・プロセス起動を行わない根拠を1行ずつ報告する。(5) 結果を実装メモへ記録する | **AC6** |
| **V-12** | **誤deny予算（第4版で新設・第5版／第6版で対象を追加）** | 既存 allow 固定テストの再実行＋`rv3.py`＋新規 deny の棚卸し | (a) **既存の allow 固定テストが全件 allow**（T-H1f 5形・T-AWKOK 5形・AW-6 の `@` 読み取り8形・T-H2f 7形・A-3a・tester の M11／M12 の2形・T-AW7OK 12形・T-QTOK 5形と T-QT2b・**T-C7c 3形／T-C7d(b)／T-C7g 3形／T-C8b 2形＝第6版**）。1件でも deny なら**設計の欠陥**として実装で回避せず orchestrator へ報告する。**とくに T-C7c（奇数個の行継続）が deny になった場合は「C7 の修正が過剰deny側へ倒れた」ことを意味し、reviewer の二方向要求への違反である。** (b) **`rv3.py`（日常読み取り70件）の新版 deny が 1/70 のまま**（`cat T \| tee /tmp/x` のみ）。2件以上ならブロッカー。(c) 新規に deny になる読み取り形が **§6 R26（第4版）・R29（第5版）・R33（第6版）の列挙内に収まる**こと。列挙外が1件でもあればブロッカー。(d) (a)〜(c) を実測値つきで報告する | **AC2・AC3** |
| **V-13** | **棚卸し表と実装の写像（P5・第4版で新設・第5版で拡張）** | §3-9／§3-9-2／§3-11 の構文の件数と `INVENTORY_CASES` の件数・ID を突き合わせる | §3-11 の写像表の全 ID（INV-AWK-01〜20・INV-SED-01〜08・**INV-SH-01〜12＝計40件**）が `INVENTORY_CASES` に存在し、T-INVENTORY が全件期待どおりに判定する。**表にあって `INVENTORY_CASES` に無い ID が1つでもあればブロッカー**（C5 の再発防止機構そのもの） | AC6 |

| **V-14** | **字句レイヤの前提の実機検証（§3-10 の根拠2の字句版・第5版で新設・P6）** | **hook が見る語トークン**と**bash が実際にコマンドへ渡す argv** を突き合わせる。使い捨ての一時ディレクトリで実施し、**リポジトリ内のファイルを対象にしてはならない** | (1) **bash 側の観測**: 各形について、先頭コマンドを `printf '<%s>\n'` へ置き換えた同型を `bash -c` で実行し、**bash が渡す argv を1要素ずつ出力させる**（`bash -c "printf '<%s>\n' \$'BEGIN{x=\"foo\x22system(…)\x22\"}'"` 等）。(2) **hook 側の観測**: `lex()` の戻り値を出力する（検証手順としての内部関数呼び出しであり**テストスイートには入れない**）。(3) **突き合わせ**: bash の argv に現れる「保護対象パス・`\|`・`>`・`system(`・`w `（sed）等の到達経路」が hook の語トークンに現れるかを1形ずつ確認する。**相違がある形は「規則で deny になる」か「`INVENTORY_CASES` に `allow` として登録され docstring の限界に記載されている」かのいずれかでなければならない。どちらでもない形が1件でもあればブロッカー。** (4) **実行効果の確認**: deny 側の代表形（C6 の5形・C6-2・SH-1）を使い捨てディレクトリで実際に実行し、**ファイルの作成・削除が起きること**（＝穴が実在すること）と hook が deny することを確認する。(5) **対象の最小セット**は §3-9-2 の全行**および §3-9-2-1 の全サブケース**（**第6版で更新＝H3／SV-15 への対処。第5版の「行継続（語内・語間・`'…'` の内側）」という3形の列挙は、bash の挙動が分岐する条件を尽くしておらず C7 を見逃した**）: `$'\x22'`／`$'\x7c'`／`$'\x74'`（証拠の符号化）／`$"…"`／**行継続の6サブケース（奇数1個・偶数2個・奇数3個・偶数4個・`#` コメント末尾・`'…'` 内側・`"…"` 内側・ダブルクォート内の置換＋コメント）**／**`#` コメントの3サブケース（語頭・語中・クォート内）**／`{a,b}`／`*`・`?`・`[…]`／`~`／`$VAR`・`${v/a/b}`／`$((…))`／履歴展開（**非対話シェルで無効であることの確認**）。(6) 結果（相違の有無・向き・対処）を実装メモへ記録し、**§3-9-2 の表の各行および §3-9-2-1 の各サブケースと1対1で照合する**。(7) **（第6版で新設）行継続とコメントの二方向検証**: 各サブケースについて **(i) `bash -c` で実行し「後続コマンドが実行されるか」を使い捨てディレクトリのファイル有無で確認する** **(ii) hook の判定（allow/deny）と突き合わせる**。**「bash が後続コマンドを実行するのに hook が allow」（＝C7 型）と「bash が実行しないのに hook が deny」（＝過剰deny）の両方をブロッカーとする。** とくに**奇数個の形が allow のまま維持されていること**を明示的に確認し報告する | **AC1・AC6** |

**V-8／V-9 の WEAKER 行（old=deny → new=allow）に許される分類 — これ以外が1行でもあればブロッカーとして報告する:**

1. **AC2／AC3 が明示的に解消を要求した誤deny** — A-1／A-2／A-3a／A-3b／A-3c、および `cd tickets/…` 後の読み取り・`pwd`・`mv`（active↔done 移動）・`git status`・`> /tmp/x` 等（旧版は `cd` を含むコマンドを一律 deny していたため＝§2-1）。
2. **`ALLOWED_GIT_SUBCOMMANDS` 収載サブコマンドの代入前置き版**（`X=1 git add …` 等）— reviewer が「旧版の deny は `git` 自身をサブコマンドと誤認したパースバグであり保護ではない」と判定済み。
3. **クォート内の区切り文字を含む読み取り**（`grep ';rm' …` 等）— AC1 が意図した緩和。
4. **M5（cwd 接頭辞と相対パスが分割された形）** — §3-8 の未閉塞の限界として文書化（reviewer 判定）。
5. **保護対象パスがリテラルに現れない形**（変数・置換の出力・断片結合。`awk -v d=tickets -v f=active/X.md '{print > (d"/"f)}'` 等）— 旧版でも `mentions_guarded` が偽で allow だった行を含む。R6 の限界。
6. **コマンドを伴わない代入のみの文**（`f=tickets/active/APP-001.md`）— 書き込みを行わない。
7. **（第4版で追加）AW-5／AW-6 の過剰一致による誤deny** — awk プログラムの**文字列／正規表現リテラルの中身**、および**プログラム以外のオペランド**（ファイル名・変数束縛値）にのみ現れる `system(`／`@ident(` 様の部分文字列。tester が **M11・M12 として AC2 の解消対象**と報告した2形を含む。awk に eval が無いため実行され得ず、deny する根拠が存在しない（§4-5-1 の解消トレース）。**この分類に該当するのは「awk のプログラム本文以外にのみ危険様パターンが現れる形」に限られ、プログラム本文に現れる形が allow へ転じたら分類外＝ブロッカーである。**
8. **（第5版で追加）行継続を bash と同じく削除することに伴い、bash が実際には保護対象を触らない形の誤denyが解消される行** — `echo x > docs/SPEC.md\`＋改行＋`.bak` のように、**連結後の語が保護対象ではなくなる**形。bash も同じ語を作るため、deny は誤りであった。**この分類に該当するのは「連結後の語が `_is_guarded_path` に一致しない」形に限られ、連結後に一致する形（`tickets/acti\`＋改行＋`ve/…`）が allow のままなら分類外＝ブロッカーである**（そちらは LX-2 条件1 が新たに deny にする形＝T-QT2／INV-SH-06）。
9. **（第6版で追加）`'…'` の内側の `\`+改行を bash と同じくリテラルとして扱った結果、連結されなくなる行** — `rm 'tickets/acti\`＋改行＋`ve/APP-001.md'` のように、**bash が触るのは「改行を含む別名のファイル」であって保護対象ではない**形（**old=allow のため V-8／V-9 の WEAKER 行としては現れない**が、第5版比では deny → allow の変化である）。**この分類に該当するのは「`'…'` の内側でのみ発生する」形に限られ、クォート外・`"…"` の内側の形（INV-SH-06・17）が allow のままなら分類外＝ブロッカーである。**
10. **（第6版で追加）C7 の修正が過剰deny側へ倒れていないことの裏返し** — **奇数個の行継続で後続コマンドが語へ吸収される形**（`echo x\`＋改行＋`rm docs/SPEC.md`）は **old=deny → new=allow** であり V-8／V-9 で WEAKER 行として現れうる。**bash も連結して後続コマンドを実行しないため allow が正しい**（reviewer が明示した二方向要求）。**この分類に該当するのは「bash が実際に後続コマンドを実行しない」ことを V-14(7) で実測確認できた形に限られる。**

**テスト観点（境界・モック方針）:**
- テストは **hook をサブプロセス起動して stdin ペイロードを与えるブラックボックス**に統一する。内部関数の直接呼び出しテストは追加しない。
- モック不要・一時ディレクトリ不要（`os.path.normpath` は純粋な文字列操作）。**実際にファイルを書き換える検証（`awk -i inplace` の実挙動確認）は reviewer が scratchpad で行うものであり、テストスイートには入れない**（テストがリポジトリのファイルを書き換えてはならない）。
- 重点エッジケース: (1) awk のオプション次元（C3・T-C3a〜e／T-AWKOK）、(2) awk の実行次元（C4・T-C4a〜d）、(3) 保護対象 cwd 下の awk/sed/sort/uniq の**書き込み形と読み取り形の両方**（T-H2a〜c／**T-H2f**）、(4) 置換 × cwd（T-H2d）、(5) 絶対パス形の sort/uniq（T-H2e）、(6) 既存の deny 集合（T-LEGACY）、**(7) awk のプログラム内構文次元の書き込み形・実行形・読み取り形の3方向（T-AW7a〜i／T-AW7OK）**、**(8) リテラル除去の健全性（T-AW7h＝2パス実装だと allow に転じる形）**、**(9) 棚卸し表と実装の写像（T-INVENTORY）**。
- **実機での副作用確認（V-11・V-14(4)）は implementer が scratchpad の使い捨てディレクトリで行い、テストスイートには入れない**（テストがリポジトリのファイルを書き換えてはならない。第3版の V-10 と同じ方針）。**V-14(2) の `lex()` 直接呼び出しも検証手順であり、テストスイートには入れない**（テストはブラックボックスに統一する）。
- **第6版で追加する重点エッジケース:** (15) **行継続の分岐条件の両方向**（T-C7a／T-C7b＝新規 deny・**T-C7c＝奇数形の allow 維持**。**片方だけでは検出できない**）、(16) **クォート種別による分岐**（T-C7d。`"…"` は連結し `'…'` は連結しない）、(17) **置換の内側の文脈**（T-C7e。LX-4 を落とすと C7-2 が開く）、(18) **`#` のモデル化がクォート内へ波及していないこと**（T-C7f／T-C7g。AC1 との整合）、(19) **degraded の関数レベルゲート**（T-C8a＝deny／**T-C8b＝波及しないことの対照**）、(20) **正常経路と degraded が同じ行構造を見ること**（T-H4a）。
- **第5版で追加する重点エッジケース:** (10) **`$'…'`／`$"…"` の正常経路・degraded の両方**（T-QT1a〜f。**degraded 側を落とすと ` #'` 1つで全形が通るため、T-QT1c が最重要の検出器**）、(11) **印トークンの挿入位置**（T-QT1e。リダイレクト先の判定を壊していないこと）、(12) **QT-1 のゲート前置き**（T-QTOK。保護対象と無関係な `$'…'` を巻き添えにしないこと）、(13) **行継続の両方向**（T-QT2＝新規 deny／T-QT2b＝許容分類8 の allow）、(14) **証拠の符号化**（T-QT3）。

## 5. 影響範囲

| 対象 | 影響の種類 | 対応方針 |
|---|---|---|
| `.claude/hooks/guard_bash_writes.py` | **変更（Phase 12：L1 の行構造の正規化を新設し `lex` の前処理を撤去。degraded のゲート1行。単一ファイル）** | §4-1（定数2件）・§4-2-1（`_skip_balanced`／`_skip_backtick` 新設・`_take_subst`／`_take_backtick` の再実装）・§4-2-2（`normalize_line_continuations` 新設）・§4-2-3（`lex` の分岐2つ追加・`replace` 2つ撤去）・§4-4(h)（ゲート1行）・§4-4(i)（入口の正規化1行）。**公開インターフェース（stdin JSON → deny/allow の hook 契約・deny メッセージ本文）は不変。`lex()` の戻り値の形（`(tokens, substs)`）とトークン種別も不変**（第6版で新しい種別は増えない） |
| **`lex()` の前提（内部契約・第6版で追加）** | **追加**: 引数は `normalize_line_continuations()` を通した文字列であること（LX-5） | 呼び出し元は `find_violation` の1箇所のみ（設計時に全呼び出しを確認）。**V-14(2) の検証手順で `lex()` を直接呼ぶ場合も、必ず正規化を通してから渡す**ことを手順に明記する |
| `.claude/hooks/guard_bash_writes.py`（第5版まで） | 変更済み（Phase 10：L1 字句レイヤ＋言及判定＋ステートメント判定の局所改修） | §4-1（定数5件）・§4-2（`lex`・`ansi_c_decode`）・§4-3（`is_guarded_token`）・§4-4(g)(h)。**公開インターフェース（stdin JSON → deny/allow の hook 契約・deny メッセージ本文）は不変。`lex()` の戻り値の形（`(tokens, substs)`）も不変**で、`tokens` に第3の種別（`ansiq`）が加わるのみ |
| **`lex()` のトークン種別（内部契約）** | **追加（第5版）**: `("ansiq", "$'" \| '$"')` | `split_statements` は種別 `op` 以外を現状どおり文へ含める（変更不要）／`pipe_segments` は **`kind != "w"` を読み飛ばす1行を追加**（印を語として扱わない）／`statement_violation`・`statement_subst_indexes`・`redirect_violation` は既に `kind == "w"` で語を選別しているため**変更不要**。**この「既存の選別が種別名で行われている」性質が、印トークン方式を安全にしている**（設計時に全消費箇所を確認済み） |
| `.claude/hooks/guard_bash_writes.py`（第4版まで） | 変更済み（Phase 8：`awk_violation` 周辺の局所改修） | §4-1・§4-4(c)(d)(h) |
| 内部シンボル | **追加（第4版）**: `awk_strip_literals`・`awk_program_texts`・`awk_program_violation`／**変更（第4版）**: `awk_violation`・`segment_violation` が `degraded` 引数（既定 `False`）を1つ増やす／第3版までの追加（`guarded_write_target`・`is_safe_awk_flag`・`sort_output_operands`・`uniq_output_operand`・`statement_subst_indexes`・`subst_violation`）と削除（`cwd_redirect_violation`）は実施済み | いずれも**既定値付きの追加引数**とし、既存の呼び出し形をそのまま動かす。**既存テストは全てブラックボックス（サブプロセス + stdin JSON）で内部シンボルを import していない**ため、シグネチャ変更によるテスト破壊は発生しない（第2版で全文確認・第3版・第4版でも維持） |
| `tests/test_guard_bash_writes.py` | 変更（追加のみ） | §4-6・§4-7。既存94件は無変更（V-4） |
| Bash ツール実行時の許可判定（全エージェント）（**第6版**） | **変更（deny 方向が主・allow 方向は2クラスのみ）** | **deny 方向**: バックスラッシュ偶数個／`#` コメント末尾の行継続で分断されていた後続コマンド（C7）・ダブルクォート内の置換にコメントを置く形（C7-2）・保護対象 cwd 下で置換の内側が degraded へ落ちる形（C8）・`"…"` 内側の行継続（INV-SH-17）。**allow 方向**: ①**奇数個の行継続で後続コマンドが語へ吸収される形**（bash も実行しない＝§4-9 分類10） ②`'…'` 内側の行継続（bash が触るのは別名＝分類9）。**`\`+改行も語頭 `#` も含まない入力の判定は完全に不変**（正規化が恒等写像になる） |
| Bash ツール実行時の許可判定（全エージェント）（第5版） | **変更（ほぼ deny 方向のみ）** | **deny 方向**: 保護対象に言及する／保護対象 cwd 下のコマンドで `$'…'`／`$"…"` を含む形（新規誤denyは §6 **R29** の4種類に限定し V-12 で上限を固定）・行継続で分断されていた保護対象への書き込み（SH-1）・符号化された保護対象パス（QT-3）。**allow 方向**: QT-2 により連結後の語が保護対象でなくなる形のみ（§4-9 分類8）。**`$'…'`・`$"…"`・行継続を含まない入力の判定は完全に不変** |
| Bash ツール実行時の許可判定（全エージェント）（第4版） | **変更（deny 方向と allow 方向の両方がある）** | **deny 方向**: C5 の各形と、AW-7 の3条件を満たさない awk プログラム全般（新規誤denyは §6 **R26** に列挙し V-12 で上限を固定）。**allow 方向**: M11・M12（AW-5／AW-6 の過剰一致による誤deny2形。**AC2 が解消を求めた側**であり、awk が実行し得ないテキストに対する deny だったため保護の後退ではない＝§4-9 分類7）。**保護対象 cwd 下の読み取り（awk/sed/sort/uniq）と日常の読み取り awk は allow のまま維持する**ことを T-AW7OK・T-H2f・V-12 で固定する |
| `.claude/hooks/README.md`・`CHANGELOG.md` | 変更（Phase 7・M7・M8） | 限界・未対応・件数の整合 |
| `.claude/hooks/_ticket_lib.py`・他hook | 影響なし | `emit_pretooluse_decision` のみ利用（D2） |
| KLK-012 | 競合なし | 対象ファイルが重ならない |
| `.claude/settings.json` | 影響なし | `permissions.allow` と `ALLOWED_HEADS` は別レイヤ |
| `check_spec_structure.py` の許可経路 | 影響なし（維持） | `READONLY_SCRIPTS` 例外と判定順序①→③を維持。**偽装の穴は D7 でスコープ外** |

## 6. リスク

| リスク | 深刻度 | 軽減策 |
|---|---|---|
| **R1: 既存 deny 対象の回帰**（AC6の中核） | high | 既存94件を1行も変更せずに通すことを Phase 6 の完了条件にする（V-4）。T-LEGACY（§4-7）・V-8・**V-9（`rv5.py` に REGRESSION 行が無いこと）**で三重に固定 |
| **R17: AW-4（awk オプションのホワイトリスト）による誤deny** | medium | 収載漏れは「保護対象に言及する awk の当該オプションだけ」が deny になる（他コマンド・他オプションに波及しない）。T-AWKOK で代表形を固定し、**V-10 で実機の `awk --help` と突き合わせる**。mawk/BWK awk 固有オプション（`-W …`）は未収載＝deny になることを docstring に明記。**失敗方向が「可視な誤deny」であることがホワイトリストを選んだ理由**（P4-1） |
| **R19: sed の `strict` 分岐による保守的 deny** | low | 保護対象 cwd 下で sed スクリプトに ` w <非空白>` が現れる形（`sed 's/a w b/c/'`）が deny になる。cwd が `tickets/active\|done` の場合に限られ old=deny のため回帰ではない。docstring に明記 |
| **R20: M5（cwd 接頭辞＋相対パスの分割）の未閉塞** | medium | `cd tickets/active && cd .. && rm active/APP-001.md` は allow のまま（old=deny）。一般解は誤deny爆発を招くため実装しない（reviewer 判定）。§3-8・§3-9・docstring・V-9 の分類4に明記 |
| **R21: `is_readonly_script_call` の許可経路偽装（任意コード実行）** | **high（本改訂後の最大の残存リスク）** | old=new=allow で本チケットの回帰ではないため D7 でスコープ外。**reviewer が5行の統一（`head_args` へ寄せる）で閉じ、正規経路を壊さないことを実測済み**。§10-1 の申し送り筆頭として即時起票を推奨。docstring の「既知の限界」に明記（Phase 7） |
| **R12: `sed -f`／GNU sed `e`／`sort --compress-program`／`git diff --output` の残存** | medium〜high（sed `e` は任意コマンド実行） | old=allow で D6-2／D6-3 によりスコープ外。§3-9 の表に**未対応**として明記し、§10-1 で1つの別チケットへまとめて申し送る |
| **R23: awk のプログラム内構文の次元に機械的な既定が無い** | **第4版で解消** | AW-7（プログラム形のホワイトリスト）が既定を与える。未知の文字・未知の呼び出し名は deny。**この次元で4ラウンド連続して穴が出た原因（規則の積み増し）を構造的に断つ。** 残る前提は R27 として管理する |
| **R26: AW-7 による新規の誤deny（第4版で新設・列挙が完全であることが V-12 の合否）** | **medium** | **保護対象に言及する／保護対象を cwd とする awk に限り**、次の読み取り形が新たに deny になる。**この列挙外の新規 deny が1件でも出たらブロッカー（V-12(d)）。** ① `@namespace` 指令（`@` が許可文字集合に無い） ② `AWK_SAFE_CALLS` 未収載の組み込み関数・拡張関数の呼び出し（gawk の新しい関数を含む） ③ `\|` を伴う形（`print \| "cmd"`・`"cmd" \| getline`。**いずれも実際に外部コマンドを起動するため意図どおり**） ④ プログラム本文にバックスラッシュ・バッククォート・シングルクォート・非ASCII文字が**リテラル外**に現れる形 ⑤ 未知の関数名に見える連結（`{print a (b)}` のような `IDENT (` 形の文字列連結） ⑥ プログラムが `$(...)`／バッククォート由来で、置換の内側テキストがシェル引用符を含む形（expanded 変体に対する評価） ⑦ 未閉じの文字列リテラルを含むプログラム ⑧ コメント（`#` 以降）に `@`・`\|` を含む形（L1 が改行を `;` へ正規化するためコメント範囲を確定できず、除去しない）。**軽減策**: いずれも「可視な誤deny」であり deny 理由文が原因（許可されない文字／関数名）を具体的に示す。docstring の「既知の限界」へ全件記載し、回避方法（`cat`／`grep` の単独使用・保護対象パスを引数に取らない形）を案内する |
| **R27: AW-7 の健全性が4つの前提に依存する（S1〜S4）** | **medium** | S1（1パスのリテラル除去が awk の字句規則と一致）・S2（許可文字集合に危険文字が残っていない）・S3（`AWK_SAFE_CALLS` に副作用のある関数が無い）・S4（出力リダイレクトは `print`/`printf` の後にしか現れない）。**破れると allow 方向（危険）**。軽減策は **V-11 の実機検証**（S1 は2パス実装なら allow に転じる4形を T-AW7h として**テストで固定**し、実装が設計どおりであることを機械的に検出する）・**S3 は man の組み込み関数一覧との突き合わせ**・`INVENTORY_CASES` による継続的な固定 |
| **R28: degraded mode は依然ブラックリスト（AW-5／AW-6）** | low〜medium | 字句解析に失敗した入力では AW-7 を適用できない（プログラム本文の同一性が失われるため）。`\|` 系は `statement.split("\|")` によるパイプ分割と非ホワイトリスト head（`getline`・`&`）で捕捉され、`system (`・`@ f(` は空白結合テキストで捕捉されるが、**この次元に機械的既定は無い**。degraded の awk は旧版で常に deny だったため保護の後退ではない。軽減策は §3-5 の明文化と docstring への記載 |
| **R25: sed のプログラム内構文次元がブラックリストのまま** | medium〜high | `w`／`W` は対応済みだが、`-f SCRIPT`／GNU sed の `e` フラグ・`e` コマンド（**任意コマンド実行**）は未対応（old=allow・D6-3 でスコープ外）。awk と同じホワイトリスト化は技術的に可能だが `sed` の `ALLOWED_HEADS` 収載可否という上位判断を含む。**`INVENTORY_CASES` の INV-SED-05〜07 に `allow` として登録し、穴をコード上で可視にする**。§10-1 申し送り2（R12 と統合）で別チケットを推奨 |
| **R24: AW-6 による誤deny（第3版の実装・第4版で縮小）** | low | 間接呼び出し構文 `@f(...)` を読み取り用途で使う awk が deny になる（実用上ほぼ無い＝gawk 4.1.2+ 限定の非移植機能・その規模のスクリプトは `-f` で与えるのが通常で AW-4 が既に deny・呼び先が組み込み `system()` かユーザー定義関数かは静的に判別不能）。**第4版では正常経路の判定が AW-7 へ移り、リテラル内・オペランド内の過剰一致（M11・M12）は解消される**。degraded 経路にのみ残る |
| **R22: 置換再帰の再構成による評価順序の変化** | low | allow/deny の結果は不変（両方を評価する）。変わるのは deny 理由の選ばれ方のみで、既存テストは理由文字列を assert していない。深さ上限の境界は「親の depth」判定のため不変（§4-4(i)） |
| **R18: `CWD_SAFE_HEADS` が現状到達不能** | low | 現行の `ALLOWED_HEADS` 全 head を収載済みのため、この規則は今は deny を生まない（誤denyリスクゼロ）。目的は将来の追加時のフォールセーフ既定であり、`op_kind` の到達不能な既定と同じ性質（第2版で reviewer が改善と評価した先例に倣う）。ブラックボックステストでは観測できないため、意図を docstring と §3-8 に明記する |
| R10: プレースホルダ解決による誤denyの増加 | low | 第2版から変更なし。解決は証拠の追加にのみ作用し、旧版と同等の広さに戻るだけ（T-C1g で固定） |
| R11: awk AW-2／AW-3 による読み取り awk の誤deny | medium | AW-3 は文字列リテラル除去後の `print`/`printf` 出力構文だけを見る。cwd ゲートを追加しても読み取り形が allow のままであることを **T-H2f** と V-9（`rv3.py`）で固定 |
| R13: degraded mode の誤deny（旧版と同一） | low | 発火条件5系統に限られ判定内容は旧版と同一。degraded の空白分割により AW-4 が保守的に発火する形（プログラム内の `-$2}` 等）があるが、degraded の awk は旧版で常に deny だったため回帰ではない |
| R14: cwd 追跡による誤deny | low | 追跡は `cd` オペランドがリテラルの場合に限り、非リテラルなら打ち切り（allow 方向）。書き込み先オペランドの解決は「構文的に書き込み先と確定した語」に限る（§3-4） |
| R15: プレースホルダ文字列の偽装 | low | 解決は証拠を増やす方向にのみ作用。インデックスが範囲外なら解決しない。investigator 実測で抜け道なし |
| **R4: `$'...'`（ANSI-C 引用）の扱い — 第2版〜第4版の記述は誤りであった（第5版で訂正）** | **第5版で解消（残るのは R29・R30）** | **誤っていた記述:** 「`$'...'` は shlex が bash と同一には解釈しないが、**語として1トークンに収まるため判定は保守側（deny 方向）へ倒れる**」。**実態:** bash は展開時に `\xHH` 等を**デコード**するため、hook が見る「1つの巨大な文字列リテラル」が実機では**文字列の外にある実行コード**になる＝**allow 方向へ倒れ、実機で実行される**（C6・tester が gawk で実証）。**この前提は一度も実測で裏付けられておらず、唯一の関連テストはエスケープを含まない形しか検証していなかった**（原則 P6 の新設理由）。**対処:** QT-1（判定不能→deny）＋QT-3（証拠の復号）。**docstring・README・設計書の当該記述を Phase 11 で全て訂正する**（V-6 の合否条件） |
| **R29: QT-1／QT-3 による新規の誤deny（第5版で新設・列挙が完全であることが V-12 の合否）** | **low〜medium** | **保護対象に言及する／保護対象を cwd とするコマンドに限り**、次が新たに deny になる。**この列挙外の新規 deny が1件でも出たらブロッカー（V-12(c)）。** ① **クォート外に `$'…'` を含む形**（`awk -F$'\t' '{print $1}' T`・`grep $'\t' T`・`sed $'s/\t/ /' T`）。**回避策が確実に存在する**: `awk -F'\t'`（awk 自身がエスケープを解釈する）・`grep -P '\t'`・リテラルのタブを書く。deny 理由文に回避策を載せる ② 同 `$"…"` を含む形（ロケール翻訳。実運用での使用例は無い） ③ **degraded 経路で、コマンド文字列に `$'`／`$"` の2文字並びが引用符の内側に現れる形**（`awk '{print "cost: $"}' T #'`）。degraded はクォート状態を追えないため部分文字列判定＝保守側。**degraded の awk は旧版で常に deny だったため保護の後退ではない** ④ **QT-3 の復号により保護対象パスが現れる語を含む形**（`\x74ickets/…` のような字面を実ファイル名として扱っている場合のみ）。**軽減策:** ①〜④はいずれも「可視な誤deny」であり deny 理由文が原因と回避策を示す。**Kit 自身のコマンド・スキル・スクリプトに `$'` の使用は1件も無い**（リポジトリ全体の grep 実測）。docstring の「既知の限界（保守側へ倒している判定）」へ全件記載する |
| **R30: `$"…"`（ロケール翻訳）は原理的にデコードできない** | low | メッセージカタログの内容は hook から見えないため、`$"…"` の展開結果を知る方法は無い。**QT-1 の「判定不能→deny」だけが対処であり、証拠の復号（QT-3）は効かない。** 逆に言えば、`$"…"` を含むコマンドが保護対象に言及しないときは allow のままである（証拠が無い＝ゲートが偽）。これは本 hook の証拠ベースの設計に由来する限界であり、docstring に明記する |
| **R31: ブレース展開・glob・パラメータ展開による保護対象パスの分断（第5版で新規に列挙・未対応）** | **medium** | `rm tickets/{active,done}/APP-001.md`・`rm tickets/acti*e/APP-001.md`・`f=…; rm $f` は **old=allow の既存穴**であり、本チケットでは閉じない（D6-2・§3-7 の却下理由）。`PATHISH_RE` へ `{},*` を単純に加えると `awk '{print}'` のようなプログラム語が保護対象と誤判定されうるため、正しく閉じるにはブレース展開の実装（入れ子・直積）が必要。**軽減策:** `INVENTORY_CASES` へ INV-SH-07・09・11 として **`allow` で登録し、穴をコード上で可視にする**（塞いだ時点で `deny` へ変える意識的な行為が必要になる）。docstring の「既知の限界」と §10-1 申し送り9 に明記し、別チケットを推奨する。**なお `tickets/active/*.md` のように保護対象ディレクトリが素で残る形は従来どおり deny される**（INV-SH-08） |
| **R32: 行継続の近似が bash の字句規則と一致しなければ両方向へ倒れる（第6版で新設・M14 の訂正）** | **high（本改訂の中核）** | **第5版の「連結は部分文字列としての証拠を失わせないため、証拠は増える方向にしか動かない」という主張は誤りであった。** 連結しすぎれば**ステートメント区切り**という証拠を失い（C7・head が前の語へ吸収されて `ALLOWED_HEADS` 判定が無効化される）、連結しなさすぎれば**語の同一性**という証拠を失う（SH-1）。**「安全側の近似」は存在しない。** 軽減策: (1) 近似を `normalize_line_continuations()` の**1箇所**へ集約し、分岐条件を §3-2-1 LX-2 として明文化する (2) 偶奇の判定をカウンタではなく**対消費の自然な帰結**にして数え間違いの故障モードを構造的に消す (3) **両方向のテストを対で置く**（T-C7a／b＝deny・T-C7c＝allow） (4) **V-14(7) で bash の実行効果と hook の判定を両方向で突き合わせる** (5) 設計書・docstring・README から「証拠は増える方向のみ」という記述を**全て削除する**ことを V-6 の合否条件にする |
| **R33: 第6版による新規の誤deny（列挙が完全であることが V-12 の合否）** | **low〜medium** | **次の4種類に限られる。列挙外の新規 deny が1件でも出たらブロッカー（V-12(c)）。** ① **`"…"` の内側の行継続**で連結された結果、保護対象に見える語ができる形（bash も同じ語を作るため誤denyではないが、第5版比では新規 deny＝INV-SH-17） ② **保護対象 cwd 下で degraded へ落ちた置換の内側**が `CWD_SAFE_HEADS` 未収載 head を含む形（現行の `ALLOWED_HEADS` は全収載のため到達しない） ③ **保護対象 cwd 下で degraded へ落ちた置換の内側**に `$'`／`$"` の2文字並びが含まれる形（C8 のゲート拡張 × QT-1 の degraded 判定の合成。R29 ③ の cwd 版） ④ **保護対象 cwd 下で degraded へ落ちた置換の内側**に `<<` が含まれる形（heredoc 判定が関数レベルゲートの後段にあるため）。**軽減策:** いずれも「保護対象ディレクトリを cwd とし、かつ置換の内側に未閉じクォートを置いた」極めて限定的な入力に限る。②〜④は**正常経路（degraded でない形）が既に同じ判定を行っており、degraded との非対称を解消する方向**である |
| **R34: `'…'` 内側の行継続を連結しなくなったことによる deny → allow（第6版）** | low | 第5版は「`'…'` 内側では bash より強く連結する（証拠が増える方向）」として意図的に連結していたが、**bash は連結せず、実際に触るファイルは「改行を含む別名」であって保護対象ではない**。したがって連結は**証拠の捏造**（誤deny）であった。**old=allow のため AC6 の回帰ではない**（§4-9 許容分類9・INV-SH-16 で `allow` として固定）。**万一この形で実害が判明した場合は、`'…'` 内側でも連結する1行を戻せば復元できる**（`normalize_line_continuations` の `sq` 分岐） |
| **R35: `#` コメント本文が `lex()`／degraded で除去されない（第6版で明記・未対応）** | medium | 第6版は `#` を**正規化走査の中だけ**でモデル化し（行継続の適用可否に限る）、**コメント本文の除去は行わない**。したがって `lex()` はコメント本文を語として走査し続ける。**相違の向きは deny 側（保守的）**であり穴ではないが、次の2つの副作用が残る: (a) コメント内の `> path`・`rm` 等が証拠として拾われ誤denyになりうる（**old も同じ挙動のため回帰ではない**） (b) **コメント内の未閉じクォート（`#'` 等）が degraded 経路を誘発する**性質は残る（C2・C8 の入口）。**除去は deny → allow 方向の変化を生む**（`cat a #> docs/SPEC.md` が allow になる＝old=deny）ため本改訂では行わず、§3-9-2 の `#` 行・docstring の「既知の限界」・§10-1 の申し送りへ記載して別チケットを推奨する |
| R6／R7／R9 | low | 第2版から変更なし（export 変数経由は検出不能・SPEC.md にはバックストップ無し／深さ上限3・非再帰の1パス置換／worktree の変更はマージまで実行時判定に反映されない） |

## 7. マイグレーション戦略

データ・API・永続状態の移行は**なし**（hook は毎回プロセス起動で状態を持たない）。運用上の注意のみ:

1. **有効化タイミング:** `.claude/settings.json` が参照するのは `${CLAUDE_PROJECT_DIR}/.claude/hooks/guard_bash_writes.py` であり、**worktree 内の変更は develop へマージされるまで実行時の判定に反映されない**。
2. **後方互換:** hook の入出力契約（PreToolUse の stdin JSON・`hookSpecificOutput.permissionDecision`・exit 0）と deny メッセージの**本文（利用者への案内文）**は変更しない。理由文字列に新規4種（awk のオプション／`system()`・`@load`・`@include`／`sort -o`／`uniq` 第2引数／保護対象 cwd 下の未収載 head）が加わるが、既存テストは理由文字列を assert していない。
3. **段階的移行（第6版）:** **Phase 12 が判定集合を変える唯一の Phase である。** 変化は (a) allow → deny（バックスラッシュ偶数個／`#` コメント末尾の行継続で分断されていた後続コマンド＝C7・ダブルクォート内の置換にコメントを置く形＝C7-2・保護対象 cwd 下で置換の内側が degraded へ落ちる形＝C8・`"…"` 内側の行継続＝INV-SH-17。**新規誤denyは R33 に列挙した範囲**） (b) deny → allow（**2クラスのみ**＝§4-9 分類9（`'…'` 内側の行継続）と分類10（奇数個の行継続で後続コマンドが語へ吸収される形。**bash も実行しないため allow が正しい**））。**(b) がこの2クラス以外に広がっていないことを V-8／V-9 で確認する。** Phase 13 はテストと文書のみ。
4. **（履歴）第5版の段階的移行:** **Phase 10 が判定集合を変える唯一の Phase であった。** 変化は (a) allow → deny（`$'…'`／`$"…"` を含み保護対象に触れる形＝**R29 に列挙した範囲**・行継続で分断されていた書き込み・符号化された保護対象パス） (b) deny → allow（**QT-2 に伴う「連結後の語が保護対象でなくなる」形のみ**＝§4-9 分類8）。**(b) がこの1クラス以外に広がっていないことを V-8／V-9 で確認する。** Phase 11 はテストと文書のみ。
5. **（履歴）第4版の段階的移行:** Phase 8 は判定集合を変える唯一の Phase であった。**第4版の変化は2方向ある** — (a) allow → deny（C5 と AW-7 の3条件を満たさない awk プログラム。**R26 に列挙した範囲**） (b) deny → allow（**M11・M12 の2形のみ**。awk が実行し得ないテキストに対する誤denyであり、AC2 が解消を求めた側＝§4-9 分類7）。**(b) が M11・M12 以外に広がっていないことを V-8／V-9 で確認する。** Phase 9 はテストと文書のみ。
6. **既存ブランチの扱い:** Phase 1〜11 のコミットは保持し、その上に Phase 12・13 を積む。**ブランチの作り直し・rebase は行わない**（Phase 1〜11 は tester／reviewer が C1／C2／H1／C3／C4／H2／`@f()`／C5／C6 の閉塞を再実測済みで、破棄する理由がない）。
7. **リトライ枠の制約（運用上の注意）:** implementer は V-11・V-12・**V-14（新しいサブケース込み）** を**実装メモへ実測値つきで記載**し、設計どおりに実装できない箇所を見つけた場合は**自己判断で逸脱せず** orchestrator へ報告すること（第2版の `OPERATORS` 並び順・第3版の `words[1:]`・第5版の C7／C8 のように、**設計の疑似コードに誤りがある可能性を前提とする**）。**第6版で落とすと穴が残る／既存 deny が回帰する要所は次の4点であり、それぞれに検出器がある:**
   - **`lex()` の `\`+改行分岐を汎用エスケープ分岐より前に置くこと**（後ろだと対消費されて区切りが消える）→ **T-C7b**
   - **`find_violation` の入口で正規化すること（`lex()` の中でやらないこと）**（`lex` の中だと degraded が生の文字列を見続ける）→ **T-H4a**
   - **`normalize_line_continuations` が置換の内側を読み飛ばすこと**（内側まで正規化すると C7-2 が開く）→ **T-C7e**
   - **`degraded_violation` の関数レベルゲートに `cwd_is_guarded` を入れること** → **T-C8a**（6形すべてが失敗する）
   - **過剰deny側へ倒していないこと** → **T-C7c／T-C7d(b)／T-C7g／T-C8b**（allow を要求するテスト。**これらを緩めたくなった時点で設計の欠陥であり、テストを変えずに orchestrator へ報告すること**）

## 8. ロールバック戦略（第2版の実測に基づく。第3版で M1 無効化手順の記述を訂正）

**Phase 粒度の `git revert` は成立しない**（実測: Phase 1 は本体・テストの両方で CONFLICT、Phase 2 はテストで CONFLICT、Phase 3 のみ CLEAN）。Phase 4・6 は Phase 1・2 が書いた関数を直接書き換えるため、この性質はさらに強まる。**ロールバックの単位は「ブランチ全体」または「マージコミット」とする。**

```bash
# (a) 未マージ段階での全体破棄（orchestrator が実施。develop は無傷）
git worktree remove ../kenesis-loop-kit.wt/KLK-010
git branch -D fix/KLK-010-guard-bash-writes-shlex

# (b) develop へマージ後の取り消し（hook ファイル全体が旧版へ戻る）
git revert -m 1 {マージコミットのhash}

# (c) done 化後に問題が発覚した場合
# /rollback KLK-010   （done → implementation_done）
```

**規則単位で戻したい場合（誤denyが業務を止めている緊急時の一時措置）:** `git revert` ではなく**順方向の1点修正**を作る。

| 無効化したい規則 | 1点修正 |
|---|---|
| C1（置換の外側方向） | `expand_substs` を恒等関数（`return text`）にする |
| C2（degraded の分割復元） | `degraded_violation` を旧実装（平坦リスト）へ差し戻す |
| H1（awk の束縛解析） | `segment_violation` の `awk_violation` 呼び出しを削る |
| **M1・H2（cwd に関わる判定すべて）** | **`cwd_is_guarded` を `return False` にする。**（第2版 §8 は「`cwd_is_guarded(cwd)` の実引数を `False` 固定にする」と書いていたが、実装は cwd **文字列**を渡すため誤り。cwd 依存の判定＝言及ゲートの強化・`guarded_write_target` の cwd 分岐・AW-3 の cwd ゲート・sed の `strict`・`CWD_SAFE_HEADS` 判定・置換再帰の cwd 伝播は**すべて `cwd_is_guarded` を経由する**ため、この1関数が単一の無効化点になる） |
| **C3（awk のオプション判定）** | `is_safe_awk_flag` を `return True` にする |
| **C4（awk の実行構文判定）** | `awk_violation` の `AWK_EXEC_RE` 分岐を削る |
| **D4 改訂分（sort / uniq）** | `sort_output_operands` / `uniq_output_operand` を空リスト・空文字列を返す形にする |
| **AW-6（間接呼び出しの検出・degraded 専用）** | `awk_violation` の `AWK_INDIRECT_CALL_RE` 分岐（語単位と結合テキストの2箇所）を削る |
| **QT-1（`$'…'`／`$"…"` の判定不能扱い・第5版）** | **`statement_violation` の ③' 分岐（`ANSI_QUOTE_KIND` の判定）と `degraded_violation` の `ANSI_QUOTE_RE` 判定を削る**（無効化点は**2箇所**。degraded 側を残したまま正常経路だけ消しても、正常経路の穴がそのまま開く）。**無効化すると C6 の5形・C6-2 が再び allow になる**ため、`LEGACY_DENY_COMMANDS` の該当7件と T-QT1 群が失敗する（保護の後退が可視化される）。**第4版 §8 が定めた「`awk_program_violation` の先頭に `return None`」は C6 に対しては方向が逆（緩める側）であり、この穴の無効化点にはならない**（tester の指摘どおり） |
| **QT-3（証拠の復号）・第5版** | `is_guarded_token` の復号分岐を削る。**単独で無効化でき、他の規則に影響しない** |
| **LX-1〜LX-5（行継続とコメントの正規化・第6版）** | **`find_violation` の入口の `command = normalize_line_continuations(command)` を削る**（単一の無効化点）。ただし**削るだけでは第4版の挙動（語が空白で割れる＝SH-1）にも第5版の挙動（無条件連結＝C7）にも戻らず、「行継続を一切処理しない」状態**になる（`lex()` は `\`+改行を汎用エスケープとして対消費し、`\n` を区切りにする＝**旧版に近い保守側**）。**無効化すると T-QT2／INV-SH-06／T-C7c／T-C7d(a)／INV-SH-17 が失敗する**（保護の後退と過剰denyの両方が可視化される）。**部分的に緩めたい場合は、規則を消さず `normalize_line_continuations` の分岐（`sq` を連結する等）を明示的に変更し、対応する `INVENTORY_CASES` の期待値を同時に更新する** |
| **C8（degraded の関数レベルゲート・第6版）** | ゲートを `if not mentions_guarded(command.split()): return None` へ戻す。**無効化すると `LEGACY_DENY_COMMANDS` の C8 5件と T-C8a が失敗する**（保護の後退が可視化される）。**他の規則に影響しない1行** |
| **H4（正規化の受け渡し・第6版）** | `degraded_violation(command, cwd)` の呼び出しを `degraded_violation(元の command, cwd)` へ戻す。**戻すと M13 の「偶発的な安全網」に依存する状態へ回帰する**ため、単独で戻してはならない（LX-1〜LX-5 の無効化と同時に行うこと） |
| **AW-7（プログラム形のホワイトリスト・第4版）** | **`awk_program_violation` の先頭に `return None` を置く**（単一の無効化点）。**無効化すると C5（`"cmd" \| getline`・`\|& getline`）と `system (`・未知関数呼び出しが正常経路で再び allow になる**ため、対応する `LEGACY_DENY_COMMANDS` の11件と T-AW7 群が失敗する（保護の後退が可視化される）。**部分的に緩めたい場合は `AWK_SAFE_PROGRAM_CHARS`／`AWK_SAFE_CALLS` へ収載を追加する**（規則を消すのではなく許可を増やす方向で対処する） |

いずれも一時措置であり、実施したら**必ず人間へ報告し、対応する `LEGACY_DENY_COMMANDS` のテストが失敗する**ことを確認する（失敗が保護の後退を可視化する）。

**緊急時の即時回避:** hook 自体が誤denyでループを止める事態になった場合は、`.claude/settings.json` の `hooks.PreToolUse` から Bash マッチャのエントリを一時的に外す（README「注意」の既定手順）。その間チケット宛ての Bash 書き込みは Stop hook のドリフト検知のみが防衛線になり、**`docs/SPEC.md` 宛ては無防備になる**ため、暫定措置に留め人間へ報告する。

## 9. 受け入れ条件

チケットの受け入れ条件（人間が承認した契約）を弱めない範囲で、設計の観点から検証可能な形に具体化する。**第6版でもチケット本文の AC1〜AC7 は1文字も変更していない。** 第4版で AC3 の elaboration を deny 方向へ強めた（§0-2）ほかは、**第5版・第6版で新たに elaboration を変更した箇所は無い**（QT-1〜QT-3・LX-1〜LX-5・C8 の修正はいずれも AC6「既存の deny 対象を回帰させない」の履行であり、AC の言い換えを要しない）。**弱めた箇所は無い。**

- [ ] **AC1**: コマンド解析が L1（クォート走査で演算子を切り出し、語は `shlex.split(chunk, comments=False, posix=True)` でクォート解除）へ置き換わっている。`STATEMENT_RE` による文字列分割・`statement.split("|")`・`"<<" in command` が**正常経路**から消えている（degraded 専用の `LEGACY_*` としてのみ残る）。**第3版の追加規則はすべて L3 内であり L1 を変更しない。** クォート内の `|` `<<` `>` `#` がステートメント分割・特殊構文判定を引き起こさないことがテストで固定されている
- [ ] **AC2**: 次の5コマンドがすべて allow される — A-1 `grep -E '^(id|title):' tickets/active/APP-001.md`／A-2 `cd tickets/active && ls`／A-3a `awk -F'|' '{print $1}' tickets/active/APP-001.md`／A-3b `grep -c '<<EOF' tickets/active/APP-001.md`／A-3c `cat tickets/active/APP-001.md && echo x > "$TMP"`。**加えて、第3版・第4版で追加した規則が読み取り操作を新たに誤denyしていない** — 特に **T-H2f**（保護対象 cwd 下の awk／sed／sort／uniq の読み取り）・**T-AWKOK**（awk の安全オプション）・**T-AW7OK（第4版・日常の読み取り awk 12形）**・**AW-6 の `@` を含む読み取り8形**と、**V-9 で `rv3.py` の新版 deny 件数が1件のまま増えていないこと**。**加えて第4版は M11（`print "contact: a@b (see docs)"`）・M12（ファイル名 `'system(1).md'`）の誤deny2件を解消する**（tester が AC2 の解消対象として報告）。**新規に deny になる読み取り形が §6 R26 の列挙内に収まること（V-12）**
- [ ] **AC3**: `ALLOWED_HEADS` に `cd`・`pwd`・`awk`・`cut`・`tr`・`echo` が含まれる。**`cd` を先頭とするセグメントは常に allow される**（cwd 追跡は後続ステートメントの言及ゲートと書き込み先オペランドの解決にのみ影響し、`cd` 自体を deny しない）。`awk`・`sed`・`sort`・`uniq` も `ALLOWED_HEADS` に残る。**第4版で awk の elaboration を deny 方向へ改める（透明性のための明記）** — 第3版までの「書き込み形のみを deny する」から「**安全と確認できるプログラム形以外を deny する**（保護対象に言及する／保護対象を cwd とする場合に限る）」へ。チケット本文の AC3 が要求するのは `awk` の `ALLOWED_HEADS` 収載であり、それは維持されている。人間が 2026-08-14 18:42 に選択した方針（§3-6 D8）に基づく変更であり、代償の上限は V-12 で固定する
- [ ] **AC4**: 次がすべて deny される — B-4a `sed --in-place=.bak …`／B-4b `sed -ni …`／B-4c `sed -i'' …`／B-6a `ls $(rm …)` とバッククォート版／B-6b `rm ./tickets/active/../../.claude/../tickets/active/APP-001.md`。**加えて置換の外側方向（T-C1a〜T-C1h）と保護対象 cwd 下の置換内書き込み（T-H2d）も deny される**
- [ ] **AC5**: `python3 -c "open('docs/SPEC.md','w').write('x')"` が deny される。パス検出が空白トークン分割に依存しない。かつ `MYSPEC.md`・`docs/SPEC.md.bak` で誤denyが起きない
- [ ] **AC6**: 既存テストが**1行も変更されずに**全パスする（V-4。**tester がコミット済みの4件を含む**）。**加えて `LEGACY_DENY_COMMANDS`（§4-7 の追記後）の全件が deny される（V-7）**。**加えて `INVENTORY_CASES` の全件が期待どおりに判定される（V-13）**。**加えて old/new 比較（V-8・V-9）の WEAKER 行が §4-9 の**7分類**すべてに収まる**（分類7＝M11・M12 の2形。プログラム**本文**に危険様パターンが現れる形が allow へ転じたら分類外＝ブロッカー）。特に次の経路が deny のまま維持される／新たに deny される — 素の `sed -i`・リダイレクト `>` `>>`・`tee`・`find -exec`・heredoc・`git checkout`／`rm`／`restore`・`test_redirect_to_variable_deny`・`READONLY_SCRIPTS` 例外とリダイレクト併用・awk 内蔵リダイレクト（変数代入形を含む）・**awk のオプション経由の in-place 書き込みと外部コード読み込み（C3）**・**awk の `system()`／`@load`／`@include`（C4）**・**awk の間接関数呼び出し `@f(...)`（AW-6）**・**awk の `"cmd" \| getline`／`\|& getline`（C5・第4版）**・コマンド置換（外側方向・リダイレクト先・**cwd 伝播**を含む）・degraded mode（複数ステートメント／パイプを含む）・**cwd が保護対象配下と解決できる形の相対パス書き込み（H2 の11形）**
- [ ] **AC7**: `tests/test_guard_bash_writes.py` に §4-6 のケースが追加され、`python3 -m unittest discover -s tests` が **Phase 12 着手時点の実測件数（reviewer 3回目時点で 405件・全パス）＋追加分**で全パスする（V-1・V-2。件数は実測値で報告し設計書に固定しない＝D3）

**第6版で AC へ加わる検証点（elaboration の追加ではなく、既存 AC の履行の具体化）:**

- **AC1 の履行:** L1 が bash の**行構造**（どこが区切りでどこが継続か）と一致する。AC1 が名指しする `#` について、**クォート外の語頭 `#` だけ**をコメントとして扱い（LX-3）、**クォート内の `#` では一切の特殊構文判定を行わない**ことを T-C7f／T-C7g／INV-SH-20 で固定する。**V-14 が §3-9-2-1 の全サブケースについて「bash の argv・実行効果」と「hook の語トークン・判定」を突き合わせ、相違がある形はすべて規則化されているか限界として登録されていることを確認する。**
- **AC2 の履行:** A-1〜A-3c は `\`+改行も語頭 `#` も含まないため、**正規化が恒等写像になり判定は完全に不変**。新規誤denyは **R33 の4種類**に限定し、V-12 で「列挙外があればブロッカー」とする。**加えて「過剰deny側へ倒れていないこと」を T-C7c／T-C7d(b)／T-C7g／T-C8b で機械的に固定する**（reviewer が明示した二方向要求）。
- **AC6 の履行:** C7 の9形（C7-2 を含む）・C8 の5形が deny になり、`LEGACY_DENY_COMMANDS` の全件（**第6版で14件追加**）が deny のまま維持される。**`INVENTORY_CASES` は 48件**（INV-SH 8件を追加）。V-8／V-9 の WEAKER 行は**分類1〜10**に収まること（**分類9・10 は第6版で追加した2クラスのみ**）。
- **P2 の履行（SV-14 への回答）:** 正常経路が旧版から落としている判定要素は §3-1 P2 に列挙した**4点のみ**であり、そのうち①（無条件の改行分割）は**第6版で bash 一致の形へ回復した**。②③④は AC1・AC2 が明示的に要求した緩和であり §9 で申告済みである。**申告していない欠落は無い**（第6版で全件棚卸し）。

**（履歴）第5版で AC へ加わった検証点:**

- **AC1 の履行:** L1 が bash の字句解析へ**近づく**（QT-2）。**近づけられないことが確定した入力クラス（`$'…'`／`$"…"`）は「判定不能」として扱い、シェルの字句解析を尊重できないまま allow することをやめる**（QT-1）。**V-14 が「hook の語トークン」と「bash の argv」を突き合わせ、相違がある形はすべて規則化されているか限界として登録されていることを確認する。**
- **AC2 の履行:** A-1〜A-3c は `$'…'`・`$"…"`・行継続のいずれも含まないため**判定は完全に不変**（§4-5-4）。新規誤denyは **R29 の4種類**に限定し、V-12 で「列挙外があればブロッカー」とする。
- **AC6 の履行:** C6 の5形・C6-2・SH-1 が deny になり、`LEGACY_DENY_COMMANDS` の全件（第5版で10件追加）が deny のまま維持される。**`INVENTORY_CASES` は 40件**（INV-SH 12件を追加）。V-8／V-9 の WEAKER 行は**分類1〜8**に収まること（**分類8 は第5版で追加した QT-2 由来の1クラスのみ**）。

## 10. Open Questions

**なし。** 第6版で判断を要した事項はすべて設計判断として決着させた。**受け入れ条件（AC1〜AC7）の改訂を要する事項は発生していない。**

**第6版の決着事項（reviewer 3回目の指摘への採否）:**

- **C7 → reviewer の修正案を採用し、実装形を強化して決着。** reviewer 案（① バックスラッシュ連の偶奇を数え奇数個で終わる形だけ結合 ② クォート外の `#` 以降は行末まで行継続を適用しない）は**方向・条件ともに正しい**。本設計は次の3点を加える: (a) **偶奇をカウンタで数えず、走査の対消費の自然な帰結にする**（数え間違いという故障モードを消す） (b) **`'…'` の内側では連結しない・`"…"` の内側では連結する**という第3・第4の分岐条件を明示する（reviewer 案は2条件のみ） (c) **置換の内側は正規化せず再帰へ委ねる**（LX-4。reviewer 案のままだとダブルクォート内のコマンド置換にコメントを置く形＝**C7-2** が残る）。**二方向要求（奇数形は allow のまま維持）は §3-9-2-1 INV-SH-15・§4-5-5(E)・T-C7c で明文化・固定した。**
- **C8 → reviewer の修正案（1行）をそのまま採用。** あわせて**同関数の5つのゲートすべてを cwd 観点で棚卸し**し、cwd を見ないゲートが他に無いことを設計へ明記した（同じ見落としを4度目にしないため）。**`_take_backtick` がクォート状態を追わないのは bash と同じ仕様であり、修正すべきはゲートの側である**（`_take_backtick` は変更しない）。
- **H4 → reviewer の構造的な解をそのまま採用。** 正規化を `find_violation` の入口で1回だけ行い、正規化後の文字列を `lex()` と `degraded_violation()` の双方へ渡す。**M13 が同時に解消され、当該入力は意図された理由（QT-1）で deny になる。**
- **H3／M17 → 原則 P7 を新設して決着。** 「表の行が現実の構文空間を尽くしているか」は P5 では保証されない。**各行に『bash の挙動が分岐する条件』を明示列挙させ、分岐ごとに INV ID と期待判定を持たせる**規律を §3-1 P7 として設け、§3-9-2 の「行継続」行を6サブケースへ分解し、**`#` コメントの行を新設**した（§3-9-2-1）。V-14(5) の対象最小セットを更新し、**両方向の実測を行う V-14(7) を追加**した。
- **M13 → reviewer の判定（許容しない）に従う。** tester の回帰ガードは**理由文字列を assert していない現在の形のまま残す**（1行も変更しない）。
- **M14 → 誤りとして訂正。** 「証拠は増える方向のみ」を設計書・docstring から**全て削除**し、正しい主張（**両方向へ倒れる**）へ置き換える。**V-6 の合否条件**とし、§6 **R32** として新規リスクに登録した。
- **M15 → §3-9-2 に `#` コメントの行を新設し、§6 R35 として限界を登録。** `#` は**正規化走査の中だけ**でモデル化し、本文の除去は行わない（除去は deny → allow 方向の変化を生む）。
- **M18 → 2つの `replace` を撤去し、行構造の近似を1関数へ集約。** あわせて `_take_subst`／`_take_backtick` の終端探索を `_skip_balanced`／`_skip_backtick` へ切り出し、**正規化走査と `lex` が同一の規則で置換領域を決める**ようにした（2箇所が別々に判断すると C7-2 と同種の故障が起きる）。
- **M16（reviewer が人間への明示報告を求めた件）→ §10-1 の申し送り筆頭として維持し、承認時の報告事項に格上げ。** **R21（`is_readonly_script_call` の許可経路偽装＝任意コード実行）は本改訂後の最大の残存リスクであり、`docs/SPEC.md` にバックストップが無いことと組み合わさる。orchestrator は本チケットの承認時に、人間へ「別チケットの即時起票」を明示的に報告すること。**
- **和集合方式（曖昧な行継続を「連結／非連結」の両解釈で評価し、いずれかが deny なら deny にする）→ 却下。** 曖昧さの無い分岐（偶奇）にまで適用すると**奇数形まで deny になり二方向要求に反する**（§3-7）。

**（履歴）第5版の決着事項:**

- **C6（`$'...'` の hex エスケープによる AW-7 全面バイパス）→ QT-1（判定不能→無条件 deny）で決着**（§3-6 D9）。**人間が選択した「degraded 相当へのフォールバック」は、語単位のトレースで C5 再開通形を閉じないことが確定したため、決定の目的と選定理由 (a)〜(d) を満たすより強い形へ置き換えた**（下の「人間への報告事項」を参照）。
- **`awk_strip_literals` のエスケープ処理（tester 第2案）→ 変更しない**（§3-6 D10）。`awk_strip_literals` は awk の字句規則と一致しており、相違は bash 側のデコードにのみ存在する。一律に安全側へ倒すと `{printf "%s\t%s\n", …}` のような日常読み取りが deny になり V-12 の誤deny予算を破る。
- **同種の穴の棚卸し → §3-9-2「シェル展開の次元」を新設**。`$"…"` は QT-1 で同時に閉じ、**行継続は QT-2 で bash と一致させる**（SH-1 を閉じる）。**ブレース展開・glob・パラメータ展開は old=allow の既存穴としてスコープ外**とし、`INVENTORY_CASES` へ `allow` で登録して可視化する（INV-SH-07・09・11）。履歴展開は**非対話シェルで無効**であるため相違なし（根拠つきで記載）。チルダ展開・算術展開は既存の扱いで相違なし。
- **証拠の符号化（`rm $'\x74ickets/…'`）→ QT-3（証拠収集に限った復号）で決着**。判定テキストには決して使わない。
- **「文書化された前提が実測で裏付けられていなかった」→ 原則 P6 と V-14 を新設**。字句レイヤについて「bash の argv」と「`lex()` の語トークン」を突き合わせる手順を固定し、**相違があるのに規則も限界記載も無い形が1件でもあればブロッカー**とする。
- **設計内部の矛盾（`xyzzy(` の `LEGACY_DENY_COMMANDS` 収載 vs 維持規律4）→ 収載基準を「old=deny か否か」に明確化して解消**（§4-7）。`xyzzy(` は old=deny であり収載は正しい。**コード変更は不要**（implementer の判断は妥当だった）。
- **R4（`$'...'` は保守側へ倒れる）→ 誤りとして訂正**（§6 R4）。docstring・README・設計書の当該記述を Phase 11 で全て訂正することを V-6 の合否条件にする。

**（履歴）第4版の決着事項:**

- C5（`"cmd" | getline`・`|& getline`）→ **§3-6 D1 の AW-7（プログラム形のホワイトリスト）**で決着。個別規則の追加は同一次元で4回失敗しているため採らない（§3-7）。`|` を許可文字集合から外す1点でこの経路群が閉じる
- ホワイトリストの allow 集合の定義 → **§3-6 D8 の16形**（出所は `rv3.py` 70件・T-H1f・T-AWKOK・T-AW6C・T-H2f・A-3a・tester の M11／M12）。§4-5-3 に3条件のトレースを置き、T-AW7OK で固定する
- M11・M12（AW-5／AW-6 の過剰一致）→ **AW-5／AW-6 を degraded 専用へ縮小**し、正常経路は AW-7 が「プログラム本文のみ・リテラル除去後」で判定することで解消。V-9 の**許容分類7**として登録する
- 誤denyの許容範囲 → **§4-9 V-12**（既存 allow 固定テスト全件 allow／`rv3.py` の新版 deny が 1/70 のまま／新規 deny は §6 R26 の列挙内）。**列挙外が1件でもあればブロッカー**
- 構文次元の実機検証 → **§4-9 V-11 を新設**（implementer の提案9）。安全形の無害性・危険形の実在性・**リテラル除去の健全性（S1）**・`AWK_SAFE_CALLS` と man の突き合わせ（S3）の4点
- §3-9 棚卸し表の実効性 → **原則 P5 と §3-11 の写像表・`INVENTORY_CASES`・T-INVENTORY・V-13** で「表にあるが実装が無い」状態を機械的に検出する
- AW-6 と `head_index` 一元化以降の実装乖離（implementer の10項目）→ §3-6 D1（①②）・§3-9（③）・§4-1（④）・§4-4(d)（⑤）・§4-5／§4-6／§4-7（⑥）・§6 R24（⑦）・§4-8（⑧）・§3-10／§4-9 V-11（⑨）・§4-9 V-9（⑩）へ**全件反映**
- sed のプログラム内構文次元 → **本チケットではホワイトリスト化しない**（D6-3・old=allow・`sed` の収載可否という上位判断を含む・リトライ枠ゼロ）。§6 R25・§3-11 INV-SED-05〜07・申し送り2に明記

**第3版の決着事項（履歴）:**

- C3 の規則 → §3-6 D1 **AW-4（オプションのホワイトリスト）**で決着。reviewer 原案（`-i`/`--include` のブラックリスト）は同次元の `-f`／`-l`／`-E`／`-o`／`-p`／`-d`／`-D` を残し、実測で `awk -f prog.awk T` が未報告の同型回帰であるため**拡張して採用**した
- C4 の規則 → §3-6 D1 **AW-5** で決着。reviewer 提案（`\bsystem\s*\(`・文字列リテラル除去前）を採用し `@load`／`@include` を追加
- H2 の修正 → §3-8 の3点（書き込み先オペランドの単一ヘルパ化＋規則の cwd 対応／置換再帰への cwd 伝播／`cwd_redirect_violation` の吸収）で決着。**「cwd を全語に join する」素朴案と「保護対象 cwd 下では sed/awk/sort/uniq を head レベルで一律 deny する」案はいずれも却下**（前者は誤deny、後者は reviewer の日常読み取り70ケースのうち3件を新規誤denyにする）
- `sort -o`／`uniq` 第2位置引数の非対称 → **§3-6 D4 を改訂し、cwd 相対形と絶対パス形の両方をスコープ内**にした（説明不能な非対称を残さない・D6-2(a)）。`sed -f`／GNU sed `e`／`sort --compress-program`／`git diff --output` は**スコープ外を維持**し §3-9・§6 R12・§10-1 に明記
- `is_readonly_script_call` の偽装 → **§3-6 D7 でスコープ外**（old=new=allow・人間はスコープ拡張を承認していない・D6-3）。§3-9・§6 R21・§10-1 に残存リスクとして明記し、implementer の逸脱4の理由付けを「無償の修正を見送った判断」へ訂正した
- SV-7（棚卸し表自体の抜け）→ **§3-9 の4次元化＋§3-10（根拠3層・追加手順5段）＋原則 P4（フォールセーフ既定2つ）**で決着。**プログラム内構文の次元には機械的な既定が無いことを R23 として明示**した
- M5（`cd ..` 部分接頭辞）→ 実装せず限界として文書化（reviewer 判定）。M6（`mv` の意図的許可）→ 文書化
- M9（設計と実装の乖離）→ §4-1（`OPERATORS` の導出。**第2版の「重要（最長一致の順序）」注記自体が誤りであったことを明記**）・§4-4(a)（`head_index` 一元化）・§4-4(c)（raw ∪ expanded の `variants`）・§4-4(g)（`cwd` は文字列）・§4-2（`display_text`）・§8（M1 無効化点は `cwd_is_guarded`）で反映
- M10（degraded の二重実装面）→ 新規判定を `segment_violation`／`awk_violation` の内側に置き、degraded は `cwd` を渡すだけにした。`cwd_redirect_violation` は削除

**人間への報告事項（Open Question ではない。決定の目的は変えておらず、判断の差分を可視化するために記載する）:**

- **【第6版・承認時に必ず報告すること（reviewer M16 の要求）】R21（`is_readonly_script_call` の許可経路偽装＝任意コード実行）は本改訂後の最大の残存リスクであり、`docs/SPEC.md` にドリフト検知のバックストップが無いことと組み合わさる。** reviewer は「スコープ外判断は妥当だが、**承認時に人間へ別チケットの即時起票を明示報告すべき**」と判定した。申し送り1 を参照。
- **【第6版】第5版が「証拠は増える方向のみ」と書いた前提は誤りであり、それがそのまま C7 になった（M14）。** 第4版の R4（`$'...'` は保守側へ倒れる）に続き、**「未検証の安全性の主張」が2ラウンド連続で穴になっている**。第6版は原則 P6 に加えて **P7（分岐条件の明示列挙）** を新設し、V-14(7) で**両方向**（bash が実行するのに allow ／ bash が実行しないのに deny）を実測する手順を固定した。
- **【第6版】判定が緩くなる（deny → allow）変化が2クラスある。** ①`'…'` 内側の行継続（分類9・old=allow） ②**奇数個の行継続で後続コマンドが語へ吸収される形**（分類10・old=deny）。**②は old=deny からの変化だが、bash も後続コマンドを実行しないため allow が正しく、reviewer が明示的に「allow のまま維持すること」を要求した形である。** V-8／V-9 で WEAKER 行として現れた場合は分類10 として扱う。
- **【最重要（第5版）】人間が選択した機構（degraded 相当へのフォールバック）を、より強い機構（QT-1＝判定不能→無条件 deny）へ置き換えた。** 理由は「**degraded 相当では穴が閉じないことが語単位のトレースで確定した**」ことに尽きる（§3-6 D9 の11段の表）。要点は `\x7c` がコマンド文字列上に `|` として現れないため `degraded_violation` の `statement.split("|")` がパイプ分割を行わず、AW-1／AW-5／AW-6／AW-3 のいずれにも一致しないことである。**置き換え後の判定は人間の決定より deny 方向へ強いだけで、緩くなる箇所は1つも無く、決定の選定理由（最小変更・deny 方向のみ・原則 P4 と一致・誤denyの実害が小さい）はすべて満たしている。**
- **誤denyの実害が小さいことの裏づけ（実測）:** `$'` の使用は Kit のコマンド・スキル・エージェント定義・スクリプトに **1件も無い**（リポジトリ全体の grep）。実用形（`awk -F$'\t'`・`grep $'\t'`）には**エスケープを使わない等価な書き方が存在する**（`awk -F'\t'`・`grep -P '\t'`）ため、回避策を deny 理由文と docstring に載せる。
- **第5版で新たに deny になる読み取り形は §6 R29 の4種類**（`$'…'` を含む形／`$"…"` を含む形／degraded でクォート内に `$'` の2文字並びがある形／復号で保護対象パスが現れる形）。いずれも**保護対象に言及する／保護対象を cwd とするコマンドに限る**。
- **本チケットのスコープ外として残す同種の穴（old=allow）:** ブレース展開 `rm tickets/{active,done}/X.md`・glob `rm tickets/acti*e/X.md`・パラメータ展開 `f=…; rm $f`（§6 R31）。**`INVENTORY_CASES` へ `allow` で登録して可視化**し、別チケットを推奨する（申し送り9）。
- **（第4版）ホワイトリスト化の代償として新たに deny になる読み取り形は §6 R26 の8種類**（`@namespace`／未収載の組み込み関数／`|` を伴う形／リテラル外のバックスラッシュ・バッククォート・シングルクォート・非ASCII／`IDENT (` の連結形／置換由来のプログラム／未閉じ文字列リテラル／`@`・`|` を含むコメント）。いずれも**保護対象に言及する／保護対象を cwd とする awk に限る**。日常の読み取り（`rv3.py` 70件）への影響はゼロを維持する（V-12(b)）。
- **逆に allow へ転じるのは M11・M12 の2形のみ**（awk が実行し得ないテキストに対する誤deny）。これは tester が AC2 の解消対象として報告した形である。
- **degraded mode（未閉じクォートを含む入力）にはホワイトリストが及ばない**（§3-5・R28）。この経路の強化は別途の設計判断を要する。

**申し送り（ブロッカーではない。人間への情報提供・別チケット候補）:**

1. **【最優先】`is_readonly_script_call()` の許可経路偽装（任意コード実行）** — `python3 x=y {READONLY_SCRIPT} docs/SPEC.md` が allow だが実行されるのは `x=y`。old=new=allow の既存穴だが**深刻度は D4 の各項より高く（書き込み限定ではなく任意コード実行）、`docs/SPEC.md` にはバックストップが無い**。reviewer が `head_args()` へ寄せた5行の統一で偽装が deny になり正規経路（環境変数前置き付きを含む）は一切壊れないことを実測済みで、**修正案は既に検証されている**。§3-6 D7 の判断（本チケットではスコープ外）を踏まえ、**即時起票を推奨**
2. **`sed -f SCRIPT`／GNU sed の `e` フラグ・`e` コマンド・`s///e`（任意コマンド実行）／`sort --compress-program=PROG`／`git diff --output=FILE`** — いずれも old=allow の既存穴。1つの別チケットにまとめるのが妥当。**`sed` を `ALLOWED_HEADS` に残すかどうかという上位判断を含む**ため人間の承認を要する。**第4版の追記: awk へ適用した「プログラム形のホワイトリスト」（AW-7）と同じ構造を sed のスクリプト引数へ適用するのが第一候補の解**である（sed のコマンド集合は awk の言語より小さく、読み取り用途は `p`／`d`／`s///`／アドレス指定にほぼ限られるため、ホワイトリスト化のコストは awk より低い）。§6 R25・§3-11 INV-SED-05〜07 を参照
3. **シェル制御構文（`for` / `while` / `if`）の誤deny**（`old=deny / new=deny` で回帰ではない）。ループ本体の解析が必要で設計判断を要する
4. **`_ticket_lib.is_ticket` と `is_guarded_token` の判定基準統一**は KLK-012 完了後に別チケットで検討（D2）
5. **`docs/SPEC.md` にドリフト検知のバックストップが無い** — `check_loop_integrity.py` へ SPEC.md のハッシュ監視を追加する等の恒久対策は別チケット候補。**本チケットで塞いだ C1・C3・C4 がいずれも SPEC.md 宛てで無防備だったこと、および申し送り1が SPEC.md へ到達できることは、この恒久対策の必要性を強く裏付ける**
6. **`.claude/settings.json` の `permissions.allow` と hook の `ALLOWED_HEADS` の二重管理**の整理は別チケット候補
7. `printf` を `ALLOWED_HEADS` に加えるかは README のローカル動作確認の注記と対で判断する必要があるため見送り継続（tester テストで現状を固定済み）
8. **`ALLOWED_HEADS` の縮小の検討** — 本チケットで3度にわたり「ホワイトリスト収載コマンドの引数だけで書ける／実行できる経路」が見つかった。`sed`・`awk`・`sort`・`uniq`・`find`・`git`・`python3` は「読み取りにも使えるが書き込みにも使える」コマンドであり、規則を積み増す方式には構造的な限界がある（§6 R23）。**中期的には「読み取り専用の狭いホワイトリスト＋明示的な読み取り用ラッパースクリプト」へ寄せる設計変更を検討する価値がある**（AC3 の改訂を伴うため人間の承認が必要）
9. **【第5版で新規】シェル展開による保護対象パスの分断（ブレース展開・glob・パラメータ展開）** — `rm tickets/{active,done}/APP-001.md`／`rm tickets/acti*e/APP-001.md`／`f=tickets/active/APP-001.md; rm $f` はいずれも **old=allow の既存穴**で、hook を完全に迂回できる（`docs/SPEC.md` 宛てはバックストップも無い）。正しく閉じるにはブレース展開の実装（入れ子・直積）が必要で、`PATHISH_RE` を単純に拡張すると awk のプログラム語が保護対象と誤判定されうる。**`INVENTORY_CASES`（INV-SH-07・09・11）に `allow` で登録して可視化済み。** パラメータ展開は**原理的に閉じられない**（値が hook から見えない）ため、申し送り5（SPEC.md のドリフト検知バックストップ）と併せて検討するのが妥当
10. **【第6版で新規】`#` コメント本文が字句レイヤで除去されないこと（§6 R35）** — 第6版は `#` を**行継続の適用可否の判定にだけ**モデル化し、本文の除去は行わない。残る性質は (a) コメント内の `> path`・`rm` 等が証拠として拾われる（**保守側・old も同じ**） (b) **コメント内の未閉じクォート（`#'`・`don't` 等）が degraded 経路を誘発する**（C2・C8 の入口そのもの）。**(b) を根本的に減らすには「コメントを除去してから字句解析する」変更が要るが、それは `cat a #> docs/SPEC.md`（old=deny）を allow に転じさせるなど deny → allow 方向の変化を伴う**ため、単独の別チケットで old/new 比較を取りながら行うのが妥当
11. **【第6版で新規】`degraded_violation` への依存度の高さ** — C2・H2・C8 と**同じ関数で3度**穴が出た。degraded はブラックリスト（§6 R28）であり、正常経路の強化がそのまま反映されない構造的な弱点を持つ。**中期的には「degraded へ落ちる入力をそもそも減らす」方向**（申し送り11 のコメント除去・`shlex` に依存しない語分割）と、**「degraded を『判定不能→deny』へ寄せる」方向**（QT-1 と同じ考え方の一般化）の2案がある。**後者は誤denyを大きく増やすため人間の承認を要する**
12. **【第5版で新規】`$'...'` の代替形と `printf` の扱い** — R29 ① の回避策として `grep -P '\t'`・`awk -F'\t'` を案内するが、`"$(printf '\t')"` 形は使えない（`printf` が意図的に `ALLOWED_HEADS` 非収載であり、置換の内側で非収載 head として deny される）。申し送り7（`printf` の収載可否）と関連する
