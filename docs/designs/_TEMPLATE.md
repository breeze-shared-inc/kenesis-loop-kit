---
ticket: "{{TICKET_ID}}"
title: "{{TITLE}}"
spec_version: "{{SPEC_VERSION}}"
created: "{{DATE}}"
updated: "{{DATE}}"
---

# [{{TICKET_ID}}] {{TITLE}} 設計

> このファイルはarchitectが生成・更新するチケット単位の設計書です。
> チケット {{TICKET_ID}} に対応します。implementer・tester・reviewerが参照します。
> 1チケット = 1ファイル（`docs/designs/{{TICKET_ID}}.md` が常に現行）。改訂時は本ファイルを上書きし、
> チケットのログに改訂理由を残す。過去の設計はGitヒストリで追える（status等の状態は持たない）。
> spec_version には設計時に準拠した docs/SPEC.md の版数（セクション0の最新版。例: "v1.1"）を記録する。
> SPEC改訂を受けて設計を見直した場合は spec_version もあわせて更新する。

## 1. 目的

<!-- SPEC.mdの該当要件を必ずID(REQ-xxx / SCR-xxx / IF-xxx / EH-xxx / NFR-xxx)で明記し、この設計が何を解決するかを1〜3行で記述する(SPEC冒頭規約「チケット・設計書には対応するIDを明記すること」の転記先) -->

## 2. 現状

<!-- 既存のコード・データ構造・フローの中で、この設計が影響を与える部分を記述する -->
<!-- investigatorの調査結果（チケットの実装メモ）を参照して記述する -->

**現在のフロー:**
```
（フローやシーケンスをテキストで記述）
```

**関連する既存ファイル:**
- `src/`:

## 3. 設計方針

<!-- 実装のアプローチと技術的な判断の根拠を記述する -->
<!-- 採用しなかった代替案とその理由も記述する -->

**採用する方針:**

**代替案と却下理由:**
| 案 | 却下理由 |
|---|---|
|  |  |

## 4. 実装詳細

<!-- implementerが迷わず実装できるレベルの詳細を記述する -->
<!-- インターフェース・データ構造・アルゴリズムを含む -->
<!-- シークレット(APIキー・パスワード・接続文字列等)の実値を記載しない。環境変数名のみを記載する(docs/security-policy.md に準拠) -->

**ファイル構成（変更・追加）:**
```
src/
├── （追加・変更するファイルを記述）
```

**主要なインターフェース:**
```typescript
// 型定義やAPIインターフェースの例
```

**処理フロー:**
```
（シーケンス図または番号付きフローを記述）
```

**実装Phase:**

<!-- 実装の段階と順序を定義する。implementerはこの順に実装する -->
<!-- Phaseの完了条件は作業チェックポイントであり、チケットの受け入れ条件を再定義・緩和しない。全受け入れ条件が少なくとも1つのPhaseでカバーされていること（architectがセルフチェックし、reviewerが検証する） -->
<!-- Phaseが4つ以上になる、または各Phaseが独立してレビュー・リリース可能な粒度になる場合は、チケット分割の見直しが必要なサイン。設計を確定せずorchestratorへ報告し、人間による /plan-tickets の差分起票を仰ぐ -->
<!-- Phaseの進行はチケットstatusに反映しない（statusは implementation_done の粒度のまま）。進行はimplementerのコミットとレポートで追う -->

| Phase | 内容 | 完了条件 | 対応する受け入れ条件 |
|---|---|---|---|
| 1 |  |  |  |
| 2 |  |  |  |

## 5. 影響範囲

<!-- この設計変更によって影響を受けるコンポーネント・APIを列挙する -->

| 対象 | 影響の種類 | 対応方針 |
|---|---|---|
|  | 変更 / 追加 / 削除 |  |

## 6. リスク

<!-- 実装上のリスクと軽減策を記述する -->

| リスク | 深刻度 | 軽減策 |
|---|---|---|
|  | high / medium / low |  |

## 7. マイグレーション戦略

<!-- データベースの変更・APIの後方互換性・段階的な移行が必要な場合に記述する -->
<!-- 不要な場合は「なし」と明記する -->

## 8. ロールバック戦略

<!-- 問題発生時の戻し方を具体的に記述する -->

```bash
# ロールバック手順の例
git revert {コミットハッシュ}
```

## 9. 受け入れ条件

<!-- チケットの受け入れ条件(/plan-ticketsで人間が承認した契約)を、それを弱めない範囲で設計の観点から補足・具体化する -->
<!-- 受け入れ条件自体の変更が必要と判明した場合は、ここで書き換えずOpen Question化して人間の判断を仰ぐ -->
<!-- /plan-ticketsでこのチケットに割り当てられたNFR-xxx・EH-xxxの「検証方法」を、検証可能な受け入れ条件としてここに具体化する -->
<!-- テスト観点(単体/結合の境界・モック方針・重点的に見るエッジケース)もここに記述し、testerへの指示とする -->
<!-- testerとreviewerがこのリストを基準に検証する -->

- [ ]
- [ ]

## 10. Open Questions

<!-- 設計中に判断できなかった事項。解消後は削除し、解消内容をレポートに含めてorchestratorがチケットのログへ1行残す -->
<!-- blockerの場合はチケットのブロッカーセクションにも記載する -->

- [ ]
