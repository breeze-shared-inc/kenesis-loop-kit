---
ticket: "KLK-019"
title: "シェル制御構文（for/while/if）の誤deny解消"
spec_version: "なし（docs/SPEC.md 不在。Kit自体の改善のため KLK-010・KLK-015・KLK-018・KLK-029 の前例に従う）"
created: "2026-08-18"
updated: "2026-08-18"
---

# [KLK-019] シェル制御構文（for/while/if）の誤deny解消 設計

> このファイルはarchitectが生成・更新するチケット単位の設計書です。
> チケット KLK-019 に対応します。implementer・tester・reviewerが参照します。

## 1. 目的

`.claude/hooks/guard_bash_writes.py` の `head_of()` は `;` 区切りの1ステートメントの
先頭語を無条件に「コマンド名」とみなす。`for`/`while`/`until`/`if`/`elif`/`case`は
もちろん、`do`/`then`/`else`/`fi`/`done`/`esac`もすべて`ALLOWED_HEADS`に無いため、
これらが先頭語になっただけで保護対象パスに言及するステートメントが一律denyに
なる（本チケットに SPEC.md 上の REQ/SCR/IF は無い。要件の正はチケット KLK-019
の受け入れ条件）。本チケットは、制御構文キーワードを**単純にALLOWED_HEADSへ
追加せず**（investigator実測: `do rm $f`等4形が deny→allow に転じる穴を確認済み）、
先頭キーワードだけを再帰的に剥がして残余語を既存のALLOWED_HEADS判定へそのまま
回す機構を新設し、読み取り専用の制御構文をallowへ、書き込みを含む制御構文
（リテラルパス・ループ変数経由の両方）を引き続きdenyへ分類する。

## 2. 現状

**現在のフロー（`for f in tickets/active/*.md; do rm $f; done` の例）:**
```
lex() → 改行/`;`を統一的にstatement区切りとして split_statements()
  → 統計1 ["for","f","in","tickets/active/*.md"]
      head_of = "for" → ALLOWED_HEADS外 → gate_hit(literal mention)=True → deny
      （"rm $f" を含む統計2 は評価に到達すらしない）
  → 統計2 ["do","rm","$f"]
      head_of = "do" → ALLOWED_HEADS外（統計1で既にdeny済みのため到達しない）
```
制御構文の特別扱いはコード中に一切存在しない（investigation.md Finding1）。
`ALLOWED_HEADS`/`CWD_SAFE_HEADS`へ制御構文キーワードを単純追加する仮想パッチ
検証では、`for f in tickets/active/*.md; do rm $f; done`／`if rm ...; then echo done; fi`／
`case x in *) rm ...;; esac`／`while true; do rm ...; break; done`の4形すべてが
deny→**allow**に転じることを実測済み（investigation.md Finding3）。原因は
「`do`/`then`/`elif`/`else`の直後に続く実コマンド語がhead判定から一切見えない」
こと（ステートメント分割が`;`単位のみのため`do rm $f`は1ステートメントであり、
`head_of`は先頭語`do`だけを見て`rm`を判定対象にしない）。

**関連する既存ファイル:**
- `.claude/hooks/guard_bash_writes.py` `split_statements()`: `"""トークン列を \`;\` \`&&\` \`||\` \`&\` でステートメントへ分割する。"""` （`;`単位のみで分割し、制御構文の入れ子は一切考慮しない）
- 同ファイル `head_index()`: `"""語リスト中で「先頭コマンド名」に当たる語の位置を返す。無ければ -1。"""` （環境変数代入前置きと `(` グループ化しか読み飛ばさない）
- 同ファイル `statement_violation()`: `"""ステートメント単位の違反理由を返す。問題なければ None。"""` （`segments = pipe_segments(statement)` が変更対象の中心行）
- 同ファイル `redirect_violation()`: `"""出力リダイレクト先が保護対象・保護対象を代入された変数なら理由を返す。"""` （`guarded_vars` のみを見る。ループ変数の追跡機構が無い）
- 同ファイル `record_assignments()`: `"""\`NAME=保護対象パス\` の前置き代入を記録する（リダイレクト先の追跡用）。"""` （`for NAME in ...` の束縛は対象外）
- 同ファイル `find_violation()`: `for statement in split_statements(tokens):` （ステートメントごとの cwd 追跡・違反判定ループの本体）
- `tests/test_guard_bash_writes.py` `INVENTORY_CASES`: `("INV-SH-11", "パラメータ展開（値は不可視＝構造的限界・**未対応**）",` （ループ変数値追跡は既存でも「原理的に閉じられない」限界として明記済み。本チケットが変えるのは「値」ではなく「変数参照の構文的存在」の扱い）

## 3. 設計方針

**採用する方針:**

1. **`ALLOWED_HEADS`/`CWD_SAFE_HEADS`は一切変更しない。** 制御構文キーワード
   （`for`/`while`/`until`/`if`/`elif`/`do`/`then`/`else`/`case`）は「コマンド」
   ではなく構文上のマーカーである、という性質をそのまま表現する。モジュール
   docstring末尾の「ALLOWED_HEADSへコマンドを追加するときの手順」（4次元棚卸し）
   はコマンド名を新規収載するときの手続きであり、本設計はこれを一切トリガー
   しない（棚卸し表・5手順の適用対象外であることを明記する）。
2. **`strip_control_prefix(statement)`** を新設し、ステートメント（`(kind,value)`
   トークン列）の先頭に連なる制御構文キーワードを再帰的に剥がし、実行されうる
   「残余コマンド」のトークン列を返す。剥がすものが無い・構造を確定できない
   （C形式for・複数パターンcase等）場合は `None` を返し、呼び出し元は**元の
   statementでそのまま判定する**（＝現状の保守的なdeny挙動を変えない。P8）。
   - `if`/`elif`/`while`/`until` の直後は**実行される条件コマンド**なので、
     キーワード1語だけを剥がして残余をそのまま次のループへ回す。
   - `do`/`then`/`else` の直後は**実行される本文コマンド**なので、同様に
     1語だけ剥がす。
   - `for NAME in LIST` は**実行されるコマンドを一切含まない**（LISTは列挙
     されるだけ）。`NAME`が識別子の形（`[A-Za-z_][A-Za-z_0-9]*`）であることを
     確認できた場合に限り、残余を空リスト`[]`として返す（ALLOWED_HEADS判定を
     スキップ＝実質的に「常に許可」だが、これは「判定するコマンドが無い」
     ためであり、ALLOWED_HEADSへの追加とは異なる）。C形式（`for ((i=0;...))`）
     など`NAME`が識別子の形を取らない場合は`None`を返しフォールバックする。
   - `case SUBJECT in PATTERN) ...` は「ヘッダー＋最初の節の本体」が同一
     ステートメントに同居する。`PATTERN`が単一トークンで`)`終端の単純な形
     （`*.md)`・`*)` 等）だけを認識し、それ以外（複数パターンの`a|b)`・
     `(`/`)`が別トークンに分割される形・`in`欠落等）は`None`を返して
     フォールバックする（理由は§6リスクのR-CTRL2）。
3. **ゲート判定・cwd追跡はすべて既存の`statement_violation`/`find_violation`の
   仕組みを再利用する。** `strip_control_prefix`の出力は「`pipe_segments`へ渡す
   語列」を差し替えるためだけに使う。`redirect_violation`・ANSI引用チェック・
   heredocチェック・`mentions_guarded_expanded`によるゲート判定は**元の
   statement**に対して行う（変更しない）。これにより新設ロジックの影響範囲が
   「ALLOWED_HEADS判定に渡す語列の選択」1点に限定される。
4. **ループ変数経由の書き込み（AC3）は値を追跡せず、構文的な束縛の事実だけを
   証拠にする。** `for NAME in LIST`のLISTが（`mentions_guarded_expanded`で）
   保護対象パスに言及していた場合、`NAME`を新設の`guarded_loop_vars`集合へ
   登録する。以降の統計で`$NAME`/`${NAME}`という**参照が構文上どこかに現れる
   だけで**（読み取りコマンドの引数であっても）そのステートメントのゲートを
   開く。ゲートが開いた後の許可・不許可は既存のALLOWED_HEADS判定にすべて
   委ねる（`cat $f`は`cat`がALLOWED_HEADSにあるため許可のまま、`rm $f`は
   `rm`が無いため拒否）。値の追跡が原理的に不可能である以上、**「参照される
   だけでゲートを開く」が唯一の保守側に倒せる設計**である（詳細根拠は
   §6 R-CTRL1）。
5. **既存の`guarded_vars`（`NAME=value`前置き代入の追跡）とは完全に分離した
   別集合`guarded_loop_vars`を使う。** 両者を統合すると、既存の許容済みギャップ
   （INV-SH-11: `f=tickets/active/APP-001.md; rm $f` → 恒久的にallow）が本
   チケットの変更で新たにdenyへ転じてしまい、スコープ外の挙動変更（回帰）に
   なる。分離することでINV-SH-11の挙動は1バイトも変わらない。

**代替案と却下理由:**
| 案 | 却下理由 |
|---|---|
| 制御構文キーワードを`ALLOWED_HEADS`/`CWD_SAFE_HEADS`へ単純追加 | investigator実測で4形すべてがdeny→allowに転じる穴を確認済み（`do`/`if`等の直後の書き込みコマンドが一切判定されなくなる） |
| ループ変数の値そのものを解決しようとする（`for f in a b c`の列挙を辿るなど） | LISTがglob（`tickets/active/*.md`）の場合は実際のファイルシステムをstatしないと値が確定せず、本hookの「テキストのみで判定する」既存アーキテクチャ（ファイルI/Oを一切行わない）を逸脱する。原理的に閉じられない値追跡は既存INV-SH-11でも明示済みの限界であり本チケットで覆さない |
| 完全な shell 文法パーサへの刷新（`case`の`\|`パターン等も含め全構文を正しく解釈） | KLK-010が13ラウンド・10件の穴を経て確立した現行アーキテクチャ（ステートメント分割＋head判定＋追加規則のOR拡張）からの逸脱が大きく、長期的複雑性が増大する。本チケットのスコープ（AC1〜7）に対して過剰であり、既存機構の再利用で安全性を保ったまま閉じられる |
| ループ変数参照を「書き込み系コマンドの引数位置」だけに限定して検出（読み取りコマンドの引数では無視） | 「書き込み系コマンド」を独自に列挙するブラックリストが必要になり、新しい列挙漏れの次元を作る（KLK-010 P4が戒める形）。既存のALLOWED_HEADS判定へ委ねる「ゲートを開くだけ」の設計なら新しい列挙を増やさずに済む |

## 4. 実装詳細

**ファイル構成（変更のみ・追加ファイル無し）:**
```
.claude/hooks/guard_bash_writes.py   # 新規関数・定数の追加、既存関数のシグネチャ拡張
tests/test_guard_bash_writes.py      # INV-CTRL-* の追加、専用回帰テストの追加
```

**新設定数（`.claude/hooks/guard_bash_writes.py`。KLK-018のブレース展開節と
同じ位置づけで、既存定数の直後に独立したセクションとして追加する）:**
```python
# --- KLK-019: シェル制御構文（for/while/until/if/elif/do/then/else/case）の
#     本体再帰判定 -------------------------------------------------------------
CONTROL_CONDITION_KEYWORDS = frozenset({"if", "elif", "while", "until"})
CONTROL_BODY_KEYWORDS = frozenset({"do", "then", "else"})
# **実装時の補正（設計書§6 R-CTRL3の前提の見直し）:** 設計書は当初
# 「fi/done/esac は常に統計の末尾にしかならず、単独語のみの統計は言及ゲートが
# 立たないため特別扱いは不要」としていたが、AC1 の実測
# （`while read l; do echo $l; done < tickets/active/APP-001.md`）で
# この前提が破れることを確認した。`done`/`fi`/`esac` はステートメント区切り
# （`;` 等）の直後にしか現れないが、**その直後に区切り無しでリダイレクト
# （`done < FILE`）が続く形は bash 文法上有効**であり、この場合
# 「`done` + リダイレクト先」が1ステートメントになる。リダイレクト先の語は
# pipe_segments では読み飛ばされる（コマンド語ではない）が、gate_hit 判定用の
# `words`（ステートメント中の全 "w" トークン）には含まれるため、保護対象へ
# 言及するリダイレクト先を持つだけで gate_hit が立ち、ストリップされない
# 生の "done" が head になって ALLOWED_HEADS 外として誤denyになっていた
# （`<` は redirect_in であり、そもそも書き込みを一切発生させないため、
# ここを deny する実益は無い）。したがって CONTROL_CLOSING_KEYWORDS も
# CONTROL_CONDITION_KEYWORDS／CONTROL_BODY_KEYWORDS と同様に
# strip_control_prefix で剥がす（下記）。**この補正は ALLOWED_HEADS／
# CWD_SAFE_HEADS を一切変更せず、剥がした残余の判定は既存の
# ALLOWED_HEADS 判定にそのまま委ねる**ため、新しい許可経路を作らない
# （`done | rm ...` のように閉じキーワードの後ろに実コマンドを伴う形は
# パイプ区間として独立に判定されるため、この剥がしとは無関係に deny の
# ままである。検出器: test_klk019_closing_keyword_trailing_redirect_allow・
# test_klk019_closing_keyword_pipe_write_still_denies）。
CONTROL_CLOSING_KEYWORDS = frozenset({"fi", "done", "esac"})
BASH_NAME_RE = re.compile(r"[A-Za-z_][A-Za-z_0-9]*")
```

**新設関数（すべて既存関数のすぐ後、`statement_violation`の手前に配置）:**
```python
def _parse_for_header(words):
    """`for NAME in LIST` の古典形なら (NAME, LIST語のリスト) を返す。
    C形式（`for ((...))`）・`in`を伴わず余分な語がある不明な形は None。
    `for NAME; do ...`（`in`省略＝$@相当）は (NAME, []) を返す
    （LISTが無いためguarded_loop_varsには登録されない＝安全側）。
    """
    if len(words) < 2 or words[0] != "for":
        return None
    name = words[1]
    if not BASH_NAME_RE.fullmatch(name):
        return None
    rest = words[2:]
    if not rest:
        return name, []
    if rest[0] == "in":
        return name, rest[1:]
    return None  # `for NAME extra...`（in を伴わない不明な形）


def _strip_case_clause(tokens):
    """`case SUBJECT in PATTERN) ...` の先頭を剥がし、本体トークンを返す。

    単一トークンの単純パターン（`*.md)`・`*)` 等）だけを認識する。認識でき
    ない形（複数パターンの `|`・分割された `(`/`)`・`in` の欠落等）は None を
    返す（P8: 曖昧な位置は保守側。呼び出し元は元の statement で判定する）。
    """
    if (len(tokens) < 4 or tokens[1][0] != "w" or
            tokens[2] != ("w", "in") or tokens[3][0] != "w"):
        return None
    pattern = tokens[3][1]
    if not pattern.endswith(")") or pattern == ")":
        return None
    return tokens[4:]


def strip_control_prefix(statement):
    """先頭に連なる制御構文キーワードを再帰的に剥がし、残余トークンを返す。

    残余は「実行されうる本体コマンド」の (kind, value) トークン列（空リストも
    ありうる＝forヘッダーのように実行コマンドを含まない場合）。剥がすものが
    無い、または構造を確定できない場合は None を返す（呼び出し元は元の
    statement で判定する＝既存の deny 側の挙動を維持する。P8）。

    **実装時の補正:** CONTROL_CLOSING_KEYWORDS（fi/done/esac）も同様に剥がす。
    設計書§6 R-CTRL3は「これらは常に統計の末尾で単独語にしかならない」と
    仮定していたが、`done`/`fi`/`esac` の直後に区切り無しでリダイレクトが
    続く形（`while read l; do echo $l; done < tickets/active/APP-001.md`。
    AC1 の実例）は bash 文法上有効であり、この場合「閉じキーワード＋
    リダイレクト」が1ステートメントになる。リダイレクト先の語は
    pipe_segments では読み飛ばされるが gate_hit 判定用の words には含まれる
    ため、剥がさないと生の "done" が head になり ALLOWED_HEADS 外として
    誤denyになる（`<` は redirect_in であり書き込みを発生させないため、
    denyする実益が無い）。剥がした残余は既存の ALLOWED_HEADS 判定に
    そのまま委ねるため新しい許可経路は作らない（詳細は
    CONTROL_CLOSING_KEYWORDS の定義コメントを参照）。
    """
    tokens, changed = statement, False
    while tokens:
        if tokens[0][0] != "w":
            break
        head = tokens[0][1]
        if head == "case":
            residual = _strip_case_clause(tokens)
            if residual is None:
                return tokens if changed else None
            tokens, changed = residual, True
            continue
        if head == "for":
            plain = [v for k, v in tokens if k == "w"]
            parsed = _parse_for_header(plain)
            if parsed is not None:
                return []  # ヘッダーに実行コマンドは無い
            return tokens if changed else None  # C形式等、判定できない形
        if (head in CONTROL_CONDITION_KEYWORDS or
                head in CONTROL_BODY_KEYWORDS or
                head in CONTROL_CLOSING_KEYWORDS):
            tokens, changed = tokens[1:], True
            continue
        break
    return tokens if changed else None


def control_stripped_segments(statement):
    """制御構文の先頭キーワードを剥がした残余からパイプ区間を得る。

    剥がせなかった場合（strip_control_prefix が None を返す場合）は元の
    statement からそのまま得る（安全側フォールバック。既存の全ステートメント
    に対して1バイトも挙動を変えない）。**この関数は ALLOWED_HEADS 判定
    （segment_violation 群）と cwd 追跡（next_cwd）の両方の呼び出し元から
    使うこと。** 片方だけに適用すると、`if cd tickets/active; then rm
    APP-001.md; fi` のように「if の条件で cd し、cwd 追跡だけが古い statement
    を見る」ことで cwd_is_guarded が発火しない穴になる（§6 R-CTRL4）。
    """
    stripped = strip_control_prefix(statement)
    return pipe_segments(stripped if stripped is not None else statement)


def mentions_guarded_var(words, names):
    """語のいずれかが names に含まれる変数を参照するか（`$NAME`/`${NAME}`）。

    読み取り・書き込みを問わず**参照が構文上に存在するだけ**で真になる
    （値の追跡は行わない＝AC3の保守的deny設計。実際に許可されるかどうかは
    このゲートの後段にある既存の ALLOWED_HEADS 判定に委ねる）。
    """
    if not names:
        return False
    for word in words:
        for ref in VAR_REF_RE.findall(word):
            if ref in names:
                return True
    return False


def record_loop_binding(statement, guarded_loop_vars, substs=()):
    """`for NAME in LIST` の LIST が保護対象に言及する場合、NAME を
    guarded_loop_vars へ登録する（KLK-019 AC3）。

    値そのもの（実行時に $NAME に束縛される個々の要素）は追跡できない
    （for の展開結果はコマンド文字列上に現れないため原理的に不可能。
    モジュール docstring「パラメータ展開」・INV-SH-11 と同型の限界）。
    ここでは「LIST全体が保護対象パスへ言及している」という構文的事実だけを
    証拠にし、`guarded_loop_vars` へ加える。既存の `guarded_vars`
    （NAME=value 前置き代入の追跡）とは完全に別集合であり、統合しない
    （INV-SH-11 の挙動を変えないため。§3 設計方針5）。
    """
    words = [v for k, v in statement if k == "w"]
    parsed = _parse_for_header(words)
    if parsed is None:
        return
    name, rest = parsed
    if not rest:
        return
    if mentions_guarded_expanded(expand_all(rest, substs)):
        guarded_loop_vars.add(name)
```

**tester差し戻しによる追加修正（`_strip_leading_outer_keyword`・
`strip_outer_control_keywords`の新設。コミット`349ec69`、ブランチ
`fix/KLK-019-control-structure-false-deny`）:**

tester差し戻し（AC6独自検証）で、`record_assignments`・`record_loop_binding`
が`control_stripped_segments`（R-CTRL4で共有必須と定めた）を経由せず**生の
statement語**を見ていたため、内側の`for NAME in LIST`ヘッダー・前置き代入が
外側制御構文の`do`/`then`/`while`等と`;`区切りで**同一statementを共有する形**
（`for i in 1 2; do for f in tickets/active/*.md; do rm $f; done; done`等）で
登録が丸ごと失敗し、新規にdeny→allowへ転じるブロッカーが判明した（経緯は
§10「設計修正の記録」を参照）。

- **`strip_control_prefix`をそのまま転用できない理由**: `strip_control_prefix`
  の`for`/`case`分岐は「ヘッダーに実行コマンドは無い」という前提でヘッダー
  ごと消費・破棄し`[]`（またはcase残余）を返す（ALLOWED_HEADS判定には正しい
  挙動）。しかし`record_assignments`/`record_loop_binding`は逆に**for-header
  自体・代入自体を見る必要がある**ため、この2関数にはそのまま転用できない。
- **`_strip_leading_outer_keyword(tokens)`（新設）**:
  ```python
  _OUTER_CONTROL_KEYWORDS = (CONTROL_CONDITION_KEYWORDS | CONTROL_BODY_KEYWORDS |
                             CONTROL_CLOSING_KEYWORDS)


  def _strip_leading_outer_keyword(tokens):
      """先頭1語が外側制御構文キーワード（if/elif/while/until/do/then/else/
      fi/done/esac）ならそれだけを剥がした残余を返す。`for`/`case`は対象外
      （呼び出し元によって扱いが違うため、ここでは判定しない）。剥がせなければ
      None を返す。
      """
      if tokens and tokens[0][0] == "w" and tokens[0][1] in _OUTER_CONTROL_KEYWORDS:
          return tokens[1:]
      return None
  ```
  「条件／本体／閉じキーワードだけを先頭から剥がし、`for`/`case`はそのまま
  残す」という`strip_control_prefix`内の既存ロジックをそのまま切り出したもの
  であり、新しい判定基準・新しい語彙は増やさない。
- **`strip_outer_control_keywords(statement)`（新設）**:
  ```python
  def strip_outer_control_keywords(statement):
      """先頭に連なる外側制御構文キーワードだけを剥がし、`for`/`case`はそのまま
      残す軽量ヘルパー。strip_control_prefixと異なり`for`/`case`に到達しても
      止まらず、常に残余トークン列を返す（「変化なし」をNoneで示す設計は
      採らない——呼び出し元は残余の先頭がfor-header／代入かどうかだけを見る
      ため、変化の有無を区別する必要が無い）。剥がすものが無ければ引数を
      そのまま返す。
      """
      tokens = statement
      while True:
          stripped = _strip_leading_outer_keyword(tokens)
          if stripped is None:
              return tokens
          tokens = stripped
  ```
- **`strip_control_prefix`自身のリファクタ**: 条件／本体／閉じキーワードの
  分岐（従来はインライン実装）を`_strip_leading_outer_keyword`の呼び出しへ
  置き換えた（`for`/`case`分岐自体は変更なし）。**外部から見た挙動は無変更**
  （純粋なロジック抽出。731件〔旧726件＋tester追加5件〕全件パスで確認済み）。
- **`record_assignments`の変更**: `for words in pipe_segments(statement)` を
  `for words in pipe_segments(strip_outer_control_keywords(statement))` へ
  差し替えた（シグネチャは無変更）。
- **`record_loop_binding`の変更**: `words = [v for k, v in statement if k ==
  "w"]` の前に `tokens = strip_outer_control_keywords(statement)` を追加し、
  以降`statement`ではなく`tokens`から語を取り出すよう変更した（シグネチャは
  無変更）。
- **`ALLOWED_HEADS`/`CWD_SAFE_HEADS`は無変更**（§3方針1を維持）。剥がした
  残余は既存の判定にそのまま委ねるため新しい許可経路を作らない。

**既存関数の変更点:**

- `record_assignments(statement, guarded_vars, substs=())`（KLK-019以前から
  存在する既存関数。§2現状参照）: tester差し戻しにより、`statement`から
  直接ではなく `strip_outer_control_keywords(statement)` の残余から前置き
  代入を走査するよう変更した（上記「tester差し戻しによる追加修正」参照）。
  シグネチャは無変更
- `redirect_violation(statement, guarded_vars, substs=(), cwd="")` に
  `guarded_loop_vars=()` を追加し、リダイレクト先の変数参照チェックへ
  `guarded_vars` とのOR条件として組み込む:
  ```python
  any(n in guarded_vars or n in guarded_loop_vars
      for n in VAR_REF_RE.findall(resolved))
  ```
  （`for f in tickets/active/*.md; do echo x > $f; done` を検出するため）
- `statement_violation(statement, guarded_vars, substs=(), cwd="", guarded_loop_vars=())`
  - `segments = pipe_segments(statement)` を `segments =
    control_stripped_segments(statement)` へ差し替える（1行）
  - `redirect_violation(...)` の呼び出しへ `guarded_loop_vars` を追加で渡す
  - gate_hit の最終行へ `or mentions_guarded_var(expanded_words,
    guarded_loop_vars)` を追加する（`do rm $f`のような「読み取りに見えない
    参照」でゲートを開くため）
- `find_violation()`:
  - ループ変数の初期化を1行追加: `guarded_vars, guarded_loop_vars, seen =
    set(), set(), set()`
  - `record_assignments(...)` の直後に `record_loop_binding(statement,
    guarded_loop_vars, substs)` を追加
  - `statement_violation(...)` 呼び出しへ `guarded_loop_vars` を追加で渡す
  - cwd 追跡行 `segments = pipe_segments(statement)` /
    `cwd = next_cwd(segments[0] if segments else [], cwd)` を
    `segments = control_stripped_segments(statement)` に差し替える
    （**理由: R-CTRL4**。§6参照）

`degraded_violation`（字句解析に失敗した入力向けフォールバック）は変更しない
——制御構文の再帰的ストリッピングは正常経路（`lex()`が成功した入力）だけを
対象とし、degradedは従来どおり素朴な分割のまま維持する（degradedは元々
「旧版の判定要素の上位互換」であり、制御構文キーワードも旧版と同じく無条件
denyのまま。読み取り専用の制御構文がdegradedへ落ちた場合に誤denyのまま
残る既知の限界として§6に明記する。KLK-029のsed/awkゲート緩和が同様に
degradedへ及ばないのと同型の縮退）。

**処理フロー（`for f in tickets/active/*.md; do rm $f; done` を再入力した場合）:**
```
find_violation
 └ statement1=["for","f","in","tickets/active/*.md"]
    ├ record_loop_binding: _parse_for_header→("f",["tickets/active/*.md"])
    │   mentions_guarded_expanded(["tickets/active/*.md"])=True
    │   → guarded_loop_vars={"f"}
    ├ statement_violation: control_stripped_segments→strip_control_prefix
    │   → "for"はNAME形式(f)を確認→[] (残余なし)
    │   segments=[[]] → segment_violation([])→head無し→None
    │   gate_hit=True(literal mention)だがsegmentsに違反無し→None（allow継続）
    └ cwd追跡: control_stripped_segments→[] → next_cwd([],cwd)→cwd不変
 └ statement2=["do","rm","$f"]
    ├ redirect_violation: リダイレクト無し→None
    ├ control_stripped_segments→strip"do"→["rm","$f"]→segments=[["rm","$f"]]
    ├ gate_hit: 文字通りの言及は無いが mentions_guarded_var(["do","rm","$f"],
    │   {"f"})=True（$fの参照）→ gate_hit=True
    └ segment_violation(["rm","$f"])→head="rm"はALLOWED_HEADS外→
        "'rm' は許可されていません" → deny
```
（読み取り版 `... do echo $f; done` は同じ経路で `segment_violation(["echo","$f"])`
→ head="echo" は ALLOWED_HEADS内 → None → allow）

**実装Phase:**

| Phase | 内容 | 完了条件 | 対応する受け入れ条件 |
|---|---|---|---|
| 1 | 制御構文の再帰的ストリッピング機構（`strip_control_prefix`・`_strip_case_clause`・`_parse_for_header`・`control_stripped_segments`）を実装し、`statement_violation`のALLOWED_HEADS判定と`find_violation`のcwd追跡の両方へ適用する | 読み取り専用の for/while/until/if-elif-else/case（単一パターン）がallowになり、既存のLEGACY_DENY_COMMANDS（リテラルパスによる書き込み）が回帰しない。**本Phase単体ではAC2/AC3（ループ変数経由の書き込み）は未充足であり、Phase2と不可分（本Phase単独でのマージ・リリースは行わない。理由: Phase1が「for」ヘッダーの誤denyを解消することで、KLK-010以前から存在した『とりあえず先頭で全部denyしていた』偶発的な保護が外れるため、Phase2の補償が無いと`do rm $f`が新たにallowへ転じる）** | AC1（読み取り形のallow）、AC5（既存denyの回帰なし）の基盤 |
| 2 | ループ変数経由の書き込み検出（`guarded_loop_vars`・`record_loop_binding`・`mentions_guarded_var`によるgate_hit拡張・`redirect_violation`への`guarded_loop_vars`追加）を実装する | `for f in tickets/active/*.md; do rm $f; done`・`do sed -i …`・`do echo x > $f`がいずれもdenyになり、`do echo $f; done`・`do cat $f; done`はallowのまま。INV-SH-11（`f=path; rm $f`）の挙動が変わらないことを確認する | AC2（書き込み含む本体のdeny）、AC3（ループ変数経由の書き込み扱いの明示） |
| 3 | `tests/test_guard_bash_writes.py`へ`INV-CTRL-*`（INVENTORY_CASES）・専用回帰テスト（`test_klk019_*`）・誤deny予算の固定allowテストを追加し、V-14（実機bashとの二方向検証。§9参照）をscratchpadで実施してレポートする | 全件パス（実測値を報告）。V-14の実測結果（新たにallowへ転じた形の一覧と、実機bashでの読み取り専用性の確認）をレポートに記載する | AC4（INVENTORY_CASES登録）、AC6（誤deny予算の逆方向確認・V-14）、AC7（全件パス） |

## 5. 影響範囲

| 対象 | 影響の種類 | 対応方針 |
|---|---|---|
| `.claude/hooks/guard_bash_writes.py`（`statement_violation`・`redirect_violation`・`find_violation`） | 変更（シグネチャ拡張・1行差し替え） | 既存呼び出し元は本モジュール内の1箇所ずつのみ（設計時に確認済み）。デフォルト引数で後方互換を保つ |
| `.claude/hooks/guard_bash_writes.py`（新規関数・定数） | 追加 | `ALLOWED_HEADS`/`CWD_SAFE_HEADS`は変更しない（§3方針1） |
| `tests/test_guard_bash_writes.py` | 追加（`INV-CTRL-*`・`test_klk019_*`） | 既存の`LEGACY_DENY_COMMANDS`・`INVENTORY_CASES`は削除・変更しない（追加のみ） |
| `.claude/hooks/README.md` | 変更なし（本チケットは同ファイルへの反映を要求していない。ただし将来README更新時にKLK-019の制御構文機構を追記する余地を残す） | 対応不要（Open Questionsではない。README更新は任意・スコープ外） |
| `degraded_violation`（字句解析失敗時のフォールバック） | 変更なし | 制御構文キーワードは従来どおりdegraded側では無条件deny扱いのまま（§4末尾参照） |

## 6. リスク

| リスク | 深刻度 | 軽減策 |
|---|---|---|
| **R-CTRL1（AC3の核心）**: ループ変数の値は原理的に追跡不能。`for f in tickets/active/*.md; do rm $f; done`で`$f`の実行時の値（`APP-001.md`等）はコマンド文字列上に現れないため、「値」を根拠にした判定は不可能 | high | 値ではなく「NAMEがガード対象パスへ束縛されるLISTを持つforの変数である」という**構文的事実**を証拠にし、`$NAME`という**参照の存在**だけでゲートを開く（`mentions_guarded_var`）。破れる形（否定形）: 本関数の呼び出し箇所が「ゲートを開く」以外の用途（例: 単独で許可判定に使う）に転用された場合。向き: 誤って強めても deny 方向にしか働かない（ゲートを開いた後の許可・不許可は既存のALLOWED_HEADS判定に委ねるため、over-denyにしかならない）。検出器: `tests/test_guard_bash_writes.py`のINV-CTRL-03〜05・`test_klk019_loop_var_*`に加え、tester差し戻しで判明した「登録自体の欠落」（`record_loop_binding`が生statementを見て内側for-headerの共有形で登録漏れを起こす穴。R-CTRL4の一般化）を閉じた回帰固定テスト`test_klk019_regression_nested_for_loop_var_write_denies_do_prefix`・`_then_prefix`・`_while_prefix`・`_redirect_write_denies`（§4・§9 AC3・§10参照） |
| **R-CTRL2**: `case`の複数パターン（`a|b)`）は本hookの字句解析器が`\|`を「パイプ演算子」として無条件に扱うため（`OPERATOR_CHARS`に`\|`を含む）、case構文上の論理和と混同し、正しく解析できない | medium | 単一パターン（`)`で終端する単一トークン）だけを認識対象とし、それ以外は`_strip_case_clause`が`None`を返して既存の（保守的な）deny挙動へフォールバックする。将来この次元を閉じるには字句解析器自体にcase文脈を持たせる別チケットが必要（本チケットのスコープ外・§6既知の限界として明記） |
| **R-CTRL3（実装時に前提の誤りが判明・補正済み）**: 当初の設計は「`fi`/`done`/`esac`は常にステートメントの末尾で単独語にしかならず、特別扱いは不要」としていたが、implementerがAC1必須例`while read l; do echo $l; done < tickets/active/APP-001.md`を設計書のコード（closing keywordを剥がさない版）のまま実行して実測したところ誤denyになった。**原因**: `done`/`fi`/`esac`の直後に区切り無しでリダイレクトが続く形（`done < FILE`）はbash文法上有効であり、この場合「閉じキーワード＋リダイレクト先」が1ステートメントになる。リダイレクト先の語はpipe_segmentsでは読み飛ばされるが、gate_hit判定用のwords（ステートメント中の全"w"トークン）には含まれるため、保護対象を言及するリダイレクト先を持つだけでgate_hitが立ち、剥がされない生の`done`がheadになりALLOWED_HEADS外として誤denyになっていた（「単独語のみのステートメントとしてしか出現しない」という前提そのものが誤りだった＝末尾ではあるが単独語ではない形が存在する）。**採用した補正**: `strip_control_prefix`の分岐条件へ`head in CONTROL_CLOSING_KEYWORDS`を追加し、`fi`/`done`/`esac`も`CONTROL_CONDITION_KEYWORDS`/`CONTROL_BODY_KEYWORDS`と同様に剥がす。`ALLOWED_HEADS`/`CWD_SAFE_HEADS`は無変更で、剥がした残余は既存のALLOWED_HEADS判定にそのまま委ねるため新しい許可経路は作らない（`<`はredirect_inであり書き込みを発生させないためdenyする実益が無い）。**安全性根拠（実測で確認済み）**: 閉じキーワードの後ろにパイプで実コマンドが続く形（`while true; do echo hi; done | rm tickets/active/APP-001.md`）は、そのパイプ区間が独立してALLOWED_HEADS判定の対象になるため引き続きdeny（`test_klk019_closing_keyword_pipe_write_still_denies`で固定）。単独`fi`/`done`/`esac`（引数を取らない形）は従来どおり構文的に保護対象パスへ言及しえないため誤allowの新規経路にもならない | low（誤denyの是正・新規許可経路なし） | `test_klk019_closing_keyword_trailing_redirect_allow`（`done`/`fi`/`esac`直後のリダイレクトがallowになることの固定）と`test_klk019_closing_keyword_pipe_write_still_denies`（パイプ越しの書き込みが引き続きdenyであることの固定）で回帰ガードする。この補正を含めた状態で既存`LEGACY_DENY_COMMANDS`130件・既存`INVENTORY_CASES`（INV-SH-11含む）は無回帰と実測確認済み（implementer報告） |
| **R-CTRL4（設計中に発見。investigation.mdには無い新規リスク。tester差し戻しにより2箇所→4箇所共有へ一般化・最終形を確定）**: 当初は「`strip_control_prefix`をALLOWED_HEADS判定にのみ適用し`find_violation`のcwd追跡（`next_cwd`）には適用しない」という**2箇所間**の共有漏れとして発見されたリスクだったが（`if cd tickets/active; then rm APP-001.md; fi`のように「ifの条件節でcdする」形でcwd追跡が古いstatementを見て`next_cwd`が発火せず、後続`then`節でcwd_is_guardedが偽のままrmが素通りするおそれ）、tester差し戻し（AC6独自検証）により**同型のリスクが`record_assignments`・`record_loop_binding`にも存在する**ことが判明し（`for i in 1 2; do for f in tickets/active/*.md; do rm $f; done; done`のように、内側for-headerが外側`do`と同一statementを共有すると`record_loop_binding`の登録自体が漏れ、`do rm $f`にゲートが立たない）、本リスクは次の**4箇所すべてが外側制御構文キーワードの剥がしを共有しなければならない**という最終形へ一般化された: ①ALLOWED_HEADS判定（`statement_violation`→`control_stripped_segments`→`strip_control_prefix`）②cwd追跡（`find_violation`→`control_stripped_segments`）③ループ変数登録（`record_loop_binding`→`strip_outer_control_keywords`）④前置き代入登録（`record_assignments`→`strip_outer_control_keywords`）。①②と③④で異なるヘルパーを使う理由は、①②が「ヘッダーに実行コマンドは無い」前提でfor/caseヘッダーごと消費してよいのに対し、③④は逆にヘッダー自体・代入自体を見る必要があるため（§4参照）。4箇所のいずれか1つでも共有から漏れると、外側制御構文と内側のcd／for-header／前置き代入が同一statementを共有する入れ子形で新規にdeny→allowへ転じる | high | `_strip_leading_outer_keyword`を共通の剥がし単位として`strip_control_prefix`（①②が使う）・`strip_outer_control_keywords`（③④が使う）の両方から呼び、4箇所の共有元を1つのロジックへ集約する（§4参照）。検出器: ①②`test_klk019_if_cd_then_rm_denies_via_cwd_tracking`・`test_klk019_if_cd_then_cat_allows_via_cwd_tracking`、③④`test_klk019_regression_nested_for_loop_var_write_denies_do_prefix`・`_then_prefix`・`_while_prefix`・`_redirect_write_denies`・`test_klk019_regression_nested_assignment_redirect_write_denies`（tester差し戻しで追加。§9 AC3・AC6・§10参照）。**未解決の懸念（architectのレビューで新規発見。R-CTRL7・§10 Open Questions参照）**: `_strip_leading_outer_keyword`は`case`を対象外とするため、`case`ヘッダーが内側のfor-header／前置き代入と同一statementを共有する形は③④の登録漏れが残存する可能性があり、本改訂時点では未検証のまま残る |
| **R-CTRL5**: `guarded_loop_vars`は一度登録した変数名を統計全体で保持し続ける（forループの`done`を過ぎても保持）ため、同名変数を無関係な用途で再利用した場合に過剰denyが起きうる | low（安全側の誤り） | 意図的な設計判断（§3方針4）。値追跡が不可能な以上、スコープ終端の判定も不可能であり、より広く保持する方が安全側（over-denyのみで新規allowの穴にはならない）。§9 Open Questionsではなく設計確定事項として明記する |
| **R-CTRL6**: `degraded_violation`は本チケットの対象外のため、字句解析に失敗する入力（未閉じクォート等）に制御構文が混在した場合、読み取り専用の制御構文でも従来どおり誤denyのまま残る | low（既存の限界の継続） | 新規の穴ではない（degradedは常により保守的であることが既存の設計原則）。§4末尾・本設計書の「既知の限界」として明記し、別チケット候補とする |
| **R-CTRL7（architectによる本改訂の設計レビュー中に発見。未検証・未解決）**: `_strip_leading_outer_keyword`／`strip_outer_control_keywords`は`for`/`case`を意図的に対象外とする設計だが（§4）、`case`のヘッダー（`case SUBJECT in PATTERN)`）が内側の`for NAME in LIST`・前置き代入と**同一statementを共有する形**（例: `case x in *) for f in tickets/active/*.md; do rm $f; done ;; esac`）では、`record_loop_binding`/`record_assignments`が受け取る残余の先頭語が`case`のままとなり、`_parse_for_header`／`ASSIGN_RE`のいずれとも一致せず登録が同様に漏れる可能性がある。静的なコード読解によるトレース: `strip_control_prefix`側は`_strip_case_clause`でcaseヘッダーを剥がす経路を持つため①ALLOWED_HEADS判定・②cwd追跡は該当statement自体を正しく`[]`残余（＝deny不要）へ倒すが、その結果このstatement単体ではdenyが出ず、後続statement`do rm $f`が③`guarded_loop_vars`未登録によりgate_hitしないため、コマンド全体としては新規にdeny→allowへ転じる可能性がある（旧版=develop相当ではstatement1自体の先頭語`case`がALLOWED_HEADS外で無条件denyだったため、この経路は今まで到達しなかった）。**architectはBashツールを持たず、この推論を実機・自動テストで検証していない** | high（未検証。確定すればAC6「これ以外の形で1件でもあればブロッカー」に抵触するN1〜N6列挙外の新規deny→allowになる） | **未着手。§10 Open Questionsへ記録し、implementer/testerによる検証（同パターンの回帰テスト追加、および必要なら`_strip_leading_outer_keyword`と同型に`case`ヘッダーも`_strip_case_clause`相当で剥がす拡張）を求める。人間の判断を仰ぐまでdoneへは進めない** |

## 7. マイグレーション戦略

なし。データ構造・外部インターフェースの変更を伴わない、hook内部ロジックの
追加のみ（既存の`ALLOWED_HEADS`/`CWD_SAFE_HEADS`/`guarded_vars`の意味・型は
変更しない）。

## 8. ロールバック戦略

本チケットの実装コミット（Phase1〜3）を `git revert` する。`strip_control_prefix`
が`None`を返すことで既存挙動へフォールバックする設計のため、実装に問題が
見つかった場合でも「新設関数の呼び出し箇所（2箇所: `statement_violation`の
`segments`行、`find_violation`のcwd追跡の`segments`行）を元の
`pipe_segments(statement)`へ戻す」だけの最小差分ロールバックも可能（ただし
正式なロールバックはコミット単位のrevertを正とする）。

```bash
# ロールバック手順の例（Phase1〜3を1コミットにまとめた場合）
git revert {コミットハッシュ}
```

## 9. 受け入れ条件

チケットのAC（引用は`tickets/active/KLK-019_...md`）を、設計の観点から具体化する。

- [ ] **AC1**: `for f in tickets/active/*.md; do echo $f; done`・
      `while read l; do echo $l; done < tickets/active/APP-001.md`・
      `if [ -f tickets/active/APP-001.md ]; then echo yes; fi`・
      `case x in *) cat tickets/active/APP-001.md ;; esac`・
      `until false; do cat tickets/active/APP-001.md; break; done`が
      いずれもallowになる（Phase1で実現）。`;`区切り形と改行区切り形の両方で
      同じ結果になることを確認する（`split_statements`は改行を`;`と同一視
      するため理論上一致するはずだが、テストで固定する）。**実装時の補正
      （§6 R-CTRL3）:** `while read l; do echo $l; done < tickets/active/APP-001.md`
      は`done`直後の区切り無しリダイレクトが原因で当初のコード（closing
      keywordを剥がさない版）では誤denyになることが実測で判明したため、
      `strip_control_prefix`が`fi`/`done`/`esac`も剥がすよう補正した。回帰
      ガードは`test_klk019_closing_keyword_trailing_redirect_allow`（本例と
      `if ...; fi < FILE`・`case ...; esac < FILE`のallow固定）
- [ ] **AC2**: `for f in tickets/active/*.md; do rm $f; done`・
      `for f in tickets/active/*.md; do sed -i 's/a/b/' $f; done`・
      `for f in tickets/active/*.md; do echo x > $f; done`がいずれもdeny
      になる（Phase2）。加えてループ変数を介さないリテラルパスの書き込み
      （`while true; do rm tickets/active/APP-001.md; break; done`）も
      denyのままであることを確認する（Phase1のみで担保される経路）
- [ ] **AC3**: ループ変数経由の書き込みの扱いを設計で次のとおり明示する
      （§3方針4・§6 R-CTRL1）: 値の追跡は原理的に不可能なため、
      `for NAME in LIST`のLISTが保護対象パスへ言及する場合、`NAME`への
      **参照の構文的存在**だけでゲートを開く保守的deny設計を採る。根拠は
      R-CTRL1、向きはover-deny方向（安全側）にのみ働く、検出器は
      `mentions_guarded_var`＋INV-CTRL-03〜05。**tester差し戻し（AC6独自
      検証）**: `record_loop_binding`が`control_stripped_segments`を経由せず
      生statement語を見ていたため、内側の`for NAME in LIST`ヘッダーが外側
      制御構文の`do`/`then`/`while`と同一statementを共有する形で登録自体が
      漏れる穴が発見され、`strip_outer_control_keywords`経由に修正した
      （§4・§6 R-CTRL4一般化・§10参照）。回帰固定テストは
      `test_klk019_regression_nested_for_loop_var_write_denies_do_prefix`・
      `_then_prefix`・`_while_prefix`・`_redirect_write_denies`の4件
- [ ] **AC4**: `INVENTORY_CASES`へ`INV-CTRL-*`（新設接頭辞。理由: 制御構文の
      再帰的ストリッピングは既存のINV-SH〔シェル展開・字句次元〕・
      INV-AWK／INV-SED〔プログラム本文次元〕のいずれとも異なる新しい判定
      次元であるため独立させる。決定はarchitectの裁量範囲＝investigation.md
      Unknown3の解消）で最低限次の分岐を登録する（P7: 挙動が分岐する条件
      まで分解）:
      classic for読み取り／for本体の書き込み（リテラル）／for本体の書き込み
      （ループ変数）／for本体の読み取り（ループ変数）／`for NAME; do`（in省略）
      ／C形式for（フォールバックdeny）／while読み取り／while条件節の書き込み
      ／until読み取り／if-then-fi読み取り／if-then-else-fi読み取り／
      if-elif-else-fi読み取り／if条件節の書き込み／if本体（then）の書き込み／
      case単一パターン読み取り／case単一パターン書き込み／case複数パターン
      `a|b)`（フォールバックdeny）／caseで`in`欠落（フォールバックdeny）／
      入れ子制御構文（読み取り）／入れ子制御構文（深い階層での書き込み）／
      `if cd DIR; then ...; fi`によるcwd追跡（R-CTRL4の回帰ガード。allow側・
      deny側の両方）／単独`fi`・`done`・`esac`のみのステートメント
- [ ] **AC5**: 既存の`LEGACY_DENY_COMMANDS`（実測値。tester/implementerが
      実装・テスト時点の実際の件数を報告する。過去チケットの目安数値
      〔投機的な126件等〕をそのまま転記しない）が全件deny のまま。既存の
      `INVENTORY_CASES`（同様に実測値を報告）が期待どおりの判定を維持する
- [ ] **AC6（誤deny予算の逆方向確認）**: 本チケインで新たにdeny→allowに
      転じるのは次のN1〜N6に限られることを確認する。**これ以外の形で
      deny→allowに転じたものが1件でもあればブロッカーとして報告する**:
      - N1: `for NAME in LIST; do BODY; done`（`;`または改行区切り）で
        BODY（および入れ子）の先頭コマンドがすべてALLOWED_HEADS所属であり、
        リダイレクト先・sed -i・awkプログラム本文等の既存の追加規則にも
        抵触しない場合
      - N2: `while COND; do BODY; done`／`until COND; do BODY; done`で
        COND・BODYの両方がN1と同条件を満たす場合
      - N3: `if COND; then BODY; [elif COND2; then BODY2;]*[else BODYn;] fi`
        で全てのCOND・BODYがN1と同条件を満たす場合
      - N4: `case SUBJECT in PATTERN) BODY ;; esac`（PATTERNが`)`終端の
        単一トークン・`|`による複数パターンを含まない）でBODYがN1と同条件
        を満たす場合
      - N5: 上記の入れ子（for内if・while内case等）で末端のCOND・BODYが
        すべてN1と同条件を満たす場合
      - N6: COND/BODY内に`cd DIR`が現れ、その後のcwd追跡が正しく行われる
        ことを前提とした上記の組み合わせ（`if cd tickets/active; then cat
        APP-001.md; fi`はallow、同じ`cd`後に`rm APP-001.md`はdenyのまま）
      - **意図的に対象外（引き続きdeny）**: C形式for、caseの複数パターン
        （`a|b)`）・パターンが複数トークンに分割される形、ループ変数経由の
        書き込み（AC2/AC3で明示的にdeny）。加えて、`fi`/`done`/`esac`を
        剥がす補正（§6 R-CTRL3）を入れた後も「閉じキーワードの直後にパイプで
        実コマンドが続く形」（`while true; do echo hi; done | rm
        tickets/active/APP-001.md`）はdenyのまま転じないことを
        `test_klk019_closing_keyword_pipe_write_still_denies`で固定する（この
        形がN1〜N6のいずれにも該当せずallowへ転じていないことの直接的な
        回帰ガード）
      - **V-14（実機bashとの二方向検証）**: implementerがscratchpadの使い
        捨てディレクトリで、N1〜N6の代表例（最低各1件）とAC2の書き込み例を
        実機bashで実行し、(a) 読み取り専用と判定した形は実機でも保護対象
        ファイルが変化しないこと、(b) deny判定した形は実機で実行すれば
        書き込みが発生する（＝denyが正しく機能を止めていること）を確認し、
        結果をレポート（`docs/reports/KLK-019/implementation.md`等）へ記載
        する。テストスイート自体には実機副作用確認を含めない（KLK-010 §9
        の既存方針を踏襲）
      - **tester差し戻しで追加された回帰固定テスト（5件）**: `record_loop_binding`・
        `record_assignments`が`control_stripped_segments`を経由せず生statement
        語を見ていたためのAC6ブロッカー（入れ子ループでの新規deny→allow）を、
        `test_klk019_regression_nested_for_loop_var_write_denies_do_prefix`・
        `_then_prefix`・`_while_prefix`・`_redirect_write_denies`・
        `test_klk019_regression_nested_assignment_redirect_write_denies`の
        5件で固定した（§4・§6 R-CTRL4一般化・§10参照。修正コミット
        `349ec69`）
      - **未解決（architectのレビューで新規発見。§6 R-CTRL7・§10 Open
        Questions参照）**: `case`ヘッダーが内側のfor-header・前置き代入と
        同一statementを共有する形（`case x in *) for f in tickets/active/*.md;
        do rm $f; done ;; esac`等）は、上記5件と同型の登録漏れが残存しうる。
        本設計書改訂時点では静的読解による指摘のみで実機検証・テスト未実施
        であり、AC6の「これ以外の形で1件でもあればブロッカー」を満たすこと
        を現時点で保証できない
- [ ] **AC7**: `tests/test_guard_bash_writes.py`の全件がpassする（実測値を
      tester が報告する）

**テスト観点（implementerへの指示）:**
- 単体境界: `strip_control_prefix`・`_strip_case_clause`・`_parse_for_header`
  はモジュール内部関数だが、既存の方針（KLK-010 §9）にならい**hookをサブ
  プロセス起動するブラックボックステストに統一**し、内部関数の直接呼び出し
  テストは追加しない
- 重点的に見るエッジケース: (1) 入れ子（for内if等）で最深部のみ書き込みが
  ある場合に正しくdenyされるか (2) `if cd tickets/active; then ...; fi`の
  cwd追跡（R-CTRL4） (3) `case`の複数パターン・C形式forが安全側（deny）へ
  フォールバックすること (4) INV-SH-11（既存の`f=path; rm $f`）の挙動が
  一切変わらないこと（`guarded_vars`と`guarded_loop_vars`の分離の回帰ガード）

## 10. Open Questions

investigation.mdのUnknown3（INVENTORY_CASESの命名規約）・Unknown4
（本体判定の実装方式）はいずれもarchitectの設計裁量として本設計書内で解消
済み（Unknown3→`INV-CTRL-*`新設・Unknown4→`strip_control_prefix`による
再帰的ストリッピング）。

**新規Open Question（本改訂で追加。§6 R-CTRL7参照）**: tester差し戻し
（AC6独自検証）で発見された「`record_loop_binding`/`record_assignments`が
生statementを見て入れ子形で登録漏れを起こす」バグは、implementerの修正
（`strip_outer_control_keywords`新設。コミット`349ec69`）により`do`/`then`/
`while`（外側制御構文キーワード）が内側for-header・前置き代入と同一
statementを共有する形については解消したことを確認した。**しかし
architectによる設計レビュー中に、`_strip_leading_outer_keyword`が意図的に
対象外とする`case`について、同型の登録漏れが残存しうることを静的な
コード読解のみで発見した**（例: `case x in *) for f in tickets/active/*.md;
do rm $f; done ;; esac`のように`case`ヘッダーが内側for-headerと同一
statementを共有する形。詳細トレースは§6 R-CTRL7）。architectはBashツール
を持たず実機・自動テストでの検証ができないため、次のいずれかを人間へ
エスカレーションする。
1. investigator/implementer/testerが上記パターンを実機bash・自動テストで
   検証し、(a) 実際にdeny→allowへ転じるなら本チケット内で
   `record_assignments`/`record_loop_binding`が`case`ヘッダーも
   `_strip_case_clause`相当で剥がすよう追加修正する、
2. 実際にはallowへ転じない（architectのトレースに誤りがある）ことが
   確認できれば、その理由を本設計書へ追記のうえ本Open Questionをクローズ
   する。
回答を受けてから本Open Questionを解消し、設計を確定させる。

### 設計修正の記録（2026-08-18・実装時発見）

Open Questionではなく、architectによる設計確定後の修正記録として残す。

- **経緯**: implementerがAC1必須例`while read l; do echo $l; done <
  tickets/active/APP-001.md`を設計書のコードそのまま（closing keywordを
  剥がさない版）で実行し、誤denyになることを実測で発見した。原因は§6
  R-CTRL3の前提「`fi`/`done`/`esac`は常にステートメントの末尾で単独語に
  しかならない」が破れる具体例（`done`直後に区切り無しでリダイレクトが
  続く`done < FILE`形はbash文法上有効であり、この場合「`done`+リダイレクト
  先」が1ステートメントになる。リダイレクト先の語はpipe_segmentsでは
  読み飛ばされるがgate_hit判定用のwordsには含まれるため、保護対象を言及
  するリダイレクト先を持つだけでgate_hitが立ち、剥がされない生の`done`が
  headになってALLOWED_HEADS外として誤denyになっていた）ことにあった。
- **採用した補正**: `strip_control_prefix`の分岐条件へ`head in
  CONTROL_CLOSING_KEYWORDS`を追加し、`fi`/`done`/`esac`も
  `CONTROL_CONDITION_KEYWORDS`/`CONTROL_BODY_KEYWORDS`と同様に剥がす。
  `ALLOWED_HEADS`/`CWD_SAFE_HEADS`は無変更、剥がした残余は既存の
  ALLOWED_HEADS判定にそのまま委ねるため新しい許可経路は作らない
  （`<`は書き込みを発生させないためdenyする実益が無い）。
- **architectによる承認**: 本補正を承認する。理由は
  (1) `ALLOWED_HEADS`/`CWD_SAFE_HEADS`を一切変更していない＝§3方針1
  「制御構文キーワードを単純にALLOWED_HEADSへ追加しない」という設計の
  中核制約を保っている、
  (2) 剥がした残余の判定を既存のALLOWED_HEADS判定に完全に委ねる
  （新しいホワイトリスト・新しい許可条件を作らない）ため、この変更単体が
  新規のallow経路を生み出す構造になっていない、
  (3) 閉じキーワード直後にパイプで実コマンドが続く形が独立したパイプ
  区間として引き続きALLOWED_HEADS判定を受けることを実測で確認済み
  （`test_klk019_closing_keyword_pipe_write_still_denies`）、
  (4) 既存`LEGACY_DENY_COMMANDS`130件・既存`INVENTORY_CASES`（INV-SH-11
  含む）が無回帰であることを実測確認済み、という4点により、§3方針1・
  §6 R-CTRL1（over-deny方向にのみ働く設計原則）との整合を維持したまま
  誤denyのみを是正する変更であるため。追加の懸念は無い。
- **本設計書への反映**: §4（`CONTROL_CLOSING_KEYWORDS`定義コメント・
  `strip_control_prefix`本体の分岐条件とdocstring）、§6 R-CTRL3、§9
  AC1・AC6を実装済みコードと整合するよう改訂した（本改訂）。
- **参照**: 実装済みコード全文はブランチ`fix/KLK-019-control-structure-false-deny`
  のコミット`e0dbb49`（`.claude/hooks/guard_bash_writes.py`）。

### 設計修正の記録（2026-08-18・tester差し戻し起因）

Open Questionではなく、architectによる設計確定後の修正記録として残す
（本改訂で新たに生じたOpen Questionは上記・§6 R-CTRL7を参照）。

- **経緯**: tester QualityGateがAC6独自検証で新規ブロッカーを検出した。
  `record_loop_binding`・`record_assignments`が`control_stripped_segments`
  （R-CTRL4で共有必須と設計書に明記済み）を経由せず生statement語を見て
  いたため、内側の`for NAME in LIST`ヘッダー・前置き代入が外側制御構文の
  `do`/`then`/`while`等と`;`区切りで同一statementを共有する形
  （`for i in 1 2; do for f in tickets/active/*.md; do rm $f; done; done`
  等）で登録が丸ごと失敗し、新規にdeny→allowへ転じていた（実機bashで
  書き込み発生を確認済み）。tester_to_implementer 0→1で差し戻し。
- **採用した修正**: implementerが以下を実施（コミット`349ec69`、ブランチ
  `fix/KLK-019-control-structure-false-deny`）。
  1. `strip_control_prefix`は`for`/`case`到達時にヘッダーごと消費・破棄
     するため`record_loop_binding`/`record_assignments`（逆にヘッダー・
     代入自体を見る必要がある）へは転用できないと判断。
  2. 「条件／本体／閉じキーワードだけを先頭から剥がし、`for`/`case`は
     そのまま残す」共通処理を`_strip_leading_outer_keyword`として切り出し。
  3. 新設`strip_outer_control_keywords`（`_strip_leading_outer_keyword`を
     繰り返し適用し常に残余を返す。「変化なし」をNoneで示す設計は採らない）
     を`record_assignments`・`record_loop_binding`の両方から呼ぶ。
  4. `strip_control_prefix`自身も`_strip_leading_outer_keyword`を呼ぶよう
     リファクタリング（外部から見た挙動は無変更）。
  5. `ALLOWED_HEADS`/`CWD_SAFE_HEADS`は無変更。
- **検証結果**: `python3 -m unittest discover -s tests -v` 731件全件PASS
  （tester追加の回帰固定テスト5件`test_klk019_regression_nested_for_loop_
  var_write_denies_do_prefix`・`_then_prefix`・`_while_prefix`・
  `_redirect_write_denies`・`test_klk019_regression_nested_assignment_
  redirect_write_denies`を含む）。`LEGACY_DENY_COMMANDS`130件・
  `INVENTORY_CASES`117件（INV-SH-11・INV-CTRL-01〜25含む）は無回帰。
  実機bash検証（scratchpad使い捨てディレクトリ）で5つの再現手順がすべて
  denyへ転じ、書き込みが発生しないことを確認済み。
- **architectによる正式判断（承認。ただし追加の懸念1件あり）**: 本修正
  自体は承認する。理由は次の4点による。
  1. `ALLOWED_HEADS`/`CWD_SAFE_HEADS`を一切変更していない＝§3方針1の
     中核制約を保っている。
  2. `strip_control_prefix`の内部リファクタは純粋なロジック抽出であり
     （分岐条件・剥がす対象語彙とも変更なし）、既存の731件（旧726件＋
     新規5件）全件PASSがこれを裏付ける。
  3. `strip_outer_control_keywords`は`record_assignments`/
     `record_loop_binding`の**登録範囲を広げる方向にのみ**作用する
     （`guarded_vars`/`guarded_loop_vars`への追加はOR的に安全側へ働き、
     既存の登録を取り消す経路が無い）ため、この変更単体が新規のallow経路
     を生む構造にはなっていない。
  4. 実機bash二方向検証・`LEGACY_DENY_COMMANDS`/`INVENTORY_CASES`の無回帰
     が実測確認済みである。
  **ただし本改訂の設計レビュー中に、`case`ヘッダーが内側for-header・前置き
  代入と同一statementを共有する形について、同型の登録漏れが残存しうる
  ことを静的コード読解で発見した（未検証。§6 R-CTRL7・上記Open Question
  参照）。この懸念が解消されるまで、AC6「これ以外の形で1件でもあれば
  ブロッカー」の充足を完全には確認できていない。** したがって本チケット
  はtest_passed/doneへ進める前に、上記Open Questionの解消（検証または
  追加修正）を経る必要がある。
- **本設計書への反映**: §4（`_strip_leading_outer_keyword`・
  `strip_outer_control_keywords`の追加・`record_assignments`/
  `record_loop_binding`の変更点）、§6 R-CTRL1・R-CTRL4（4箇所共有への
  一般化）・R-CTRL7（新規）、§9 AC3・AC6を実装済みコードと整合するよう
  改訂した（本改訂）。
- **参照**: 実装済みコード全文はブランチ`fix/KLK-019-control-structure-false-deny`
  のコミット`349ec69`（`.claude/hooks/guard_bash_writes.py`）。tester報告は
  `docs/reports/KLK-019/test-report.md`。
